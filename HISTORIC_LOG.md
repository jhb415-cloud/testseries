# HISTORIC_LOG.md — 5-in-1 Dashboard SPA

---

## 세션 #8 — 2026-07-03

### 현재 버전: v0.0.42

### 작업 내용
- 세션 시작 시 사용자가 "어디까지 했지" 재확인 요청 → 세션#7 마지막 메모(사용자가 Stage D/Supabase 착수 의사를 밝혔고, 다음 세션에서 Claude가 사전 준비사항을 설명하기로 함)를 근거로 현재 상태 안내.
- 사용자가 "지금까지 안내했던 내용(Supabase/보안/로그인)을 전체적으로 제안해달라"고 요청 → PRD.md 9-1 Stage D/E 로드맵을 다시 확인하고, 스키마 설계 방향/인증 방식(익명 UID vs 소셜 vs 하이브리드)/보안 고려사항/마이그레이션 전략을 정리해 설명. AskUserQuestion으로 인증 방식(**익명 UID, 추천안 채택**)과 착수 시점(**스키마·보안정책을 먼저 구체화한 뒤 프로젝트 생성 안내부터**, 사용자가 GitHub으로 Supabase 계정에 이미 로그인돼있음을 알려줌) 확인.
- 사용자가 "댓글기능은 나중에 넣자, 괜히 욕만 먹을 것 같음"이라고 요청 → `comments` 테이블/쿨다운 트리거를 최종 스키마에서 제외 결정. 이 스코프 결정은 향후 세션에서도 참고해야 해서 메모리(`project_stage_d_backend.md`)에 저장.
- 확정 SQL(`profiles`/`test_results`/`percentile_cache` + RLS)을 제공하고, Supabase 대시보드에서 프로젝트 생성 → SQL 실행 → Anonymous Sign-in 활성화 → API 키 확인까지 사용자가 보낸 스크린샷을 보며 실시간으로 다음 클릭 지점을 안내(Organization 유지/GitHub 연동 스킵/비밀번호 자동생성/리전 Asia-Pacific/Security 체크박스 3개 중 "Enable automatic RLS" 추가 체크 권장). 도중 Supabase가 API 키 체계를 anon/service_role → **Publishable/Secret key**로 개편한 것을 확인해 새 UI에 맞게 안내를 조정했고, Data API URL 위치도 새 UI("Data API" 좌측 메뉴) 기준으로 재안내.
- Publishable key(`sb_publishable_...`)와 Project URL(`https://yovwvcjuadfieprvsooo.supabase.co`) 확보 후 코드 작업 착수: `index.html`에 `@supabase/supabase-js` CDN 스크립트 + 신규 `supabase-client.js` 추가(`window.sb` 클라이언트, 페이지 로드 시 자동 `signInAnonymously()`, `syncResultToSupabase()` 헬퍼). `app.js`의 모든 테스트가 공통 호출하는 `saveRanking()` 맨 끝에 한 줄만 추가해 로컬스토리지는 그대로 유지한 채 Supabase에도 결과를 이중 기록(기존 15개 테스트 로직 무변경, PRD 마이그레이션 방침대로 1단계만 우선 구현).
- **검증**: 로컬 `python -m http.server` + 캐시된 npx playwright로, 실제로 생성된 Supabase 프로젝트를 대상으로 (1) 페이지 로드 시 익명 세션이 자동 수립되는지 (2) `saveRanking('reaction', ...)` 호출 후 `profiles`/`test_results`에 실제 row가 RLS를 통과해 정상 insert되는지(tier 파싱도 정확히 'S'로 저장됨 확인) (3) 로컬스토리지 `ranking_reaction` 기록도 그대로 유지되는지, 3가지를 실제 계정으로 end-to-end 확인. 콘솔 에러 없음. **주의**: 검증 과정에서 QA용 테스트 row 1개(user_id `9fd10300-815b-494d-b7b3-4bcc0e334d4d`, section `reaction`)가 실제 프로덕션 `test_results` 테이블에 남아있음 — anon(publishable) 키로는 delete 정책이 없어 앱에서 지울 수 없고, 필요하면 사용자가 Supabase SQL Editor에서 직접 삭제해야 함.
- CLAUDE.md(기능현황표에 Stage D 행 추가, 댓글 백로그 각주 갱신, 진행중 결정사항 로그 갱신, 변경이력 v0.0.40 추가)와 PRD.md(9-1 D/E 항목을 "보류"→"착수함"으로 갱신) 동시 반영.
- 사용자가 "한번 싹 정리하고 넘어가자 단계별로 말해봐"라고 요청 → Stage D 1단계 작업을 결정/구축/코드/검증/문서 5단계로 요약해 보고.
- "댓글은 나중에라도 작업하자"는 재확인 요청 → 메모리(`project_stage_d_backend.md`)에 "취소가 아니라 백로그"임을 명시적으로 갱신.
- "다음단계 진행하자"는 요청이 Stage D 2단계/카카오공유/로또통계 중 무엇인지 모호해 AskUserQuestion으로 확인 → 사용자가 **①카카오톡 공유 먼저 ②Stage D 2단계+로또 통계기반은 나중에 같이 ③두뇌나이 터치 지연 보정은 "나중에 문제되면"으로 백로그에서 제외**를 직접 결정.
- **v0.0.41 (카카오톡 공유)**: Kakao Developers에서 앱("과몰입 연구소", ID 1503406) 생성 → JavaScript 키 발급 → Web 플랫폼 도메인(`https://testseries1.pages.dev`) 등록까지 사용자 스크린샷을 보며 실시간 안내. 최근 개편된 Kakao Developers UI 때문에 "Web 플랫폼 등록" 위치를 몇 차례 잘못 짚었음(일반→고급→카카오로그인 순으로 헤맴) — 사용자가 "최근 내용을 제대로 찾아보고 알려줘"라고 지적해 WebSearch+WebFetch로 공식 문서를 재확인, 정확한 경로("앱 > 플랫폼 키" 페이지에서 **"Default JS Key" 박스 자체를 클릭**하면 열리는 "JavaScript SDK 도메인" 입력창)를 찾아 안내 — 이 UI 구조는 향후 세션에서도 유효하니 기억해둘 것. 코드: `index.html`에 Kakao JS SDK CDN(v2.8.1) 스크립트 추가(SRI `integrity` 해시는 문서에 텍스트로 없어서 `curl`+`openssl dgst -sha384`로 직접 계산), 신규 `kakao-share.js`(`Kakao.init()` + `shareToKakao(text)`, `Kakao.Share.sendDefault({objectType:'text', link:{webUrl,mobileWebUrl}})` 사용, 실패 시 토스트로 조용히 처리). 기존 "📤 내 결과 공유하기" 버튼 14곳(모든 결과 화면) 바로 아래에 동일 `shareText`를 재사용하는 "💬 카카오톡 공유" 버튼을 Node 스크립트로 일괄 삽입(문자열 치환 정규식이 처음엔 이스케이프된 백틱(`` \` ``) 패턴을 못 찾아 0건 치환되는 실수가 있었음 — app.js 소스 자체가 템플릿 리터럴 안에 `\`${shareText}\`` 형태로 이스케이프돼 있다는 걸 확인 후 마커 문자열 수정해 14건 정상 치환).
- **검증**: Playwright로 Kakao SDK 로드+`Kakao.isInitialized()===true` 확인(도메인 등록 자체는 `Kakao.init()` 단계에서 검증되지 않고 실제 API 호출 시점에 걸리므로 로컬 도메인에서도 init은 성공). 구조가 다른 두 테스트(MBTI=선택형 즉시진행, 속담완성=1.4초 피드백 딜레이형)에서 각각 닉네임 입력→모드/문제 진행→결과화면까지 실제로 완주시켜 "카카오톡 공유" 버튼이 정상 렌더링되고 클릭 시 콘솔 에러 없음을 확인. 단, 실제 카카오톡 앱으로 전송되는 것까지의 완전한 end-to-end는 로컬/Codespaces 도메인이 화이트리스트에 없어 이번 세션에서는 불가능 — 실제 도메인 배포 후 재확인 필요.
- CLAUDE.md(기능현황표 카카오톡 공유 행을 완료로 변경, 결정사항 로그 갱신, 변경이력 v0.0.41 추가)와 PRD.md(9-1 D 항목 각주에서 카카오 분리 언급) 동시 반영.
- 사용자가 Cloudflare Pages 대시보드 스크린샷("testseries1", `testseries1.pages.dev`, `jhb415-cloud/testseries` GitHub 연동, v0.0.41 커밋이 1분 전에 이미 자동배포됨)을 보내며 "그 도메인 맞지?" 확인 요청 → 카카오에 등록한 도메인과 정확히 일치함을 확인. Playwright로 실제 라이브 도메인에서 카카오톡 공유 버튼 클릭 → `accounts.kakao.com` 로그인 팝업이 `sharer.kakao.com/picker/link?app_key=...&short_key=...`로 정상 오픈되는 것까지 end-to-end 검증(도메인 화이트리스트 통과 확인).
- 사용자가 "다음단계로 가보자 구도 잡아줘봐"라고 요청 → Stage D 2단계(퍼센타일 집계)+로또 통계기반을 하나의 Cloudflare Pages Functions 인프라로 묶는 구조를 먼저 설계해 제시(별도 API 서버 없이 기존 Pages 프로젝트에 `functions/` 폴더만 추가하면 같이 자동배포됨). AskUserQuestion으로 ①스케줄링 방식(**GitHub Actions cron, 추천안** — Cloudflare Worker+Cron Trigger를 별도로 새로 만들지 않고 이미 쓰는 GitHub 저장소 재사용) ②로또 데이터 소스(**동행복권 비공식 엔드포인트 사용, 추천안** — PRD 기존 방침) 확정.
- **v0.0.42 (Stage D 2단계 + 로또 통계기반)**: 신규 `functions/api/percentile-refresh.js`(POST, `X-Cron-Secret` 헤더로 보호 — 프로젝트에서 `service_role` 키를 쓰는 유일한 지점. Tier 8개 섹션의 `test_results`를 집계해 "해당 등급 이상 비율"을 퍼센타일로 계산 후 `percentile_cache`에 upsert) + `functions/api/lotto-stats.js`(GET, 동행복권 `getLottoNumber` 비공식 엔드포인트에서 최근 30회차를 서버사이드로 가져와 CORS 우회 — 1회차 날짜(2002-12-07) 기준으로 최신 회차를 추정한 뒤 `returnValue !== 'success'`면 하나씩 감소시키며 보정, 30회차는 Cloudflare Functions 무료 플랜 서브요청 한도 50건 안에 여유있게 들어가도록 설정. `caches.default` Cache API로 24시간 엣지 캐싱해 매 요청마다 동행복권을 다시 두드리지 않게 함). `.github/workflows/percentile-cron.yml` 신설(매일 UTC 18:00=KST 03:00, `curl -X POST`로 secret 헤더와 함께 엔드포인트 호출). 클라이언트: 로또 조합기의 기존 "준비중" 비활성 버튼을 `lottoRunStats()`로 교체 — `/api/lotto-stats` 호출 후 `lottoWeightedPick()`이 빈도 가중 랜덤(출현 0회 번호도 최소 확률은 보장하도록 weight+1)으로 5게임 생성. `percentile_cache`는 이미 공개 읽기 RLS라 클라이언트 쪽에 신규 읽기 API를 만들지 않고 기존 `window.sb`로 바로 조회 가능하게 설계.
- **검증**: `node --check`로 두 Function 파일 문법 확인(Node 24의 ESM 자동 감지로 `export` 구문도 별도 설정 없이 통과). 코드 커밋+푸시 후 Cloudflare Pages 자동배포로 실제 엔드포인트가 살아있는지, 응답 스키마가 예상대로인지는 다음 턴에서 라이브로 확인 예정 — 아직 Cloudflare Pages 환경변수(`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`CRON_SECRET`)와 GitHub Actions 시크릿(`CRON_SECRET`)을 사용자가 등록하지 않아서 `percentile-refresh`는 401을 반환하는 상태, `lotto-stats`는 시크릿이 필요 없어 배포 직후 바로 동작 가능.
- CLAUDE.md(기능현황표에 카카오 후속검증 내용 추가, Stage D 2단계 행 신설, 로또 통계기반 행을 완료로 변경, 결정사항 로그 갱신, 변경이력 v0.0.42 추가)와 PRD.md(로또 입력/제약사항/9-1 D 항목 갱신) 동시 반영.

### 다음에 이어서 해야 할 작업 리스트
- [x] Stage D 1단계(익명 인증 + `test_results` 이중 기록) 완료 (v0.0.40)
- [x] 카카오톡 공유 완료, 라이브 도메인 end-to-end 검증까지 완료 (v0.0.41)
- [x] Stage D 2단계(퍼센타일 배치 집계) + 로또 통계기반 추천 코드 완료 (v0.0.42)
- [ ] **다음 세션 시작점 ★**: Cloudflare Pages 프로젝트(`testseries1`) 환경변수에 `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`(Supabase 대시보드 Settings→API에서 secret/service_role 키 확인)/`CRON_SECRET`(임의 문자열 새로 생성) 등록 필요 — Claude에게 값을 절대 공유하지 말고 Cloudflare 대시보드에 직접 입력할 것. 같은 `CRON_SECRET` 값을 GitHub 저장소 Settings→Secrets and variables→Actions에도 등록해야 `.github/workflows/percentile-cron.yml`이 정상 동작. 둘 다 등록되면 `POST /api/percentile-refresh` 수동 호출로 실제 동작 검증, GitHub Actions 수동 실행(workflow_dispatch)으로 cron 경로도 검증 필요. `/api/lotto-stats`는 시크릿 불필요하니 배포 직후 바로 `curl`로 응답 확인 가능
- [x] (제외) 두뇌나이 모바일 터치 지연 보정 — 사용자가 "나중에 문제되면 작업하자"며 우선순위에서 제외 결정. 완전 폐기 아님, PRD 체크리스트에는 여전히 필수 항목으로 남아있음
- [ ] (보류) 댓글 백엔드 연동 — 모더레이션 정책(신고/금칙어 필터) 설계 없이는 착수 안 하기로 결정, 사용자가 "나중에라도 작업하자"고 재확인(취소 아님)
- [ ] (보류) Stage E(동물비유카드, 이제 D의 퍼센타일 집계가 완료됐으니 재검토 가능) / Stage G(라이트모드, 프로젝트 전체 최후 고정)
- [ ] QA 테스트로 남은 `test_results` row(user_id `9fd10300...`) 정리는 사용자 판단에 맡김(필요시 SQL Editor에서 직접 delete)

---

## 세션 #7 — 2026-07-03

### 현재 버전: v0.0.39

### 작업 내용
- 세션 시작 시 사용자가 "어디까지 진행했었는지" 재확인 요청 → git 상태(clean, origin과 동기화)와 HISTORIC_LOG.md 세션 #6 마지막 메모를 근거로 v0.0.33까지 전부 커밋·푸시 완료 상태임을 확인, 남은 선택지(백로그 5순위 / 뒤로가기·홈 버튼 / Stage D 재확인)를 사용자에게 제시.
- **v0.0.34 (테스트 화면 뒤로가기/홈 버튼)**: 사용자가 v0.0.33에서 후순위로 미뤄뒀던 항목을 진행 요청. 착수 전 배치 방식(고정 플로팅 vs 화면 상단 인라인)과 뒤로가기 동작(이전 화면 이동 vs 무조건 홈)을 AskUserQuestion으로 확인받아 둘 다 추천안으로 결정. `index.html`에 홈을 제외한 전 섹션에서 노출되는 우하단 고정 플로팅 버튼(`#floating-nav` — ◀ 뒤로가기 `#floating-back-btn` / 🏠 홈 `#floating-home-btn`) 마크업 1곳 추가. `app.js`의 핵심 `navigate()` 메서드에 `_sectionHistory` 스택을 추가해 섹션 전환마다 이전 섹션을 기록하고, `goBack()`이 스택에서 pop해 "직전에 있던 화면"으로 복귀하도록 구현 — 무조건 홈이 아니라 테스트→테스트 직접 이동 시에도 원래 왔던 화면으로 돌아감. 딥링크(공유 링크 등)로 테스트 화면에 바로 진입해 스택이 비어있는 경우엔 뒤로가기 시 홈으로 폴백. 15개 이상 테스트 섹션 HTML을 개별 수정하지 않고 `navigate()`/`goBack()` 두 함수와 버튼 클릭 리스너 한 곳에만 로직을 추가해 구현(v0.0.33 칭호/레벨 시스템 때와 동일한 "공통 진입점 한 곳만 수정" 패턴 재사용).
  - **검증**: 캐시된 npx playwright(Chromium)로 6가지 시나리오 전부 확인 — ①홈 화면에서 `#floating-nav`가 `hidden`인지 ②MBTI 진입 시 버튼 노출 ③🏠 버튼 클릭 시 홈으로 이동 후 버튼 재차 숨김 ④MBTI→두뇌나이로 직접 이동 후 ◀ 클릭 시 두뇌나이가 아닌 MBTI(직전 화면)로 복귀 ⑤`#brain` 딥링크로 새로고침 진입 시 버튼이 즉시 노출되고 ◀ 클릭 시 홈으로 폴백 ⑥전 과정 콘솔 에러 없음. 6개 전부 통과.
- 사용자가 이어서 "전체적으로 콘텐츠가 부족한 쪽을 검토해 아이디어를 달라"고 요청 → `data.js`/`app.js`를 직접 훑어 콘텐츠 볼륨을 실측 조사(아래 "콘텐츠 갭 분석" 섹션 및 다음 세션 작업 리스트 참고). 아직 실제 콘텐츠 확장 작업은 미착수 — 사용자에게 우선순위 확인 후 진행 예정.

- **v0.0.35 (오늘의 운세 실제 일별 로테이션 착수 — 샘플 배치)**: 사용자가 콘텐츠 갭 분석 중 ①번(오늘의 운세)을 "거짓으로 말고 진짜로" 고도화해달라고 요청 → 목표 개수(띠×카테고리 조합당 variant 몇 개)와 진행 방식을 AskUserQuestion으로 확인, **7개(추천, 총 336문장 목표)** + **배치로 나눠 샘플 먼저 승인받는 방식(추천)** 둘 다 채택. `fortuneData`의 `base`(카테고리 4개 고정 문장) 구조를 `categories: [{title, variants:[]}]`로 전면 개편(기존 문구는 항상 `variants[0]`으로 보존 — 무손실 마이그레이션). `app.js`의 `renderFortuneView('result')`가 `dailyQuoteSeed()`(오늘의 한마디와 동일한 24시간 KST 시드) + 띠 인덱스 + 카테고리 인덱스 조합으로 매일 다른 variant를 결정론적으로 선택하도록 변경 — 부수적으로 별점 계산도 기존 `todaySeed()`(로컬 디바이스 타임존 기준이라 KST와 자정 경계가 미묘하게 어긋날 수 있던 기존 버그성 코드)에서 동일한 KST 시드 체계로 통일해 일관성 개선. **샘플 배치로 12띠 중 쥐·소 2개만 먼저 7벌씩 완성**(총 56문장, 쥐: 부지런함/분석력/절약 톤, 소: 성실함/신뢰/꾸준함 톤 — 기존 문구의 캐릭터성을 유지하며 확장), 나머지 10띠(호랑이/토끼/용/뱀/말/양/원숭이/닭/개/돼지)는 이번 버전에서 구조만 변환하고 문구는 기존 1벌 그대로 유지(다음 배치에서 순차 확장 예정).
  - **검증**: Node 스크립트로 12띠×4카테고리 전부에 `categories`/`variants` 구조와 `positive`/`detail` 필드 누락 여부를 기계 검증(전부 통과). Playwright로 쥐띠(variants 7개, 정상 렌더링) 결과 화면과 호랑이띠(variants 1개, 아직 미확장 상태) 결과 화면 둘 다 인덱스 아웃오브바운드 없이 정상 동작함을 확인, `dailyQuoteSeed()`/`seededRandom()` 선택 로직이 시드값에 따라 실제로 다른 인덱스를 반환함을 별도 확인, 콘솔 에러 없음.
  - **다음 배치 예정**: 나머지 10띠도 순차적으로 7벌씩 채워야 336개 목표 달성. 진행 속도는 "오늘의 한마디" 때처럼 세션 진행하며 배치 크기를 조율할 것.
- **v0.0.36 (오늘의 운세 나머지 10띠 확장 — 336개 목표 완전 달성)**: 사용자가 "계속 진행해줘"로 승인 → 나머지 10띠(호랑이/토끼/용/뱀/말/양/원숭이/닭/개/돼지)를 한 번에 전부 7벌씩 작성해 확장(쥐·소 때와 달리 이번엔 배치를 더 쪼개지 않고 한 번에 완료 — 톤이 이미 승인된 상태였고 나머지는 동일 패턴 반복이라 판단). 각 띠의 기존 문구(v1)에 담겨 있던 캐릭터성을 그대로 살려서 확장: 호랑이=대담함/결단력, 토끼=섬세함/감수성, 용=리더십/존재감/스케일, 뱀=통찰력/신중함/침착함, 말=자유로움/즉흥성/활동성, 양=평화/배려/조화, 원숭이=재치/유머/순발력, 닭=꼼꼼함/완벽주의/규칙성, 개=충성심/신뢰/책임감, 돼지=풍요/너그러움/낙천성.
  - **검증**: Node 스크립트로 12띠×4카테고리 전부 정확히 7벌씩(총 336개) 채워졌는지, `positive`/`detail` 필드 누락이 없는지, 같은 띠-카테고리 내에서 `positive` 문구가 중복되지 않는지까지 기계 검증(전부 통과). Playwright로 쥐·호랑이 외 추가로 용띠(2000년생)/닭띠(2005년생)/말띠(1990년생)/돼지띠(2007년생) 4개를 실제 렌더링해 undefined/NaN 노출 없이 4개 카테고리 카드가 정상 표시됨을 확인, 콘솔 에러 없음.
  - **"오늘의 운세 실제 일별 로테이션" 작업이 이번 버전으로 전체 완료됨.**
- **v0.0.37 (Tier 등급 코멘트뱅크 확장 — ②번 완료)**: 사용자가 "②번은 뭔말이야 자세히 풀어서 설명해줘봐"라고 물어봐서, 실제 코드(`tierMsg = '...'` 패턴)를 예시로 들어 "문제는 절차적으로 랜덤 생성되는데 결과 코멘트는 등급당 딱 1줄이라 재도전해도 항상 같은 문구가 뜬다"는 걸 구체적으로 설명 → "진행하자" 승인받고 착수. 공용 헬퍼 `pickOne(arr)`를 `app.js`의 `seededRandom()` 바로 아래에 신설. 두뇌나이는 `tierMsg` 객체 값들을 배열로 바꿔 `pickOne(tierMsgBank[tier])`로, 나머지 7개(반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력)는 if/else 분기의 `tierMsg` 변수를 배열로 바꾸고 분기 종료 직후 `tierMsg = pickOne(tierMsg);` 한 줄만 추가하는 방식으로 구현 — 변수명을 그대로 유지해서 `shareText`나 화면 표시 코드는 전혀 손대지 않고 재사용됨(각 테스트마다 딱 1줄씩만 추가하면 되는 최소 침습 설계). 등급당 1개→3개로 확장해 8테스트×5등급×3개 = 총 120개 문구 완성(기존 문구는 항상 배열 첫 번째로 보존), 각 테스트의 기존 유머 소재/톤을 유지해서 새 문구 작성(예: 반응속도=교통/스포츠 비유, 숏폼집중력=알고리즘/도파민 소재).
  - **검증**: Node로 8개 테스트 전부 배열/객체 형태로 정상 치환됐는지, 단독 문자열 대입이 남아있지 않은지 확인 + app.js 파싱 에러 없음 재확인. Playwright로 `logic`/`reaction` 두 테스트를 각각 동일한 고정 입력(S등급 조건)으로 결과 화면을 15회 반복 렌더링해 매번 다른 코멘트 문자열이 등장함을 실제로 확인(우연이 아니라 진짜 랜덤 픽 로직이 동작함을 증명), 8개 테스트 섹션 전체 진입까지 콘솔 에러 없음 확인.
- **v0.0.38 (콘텐츠 갭 ③번 — 속담완성/물가퀴즈 대폭 확장, 방향 전환)**: 사용자에게 ③번(문항 순서 랜덤화)을 설명하자 "이 컨텐츠 양을 지금의 5배 이상으로 늘리자, 재미를 줘야 될 것 같다, 한두 번 해보고 끝나는 게 아니라 계속 하고 싶게 해야 된다"는 훨씬 큰 요청으로 확장. Claude가 "단순히 기존 10문항 순서만 섞는 것보다, 풀 자체를 키우고 매판 그 풀에서 랜덤 10문항을 뽑는 방식이 재방문성에 더 강력하다"고 제안, AskUserQuestion으로 ①목표 풀 크기(속담 100개/물가 50개 — 물가는 실제 통계 확인이 필요해 보수적으로, 사용자가 "속담은 더 많이, 물가는 적게" 선택) ②한 판 문항 수(10문항 유지하고 풀에서 랜덤 추출, 추천안 채택) 확인 후 착수.
  - **코드 변경**: `app.js`의 `proverbStart()`/`pricequizStart()`에 `shuffleArray(AppData.xxxQuestions).slice(0, 10)`을 추가해 시작할 때마다 전체 풀에서 10문항을 새로 뽑아 `state.questions`에 저장, `renderXxxView`(question/result)·`xxxAnswer`·`xxxAdvance` 전체(2테스트×4곳=8곳)의 `AppData.xxxQuestions` 직접 참조를 `state.questions`로 교체 — 이제 "몇 번째 문제"가 아니라 "이번 판에 뽑힌 10개 중 몇 번째"를 가리키도록 통일.
  - **속담 콘텐츠(10→100개)**: 신규 90개는 초등 교과서 수준으로 널리 알려진 표준 한국 속담만 선정해 정확성 리스크를 최소화(Claude 자체 지식 기반, 애매하거나 지역/세대별로 다르게 전해지는 속담은 배제). 기존 10개와 동일한 포맷(front/correct/decoys 3개) 유지.
  - **물가퀴즈 콘텐츠(10→50개)**: "추측 금지, 실제 통계만" 원칙(v0.0.21 때부터의 기존 방침)을 그대로 유지 — WebSearch로 13개 카테고리(짜장면/버스/라면/소주/담배/영화관람료/택시/지하철 기존 카테고리의 다른 연도 + 커피자판기/새우깡/초코파이/롯데리아/우표 5개 신규 카테고리)를 각각 검색, 나무위키·경향신문·아시아경제·서울시 공식 물가정보·우정사업본부 공식 우편요금표 등 복수 출처로 교차 확인한 뒤 40개를 확정(1971~2023년). 검색 결과가 범위로만 나오거나("1990년대 500원대") 단일 출처만 있어 불확실한 항목은 채택하지 않고 연도가 명시된 확실한 데이터만 사용 — 예를 들어 서울우유 흰우유·PC방 요금·신문구독료·목욕탕 요금은 검색했지만 신뢰할 만한 연도별 데이터를 찾지 못해 최종 목록에서 제외.
  - **검증**: Node로 속담 100개(중복 없음, 필드 완결성)·물가 50개(중복 없음, 필드 완결성) 기계 검증 전부 통과. Playwright로 두 테스트 모두 시작할 때마다 다른 10문항 조합이 나오는지(3회 재시작해 첫 문항 비교) 확인, 물가퀴즈는 실제 클릭으로 10문항 전체를 완료해 결과화면·정답확인 리캡 10개까지 정상 동작 확인.
  - **"속담완성/물가퀴즈 콘텐츠 확장" 작업이 이번 버전으로 완료됨 — 콘텐츠 갭 분석에서 나온 3개 후보(①오늘의 운세 ②Tier 코멘트뱅크 ③속담완성/물가퀴즈) 전부 완료.**
- **v0.0.39 (꿈해몽 variants 잔여 28개 완료)**: 사용자가 "꿈해몽 잔여 내용 업데이트하자 빠르게"라고 요청 — v0.0.32에서 "정식 배포 전 콘텐츠 보강 단계"로 미뤄뒀던 항목. `dreamData` 74개 테마 중 variants가 없는 28개(호랑이/용/개/고양이/새/다리/산/계단/엘리베이터/지진/번개천둥/눈/꽃/나무/과일/음식/술/우는꿈/웃는꿈/옷분실/신발분실/휴대폰분실/길잃음/감옥/병원/수술/별/달)를 Node 스크립트로 먼저 정확히 식별한 뒤, 각 테마 2개씩 variants를 작성(총 56개). 이미 확립된 46개 테마의 톤·포맷을 그대로 따라감 — 예를 들어 호랑이 테마의 기존 summary("강한 힘과 명예의 상징")를 이어받아 "호랑이에게 쫓기는 꿈"(부담감), "호랑이를 타는 꿈"(권력·기회 획득) 두 하위 꿈으로 세분화하는 식. 74개 테마 전체가 variants를 보유하게 되어 목표 완전 달성.
  - **검증**: Node로 74개 테마 전부 `variants.length > 0`인지, 각 variant의 6개 필드(keywords/title/detail/lucky/luckyNum/action)가 모두 채워졌는지, 테마+variant를 통틀어 제목 중복이 없는지(146개 variant 전부) 기계 검증 — 전부 통과. Playwright로 이번에 새로 추가한 3개 테마(호랑이/달/별)를 검색→클릭→모달 오픈→변형 칩 클릭까지 실제 구동해 확인, 기존에 이미 variants가 있던 테마(뱀, 4개 변형 유지)도 회귀 없이 정상 동작함을 재확인, 콘솔 에러 없음.
  - **"D 무관 백로그" 중 마지막으로 남아있던 콘텐츠 보강 항목이 이번 버전으로 전체 완료됨.**

### 콘텐츠 갭 분석 (조사만 완료, 실행 전)
- **오늘의 운세(fortune)**: `fortuneData`의 띠별 `base`(총운/재물운/애정운/건강운 4개 카테고리 텍스트)가 12띠 전부 **완전 고정** — `renderFortuneView('result')`에서 별점(★ 점수)만 `todaySeed()` 기반으로 매일 바뀌고, 실제 문구는 하루 1개씩만 있어 며칠만 재방문해도 똑같은 텍스트를 보게 됨. "오늘의"라는 브랜딩과 실제 콘텐츠 양(띠당 4문장, 총 48문장)의 괴리가 가장 큼 — 홈 화면 최상단 노출 기능이라 체감 임팩트도 큼.
- **Tier 등급 코멘트뱅크(8개 게임형 테스트: 두뇌나이/반응속도/숫자기억/순서기억/색각/논리력/충동억제/숏폼집중력)**: 각 테스트마다 S/A/B/C/D 등급별로 **정확히 1개**의 고정 유머 코멘트만 존재(총 8×5=40개 문구). 문제 자체는 절차적으로 생성돼 매번 다르지만, 결과 코멘트는 같은 등급을 받을 때마다 토씨 하나 안 틀리고 반복됨 — 친구 대결/오늘의 챌린지로 재방문을 유도하는 구조와 맞물려 반복 노출 빈도가 높음.
- **속담 완성 퀴즈 / 그 시절 물가 맞히기**: 각각 `proverbQuestions`/`priceQuizQuestions` **10문항 고정, 순서도 고정**(`questions[state.step]`으로 인덱스 순차 접근, 선택지 4개만 `shuffleArray`로 섞임) — 재도전해도 완전히 동일한 10문항이 동일한 순서로 나옴. 어르신향 "가족 대화 소재"로 설계된 기능이라 반복 방문 시 신선함이 특히 중요한데 반복 방지 장치가 전혀 없음.
- (참고용, 이미 CLAUDE.md에 백로그로 기록됨— 재조사 아님) 꿈해몽 74개 테마 중 28개는 아직 variants 없음.
- MBTI(famous 필드 v0.0.32에서 이미 검수 완료)·ADHD·인싸력(10문항, 표준 심리검사 문항 수라 정상)은 이번 조사에서 특별한 결함 발견되지 않음.

### 다음에 이어서 해야 할 작업 리스트 (갱신)
- [x] 테스트 화면 뒤로가기/홈 버튼 완료 (v0.0.34)
- [x] 오늘의 운세 고도화 — 구조 개편 + 쥐·소 2띠 7벌 샘플 완료 (v0.0.35)
- [x] **오늘의 운세 나머지 10띠 확장 — 336개 목표 완전 달성** (v0.0.36)
- [x] **Tier 등급 코멘트뱅크 확장 — 8개 테스트 × 5등급 × 3개(총 120개) 완료** (v0.0.37)
- [x] **속담완성 100개 + 물가퀴즈 50개 확장, 매판 랜덤 10문항 추출 방식 전환 완료** (v0.0.38) — 콘텐츠 갭 분석 3개 후보(①오늘의 운세 ②Tier 코멘트뱅크 ③속담완성/물가퀴즈) 전부 완료
- [x] **꿈해몽 variants 잔여 28개 완료 — 74개 테마 전체 variants 보유** (v0.0.39)
- [ ] **다음 세션 시작점 ★**: 사용자가 D(백엔드) 착수 의사를 밝힘 — **Supabase 사용 확정**. 아직 실제 구현은 시작 안 함, Claude가 사전 준비사항(스키마 설계 방향, 인증 방식, 어떤 기능부터 마이그레이션할지, 로컬스토리지→서버 전환 전략, 카카오공유 동시 진행 여부 등)을 먼저 정리해 사용자에게 설명하기로 함. CLAUDE.md "진행 중 결정사항 로그"에 있던 기존 합의(D는 "실제 운영 단계 진입 시" 착수, Supabase+Cloudflare R2 언급)를 참고해서 구체화할 것. 이 세션에서 설명까지 하고 실제 코드 작업은 다음 세션 이후로 예상.
- [ ] (백로그) 5순위: 카카오톡 공유(Kakao Developers 키 발급 필요, D와 함께 진행 예정이었음) / 로또 통계기반 추천(Cloudflare Functions 구축 필요)
- [ ] (보류) Stage E(동물비유카드, D 의존) — D 착수하면 재검토
- [ ] (보류) Stage G(라이트모드, 프로젝트 전체 최후 고정)
- [ ] (백로그) 5순위: 카카오톡 공유(Kakao Developers 키 발급 필요) / 로또 통계기반 추천(Cloudflare Functions 구축 필요)
- [ ] (백로그) 테스트 화면 뒤로가기/홈 버튼과 함께 후순위였던 항목 없음(이번에 완료)
- [ ] (보류) Stage D 백엔드 — 실제 운영 단계 진입 여부 계속 리마인드할 것
- [ ] (보류) Stage E(동물비유카드, D 의존), Stage G(라이트모드, 프로젝트 전체 최후 고정)
- [ ] (백로그) 꿈해몽 variants 잔여 28개 — "정식 배포 전 콘텐츠 보강 단계"로 순위 완전히 뒤로 미뤄진 상태(v0.0.33 결정 유지)
- [ ] (백로그) 미스터리박스, 시즌테마 — 보류/제외 상태 유지

---

## 세션 #6 — 2026-07-03

### 현재 버전: v0.0.33

### 작업 내용
- 세션 시작 시 사용자에게 진행 상황을 재확인 요청받음 → Stage B(신규 인지테스트 10종)가 v0.0.21로 전체 완료되어 있음을 확인. 다음 우선순위(오늘의 한마디 개편 vs Stage C 난이도체계)를 사용자에게 질문해 **오늘의 한마디 개편을 먼저 진행**하기로 결정.
- **v0.0.22 (오늘의 한마디 개편 — 1단계 착수)**: 사용자가 상세 스펙을 프롬프트로 제공(주제 2갈래/인물 카테고리/인용 정확도 기준/데이터 구조/700개 목표/단계적 진행/12시간 순환 로직). PRD.md 9-4의 기존 스펙(200개, 날짜순 순환)을 이번 지시대로 갱신.
  - 1단계로 20개 샘플(철학자 5·동양사상가 4·투자자 3·과학자 4·작가 4)을 작성해 사용자에게 먼저 제시 — 인용은 위키피디아/전기/원전에서 널리 검증된 것만 선정(예: 아인슈타인 "삶은 자전거를 타는 것과 같다"는 실제 1930년 아들에게 보낸 편지에서 확인된 문구를 사용하고, 출처 불확실한 "같은 일을 반복하며 다른 결과를 기대하는 건 미친 짓" 같은 인터넷 밈 명언은 제외함). 번역 문구는 기존 번역을 베끼지 않고 새로 표현.
  - 사용자 승인 확인 후 실제 구현: `data.js`의 `quotes`를 문자열 배열 → `{ text, author, role }` 객체 배열로 구조 변경(20개 반영). `app.js`에 `halfDaySeed()` 함수 신설(UTC ms에 KST 보정 +9시간 후 12시간 단위로 버킷화) 및 `initHome()`을 새 구조에 맞게 수정(작성자/역할 표시 엘리먼트 추가, 복사 버튼에 출처 포함). `index.html` 섹션 제목을 "오늘의 폼 미친 한마디" → "오늘의 인생 한마디"로 변경하고 `#home-quote-author` 엘리먼트 추가.
  - **검증**: 캐시된 npx playwright 설치본을 사용해 헤드리스 브라우저로 홈 화면 스크린샷 확인 — 인용문+저자/역할 정상 렌더링, 콘솔 에러 없음. `halfDaySeed()` 로직은 Node 스크립트로 별도 검증(같은 12시간 버킷 내에는 동일 인덱스, 버킷이 바뀌면 인덱스도 바뀜을 확인).
  - PRD.md 9-4, CLAUDE.md 기능현황표(오늘의 한마디 항목을 백로그→진행중으로 이동, 20/700 표기)·데이터구조 설명·변경이력 갱신.
- **v0.0.23 (오늘의 한마디 개편 — 2단계 배치 #1)**: 명언 36개를 추가로 조사·작성해 20개→56개로 확장. 서양 철학자 14명(플라톤/데카르트/스피노자/칸트/쇼펜하우어/키르케고르/사르트르/카뮈/러셀/밀/로크/흄/아렌트/보부아르), 동양 사상가 4명(장자/왕양명/손자/간디), 경제학자 1명(애덤 스미스), 작가 8명(타고르/도스토옙스키/카프카/울프/오웰/헤밍웨이/트웨인/괴테/위고 — 실제론 9명), 과학자 4명(뉴턴/보어/다윈/호킹), 심리학자 1명(융), 투자자 3명(그레이엄/템플턴/린치) 추가. **정확도 검증 과정에서 유명하지만 오귀속·출처불명으로 확인된 명언들을 의도적으로 배제**함 — 아인슈타인의 "미친 짓" 정의(반복+다른결과 기대), 다윈의 "가장 강한 종이 살아남는 게 아니라" 정확 문구(1963년 경영학자 메긴슨의 다윈 사상 요약이 명언화된 것), 간디의 "네가 보고 싶은 변화가 되어라" 정확 문구(간디 저작에 원문 없음), 아리스토텔레스의 "우리는 반복적으로 행하는 것에 의해 만들어진다"(실제로는 윌 듀런트가 니코마코스 윤리학을 요약한 문장), 괴테의 "결단을 내리는 순간 온 우주가 돕는다"류 명언(W.H. 머레이의 글이 잘못 붙은 것), 갈릴레오의 "그래도 지구는 돈다"와 "가르칠 수 없다, 발견하도록 도울 뿐" 두 명언(모두 후대에 지어진 것으로 추정), 마크 트웨인의 "20년 후엔 한 일보다 안 한 일을 후회할 것" 명언(Quote Investigator에서 출처 불명 판정), 프로이트의 "표현되지 않은 감정은 죽지 않는다" 명언(원전 확인 불가) — 대신 각 인물의 실제 저작(편지·논문·연설·자서전)에서 확인 가능한 다른 문구로 대체하거나 해당 인물 자체를 이번 배치에서 제외함.
  - **검증**: 캐시된 npx playwright로 홈 화면 재확인 — 새로 추가된 명언(예: 버트런드 러셀)이 정상 노출, 콘솔 에러 없음. Node 스크립트로 `data.js`를 직접 require해 전체 quotes 배열 길이(56)와 author 중복 여부(없음)를 기계적으로 검증.

- **v0.0.24 (오늘의 한마디 개편 — 2단계 배치 #2)**: 사용자가 "장기작업처럼 느껴지니 배치를 키워서 빨리 진행해달라"고 요청 → 배치 크기를 키워 60개를 한 번에 추가(56→116개). 서양 철학자/경제학자 15명, 동양 사상가(한국 인물 다수 포함 — 원효대사/법정스님/신영복/정약용/이황/이이/최제우/김구/안창호/함석헌) 15명, 작가/시인 15명, 과학자 9명, 투자자 4명, 인권운동가 2명(마틴 루터 킹/넬슨 만델라, 간디 사례를 따라 "사상가"로 분류)을 추가. 이번에도 정확도 검증을 계속 유지 — 볼테르의 "관용" 명언(실제로는 전기작가 이블린 홀의 창작), 오스카 와일드 "자기 자신이 되어라", 에머슨의 "성공" 정의 명언, 조지 엘리엇 "늦은 때는 없다", 앨런 튜링의 영화 대사(실제 발언 아님), 넬슨 만델라 "나는 결코 지지 않는다" 버전 등을 확인 후 배제하고 각자의 실제 저작/연설/자서전에서 확인되는 문구로 대체.
  - **검증**: Node로 `data.js` 직접 require해 전체 116개 배열 길이·author 중복(없음)·필드 누락(없음) 기계 검증. 캐시된 npx playwright로 홈 화면 재확인, 새 인물(파스칼) 정상 노출, 콘솔 에러 없음.

- **v0.0.25 (오늘의 한마디 개편 — 2단계 배치 #3)**: 사용자가 "그렇게 오래 걸리는 작업이냐, 후딱 해달라"고 재차 요청 → 새 인물 21명(고대 그리스 철학자 헤라클레이토스/제논/디오게네스, 시몬 베유/비트겐슈타인/아우구스티누스, 찰스 디킨스/제인 오스틴, 한국 근대 시인 윤동주/한용운/김소월/이육사/이상, 무라카미 하루키/솔제니친/프리모 레비/T.S.엘리엇, 오펜하이머/암스트롱/그레이스 호퍼, 투자자 앙드레 코스톨라니) 추가 + 이미 검증된 인물(괴테/아리스토텔레스/오웰/아인슈타인/버핏/세이건/템플턴/케인즈)의 2번째 어록 8개 추가, 총 29개(116→145개). 새로운 인물 발굴이 점점 어려워지므로 이번 배치부터 "1인당 최대 2개 어록" 원칙을 본격 활용하기 시작.
  - **검증**: Node로 전체 145개 배열 길이·author별 개수(2개 초과 없음)·필드 누락(없음) 기계 검증. 캐시된 npx playwright로 홈 화면 재확인, 콘솔 에러 없음.

- **v0.0.26 (오늘의 한마디 개편 — 방향 재조정 + 배치 #4)**: 145/700에서 사용자가 "이 페이스로 700개까지 가는 게 비현실적으로 오래 걸리지 않냐"고 재차 문제 제기 → Claude가 "제 지식만으로 검증 가능한 고신뢰도 인물+어록 조합에는 현실적 한계가 있다"고 솔직히 알리고 진행 방식을 질문. 사용자가 최종 결정: **①목표를 700→365개로 하향(하루 1개 × 1년 분량)** ②**로테이션을 12시간→24시간(하루) 단위로 변경** ③**인물 소재가 바닥나면 삶/멘탈 관련 속담·격언도 포함**. 이에 따라 `app.js`의 `halfDaySeed()`를 `dailyQuoteSeed()`로 교체(24시간 KST 버킷). 실행 내용: 인물 21명 추가(데즈먼드 투투/치누아 아체베/칼릴 지브란/루미/오마르 하이얌/파울로 코엘료/비베카난다/미야모토 무사시/후쿠자와 유키치/조너스 소크/나이팅게일/매슬로/칙센트미하이/칼 로저스/안중근/갤브레이스 등, 145→166) + **처음으로 속담·격언을 대량 투입**(한국 50개/중국 21개/일본 3개/영미 15개/탈무드 8개/아프리카 10개/인도 8개/아랍 8개/독일 7개/프랑스 7개/스페인 7개 = 144개, 166→310). 속담은 특정 인물에게 잘못 귀속시킬 위험이 없어 인물 어록보다 검증 부담이 훨씬 낮음 — 문화권 단위로 널리 알려진 것만 사용.
- **v0.0.27 (오늘의 한마디 개편 — 완료)**: 마지막 배치로 속담·격언 55개 추가(러시아 10/아일랜드 8/북유럽 7/몽골 6/베트남 6/라틴 8/그리스 10)해 **목표 365개를 정확히 달성**(310→365). 최종 구성: 검증된 인물 어록 약 166개(1인당 최대 2개) + 세계 각국 속담·격언 약 199개.
  - **검증**: Node 스크립트로 `data.js`를 직접 require해 전체 365개 배열의 `text` 필드 완전 중복 없음, 필드 누락 없음을 기계적으로 재확인. 캐시된 npx playwright로 홈 화면 최종 스크린샷 촬영 — 인용문+저자/역할 정상 렌더링, 사이드바 버전 표기(v0.0.27)까지 정상 반영, 콘솔 에러 없음.
  - **Stage B 이후 착수한 "오늘의 한마디 개편" 작업이 이번 버전으로 전체 완료됨.**
- **v0.0.28 (Stage C: 난이도/모드 체계 개편 — 완료)**: 사용자가 "이어서 진행하되, 어떻게 바꿀지 먼저 디테일하게 설명해달라"고 요청 → Explore 서브에이전트로 두뇌나이(스트룹)/MBTI/ADHD 3개 기능의 현재 코드를 먼저 조사(각 함수 위치, 데이터 구조, 채점 로직 파악). 조사 결과를 바탕으로 상세 변경안을 사용자에게 설명하고, 3가지 열린 결정사항(①스트룹 어려움 단계에 6번째 색상 추가 여부 ②MBTI 정밀 문항수/축 비율바 추가 여부 ③ADHD 정밀 모드를 2개 하위영역으로 분리할지 여부)을 AskUserQuestion으로 확인받은 뒤(모두 추천안으로 승인) 구현 착수.
  - **두뇌나이(스트룹)**: 기존엔 "쉬움/어려움" 버튼이 있어도 실제로는 색상 개수(4/5색)만 다르고 문항수(20 고정)·제한시간(4초 고정)이 동일했던 버그성 설계를 이번에 발견해 함께 수정. `STROOP_COLORS`에 `medium`(5색, 보라 추가) 신설, `hard`를 6색(주황 추가)으로 강화. 새 `STROOP_CONFIG` 객체로 난이도별 문항수(15/20/25)·제한시간(5000/4000/3000ms)을 관리하도록 전역 상수(`TOTAL_QUESTIONS`,`TIME_LIMIT`)를 상태 기반 값으로 전면 교체. `style.css`에 `.stroop-orange` 클래스 추가.
  - **MBTI**: 간단(기존 12문항 그대로)/정밀(24문항) 2모드 추가. `data.js`에 정밀 모드 전용 추가 12문항(`mbtiQuestionsExtra`, 축당 3문항씩)을 새로 작성 — 기존 12문항과 톤을 맞춰 "회식 자리", "쇼핑할 때" 등 일상 시나리오로 구성. `app.js`는 `mbtiStart(mode)`로 시그니처 변경, `state.questions`에 모드별 문항 배열을 담아 렌더링/채점 모두 이 배열 기준으로 동작하도록 리팩터링. 정밀 모드 결과 화면엔 E/I, S/N, T/F, J/P 4개 축의 비율(%) 바를 추가 — 답변 개수만 세면 계산되므로 16개 MBTI 타입 결과 데이터(강점/도전과제/팁/유명인)는 전혀 손대지 않고 간단/정밀 공용으로 재사용.
  - **ADHD**: 간단(기존 10문항 그대로)/정밀(20문항, 2개 하위영역) 2모드 추가. `data.js`에 `adhdQuestionsPrecise`(20문항, 각 문항에 `domain:'inattention'|'hyperactivity'` 태그) 신규 작성 — DSM-5의 부주의/과잉행동-충동성 구조를 참고했으나 결과 화면의 "임상 진단 아님" 안내문구는 계속 유지. 응답 척도도 정밀 모드에서는 진짜 4단계(전혀0/가끔1/자주2/항상3)로 세분화(간단 모드는 기존 2단계에 가까운 척도 그대로 둠). 결과 화면에 부주의/과잉행동-충동성 두 영역 점수 바 추가. 종합 등급 산정 시, 정밀 모드 합산점수(문항당 최대3점×20문항=최대60점)를 3으로 나누면 정확히 기존 20점 만점 등급표 스케일과 일치한다는 점을 이용해 새 등급 데이터를 만들지 않고 기존 `adhdResults`를 그대로 재사용하도록 설계.
  - **검증**: 캐시된 npx playwright로 3개 기능 모두 실제 플로우 구동 — 스트룹 어려움 모드에서 색상 버튼 6개 정상 노출, MBTI 정밀 모드 24문항 진행 후 결과 화면에 축별 비율 바 정상 표시(스크린샷으로 ISTJ 100%/100%/100%/100% 편향 케이스 확인 — 의도적으로 한쪽만 응답한 테스트라 정상), ADHD 정밀 모드 20문항 진행 후 부주의/과잉행동-충동성 각 20/30점, 종합 등급 D(40/60점) 정상 계산 확인. 콘솔 에러 없음. (참고: 이 세션 도중 Bash/ScheduleWakeup 도구가 "claude-sonnet-5 temporarily unavailable" 이유로 잠시 막혔던 적이 있었음 — 읽기 전용 도구로 대기하다 재시도하니 정상 복구됨. 향후 유사 상황 시 참고)

### 다음에 이어서 해야 할 작업 리스트 (갱신)
- [x] 오늘의 한마디 개편 1단계 (샘플 20개 + 데이터구조/로테이션 로직 구현) 완료 (v0.0.22)
- [x] 오늘의 한마디 개편 2단계 배치 #1 (36개 추가, 20→56개) 완료 (v0.0.23)
- [x] 오늘의 한마디 개편 2단계 배치 #2 (60개 추가, 56→116개, 배치 크기 확대) 완료 (v0.0.24)
- [x] 오늘의 한마디 개편 2단계 배치 #3 (29개 추가, 116→145개, 1인당 2개 어록 원칙 도입) 완료 (v0.0.25)
- [x] 오늘의 한마디 개편 방향 재조정(700→365개, 12h→24h 순환, 속담 병행) + 배치 #4 (166개) 완료 (v0.0.26)
- [x] **오늘의 한마디 개편 — 365개 목표 완전 달성, 작업 종료** (v0.0.27)
- [x] **Stage C: 난이도/모드 체계 개편 — 두뇌나이 3단계/MBTI 간단·정밀/ADHD 간단·정밀 전부 완료** (v0.0.28)
- **v0.0.29 (Stage F: 종합 인지 프로필 레이더차트 — 완료)**: 사용자가 "D/E/F/G 우선순위를 네가 정해봐, 대신 라이트모드(G)는 프로젝트 전체를 통틀어 제일 마지막"이라고 요청. Claude가 우선순위를 결정: PRD.md에 D는 "실제 운영 단계 진입 시(카카오공유와 함께) 착수, 그 시점엔 Claude가 먼저 리마인드"하기로 합의돼 있어 그 시점인지 사용자에게 AskUserQuestion으로 재확인(→ 아직 아님) → D 계속 보류, D에 의존하는 E도 자연히 보류, 백엔드가 필요 없는 F(레이더차트)를 먼저 착수하기로 결정.
  - 구현 전 설계를 먼저 설명(대상 축 7개 선정 기준, Tier→점수 환산 방식, "5개 이상 완료" 기준을 전체 15개가 아닌 인지테스트 7개로 해석한 이유, 외부 라이브러리 없이 순수 SVG로 구현하는 이유)하고 바로 진행.
  - 두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제 7개를 레이더 축으로 선정(숏폼집중력은 충동억제와 완전히 같은 엔진이라 중복 측정 방지 차원에서 제외). 각 축은 `localStorage.ranking_{section}`의 가장 최근 기록 문자열에서 정규식 `/Tier ([SABCD])/`로 등급만 파싱해 S=100/A=80/B=60/C=40/D=20으로 환산 — 기존 8개 테스트의 채점 로직·데이터는 전혀 건드리지 않고 순수하게 "읽기"만으로 구현.
  - `app.js`에 `RADAR_AXES`(축 정의)·`RADAR_TIER_SCORE`(환산표)·`RADAR_MIN_DONE`(=5)·`renderCognitiveRadarCard()` 신설, `renderHomeMypage()`의 "테스트 완주 현황"과 "최근 테스트 기록" 카드 사이에 삽입. 7개 중 5개 미만 완료 시엔 진행률만 보여주는 티저 카드로 대체.
  - 외부 차트 라이브러리 없이 SVG `<polygon>`(그리드 4겹+데이터 폴리곤)·`<line>`(축선)·`<text>`(라벨)를 각도 계산(삼각함수)으로 직접 그림. 색상은 하드코딩 hex 대신 `stroke-slate-600`/`fill-violet-500/25` 같은 Tailwind 유틸리티 클래스를 써서, 나중에 라이트모드(Stage G) 반영 시 CSS 변수 remap만으로 자동 대응되게 설계.
  - **버그 발견 및 수정**: 처음 구현했을 때 SVG viewBox 폭이 좁아 오른쪽/왼쪽 끝 라벨("숫자기억", "충동억제" 등)이 잘려 보이는 문제를 Playwright 스크린샷으로 발견. 중심좌표(140→190)·반지름(100→90)·라벨 배치 비율(1.22→1.25)·viewBox(280→380)를 함께 조정해 모든 라벨이 잘리지 않게 수정, 재스크린샷으로 확인.
  - **검증**: Playwright로 (1)5개 미만 완료 시 티저 카드 노출 (2)5개 이상 완료 시 SVG 레이더 정상 렌더링(폴리곤 개수 등 구조 검증) (3)7개 전부 완료 상태에서 라벨 잘림 없이 전부 표시되는 것을 스크린샷으로 확인. 콘솔 에러 없음.
- **v0.0.30 (D 착수 전 프론트엔드 개선 4건)**: 사용자가 "D(백엔드)가 필요한 항목 중 먼저 고칠 것들"이라며 4가지를 요청 — 실제로는 4개 모두 D(백엔드)와 무관한 프론트엔드/로컬스토리지 작업이었음(사용자에게도 이 점을 명확히 설명).
  - **①사이트 브랜드명 변경**: "5-in-1 대시보드/테스트 대시보드" → 제목 "과몰입 연구소", 부제 "재미로 시작했다가 뼈 맞고 공유하는 종합 테스트 모음"으로 전면 교체. `<title>`, meta description, 사이드바 로고, 모바일 헤더, 푸터 카피라이트까지 5곳 전부 반영. 파일 상단의 내부 개발용 버전 코멘트(`5-in-1 Dashboard SPA`)와 CLAUDE.md 문서 제목은 사용자가 요청한 "사이트 제목"과는 별개인 내부 명칭이라 그대로 유지(CLAUDE.md에 이 구분을 명시).
  - **②홈 "서비스 바로가기" 그리드 개편**: 코드를 열어보니 5개 카드(MBTI/꿈해몽/운세/두뇌나이/ADHD)만 있고 로또와 Stage B 신규테스트 10종이 전부 누락된 옛날 버전이었음. `initHome()`을 `toolCards`(꿈해몽/운세/로또, "🎯 바로 써먹는 도구")와 `testCards`(MBTI/두뇌나이/ADHD+신규10종, "🧪 테스트 모음")로 분리하고 `renderGrid()` 헬퍼로 공통 렌더링, index.html에 그리드 컨테이너 2개로 분리. 각 카드에 서로 겹치지 않는 그라디언트 색상 배정.
  - **③닉네임 전역 유지**: 사용자가 "이거 DB/백엔드 작업이 필요한 거냐"고 직접 질문 → **아니다**, 순수 로컬스토리지로 해결 가능하다고 답변하고 구현. 이미 홈 화면 마이홈에서 쓰던 `app_nickname`/`getNickname()`/`setNickname()`을 닉네임을 쓰는 13개 테스트(mbti/brain/adhd/reaction/memdigit/seqmem/colorvision/logic/impulse/shortfocus/insa/proverb/pricequiz) 전체에 공용 적용 — 13개 입력창 전부에 `value="${getNickname()}"` 프리필 추가(개별 Edit 13회), 각 Start 함수의 "닉네임 미입력 시 토스트" 가드문 직후에 `setNickname(nickname);`을 `replace_all`로 일괄 삽입(13곳 동시 처리, 가드문 텍스트가 13개 함수 전부 완전히 동일해 안전하게 일괄 처리 가능했음).
  - **④다크모드 기본값 재확인**: "무조건 다크모드로 시작하게" 요청 → 코드 확인 결과 이미 그렇게 동작 중이었음(`prefers-color-scheme` 감지 코드 자체가 없어서, localStorage에 테마 기록이 없는 최초 방문은 예외 없이 다크). Playwright로 완전히 새 브라우저 컨텍스트(localStorage 없음)에서 다크로 시작함을 실증한 뒤, "이미 만족 vs 매번 강제(라이트 기록 무시)" 중 어느 쪽을 원하는지 AskUserQuestion으로 확인 → 현재 동작으로 충분하다는 답변을 받아 **코드 변경 없이 종료**.
  - **검증**: Playwright로 title/사이드바명/부제 텍스트 확인, 홈 화면 두 그리드의 카드 개수(3+13) 확인, MBTI에서 닉네임 저장 후 브레인/충동억제 테스트로 이동해 닉네임이 자동으로 채워지는지 확인. 전 과정 콘솔 에러 없음.
- **v0.0.31 (D-무관 백로그 2순위: 친구 대결 모드 + 오늘의 챌린지)**: 사용자가 "D 무관 항목들 중요도 순으로 정해달라"고 요청 → Claude가 1순위(스트룹 모바일 터치 버그)~5순위(카카오공유/로또통계 등 외부연동)까지 순서를 제시했고, 사용자가 "1순위는 터치 잘 되는 것 같으니 스킵, 2순위(친구대결+오늘의챌린지)부터 진행"이라고 확정.
  - **친구 대결 모드 설계**: 별도 백엔드 없이 URL만으로 구현하기 위해, 이미 Stage F(레이더차트)에서 만든 Tier(S~D)→100/80/60/40/20점 환산 체계를 그대로 재사용하기로 결정(새 비교 로직을 만들지 않음). 대상은 Tier 채점을 쓰는 8개 테스트(두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력) — MBTI/ADHD/인싸력/속담완성/그시절물가는 "승패"개념이 안 맞아 제외.
  - **구현**: `app.js`에 공용 헬퍼 3종 신설 — `challengeFriend(section, nickname, result)`(도전장 URL 생성+공유), `renderChallengeCompareCard(myResult, challenge)`(Tier 비교해 승/패/무 카드 HTML), `renderChallengeBanner(challenge)`(도전장으로 진입했을 때 시작화면에 보여줄 배너). 도전장 URL은 `#{section}?vs=<encodeURIComponent(JSON)>` 형태 — 기존 해시 라우팅(`location.hash = sectionId`처럼 섹션 id만 쓰던 방식)에 쿼리스트링이 껴들어오면 라우팅이 깨지는 문제가 있어서, 초기 라우팅 로직에서 `rawHash.split('?')`로 섹션id와 쿼리를 분리하고 쿼리는 `App.pendingChallenge`에 임시 저장하도록 수정. 8개 테스트 각각의 `init{Test}()`에서 `App.pendingChallenge`를 소비해 `state.challenge`로 옮기고(1회성, 소비 후 즉시 null 처리), 시작화면에 배너, 결과화면에 비교카드+도전장 버튼을 삽입 — 테스트당 3곳씩 총 24개 지점을 반복 수정(닉네임 전역화 때와 비슷한 기계적 작업 패턴).
  - **오늘의 챌린지**: 오늘의 한마디(`dailyQuoteSeed()`)와 마찬가지로 `todaySeed()` 기반 결정론적 셔플로, "테스트" 13개(MBTI/두뇌나이/ADHD+신규10종) 중 매일 다른 3개를 골라 홈 화면 상단(오늘의 한마디 카드 바로 아래)에 노출. `getDailyChallengeTests()`+`renderDailyChallengeCard()` 신설, index.html에 `#home-daily-challenge` 컨테이너 추가.
  - **검증**: Playwright로 (1)홈 화면 오늘의챌린지 카드 3개 노출 및 같은 날 재호출 시 동일한 3개 반환(결정론적) 확인 (2)두뇌나이 테스트를 만점(Tier S)으로 완료 후 `shareResult`를 오버라이드해 도전장 공유 텍스트/URL을 가로채 형식 검증 (3)그 URL로 완전히 새 페이지를 열어 도전장 배너가 뜨는지 확인 (4)일부러 0점(Tier D)으로 테스트를 마쳐 "아쉬운 패배" VS 카드가 정확히 뜨는지 스크린샷으로 확인. 콘솔 에러 없음.

### 다음에 이어서 해야 할 작업 리스트 (갱신)
- [x] **Stage C: 난이도/모드 체계 개편 — 두뇌나이 3단계/MBTI 간단·정밀/ADHD 간단·정밀 전부 완료** (v0.0.28)
- [x] **Stage F: 종합 인지 프로필 레이더차트 완료** (v0.0.29)
- [x] **사이트 브랜드명 변경 + 홈 서비스그리드 개편 + 닉네임 전역 유지 + 다크모드 기본값 확인 완료** (v0.0.30)
- [x] **D-무관 백로그 2순위: 친구 대결 모드(8개 테스트) + 오늘의 챌린지 완료** (v0.0.31)
- **v0.0.32 (D-무관 백로그 3순위: 콘텐츠 보강 — MBTI 검수 + 꿈해몽 확장)**: 착수 전 Explore 에이전트로 MBTI 16유형 데이터와 꿈해몽 59개 테마 현황을 먼저 조사. MBTI는 구조적 결측 없이 품질 균일하다고 판단, famous(유명인) 필드에서만 문제 발견 — 스티브 잡스가 ENTP/ENTJ 두 유형에 중복 기재된 버그와 ESTP의 도널드 트럼프(정치적으로 민감한 현직 인물)를 확인해 사용자에게 보고 후 승인 없이 바로 수정 진행(명백한 버그·리스크라 판단).
  - 꿈해몽은 사용자가 "API로 자동으로 가져올 수 없냐"고 질문 → WebSearch로 조사한 결과 **꿈해몽 전용 무료 오픈API는 존재하지 않음**을 확인(네이버 등 어디에도 없고, 있는 건 실시간 AI 챗봇형 유료 서비스뿐이라 정적 `data.js` 구조에 맞지 않음). 이 사실을 사용자에게 알리고, 명언·속담·물가 트리비아 작업 때와 동일한 "웹 검색으로 여러 출처 조사 후 직접 집필" 방식을 다시 채택.
  - 신규 테마 개수와 variants 확장 범위를 배치 단위로 나눠 진행하기로 사용자와 합의(한 번에 다 하지 않고 중간 확인). 1차로 검색 빈도가 높아 보이는 신규 테마 15개(로또당첨/거울/무지개/나비/추락/곰/거북이/좀비/태풍/화산/시체/연예인/폭포/코끼리/UFO)를 각각 웹 검색으로 조사해 작성, 59→74개로 확장. 2차로 기존 테마 중 variants가 없던 36개 중 검색 빈도가 높을 것으로 판단한 8개(학교/전애인/짝사랑/키스/싸움/알몸/이사/지갑을 잃어버리는 꿈)에 웹 검색 기반으로 variants 2개씩 추가(variants 보유 테마 23→46개).
  - **검증**: Node로 `data.js`를 직접 require해 전체 74개 테마의 구조 결측 여부 기계 검증. Playwright로 신규 테마(로또) 검색→모달 오픈→하위 variants 칩 네비게이션까지 실제 UI로 확인, 기존 8개 확장 테마도 전부 검색 가능함을 확인. 최초 테스트 시 `fill()`만 하고 검색을 실행하지 않아 거짓 양성이 나왔던 실수를 발견해 `dreamSearchBy()` 직접 호출로 재검증. 콘솔 에러 없음.
- **v0.0.33 (D-무관 백로그 4순위: 재미요소 — 칭호/레벨/궁합/사운드 + UI 수정 2건)**: 사용자가 남은 꿈해몽 variants 28개는 "정식 배포 전 콘텐츠 보강 단계"로 순위를 완전히 뒤로 미루라고 지시. 이어서 4순위 착수 전 계획을 먼저 보고하도록 요청받아, 사운드/칭호/레벨/시즌테마/궁합 5개 항목별 구현 방식을 설명 → 사용자가 순서 재조정(칭호→레벨→궁합→사운드, 시즌테마 제외, 미스터리박스 보류)해 그 순서로 진행. 추가로 소소한 요청 2건: ①사이드바 "테스트" 그룹 삼각형 아이콘이 안 보인다는 피드백(font-size 0.7rem→1.1rem+bold로 즉시 수정) ②테스트 화면에 홈/뒤로가기 버튼 추가 요청은 사용자가 명시적으로 후순위로 미룸(백로그 등록만, 이번엔 미구현).
  - **칭호 시스템**: `getEarnedBadges()`가 `BADGES` 배열(5종: 🔥개근왕=스트릭7일+, 🌟올라운더=15개 테스트 전부완료, 🧠천재 과몰입러=Tier 8개 테스트 전부 S, 🎉인싸력 만렙=insa 등급S, 📜지혜로운 어른=proverb 등급S)을 체크해 홈 화면 닉네임 카드에 표시. 전부 기존 localStorage 기록(`ranking_*`, `done_*`, `visit_streak`)만 읽어서 계산, 신규 저장 항목 없음.
  - **레벨/경험치**: 15개 테스트가 전부 공통으로 호출하는 `saveRanking()` 함수 단 한 곳에 `addXP(computeXP(result))` 추가 — Tier 결과 문자열이면 `parseTierFromResult()`(친구대결 기능에서 이미 만든 헬퍼)로 등급 추출해 S=30/A=25/B=20/C=15/D=10 XP, Tier가 없는 테스트(MBTI/ADHD/인싸력/속담/물가/운세)는 완료 시 고정 15XP. `localStorage.app_xp` 누적, 100XP당 1레벨(`getLevelInfo()`), 홈 화면에 Lv./XP 바 추가.
  - **결과 궁합**: 친구 대결 때 만든 URL 공유 인프라(해시 쿼리스트링 파싱, `App.pendingChallenge` 패턴)를 그대로 본떠 `App.pendingMatch`/`#{section}?match=...`로 별도 채널 신설(vs와 충돌 없게 파라미터명 분리). MBTI는 4축(E/I,S/N,T/F,J/P) 중 일치하는 축 개수/4*100으로, 인싸력은 100-(점수차/30*100)으로 소수 첫째자리까지 계산. PRD에 명시된 "상위 % 표시"는 여전히 Stage D 필요해 이번에도 제외.
  - **사운드/애니메이션**: `playSound(type)`을 Web Audio API `OscillatorNode`로 직접 구현(정답 880Hz/오답 220Hz 사인파, 0.25초, 외부 음원 파일 전혀 없음 — 브라우저 자동재생 정책도 사용자 클릭 이후 호출이라 문제 없음). `pulseElement()`+CSS keyframe(`anim-pop`/`anim-shake`)으로 순서기억력 타일탭, 충동억제·숏폼집중력 Go/No-Go탭, 속담완성·물가퀴즈 4지선다 응답에 마이크로 애니메이션 적용.
  - **검증**: Playwright로 (1)7일 스트릭+8개 Tier S+insa/proverb S 동시 충족시켜 배지 5종 전부 노출 확인 (2)app_xp=250 설정 후 Lv.3/50XP 표시 확인 (3)MBTI 궁합 — 정반대 유형(ISTJ vs ENFP)으로 0.0% 확인, 인싸력 궁합 — 동일 점수로 100.0% 확인 (4)proverb/seqmem 답변 시 `anim-pop`/`anim-shake` 클래스 실제 적용 확인. 전체 홈 화면 최종 스크린샷으로 레이아웃 회귀 없음 확인. 콘솔 에러 없음.
- [ ] **다음 세션 시작점 ★**: D-무관 백로그 4순위 완료. 남은 건 5순위(카카오공유-Kakao키 발급 필요, 로또통계-Cloudflare Functions 구축 필요)와 보류 항목들(미스터리박스, 꿈해몽 variants 28개 잔여분은 정식배포 전 콘텐츠 보강 단계로 이동, 뒤로가기/홈 버튼). Stage D(백엔드)는 여전히 보류 — 실제 운영 단계 진입 여부를 계속 리마인드할 것. E(동물비유카드, D 의존)·G(라이트모드, **프로젝트 전체 최후로 고정**)도 계속 보류.
- [ ] (보류) Stage D 백엔드 구축 — 콘텐츠 확보 후 카카오공유와 함께, 그 시점 되면 먼저 리마인드
- [ ] (보류) Stage E 동물 비유 결과카드 + 바이럴 공유 — Stage D 이후
- [ ] (보류, 프로젝트 전체 최후로 고정) Stage G 라이트모드 신규 콘텐츠 일괄 반영
- [ ] (보류) 미스터리 박스 — 범위 모호, 재논의 필요(구현 안 할 가능성 높음)
- [ ] (보류) 테스트 화면 홈/뒤로가기 버튼 추가 — 사용자가 후순위로 명시 지정
- [ ] (백로그) 오늘의 운세 고도화, 글로벌 커뮤니티 랭킹
- [ ] 카카오톡 공유 — Kakao Developers JS 키 발급(사용자 액션 필요) 후 SDK 연동
- [ ] 로또 통계 기반 추천 — Cloudflare Functions 프록시로 동행복권 데이터 자동 연동 (B안)
- [ ] 구글 애드센스 심사 후 publisher ID 삽입
- [ ] 댓글 섹션 백엔드 연동 (현재 로컬 임시 저장)
- [ ] 모바일 사이드바 애니메이션 개선
- [ ] PWA 오프라인 지원 고려
- [ ] (정식 배포 전 콘텐츠 보강 단계로 순위 이동) 꿈해몽 나머지 28개 테마 variants 확장 (현재 74개 중 46개가 variants 보유)
- [ ] 라이트 모드 색상은 1차 구현 상태 — 실사용 피드백 받아 미세 톤 조정 여지 있음

---

## 세션 #5 — 2026-07-03

### 현재 버전: v0.0.21

### 작업 내용
- **v0.0.18 (Stage B 계속 — 숏폼 집중력 테스트)**: 일곱 번째 신규 테스트로 **숏폼 집중력 테스트** 구현. PRD.md 9-1에서 확정된 대로 충동억제(Go/No-Go) 테스트의 엔진(상태 구조, 라운드 진행, 타이머 로직)을 그대로 재사용하고 MZ향 "숏폼 뇌 지수" 컨셉으로 리스킨만 함 — 🔥(꿀잼 콘텐츠, Go)엔 빠르게 탭, 📢(광고, No-Go)엔 참기. 색상 테마는 충동억제(emerald/rose)와 구분되도록 fuchsia/slate 톤으로 차별화. 채점 공식(정확도만으로 티어 S~D 산정)과 "성급한 반응" 지표(여기선 "광고에 낚인 횟수"로 리네이밍)는 충동억제와 동일하게 유지, 티어별 코멘트만 "알고리즘 잠식" 소재의 유머러스한 문구로 교체.
  - **검증**: 브라우저 자동화(Playwright, Chromium)로 실제 플로우 전체를 구동 — 닉네임 입력→난이도 선택→8라운드 진행(Go 신호 탭, No-Go 신호 무시)→결과 화면(티어/공유문구/랭킹 저장) 정상 동작 확인, 콘솔 에러 없음. 홈 화면 완주현황 위젯에도 "숏폼 집중력" 라벨이 정상 반영됨을 확인.
- **v0.0.19 (Stage B 계속 — 인싸력 테스트)**: 여덟 번째 신규 테스트로 **인싸력 테스트** 구현. 지금까지의 반응속도/기억력/Go-No-Go류 타이머 엔진과 달리, 이번엔 **MBTI/ADHD와 동일한 "문항→점수 누적→등급" 패턴**을 재사용 — 10문항 4지선다(항상/자주/가끔/전혀, 각 3/2/1/0점, 총 0~30점), 점수 구간별 D~S 4등급(고요한 확신형→밸런스 유지형→핵인싸 예비군→인싸력 만렙). PRD.md 9-1 지침대로 인싸력은 "문항 기반 사교성 성향 퀴즈"로 명시되어 있어 임의로 다른 엔진을 붙이지 않고 그대로 따름. 인싸/아싸 어느 쪽도 우열이 없다는 톤을 결과 문구와 면책 문구에 명시(저점수=문제가 아니라 소수와의 깊은 관계 선호로 프레이밍).
  - **검증**: Playwright로 닉네임 입력→10문항 순차 응답→결과 화면(등급/점수/꿀팁/공유문구/랭킹 저장) 전체 플로우 구동, 콘솔 에러 없음. 홈 완주현황 위젯에 "인싸력" 라벨 정상 반영 확인.
- **v0.0.20 (Stage B 계속 — 속담 완성 퀴즈)**: 아홉 번째 신규 테스트로 **속담 완성 퀴즈** 구현. PRD.md 9-1에서 "어르신향, 시간압박 없음, 긍정적 프레이밍"으로 명시된 대로 난이도/타이머 개념을 넣지 않고, 속담 앞부분을 보여주고 4지선다(정답 1개+decoy 3개, 논리력 테스트에서 만든 `shuffleArray()` 재사용)로 뒷부분을 맞히는 단일 모드로 설계. 정답/오답 즉시 시각 피드백(정답 초록/오답 빨강) 후 자동 진행, 채점은 점수 누적이 아니라 **정답 개수 기반**(10문항 중 맞힌 개수)으로 D~S 4등급. 오답 문항도 결과 화면 "정답 확인" 리캡 섹션에서 정답을 함께 보여줘 가족과의 대화 소재로 이어지도록 구성 — 저득점자에게도 "시험이 아니라 지혜를 나누는 소재"라는 톤으로 위로/격려 문구를 넣어 어르신 대상에 맞는 존중하는 어조 유지.
  - **검증**: Playwright로 닉네임 입력→10문항 순차 응답(피드백 지연 포함)→결과 화면(등급/정답확인 리캡/공유/랭킹) 전체 플로우 구동, 콘솔 에러 없음. 홈 완주현황 위젯에 "속담 완성" 라벨 정상 반영 확인.
- **(부가 작업) 코드스페이스 유휴 자동 종료 워처 (2026-07-03 한정)**: 사용자 요청으로 "3시간 이상 작업 없으면 git push 후 코드스페이스 종료" 로직을 오늘 하루만 적용하는 백그라운드 스크립트 추가. 프로젝트 코드가 아닌 `~/.claude_idle/watcher.sh`에 별도로 구현(레포에는 커밋하지 않음 — 일회성 요청이라 CLAUDE.md 영구 규칙으로 편입하지 않음). 로직: 5분 간격으로 "마지막 git 커밋 시각"과 "heartbeat 파일 시각" 중 더 최근 값을 유휴 기준점으로 삼아 3시간(10800초) 경과 시 `git push` 후 `gh codespace stop` 실행. 오늘 날짜(2026-07-03)가 지나면 아무 동작 없이 스스로 종료되도록 안전장치 포함. `setsid`로 완전히 분리된 프로세스(PPID 1)로 실행해 이 대화 세션이 끝나도 계속 동작함을 확인.
- **v0.0.21 (Stage B 완료 — 그 시절 물가 맞히기)**: 열 번째이자 Stage B 마지막 신규 테스트로 **그 시절 물가 맞히기** 구현, Stage B(신규 인지테스트 10종) 전체 완료. 착수 전 사용자에게 3가지(문제 방식/데이터 출처/문항 수)를 확인받아 진행 — 4지선다 객관식(속담완성과 동일 패턴), 실제 물가 데이터는 웹 검색으로 조사, 10문항으로 확정. 실제 물가 데이터는 정확성이 중요해 임의로 지어내지 않고 웹 검색으로 짜장면·버스요금·소주·라면·영화관람료·담배·택시기본요금·지하철기본요금 8개 카테고리(1963~2004년, 총 10개 연도x품목 조합)를 조사, 각각 복수 기사/자료로 교차 확인 후 확정(예: 1970년 짜장면 100원, 1974년 지하철 기본요금 30원, 2004년 담뱃값 2,500원 등). 코드 구조는 속담완성 퀴즈를 거의 그대로 재사용(10문항 4지선다, `shuffleArray()`로 정답+decoy 셔플, 시간 제한 없음, 정답 개수 기반 D~S 4등급 채점) — decoy는 같은 카테고리의 다른 연도 실제 가격을 사용해 그럴듯하면서도 명확히 오답이 되도록 구성. 결과 화면 "정답 확인" 리캡에 각 문항마다 짧은 트리비아(note) 문구를 추가해 속담완성보다 한 단계 더 가족 대화소재로 활용하기 좋게 보강.
  - **검증**: Playwright로 닉네임 입력→10문항 순차 응답(정답/오답 섞어서)→결과 화면(등급 C, 4/10, 정답확인 리캡에 문항별 트리비아 정상 표시, 공유버튼, 랭킹 저장) 전체 플로우 구동, 콘솔 에러 없음. 홈 완주현황 위젯이 "0/15"로 정상 확장되고 "그 시절 물가" 라벨도 반영됨을 확인. (트러블슈팅 메모: 사이드바 "테스트" 그룹은 기본값이 이미 펼침(open) 상태라 Playwright에서 토글을 추가로 클릭하면 오히려 접혀서 클릭이 다른 요소에 가로채이는 문제가 있었음 — 그룹 토글 로직 자체는 정상이므로 향후 유사 테스트 시 참고)

### 다음에 이어서 해야 할 작업 리스트 (갱신)
- [x] **Stage B - 숏폼 집중력 테스트** 완료 (v0.0.18)
- [x] **Stage B - 인싸력 테스트** 완료 (v0.0.19)
- [x] **Stage B - 속담 완성 퀴즈** 완료 (v0.0.20)
- [x] **Stage B - 그 시절 물가 맞히기** 완료 (v0.0.21) — **Stage B(신규 인지테스트 10종) 전체 완료**
- [ ] **다음 세션 시작점 ★**: Stage B가 전부 끝났으므로 다음은 (a) 오늘의 한마디 개편(9-4, 유명인 명언 200개) 또는 (b) Stage C(난이도/모드 체계) 중 사용자와 우선순위 재확인 후 착수. 둘 다 착수 전 기획 세부사항이 이미 PRD.md 9-2/9-4에 있으나, 실제 데이터(명언 200개 문구, MBTI 정밀모드 문항 수 등)는 착수 시점에 다시 확정 필요.
- [ ] 오늘의 한마디 개편 — 유명인 명언 200개로 교체, 날짜순 200일 비중복 순환. 상세 스펙 PRD.md 9-4
- [ ] Stage C: 난이도/모드 체계 — 신규테스트는 쉬움/보통/어려움 3단계, 두뇌나이(스트룹)도 2단계→3단계 확장, MBTI·ADHD는 간단/정밀 2모드로 재설계
- [ ] (보류) Stage D 백엔드 구축 — 콘텐츠 확보 후 카카오공유와 함께, 그 시점 되면 먼저 리마인드
- [ ] (보류) Stage E 동물 비유 결과카드 + 바이럴 공유 — Stage D 이후
- [ ] (보류) Stage F 종합 인지 프로필 레이더차트 — 테스트 5개+ 완료자 대상
- [ ] (보류) Stage G 라이트모드 신규 콘텐츠 일괄 반영
- [ ] (백로그) 재미 요소 9종 — 친구대결/오늘의챌린지 우선순위 높음, 나머지는 PRD.md 9-3 참고
- [ ] (백로그) 오늘의 운세 고도화, 글로벌 커뮤니티 랭킹
- [ ] 카카오톡 공유 — Kakao Developers JS 키 발급 후 SDK 연동 (Stage D와 함께 진행 예정)
- [ ] 로또 통계 기반 추천 — Cloudflare Functions 프록시로 동행복권 데이터 자동 연동 (B안)
- [ ] 구글 애드센스 심사 후 publisher ID 삽입
- [ ] MBTI 결과 유형 16개 전체 상세 텍스트 검수/보강
- [ ] 댓글 섹션 백엔드 연동 (현재 로컬 임시 저장)
- [ ] 모바일 사이드바 애니메이션 개선
- [ ] PWA 오프라인 지원 고려
- [ ] 꿈해몽 데이터 추가 확장 여지 있음 (현재 59개, 필요 시 더 늘리기)
- [ ] 라이트 모드 색상은 1차 구현 상태 — 실사용 피드백 받아 미세 톤 조정 여지 있음

---

## 세션 #4 — 2026-07-02

### 현재 버전: v0.0.17

### 작업 내용
- **v0.0.10 (라이트/다크 모드 토글)**: 기존 다크 전용 UI에 라이트 모드를 추가. 순백(#fff)이 아닌 은은한 블루그레이 톤으로 설계. 구현 방식은 개별 요소에 `dark:`/`light:` variant를 붙이는 대신, Tailwind `slate` 팔레트 및 강조색(blue/indigo/violet/emerald/rose/amber/yellow) 일부 shade를 CSS 변수로 remap하여 `<html>.light` 클래스 토글만으로 전체 화면이 한 번에 바뀌도록 함 (자세한 내용은 CLAUDE.md "다크/라이트 테마 아키텍처" 섹션 참고). 토글 버튼은 사이드바 상단(PC)과 모바일 헤더 우측에 배치, `localStorage.theme`에 저장되어 새로고침해도 유지됨. 기본값은 계속 다크.
  - 부수 작업: 라이트 모드 적용 중 `text-white`가 중립 배경(slate 계열)에 하드코딩된 곳이 다수 발견되어(입력창, 헤딩, 결과 텍스트 등 약 30곳) `text-slate-100`으로 전환. 채도 높은 색상 버튼(violet-600 등) 위의 `text-white`는 그대로 유지.
  - 부수 작업: `bg-{color}-900/30` + `text-{color}-300` 형태의 "부드러운 강조 박스"(꿈해몽 요약, MBTI 강점/도전과제, 경고 문구 등)도 라이트 모드에서 저대비였음 → 해당 shade들도 CSS 변수로 remap해 라이트에서는 중간 채도 배경 + 짙은 텍스트로 자동 전환되게 수정.
- **v0.0.10 (마이홈 → 홈 통합)**: 별도 "마이홈" 메뉴/섹션을 제거하고, 닉네임 입력·연속방문 스트릭·5개 테스트 완주현황·최근 테스트 기록을 홈 화면 안으로 병합(`renderHomeMypage()`). `#mypage` 해시로 진입 시 자동으로 홈으로 대체됨(하위 호환).
- **v0.0.11 (Phase 2 확장 기획 + Stage A)**: strooptest.online을 레퍼런스로 검토(테스트 종류는 참고할 만하나 재미·공유 요소 전무 — 반면교사). 사용자와 대규모 확장 기획을 논의해 `PRD.md` "9. 확장 로드맵"에 전체 계획을 문서화(신규 인지테스트 6종+α, 퍼센타일/동물비유 결과카드, 종합 인지 프로필 레이더차트, 재미 요소 백로그, 난이도/모드 체계, 백엔드 도입 시점). 그 중 **Stage A(메뉴 구조 개편)**를 이번 세션에서 실제 구현 — 사이드바에 "🧪 테스트" 접이식 그룹을 신설해 MBTI/두뇌나이/ADHD를 묶고, 꿈해몽/운세/로또는 최상위 메뉴로 유지. 그룹 펼침 상태는 `localStorage.nav_tests_collapsed`로 기억되고, 그룹 내부 항목으로 직접 진입(딥링크) 시 자동으로 펼쳐짐.
- **v0.0.12 (Stage B 착수 — 반응속도 테스트)**: Stage B 신규 인지테스트 10종의 최종 목록·제작순서를 확정(반응속도→숫자기억→순서기억→색각→논리력→충동억제→숏폼집중력→인싸력→속담완성→그시절물가, `PRD.md` 9-1 참고). 그중 첫 번째로 **반응속도 테스트**를 실제 구현 — 쉬움(5회,반칙무패널티)/보통(7회,반칙시 페널티기록)/어려움(10회,30% 확률로 가짜신호 등장) 3단계, 결과는 티어(S~D)+유머러스한 코멘트+공유 기능까지 기존 테스트들과 동일한 UX 패턴으로 구현. 모바일 터치 지연 최소화를 위해 `click` 대신 `pointerdown` 이벤트 사용(PRD.md 모바일 우선 원칙 반영, 두뇌나이 스트룹의 미해결 백로그 항목과 동일한 방식 선제 적용). 홈의 "테스트 완주 현황" 위젯도 반응속도 테스트를 포함하도록 확장하고 "5개" 하드코딩 라벨을 동적 개수로 변경. 재미요소 백로그 7번(가족 그룹 비교판)·9번(결과 궁합 보기)에 구체 스펙 추가 확정 (그룹이름 직접 설정+공유초대, 궁합 %는 소수 첫째자리+상위% 병기하되 상위%는 백엔드 필요).
- **v0.0.13 (Stage B 계속 — 숫자 기억력 테스트)**: 두 번째 신규 테스트로 **숫자 기억력 테스트**(디지트 스팬) 구현. 화면에 숫자가 하나씩 순서대로 나타났다 사라지면 기억한 뒤 탭 키패드로 그대로 입력하는 방식. 적응형(staircase) 난이도 채택 — 맞히면 다음 라운드 자릿수 +1, 틀리면 -1. 쉬움(3자리부터,6라운드)/보통(4자리부터,8라운드)/어려움(5자리부터,10라운드+정답 직후 3초간 "다른 생각 금지" 방해단계 삽입, 작업기억 간섭 부여로 난이도 체감 강화). 점수는 라운드 중 실제로 정답 처리된 최대 자릿수(maxCorrectLen)를 기준으로 티어 산정(S=9+,A=8+,B=6+,C=4+,D=미만). 모바일에서 숫자 키보드 대신 큼직한 탭 패드(0~9+삭제+확인) UI로 구현해 입력 편의성 확보. 반응속도 테스트와 동일하게 공유/랭킹/댓글/완주현황 연동까지 전부 반영.
- **v0.0.14 (Stage B 계속 — 순서 기억력 테스트)**: 세 번째 신규 테스트로 **순서 기억력 테스트** 구현. 3x3(어려움은 4x4) 격자 타일이 순서대로 반짝이면 같은 순서로 탭하는 방식 — 숫자 기억력과 달리 "입력 후 확인" 대신 탭할 때마다 즉시 정오답을 판정하는 Simon-says류 UX로 차별화(정답 타일은 초록, 오답은 빨강 즉시 피드백). 적응형 난이도(맞히면 칸수 +1/틀리면 -1)는 숫자 기억력과 동일 패턴 유지. 쉬움/보통은 3x3, 어려움은 4x4 격자로 확대 + 암기 후 3초 방해단계. 오늘의 한마디 개편 요청(유명인 명언 200개, 200일 비중복 순환)은 Stage B 10종 전부 끝난 뒤 진행하기로 하고 `PRD.md` 9-4에 상세 스펙 기록.
  - **버그 발견 및 수정**: 사이드바 "테스트" 그룹의 펼침 높이가 `max-height: 220px`로 고정되어 있었는데, 하위 테스트가 6개로 늘어나며 실제 콘텐츠 높이(283px)가 이를 초과 → 맨 위 MBTI 항목이 잘려 보이지 않는 버그를 브라우저 실제 테스트 중 발견. `max-height`를 2000px로 넉넉하게 올려 향후 테스트가 더 늘어나도(Stage B 10종 전부 이 그룹에 들어감) 잘리지 않도록 수정.
- **v0.0.15 (Stage B 계속 — 색각 테스트)**: 네 번째 신규 테스트로 **색각 테스트** 구현. 실제 이시하라 판 같은 임상 색맹/색약 검사를 흉내내면 부정확한 의학적 오해를 줄 수 있어 지양하고, 대신 격자 안에서 미묘하게 다른 색(HSL 명도 차이) 타일 하나를 제한시간 안에 찾는 "색 구별 감각" 게임으로 설계 — 결과 화면에 "실제 색각 임상 검사를 대체하지 않는다"는 안내 문구 명시. 난이도별로 격자 크기(3x3→4x4→5x5)·색상 차이(delta 42→26→14)·제한시간(5초→4초→3초)이 동시에 빡빡해짐. 점수는 두뇌나이(스트룹) 테스트와 동일한 "정확도 - 평균반응시간/100" 공식으로 티어 산정해 유사 장르 테스트 간 채점 언어를 통일. 정답/오답 시 타일에 초록/빨강 즉시 시각 피드백.
- **v0.0.16 (Stage B 계속 — 논리력 테스트)**: 다섯 번째 신규 테스트로 **논리력 테스트** 구현. 등차수열/등비수열/피보나치식 규칙으로 생성된 숫자열의 다음 값을 4지선다로 맞히는 방식. 난이도별로 규칙 종류(쉬움=등차만, 보통=등차+등비, 어려움=+피보나치)와 제한시간(8초→6초→4.5초)이 함께 확장. 오답 3개는 정답 근처 값으로 자동 생성(Fisher-Yates 셔플 유틸 `shuffleArray()` 신규 추가, 재사용 가능). 채점은 색각 테스트와 동일한 "정확도-반응시간" 공식 재사용.
- **v0.0.17 (Stage B 계속 — 충동억제 테스트)**: 여섯 번째 신규 테스트로 **충동억제(Go/No-Go) 테스트** 구현. 초록 신호(Go)엔 최대한 빨리 탭, 빨간 신호(No-Go)엔 절대 누르지 않고 참아야 함 — 고전적인 Go/No-Go 패러다임을 그대로 게임화. No-Go 비율은 난이도별 25%→30%→35%, 자극 표시시간은 1300ms→950ms→700ms로 단축. "성급한 반응"(commission error, No-Go인데 탭함)을 핵심 충동성 지표로 별도 집계해 결과 화면에 노출 — 다른 테스트들의 "정확도-반응시간" 복합 공식과 달리 이번엔 정확도(Go 정답+No-Go 올바른 억제)만으로 단순하게 티어 산정(S≥95%,A≥85%,B≥70%,C≥50%,D미만). 타임아웃 시 동작이 방향에 따라 반대로 해석되는 게 이 테스트의 핵심 로직(Go인데 타임아웃=놓침/실패, No-Go인데 타임아웃=올바른 억제/성공).

### 유저와 합의한 사항 (이번 세션)
- 라이트 모드를 만들되 순백 화면은 원하지 않음 → 블루그레이 톤으로 설계
- 다크/라이트 토글 버튼 위치: 오른쪽(콘텐츠 영역)은 섹션마다 내용이 계속 바뀌므로, 항상 고정으로 보이는 왼쪽 사이드바 상단에 배치하기로 함 (모바일은 헤더 우측에 별도 배치)
- 마이홈 내용은 완전히 홈 화면에 흡수 — 별도 페이지로 남기지 않음
- **Phase 2 확장 방향**: 재미·체류시간·실용성·가족공유 4가지를 최우선 가치로 삼기로 합의. 상세 로드맵/합의사항은 `PRD.md` 9번 섹션에 전부 기록해둠 (백엔드는 Supabase+Cloudflare R2로 나중에, 카카오공유와 함께 착수 예정 — 그 시점에 Claude가 리마인드하기로 함)
- 진행 순서: A(메뉴개편, 완료) → B(신규 인지테스트 6종+α) → C(난이도체계, B와 병행) → D(백엔드, 보류) → E(동물비유카드) → F(레이더차트) → G(라이트모드 일괄반영)

### 다음에 이어서 해야 할 작업 리스트 (갱신)
- [x] **Stage B - 반응속도 테스트** 완료 (v0.0.12)
- [x] **Stage B - 숫자 기억력 테스트** 완료 (v0.0.13)
- [x] **Stage B - 순서 기억력 테스트** 완료 (v0.0.14)
- [x] **Stage B - 색각 테스트** 완료 (v0.0.15)
- [x] **Stage B - 논리력 테스트** 완료 (v0.0.16)
- [x] **Stage B - 충동억제(Go/No-Go) 테스트** 완료 (v0.0.17)
- [ ] **Stage B 계속 (다음 세션 시작점 ★)**: **숏폼 집중력 테스트**(MZ향, Go/No-Go 엔진 재활용해 "숏폼 뇌 지수" 프레이밍)부터 시작 → 인싸력 테스트(MZ향, 문항기반 성향퀴즈) → 속담완성 퀴즈(어르신향) → 그시절물가 맞히기(어르신향) 순. Stage B는 이제 6/10 완료, 남은 4개 컨셉은 모두 확정됨(PRD.md 9-1). 지금까지 만든 6개 테스트(반응속도/숫자기억/순서기억/색각/논리력/충동억제) 코드 패턴을 그대로 재사용하면 됨: `init{Name}()`→`render{Name}View(start/round/result)`→난이도3단계 시작화면→라운드진행→`saveRanking`+`renderPlaceholderUI`+공유 결과화면. 새 테스트 추가 시 매번: ①index.html에 nav-subitem+section div ②app.js에 App.state 초기값+전체 모듈+sectionInits 등록 ③renderHomeMypage()의 sections/sectionLabels 배열 2곳에 추가 ④브라우저(Playwright)로 실제 플로우 검증(마지막 라운드로 state.round 점프시키는 트릭 쓰면 빠름) ⑤버전업+CLAUDE.md/HISTORIC_LOG.md 갱신 ⑥커밋+push. 이 6단계가 매 테스트마다 반복되는 표준 절차. 주의: 숏폼집중력·인싸력은 MZ향, 속담완성·그시절물가는 어르신향으로 프레이밍/문구 톤을 그에 맞게 조정할 것(어르신향은 시간압박 없이 긍정적 톤 — 이미 색각 테스트에서 "느려짐 부각 금지" 원칙 적용한 사례 참고).
- [ ] (Stage B 전부 완료 후) 오늘의 한마디 개편 — 유명인 명언 200개로 교체, 날짜순 200일 비중복 순환. 상세 스펙 PRD.md 9-4
- [ ] Stage C: 난이도/모드 체계 — 신규테스트는 쉬움/보통/어려움 3단계, 두뇌나이(스트룹)도 2단계→3단계 확장, MBTI·ADHD는 간단/정밀 2모드로 재설계
- [ ] (보류) Stage D 백엔드 구축 — 콘텐츠 확보 후 카카오공유와 함께, 그 시점 되면 먼저 리마인드
- [ ] (보류) Stage E 동물 비유 결과카드 + 바이럴 공유 — Stage D 이후
- [ ] (보류) Stage F 종합 인지 프로필 레이더차트 — 테스트 5개+ 완료자 대상
- [ ] (보류) Stage G 라이트모드 신규 콘텐츠 일괄 반영
- [ ] (백로그) 재미 요소 9종 — 친구대결/오늘의챌린지 우선순위 높음, 나머지는 PRD.md 9-3 참고
- [ ] (백로그) 오늘의 운세 고도화, 글로벌 커뮤니티 랭킹
- [ ] 카카오톡 공유 — Kakao Developers JS 키 발급 후 SDK 연동 (Stage D와 함께 진행 예정)
- [ ] 로또 통계 기반 추천 — Cloudflare Functions 프록시로 동행복권 데이터 자동 연동 (B안)
- [ ] 구글 애드센스 심사 후 publisher ID 삽입
- [ ] MBTI 결과 유형 16개 전체 상세 텍스트 검수/보강
- [ ] 댓글 섹션 백엔드 연동 (현재 로컬 임시 저장)
- [ ] 모바일 사이드바 애니메이션 개선
- [ ] PWA 오프라인 지원 고려
- [ ] 꿈해몽 데이터 추가 확장 여지 있음 (현재 59개, 필요 시 더 늘리기)
- [ ] 라이트 모드 색상은 1차 구현 상태 — 실사용 피드백 받아 미세 톤 조정 여지 있음

---

## 세션 #3 — 2026-07-02

### 현재 버전: v0.0.9 (v0.0.4~v0.0.9 누적 반영)

### 작업 내용
- **v0.0.4**: 꿈해몽 데이터 구조를 "테마 + 하위 variants" 방식으로 전면 개편. "오늘의 행동" 텍스트가 14자로 강제 잘리던 버그 수정 (미니카드 3칸→2칸으로 정리, 전체 텍스트는 하단 박스 하나로 통합)
- **v0.0.5**: 꿈해몽 테마를 15개 → 59개로 확장 (기존 15개는 variants 추가로 개선, 신규 44개 테마 추가). 약 20개 테마에 하위 variants(뱀/물/불/이빨/시험/돈/쫓기는/집/임신/귀신/자동차/죽음/돼지/화장실/조상/결혼식/벌레/거미/반지/여행 등) 포함. 검색 시 테마+variants 키워드 동시 매칭, 매칭된 variants는 모달에 칩 형태로 노출되어 로딩 없이 즉시 전환
- **v0.0.6**: 띠별 운세(fortuneData) 12개 전체에 `luckyNum` 필드 추가
- **v0.0.7**: 공유하기 기능 — `shareResult()` 헬퍼 추가(Web Share API, 미지원 시 링크복사로 대체). MBTI/오늘의 운세/두뇌나이/ADHD 결과 화면에 공유 버튼 적용
- **v0.0.8**: 마이홈(대시보드) 섹션 신설 — 닉네임 저장, 5개 테스트 완주 프로그레스, 연속 방문 스트릭(🔥), 최근 테스트 기록 모아보기. `saveRanking()` 호출 시 자동으로 완주 플래그(`done_{section}`) 기록되도록 연결
- **v0.0.9**: 로또 번호 조합기 섹션 신설 — ① 완전 랜덤 ② 숫자 직접 지정(1~5개) ③ 오늘의 운세·꿈 행운숫자 연동 ④ 통계 기반 추천(UI만 노출, 비활성 처리 — 백로그)

### 유저와 합의한 사항 (이번 세션)
- 카카오톡 공유: JS 키 미보유로 이번엔 보류. 링크복사 + Web Share API로 우선 대응. **백로그로 이월**
- 로또 통계 기반 추천(과거 당첨번호): 실시간 API 연동은 추후 Cloudflare Functions 프록시 방식(B안)으로 진행하기로 합의. **백로그로 이월**
- 꿈해몽 데이터 규모: 기존 15개 유지 + 신규 44개 추가 = 총 59개 (요청한 "60개 안팎" 충족)
- 꿈해몽 검색 UX: "검색 → 통합설명 먼저 → 하위 관련꿈 칩(chip)" 방식으로 확정 및 구현 완료

### 다음에 이어서 해야 할 작업 리스트 (갱신)
- [ ] 카카오톡 공유 — Kakao Developers JS 키 발급 후 SDK 연동
- [ ] 로또 통계 기반 추천 — Cloudflare Functions 프록시로 동행복권 데이터 자동 연동 (B안)
- [ ] 구글 애드센스 심사 후 publisher ID 삽입
- [ ] MBTI 결과 유형 16개 전체 상세 텍스트 검수/보강
- [ ] 댓글 섹션 백엔드 연동 (현재 로컬 임시 저장)
- [ ] 모바일 사이드바 애니메이션 개선
- [ ] PWA 오프라인 지원 고려
- [ ] 꿈해몽 데이터 추가 확장 여지 있음 (현재 59개, 필요 시 더 늘리기)

---

## 세션 #2 — 2026-06-29

### 현재 버전: v0.0.3

### 작업 내용
- **v0.0.2**: `style.css`에 `.hidden { display: none !important; }` 추가 — Tailwind CDN 지연 시 프리로더 고착 버그 수정 (Cloudflare Pages 배포 환경)
- **v0.0.3**: 광고 플레이스홀더 영역 전체 제거 — 홈/MBTI/꿈해몽/운세/두뇌나이/ADHD 하단 광고박스 + 프리로더 내부 광고박스 + `.ad-placeholder-box` CSS 스타일 삭제 (애드센스 승인 후 삽입 예정)

### 다음에 이어서 해야 할 작업 리스트
- [ ] 구글 애드센스 심사 후 publisher ID 삽입
- [ ] 꿈 해몽 데이터 확장 (현재 15개 샘플 → 100개 이상 목표)
- [ ] MBTI 결과 유형 16개 전체 상세 텍스트 작성
- [ ] LocalStorage 랭킹 보드 실제 연동 (현재 더미 UI)
- [ ] 댓글 섹션 백엔드 연동 (현재 더미 레이아웃)
- [ ] 모바일 사이드바 애니메이션 개선
- [ ] PWA 오프라인 지원 고려

---

## 세션 #1 — 2026-06-28

### 현재 버전: v0.0.1

### 작업 내용
- 프로젝트 최초 생성
- 파일 구조: index.html / style.css / data.js / app.js / CLAUDE.md / HISTORIC_LOG.md
- SPA 사이드바 내비게이션 구조 설계 및 전체 구현

### 구현 완료 항목
| 섹션 | 기능 | 상태 |
|------|------|------|
| 🏠 홈 | 오늘의 폼 미친 한마디 (Seeded Random) + 5서비스 그리드 | ✅ |
| 🧠 MBTI | 12문항 팩폭 테스트 + 결과 분석 + 보완 가이드 | ✅ |
| 🌙 꿈 해몽 | 키워드 검색 + 3초 광고 로딩 + 상세 팝업 + 추천어 | ✅ |
| 🔮 오늘의 운세 | 띠별/출생연도 입력 + 자정 리셋 난수 운세 | ✅ |
| 🧠 두뇌 나이 | 스트룹 테스트 (쉬움/어려움) + 20문항 + 4초 게이지 바 + 결과 티어 | ✅ |
| ⚡ 프로 미루러 | 10문항 ADHD 진단 + 행동 개선 가이드 | ✅ |
| 공통 | 3초 광고 프리로더 + 확장 Placeholder UI (통계/랭킹/댓글) + 면책조항 | ✅ |

### 유저와 합의한 사항
- Tailwind CSS CDN + 순수 Vanilla JS (프레임워크 없음)
- 버전 규칙: 의미 있는 코드 변경 시마다 0.0.1씩 증가
- 모호한 사항 발생 시 임의 코딩 금지 → 즉시 질문

### 다음에 이어서 해야 할 작업 리스트
- [ ] 실제 구글 애드센스 publisher ID 교체 (현재 더미 주석으로 처리)
- [ ] 꿈 해몽 데이터 확장 (현재 15개 샘플 → 100개 이상 목표)
- [ ] MBTI 결과 유형 16개 전체 상세 텍스트 작성
- [ ] LocalStorage 랭킹 보드 실제 연동 (현재 더미 UI)
- [ ] 댓글 섹션 백엔드 연동 (현재 더미 레이아웃)
- [ ] 모바일 사이드바 애니메이션 개선
- [ ] PWA 오프라인 지원 고려

---
