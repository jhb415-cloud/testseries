# 내 뇌 구조 분석 보고서 — AI 이미지 생성 프롬프트 (레퍼런스 기반, 임상 뇌스캔 모니터)

사용자가 Gemini로 직접 생성한 레퍼런스(2패널: 왼쪽=측면 프로필 뇌스캔 모니터 창, 오른쪽=상단뷰
뇌스캔에 색색의 영역 블롭이 표시된 모니터 창)를 `assets/reference/`에 넣어줌 — images/edits로 이
레퍼런스의 사이언 홀로그램 HUD 렌더링을 그대로 따라가게 함.

## cover
SINGLE unified illustration, ONE HUD monitor frame only — do NOT split the image into two separate
monitor windows, do NOT depict two frames side by side. Futuristic cyan hologram medical scanner UI
illustration on a dark navy background filling the entire square canvas edge-to-edge, glowing cyan
line-art brain scan inside ONE large bracketed HUD monitor frame with fine grid lines and small orange
indicator dots in the corners, clean clinical sci-fi diagnostic display aesthetic. IMPORTANT: use the
attached reference image only to match the cyan hologram HUD rendering technique, glow style, grid
frame, and dark navy palette — completely ignore the reference's two-separate-window layout, this
output must be one single large HUD frame filling most of the canvas, showing only the side-profile
scan (the reference's LEFT window content only). Scene: a glowing cyan side-profile brain scan of a
human head, rendered as soft luminous line-art with visible brain fold detail, sitting inside one
large rectangular HUD monitor frame with corner brackets and a thin horizontal scan line crossing it.
No text, no watermark, no logos, no real people, no second window, square format.

## generic
SINGLE unified illustration, ONE HUD monitor frame only — do NOT split the image into two separate
monitor windows, do NOT depict two frames side by side, do NOT include any small side-profile head
silhouette anywhere in this image (that belongs only to a different image, not this one). Futuristic
cyan hologram medical scanner UI illustration on a dark navy background filling the entire square
canvas edge-to-edge, glowing cyan line-art brain scan inside ONE large bracketed HUD monitor frame
with fine grid lines and small orange indicator dots in the corners, clean clinical sci-fi diagnostic
display aesthetic. IMPORTANT: use the attached reference image only to match the cyan hologram HUD
rendering technique, glow style, grid frame, and dark navy palette — completely ignore the reference's
two-separate-window layout, this output must be one single large HUD frame that completely fills the
canvas, showing ONLY the top-down colorful zone-map scan (the reference's RIGHT window content only,
with NO trace of the reference's left side-profile window). Scene: one large glowing cyan top-down
brain scan viewed from directly above, centered and filling almost the whole frame, rendered as soft
luminous line-art, with five or six small pastel-colored oval blobs (yellow, purple, orange, red,
cyan, blue) scattered across the brain surface marking different "personality zones", sitting inside
one large rectangular HUD monitor frame with corner brackets and a thin diagonal scan line crossing
it. No text, no watermark, no logos, no real people, no second window, no head silhouette, square
format.
