# CONTENT_PROMPTS.md — 심리테스트존 AI 콘텐츠 생성 프롬프트

이 문서는 심리테스트존(`#psychtest`)에 새 테스트를 추가할 때 매번 코드를 직접 만지지 않고,
아래 프롬프트로 ChatGPT/Claude 등에서 콘텐츠를 생성한 뒤 결과 JSON만 전달받아 반영하기 위한
서식이다. `BUSINESS_TEMPLATES.md`와 같은 성격의 코드 무관 운영 문서.

## 사용 방법
1. 아래 프롬프트를 그대로 복사해 원하는 LLM에 붙여넣는다.
2. "[여기에 주제 입력]" 부분만 원하는 주제로 바꿔 요청한다.
3. 나온 JSON을 그대로 Claude Code(이 프로젝트 세션)에 붙여넣으면, 중복·필드 검증 후
   `data.js`의 해당 카테고리에 추가하고 공유 이미지 생성까지 처리한다.

## 카테고리 4종 (심리테스트존 하위 그룹, 2026-07-05 확정)
| 카테고리 값 | 메뉴명 | 성격 |
|---|---|---|
| `character` | 캐릭터 테스트 | 세계관/IP 기반, 서사적 재미 |
| `trait` | 성향 테스트 | 심리학 이론 기반, 자기 분석 |
| `taste` | 취향 테스트 | 소비/라이프스타일 선택 |
| `national` | 국민테스트 | 오래된 스테디 콘텐츠, 향수 자극 |

각 카테고리는 정원 3개(1개 오픈+2개 잠금)로 운영 중 — 신규 주제 추가 시에도 이 구조를 유지할 것
(정원을 늘리고 싶으면 이 문서와 함께 사용자에게 먼저 확인).

## 프롬프트

```
너는 "과몰입 연구소"라는 한국 심리테스트 사이트의 콘텐츠 작가야.
타겟은 10~30대, MZ 트렌드에 민감한 SNS 유저. poomang.com 같은 사이트의
"에겐녀 테토녀 테스트", "멘헤라 테스트"처럼 짧고 신조어스러운 제목과
FOMO(소외 공포)를 자극하는 후킹 문구가 특기야.

아래 주제로 심리테스트 1개를 만들어줘: [여기에 주제 입력, 예: "직장인 퇴근 욕구 테스트"]
카테고리는 다음 중 하나로 골라줘:
- character (세계관/IP 기반, 서사적 재미 — 전생/캐릭터/동물 유형 등)
- trait (심리학 이론 기반, 자기 분석 — 애착유형/성격 스펙트럼 등)
- taste (소비/라이프스타일 선택 — 취향/소비패턴 등)
- national (오래된 스테디 콘텐츠, 향수 자극 — 전 국민이 아는 소재)

규칙:
1. 제목은 결과 유형 이름을 코인 신조어처럼 짧게(예: "OO러 vs OO러 테스트")
2. hook(후킹 문구)은 2문장 이내, 이모지 1~2개, "아직도 안 해본 사람 있어?" 같은 FOMO 톤
3. 문항은 4~6개, 각 문항마다 선택지(options)를 3개씩 직접 작성 — 각 선택지는
   [라벨, 점수] 형태이고 점수는 0부터 시작해 오름차순(0,1,2)으로 매길 것. 문항 성격에 맞는
   자연스러운 선택지 문구를 매번 새로 써야 함(모든 문항에 같은 선택지를 재사용하지 말 것).
   (단, "혈액형/별자리/탄생월"처럼 답 자체가 정체성인 국민테스트류는 문항을 1개만 만들어
   선택지 자체가 결과로 직접 이어지게 하고, 결과 range도 선택지 값과 동일한 단일 구간
   [0,0]/[1,1]/[2,2]/[3,3]으로 지정)
4. 결과 유형은 3~4개, 각각 grade(A/B/C/D)·range(점수구간, 문항 수×최대점수 범위를 결과
   개수만큼 나눠서 배분)·emoji·title(코인 신조어 이름)·desc(1문장 요약)·detail(3~4문장 해설)·
   tips(2~3개 실전 팁)
5. 절대 금지: 특정 실존 인물 저격, 정치·종교·차별적 내용, 의학적 진단처럼 보이는 단정적 문구
6. 톤은 항상 "재미로 보는 것"이라는 게 느껴지게, 어느 결과든 부정적으로만 느껴지지 않게 작성

출력은 아래 JSON 스키마만, 다른 설명 없이 그대로 출력해:
{
  "id": "영문-소문자-슬러그",
  "title": "...",
  "emoji": "대표 이모지 1개",
  "category": "character|trait|taste|national",
  "estMinutes": 1,
  "tags": ["...", "..."],
  "hook": "...",
  "questions": [
    { "q": "...", "options": [["선택지1", 0], ["선택지2", 1], ["선택지3", 2]] },
    ...
  ],
  "results": [
    { "grade": "A", "range": [0, 4], "emoji": "...", "title": "...",
      "desc": "...", "detail": "...", "tips": ["...", "..."] },
    ...
  ]
}
```

## 참고: 실제 데이터 스키마 위치
- `data.js`의 `AppData.psychTests` 배열 항목 형식과 완전히 동일해야 함(`app.js`의
  `renderPsychtestQuestion()`/`renderPsychtestResult()`가 이 필드들을 그대로 사용).
- (v0.2.4~) 문항별 `options`는 이제 데이터에서 직접 오며, 화면은 `q.options`를 그대로
  버튼으로 렌더링함 — 예전엔 모든 문항이 "바로 답장한다/조금 뜸 들이다/한참 뒤에"라는
  고정 문구를 공유해 다른 주제 문항에는 안 맞았던 문제를 v0.2.4에서 수정함.
- 카테고리(`character`/`trait`/`taste`/`national`)당 정원 3개(1개 오픈+2개 잠금) 운영 원칙은
  이 문서 상단 표 참고. 잠금 상태 테스트의 제목/이모지는 `app.js`의
  `PSYCHTEST_LOCKED`에 카테고리별로 정리돼 있음.
  답장한다"/"조금 뜸 들이다"/"한참 뒤에")을 문항 공통으로 재사용하는 구조라, 문항 성격이
  많이 다르면 응답 옵션 3종 문구도 함께 조정이 필요할 수 있음(반영 시 확인).
