# PRD.md — 5-in-1 Dashboard SPA

> 이 문서는 "무엇을 만들 것인가(What/Why)"를 정의한다.
> "어떻게 만들 것인가(How, 코드 규칙)"는 `CLAUDE.md`를 참고한다.
> AI가 요구사항을 임의로 추측하지 않도록, 모호한 표현 없이 목적/입력/출력/규칙/제약을 명시한다.

---

## 1. 목적 (Why)
- 10대~60대가 부담 없이 즐기는 미니테스트 모음 SPA를 만들어 트래픽을 확보하고, 구글 애드센스로 수익화한다.
- 단발성 방문이 아니라 **재방문**을 유도한다 (완주 현황, 방문 스트릭, 결과 공유).
- 목표는 "코드가 작동하는 것"이 아니라 **실제 배포되어 광고 심사를 통과하고 운영되는 것**이다.

## 2. 대상 사용자
- 연령: 10대~60대 (특정 세대에 치우치지 않는 UI/문구)
- 진입 경로: SNS 공유 링크, 검색(Naver 등)
- 기기: 모바일 우선 (터치 반응성 중요 — 두뇌나이 스트룹 테스트는 ms 단위 정밀도 필요)

## 3. 핵심 기능 — 입력/출력 정의
AI가 추측하지 않도록 기능별 입력·출력을 명시한다.

| 기능 | 입력 | 출력 | 비고 |
|---|---|---|---|
| MBTI | 12문항 답변 | E/I·S/N·T/F·J/P 4축 → 16유형 결과 카드 | |
| 꿈 해몽 | 검색 키워드(텍스트) | 매칭된 테마/variant 상세 카드 | 59개 테마, 매칭 없으면 추천 키워드 제시 |
| 오늘의 운세 | 출생년도(띠 계산) | 5개 영역 별점 + 행운숫자 | 날짜 seed 기반 결정론적 결과 |
| 두뇌 나이 | 스트룹 자극 반응시간(ms) | 등급 + 두뇌나이 | **모바일 터치 지연(Touch Delay) 보정 필수** |
| ADHD 진단 | 10문항 답변(점수) | 등급(A~D) + 행동 가이드 | |
| 로또 조합기 | 모드 선택(랜덤/직접지정/운세연동) | 5게임 번호 세트 | 오락 목적 명시 필수 |
| 마이홈 | localStorage 기록 | 완주현황/스트릭/최근기록 | 서버 없음, 클라이언트 저장만 |

## 4. 규칙 (Rules)
- 순수 Vanilla JS + Tailwind CDN, 빌드 도구 없음 (단일 정적 배포)
- 모든 텍스트/문구는 특정 연령대에 치우치지 않게 작성
- 새 기능 추가 시 기존 5개 테스트의 UX 패턴(닉네임 입력 → 진행 → 결과 → 공유)을 따를 것
- 스트룹 테스트 등 시간 측정 로직은 `touchstart` 우선 처리로 터치 지연 최소화할 것
- 상세 코드 컨벤션(파일구조, 네이밍, 데이터 스키마)은 `CLAUDE.md` 따름

## 5. 제약 사항 (Constraints)
- 백엔드 없음 → 실시간 외부 데이터(예: 로또 당첨번호 통계) 연동 불가, 필요 시 별도 서버리스 프록시 검토
- 광고 심사 통과 전까지 실제 AdSense 코드 대신 플레이스홀더만 유지
- 민감정보(API 키 등)는 반드시 `.env` 또는 배포 플랫폼 환경변수로 분리, 코드에 하드코딩 금지

## 6. 상태 보존 전략 (작게 자르고 자주 저장)
- 기능 단위(버전 0.0.x)로 작게 쪼개서 커밋 — 한 커밋에 여러 기능 섞지 않기
- 코드가 꼬였다고 판단되면 전체 재작성 대신, **직전 정상 커밋으로 롤백 후 재시도**
- 매 버전마다 `CLAUDE.md`의 기능 현황 표 + `HISTORIC_LOG.md` 동시 갱신 (이미 적용 중인 규칙)

## 7. 부분 검증 체크포인트 (AI 의심하기)
새 기능을 한 번에 통째로 요구하지 않고, 아래 단위로 끊어서 검증한다.
1. UI 뼈대(버튼/레이아웃)만 먼저 렌더링 확인
2. 핵심 로직(계산/판정) 단위 테스트 — 콘솔 로그로 값 확인
3. 실제 데이터 연결 후 화면 표시 확인
4. 모바일 실기기(또는 브라우저 반응형 모드)에서 터치/타이밍 확인
5. 에러 발생 시 "어느 단계에서 멈췄는지"를 구체적 증상 + 로그와 함께 전달 (전체 재작성 요청 금지)

## 8. 목표 재정의 (Definition of Done)
아래 조건을 모두 만족해야 "완료"로 간주한다.
- [ ] 로컬/Codespaces에서 정상 작동
- [ ] 실제 도메인(testseries1.pages.dev)에 배포 완료
- [ ] 모바일 터치 지연 보정 확인 완료
- [ ] 애드센스 심사 요건(콘텐츠량, 정책페이지 등) 충족
- [ ] `.env` 등으로 민감정보 분리 확인

## 9. 확장 로드맵 (Phase 2, 2026-07-02 기획 합의)

### 배경 / 방향성
레퍼런스로 strooptest.online을 검토함 — Stroop/Go-No-Go/반응속도/기억력/N-Back 등 테스트 구성은 참고할 만하지만, 임상 도구 톤이라 재미가 전혀 없고 공유 기능도 없음. 이 사이트는 아래 4가지를 최우선 가치로 삼아 차별화한다.
1. 재미가 있을 것
2. 사이트 내 다른 테스트로 유입되어 체류시간이 길어질 것
3. 테스트 결과가 실제로 유용할 것
4. 가족에게 공유하고 싶어질 것

### 9-1. 진행 단계 (순서대로 진행, 단계별로 다시 확인하며 작업)
- **A. 메뉴 구조 개편** — 사이드바에 "테스트" 그룹(하위 접기/펼치기) 신설, 로또/꿈해몽/운세는 별도 최상위 메뉴 유지. ← 현재 착수 단계
- **B. 신규 인지테스트 콘텐츠 제작** (테스트별로 나눠서 진행)
  - 반응속도, 숫자 기억력, 순서 기억력, 색각, 논리력, 충동억제(Go/No-Go)
  - MZ세대 취향 테스트 1~2개, 어르신 친화 테스트 1~2개 — 콘셉트는 착수 시 별도 브레인스토밍 후 승인받고 진행 (임의 확정 금지)
- **C. 난이도/모드 체계 적용** — B와 병행 (아래 9-2 참고)
- **D. 백엔드 구축 (퍼센타일 집계용)** — **보류.** 콘텐츠가 충분히 쌓여 실제 운영 단계에 들어갈 때, 카카오톡 공유 기능과 함께 착수하기로 함. DB는 Supabase, 파일 스토리지가 필요하면 Cloudflare R2 사용 예정. **그 시점이 되면 Claude가 먼저 리마인드할 것** (사용자 요청 — 강제는 아님)
- **E. 동물 비유 결과 카드 + 바이럴 공유** — D 완료 후 진행 (퍼센타일 데이터 필요). "…에 사는 …의 …" 형식으로 최소 100가지 이상 조합, 유머러스한 행동 팁 포함, 예쁜 공유 카드 자동 생성 + URL에 유입 링크 포함
- **F. 종합 인지 프로필(레이더 차트)** — 테스트 5개 이상 완료 시 홈 화면에 강점/약점 시각화
- **G. 라이트모드를 신규 콘텐츠 전체에 반영** — 전체 컨셉이 안정된 뒤 마지막에 일괄 적용 (매번 새 기능마다 라이트모드까지 같이 만들면 재작업이 반복되므로)
- **백로그**: 글로벌 커뮤니티 랭킹 (D/E 이후, 나중 논의)
- **백로그**: 오늘의 운세 기능 고도화 (이번 라운드 제외, 나중 별도 논의)

### 9-2. 난이도/모드 체계 (2026-07-02 합의)
- **신규 인지테스트**(반응속도/기억력/색각/논리력/충동억제 등): 쉬움/보통/어려움 3단계. 어려울수록 문항 수·속도·디테일이 늘어나는 느낌으로 설계
- **두뇌 나이(스트룹)**: 기존 쉬움/어려움 2단계 → 쉬움/보통/어려움 3단계로 확장
- **MBTI**: 간단 / 정밀 2모드. 간단=현재 12문항 그대로 유지, 정밀=문항 수 확대 + 결과 카드 상세도 강화 (세부 문항 수·항목은 착수 시 확정)
- **ADHD**: 간단 / 정밀 2모드 (난이도가 아니라 "진단 깊이" 개념). 점수 체계는 모드별로 재설계 필요
- **오늘의 운세**: 이번 라운드 대상 아님, 나중에 별도 고도화

### 9-3. 재미 요소 백로그 (구조가 안정된 뒤 순차 반영, 지금은 구현하지 않음)
우선순위 확정: 친구 대결 모드, 오늘의 챌린지 → 우선 반영 후보. 사운드/애니메이션도 반영 예정(시점 미정).
1. 친구 대결 모드 — 결과 링크 공유 → 동일 조건으로 상대도 도전 → 결과 비교 카드
2. 오늘의 챌린지 — 매일 다른 테스트 조합을 홈에 노출해 재방문 유도
3. 정답/콤보 사운드 · 마이크로 애니메이션
4. 칭호 시스템 — 누적 기록 기반 재미있는 별명 부여
5. 레벨/경험치 시스템 — 참여 자체로 쌓이는 성장형 게임화 요소
6. 미스터리 박스 — 특정 조건(스트릭 7일 등) 달성 시 히든 카드 프레임/코멘트 잠금 해제
7. **가족 그룹 비교판** (2026-07-02 확정, 나중에 반영) — 가족이 직접 그룹 이름을 정해서 그룹을 만들고, 공유 링크로 초대받은 가족 구성원이 그 그룹에 묶여 들어옴. 그룹 안에서만 보이는 소규모 랭킹/결과 비교 제공 (글로벌 랭킹과 별개, 가족 공유 목표에 가장 직접적으로 부합)
8. 시즌/시간대 한정 테마 — 이스터에그성 UI 변화 (할로윈 스킨, 심야 접속 문구 등)
9. **결과 궁합 보기** (2026-07-02 확정, 나중에 반영) — MBTI 한정이 아니라 성격/성향 계열 테스트 전반(향후 추가되는 성향 테스트 포함)을 서로 엮어서 궁합을 볼 수 있게 확장. 일치율(%)은 소수 첫째자리까지 표시(예: "87.3% 일치")하고, 그 일치율이 전체 사용자 중 상위 몇 %인지도 함께 표시 — **단, 상위 % 표시는 전체 사용자 데이터가 필요해 Stage D(백엔드) 완료 후에만 가능**. 그 전까지는 일치율(%)만 먼저 제공 가능
