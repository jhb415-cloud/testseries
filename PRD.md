# PRD.md — 5-in-1 Dashboard SPA

> 이 문서는 "무엇을 만들 것인가(What/Why)"를 정의한다.
> "어떻게 만들 것인가(How, 코드 규칙)"는 `CLAUDE.md`를 참고한다.
> AI가 요구사항을 임의로 추측하지 않도록, 모호한 표현 없이 목적/입력/출력/규칙/제약을 명시한다.

---

## 1. 목적 (Why)
- 10대~60대가 부담 없이 즐기는 미니테스트 모음 SPA를 만들어 트래픽을 확보하고, 구글 애드센스로 수익화한다.
- 단발성 방문이 아니라 **재방문**을 유도한다 (완주 현황, 방문 스트릭, 결과 공유).
- 목표는 "코드가 작동하는 것"이 아니라 **실제 배포되어 광고 심사를 통과하고 운영되는 것**이다.

## 2. 대상 사용자
- 연령: 10대~60대 (특정 세대에 치우치지 않는 UI/문구)
- 진입 경로: SNS 공유 링크, 검색(Naver 등)
- 기기: 모바일 우선 (터치 반응성 중요 — 두뇌나이 스트룹 테스트는 ms 단위 정밀도 필요)

## 3. 핵심 기능 — 입력/출력 정의
AI가 추측하지 않도록 기능별 입력·출력을 명시한다.

| 기능 | 입력 | 출력 | 비고 |
|---|---|---|---|
| MBTI | 12문항 답변 | E/I·S/N·T/F·J/P 4축 → 16유형 결과 카드 | |
| 꿈 해몽 | 검색 키워드(텍스트) | 매칭된 테마/variant 상세 카드 | 59개 테마, 매칭 없으면 추천 키워드 제시 |
| 오늘의 운세 | 출생년도(띠 계산) | 5개 영역 별점 + 행운숫자 | 날짜 seed 기반 결정론적 결과 |
| 두뇌 나이 | 스트룹 자극 반응시간(ms) | 등급 + 두뇌나이 | **모바일 터치 지연(Touch Delay) 보정 필수** |
| ADHD 진단 | 10문항 답변(점수) | 등급(A~D) + 행동 가이드 | |
| 로또 조합기 | 모드 선택(랜덤/직접지정/운세연동) | 5게임 번호 세트 | 오락 목적 명시 필수 |
| 마이홈 | localStorage 기록 | 완주현황/스트릭/최근기록 | 서버 없음, 클라이언트 저장만 |

## 4. 규칙 (Rules)
- 순수 Vanilla JS + Tailwind CDN, 빌드 도구 없음 (단일 정적 배포)
- 모든 텍스트/문구는 특정 연령대에 치우치지 않게 작성
- 새 기능 추가 시 기존 5개 테스트의 UX 패턴(닉네임 입력 → 진행 → 결과 → 공유)을 따를 것
- 스트룹 테스트 등 시간 측정 로직은 `touchstart` 우선 처리로 터치 지연 최소화할 것
- 상세 코드 컨벤션(파일구조, 네이밍, 데이터 스키마)은 `CLAUDE.md` 따름

## 5. 제약 사항 (Constraints)
- 백엔드 없음 → 실시간 외부 데이터(예: 로또 당첨번호 통계) 연동 불가, 필요 시 별도 서버리스 프록시 검토
- 광고 심사 통과 전까지 실제 AdSense 코드 대신 플레이스홀더만 유지
- 민감정보(API 키 등)는 반드시 `.env` 또는 배포 플랫폼 환경변수로 분리, 코드에 하드코딩 금지

## 6. 상태 보존 전략 (작게 자르고 자주 저장)
- 기능 단위(버전 0.0.x)로 작게 쪼개서 커밋 — 한 커밋에 여러 기능 섞지 않기
- 코드가 꼬였다고 판단되면 전체 재작성 대신, **직전 정상 커밋으로 롤백 후 재시도**
- 매 버전마다 `CLAUDE.md`의 기능 현황 표 + `HISTORIC_LOG.md` 동시 갱신 (이미 적용 중인 규칙)

## 7. 부분 검증 체크포인트 (AI 의심하기)
새 기능을 한 번에 통째로 요구하지 않고, 아래 단위로 끊어서 검증한다.
1. UI 뼈대(버튼/레이아웃)만 먼저 렌더링 확인
2. 핵심 로직(계산/판정) 단위 테스트 — 콘솔 로그로 값 확인
3. 실제 데이터 연결 후 화면 표시 확인
4. 모바일 실기기(또는 브라우저 반응형 모드)에서 터치/타이밍 확인
5. 에러 발생 시 "어느 단계에서 멈췄는지"를 구체적 증상 + 로그와 함께 전달 (전체 재작성 요청 금지)

## 8. 목표 재정의 (Definition of Done)
아래 조건을 모두 만족해야 "완료"로 간주한다.
- [ ] 로컬/Codespaces에서 정상 작동
- [ ] 실제 도메인(testseries1.pages.dev)에 배포 완료
- [ ] 모바일 터치 지연 보정 확인 완료
- [ ] 애드센스 심사 요건(콘텐츠량, 정책페이지 등) 충족
- [ ] `.env` 등으로 민감정보 분리 확인

## 9. 확장 로드맵 (Phase 2, 2026-07-02 기획 합의)

### 배경 / 방향성
레퍼런스로 strooptest.online을 검토함 — Stroop/Go-No-Go/반응속도/기억력/N-Back 등 테스트 구성은 참고할 만하지만, 임상 도구 톤이라 재미가 전혀 없고 공유 기능도 없음. 이 사이트는 아래 4가지를 최우선 가치로 삼아 차별화한다.
1. 재미가 있을 것
2. 사이트 내 다른 테스트로 유입되어 체류시간이 길어질 것
3. 테스트 결과가 실제로 유용할 것
4. 가족에게 공유하고 싶어질 것

### 9-1. 진행 단계 (순서대로 진행, 단계별로 다시 확인하며 작업)
- **A. 메뉴 구조 개편** — 사이드바에 "테스트" 그룹(하위 접기/펼치기) 신설, 로또/꿈해몽/운세는 별도 최상위 메뉴 유지. ← 현재 착수 단계
- **B. 신규 인지테스트 콘텐츠 제작** (테스트별로 나눠서 진행) — 2026-07-02 제작 순서 확정:
  1. 반응속도 테스트
  2. 숫자 기억력 테스트
  3. 순서 기억력 테스트
  4. 색각 테스트
  5. 논리력 테스트
  6. 충동억제 테스트 (Go/No-Go)
  7. 숏폼 집중력 테스트 (MZ향) — Go/No-Go 엔진 재활용, "숏폼 뇌 지수" 프레이밍
  8. 인싸력 테스트 (MZ향) — 문항 기반 사교성 성향 퀴즈
  9. 속담 완성 퀴즈 = 지혜 테스트 (어르신향) — 시간압박 없음, 긍정적 프레이밍
  10. 그 시절 물가 맞히기 (어르신향) — 향수 트리비아, 가족 대화소재용
- **C. 난이도/모드 체계 적용** — B와 병행 (아래 9-2 참고)
- **D. 백엔드 구축 (퍼센타일 집계용)** — **착수함 (2026-07-03, v0.0.40에서 1단계 완료).** 콘텐츠가 충분히 쌓여 실제 운영 단계 진입으로 판단, Supabase로 착수. 1단계: Anonymous Auth(로그인 UI 없음, 나중에 카카오 연결 가능하도록 manual linking 활성화) + `profiles`/`test_results`/`percentile_cache` 테이블 + RLS + `saveRanking()`을 통한 결과 이중 기록(로컬스토리지 유지). 댓글 백엔드는 모더레이션 리스크로 이번 스키마에서 제외(사용자 결정). 카카오톡 공유는 v0.0.41에서 별도로 완료(Kakao Developers 앱 생성 + JS 키 발급 + 도메인 등록). 퍼센타일 배치 집계는 Stage D 2단계로 로또 통계기반 추천과 함께 순차 진행 예정, 파일 스토리지 필요 시 Cloudflare R2 사용 예정
- **E. 동물 비유 결과 카드 + 바이럴 공유** — D의 퍼센타일 집계(다음 단계) 완료 후 진행 예정. "…에 사는 …의 …" 형식으로 최소 100가지 이상 조합, 유머러스한 행동 팁 포함, 예쁜 공유 카드 자동 생성 + URL에 유입 링크 포함
- **F. 종합 인지 프로필(레이더 차트)** — 테스트 5개 이상 완료 시 홈 화면에 강점/약점 시각화 — **완료(v0.0.29)**. D/E/F/G 우선순위를 사용자가 Claude에게 위임, D 착수 시점이 아직 아님을 재확인한 뒤 백엔드 불필요한 F부터 먼저 진행하기로 결정. "5개 이상"은 전체 15개 테스트가 아니라 인지테스트 성격의 7개(두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제, 숏폼집중력은 충동억제와 동일 엔진이라 제외)를 기준으로 해석해 구현. 각 테스트의 기존 Tier(S~D) 랭킹 기록을 100/80/60/40/20점으로 환산해 순수 SVG로 레이더 차트 렌더링(외부 라이브러리 없음, 신규 데이터/채점 로직 변경 없음)
- **G. 라이트모드를 신규 콘텐츠 전체에 반영** — 전체 컨셉이 안정된 뒤 마지막에 일괄 적용 (매번 새 기능마다 라이트모드까지 같이 만들면 재작업이 반복되므로). **2026-07-03 사용자 재확인: 프로젝트 전체를 통틀어 가장 마지막에 진행 (고정)**
- **백로그**: 글로벌 커뮤니티 랭킹 (D/E 이후, 나중 논의)
- **백로그**: 오늘의 운세 기능 고도화 (이번 라운드 제외, 나중 별도 논의)

### 9-2. 난이도/모드 체계 (2026-07-02 합의, 2026-07-03 v0.0.28에서 완료)
- **신규 인지테스트**(반응속도/기억력/색각/논리력/충동억제 등): 쉬움/보통/어려움 3단계. 어려울수록 문항 수·속도·디테일이 늘어나는 느낌으로 설계 — Stage B 진행 중 이미 각 테스트 구현 시점에 반영 완료
- **두뇌 나이(스트룹)**: 기존 쉬움/어려움 2단계 → 쉬움/보통/어려움 3단계로 확장 — **완료(v0.0.28)**. 기존엔 색상 개수(4/5색)만 다르고 문항수·시간이 고정이었던 걸 개선: 쉬움(4색·15문항·5.0초)/보통(5색·20문항·4.0초)/어려움(6색·주황 추가·25문항·3.0초). 채점 공식은 기존 유지
- **MBTI**: 간단 / 정밀 2모드 — **완료(v0.0.28)**. 간단=기존 12문항 그대로, 정밀=24문항(축당 6개, 신규 `mbtiQuestionsExtra` 12문항 추가)+결과 화면에 축별 비율 바(E73%/I27% 등, 답변 개수로 계산해 신규 데이터 작성 불필요) 추가. 16개 타입 결과 텍스트(강점/도전과제/팁/유명인)는 간단/정밀 공용으로 재사용
- **ADHD**: 간단 / 정밀 2모드 (난이도가 아니라 "진단 깊이" 개념) — **완료(v0.0.28)**. 간단=기존 10문항 단일 총점 그대로, 정밀=20문항(`adhdQuestionsPrecise`)을 부주의10+과잉행동-충동성10 두 영역으로 분리(DSM-5 구조 참고, 임상 진단 도구 아님을 계속 명시), 응답 척도도 정밀 모드에서 진짜 4단계(전혀0/가끔1/자주2/항상3)로 세분화. 종합 등급은 정밀 합산점수(최대60)를 3으로 나눠 기존 20점 만점 등급표에 그대로 대입(신규 등급 데이터 작성 없이 재사용)
- **오늘의 운세**: 이번 라운드 대상 아님, 나중에 별도 고도화

### 9-3. 재미 요소 백로그 (구조가 안정된 뒤 순차 반영, 지금은 구현하지 않음)
우선순위 확정: 친구 대결 모드, 오늘의 챌린지 → 우선 반영 후보. 사운드/애니메이션도 반영 예정(시점 미정).
- **2026-07-03 D-무관 백로그 순서 정리**: Stage D 착수 전 사용자가 "D 무관 항목들 중요도 순으로 정해달라"고 요청 → Claude가 순서 결정(버그수정 1순위는 사용자가 스킵 지시, 2순위인 친구대결·오늘의챌린지부터 진행). 3순위는 콘텐츠 보강(MBTI 16유형 텍스트 검수, 꿈해몽 데이터 확장 — **완료, v0.0.32**), 4순위는 재미요소 나머지(칭호/레벨/사운드/시즌테마/결과궁합 일치율), 5순위는 외부연동 필요 항목(카카오공유-Kakao키 발급 선행, 로또통계-Cloudflare Functions 프록시 구축 선행). **4순위 착수 시 사용자가 세부 순서를 재조정**: 칭호→레벨→결과궁합→사운드 순, 시즌테마는 불필요 판단으로 제외, 미스터리박스는 보류 — **전부 완료(v0.0.33)**. 꿈해몽 나머지 28개 테마 variants 확장은 "정식 배포 전 콘텐츠 보강" 단계로 순위를 완전히 뒤로 이동
1. 친구 대결 모드 — 결과 링크 공유 → 동일 조건으로 상대도 도전 → 결과 비교 카드 — **완료(v0.0.31)**. Tier(S~D) 채점 쓰는 8개 테스트 대상, URL 쿼리스트링에 결과 인코딩(백엔드 불필요)
2. 오늘의 챌린지 — 매일 다른 테스트 조합을 홈에 노출해 재방문 유도 — **완료(v0.0.31)**. 오늘의 한마디와 동일한 날짜 시드 방식, 13개 테스트 중 3개 로테이션
3. 정답/콤보 사운드 · 마이크로 애니메이션 — **완료(v0.0.33)**. Web Audio API 오실레이터로 정답/오답 비프음 직접 생성(외부 음원 없음), CSS keyframe(pop/shake)으로 즉시판정형 테스트(순서기억력/충동억제/숏폼집중력/속담완성/물가퀴즈)에 적용
4. 칭호 시스템 — 누적 기록 기반 재미있는 별명 부여 — **완료(v0.0.33)**. 방문 스트릭·완주 개수·Tier 성적 등 기존 로컬 기록만으로 뱃지 5종(개근왕/올라운더/천재 과몰입러/인싸력 만렙/지혜로운 어른) 계산
5. 레벨/경험치 시스템 — 참여 자체로 쌓이는 성장형 게임화 요소 — **완료(v0.0.33)**. `saveRanking()` 한 곳에 XP 적립 로직 추가(Tier 등급별 차등, 그 외 고정), 100XP당 1레벨
6. 미스터리 박스 — 특정 조건(스트릭 7일 등) 달성 시 히든 카드 프레임/코멘트 잠금 해제 — **범위가 모호해 2026-07-03 보류, 추후 재논의(구현 안 할 가능성 높음)**
7. **가족 그룹 비교판** (2026-07-02 확정, 나중에 반영) — 가족이 직접 그룹 이름을 정해서 그룹을 만들고, 공유 링크로 초대받은 가족 구성원이 그 그룹에 묶여 들어옴. 그룹 안에서만 보이는 소규모 랭킹/결과 비교 제공 (글로벌 랭킹과 별개, 가족 공유 목표에 가장 직접적으로 부합). 멀티 디바이스 그룹 멤버십이 필요해 Stage D(백엔드) 의존
8. 시즌/시간대 한정 테마 — 이스터에그성 UI 변화 (할로윈 스킨, 심야 접속 문구 등) — **2026-07-03 사용자가 불필요 판단, 백로그에서 제외**
9. **결과 궁합 보기** (2026-07-02 확정) — MBTI 한정이 아니라 성격/성향 계열 테스트 전반을 서로 엮어서 궁합을 볼 수 있게 확장 — **완료(v0.0.33, 일치율 %만)**. 친구 대결과 동일한 URL 공유 인프라(`#{section}?match=...`) 재사용, MBTI(4축 일치 비율)·인싸력(점수차 기반) 궁합 계산·표시. 그 일치율이 전체 사용자 중 상위 몇 %인지 표시하는 기능은 **여전히 Stage D(백엔드) 완료 후에만 가능**해 이번 범위에서 제외

### 9-4. 오늘의 한마디 개편 (2026-07-03 착수, v0.0.22~)
- **변경 전(v0.0.21까지)**: `data.js`의 `quotes` 배열(성격 불명확한 밈 문구) + `todaySeed()` 기반 의사난수로 매일 하나 뽑는 방식(`app.js` `initHome()`)
- **변경 후(v0.0.22~ 확정 스펙)**:
  - 주제: "삶을 통찰하는 이야기" 또는 "멘탈 관리 조언" 두 갈래로 한정
  - 인물: 철학자/사상가/작가/과학자 + 월가·투자 저명인사(워런 버핏, 찰리 멍거 등). 생존/고인·동서양 제한 없음, 한 인물당 1~2개 어록만 사용
  - 인용 정확도: 널리 검증된 실제 어록만 사용, 출처 불확실한 인터넷 밈 명언 제외. 번역 문구는 기존 번역을 베끼지 않고 사실관계만 참고해 새로 표현
  - 데이터 구조: `{ text, author, role }` (기존 문자열 배열 → 객체 배열로 변경)
  - **최종 목표 365개(v0.0.22 착수 시엔 700개였으나, v0.0.26에서 현실적으로 조정)** — 검증 가능한 고신뢰도 인물 어록만으로는 700개 확보가 비현실적이라 판단해 365개(하루 1개 × 1년)로 하향. 한 번에 만들지 않고 단계적으로 진행: 1단계 20개 샘플 승인 → 2단계 배치(36→60→29→21개 등)로 순차 확장하며 완성(v0.0.22~v0.0.27)
  - 소재: 인물 어록만으로 365개를 채우기 어려워, **인물 어록으로 최대한 채운 뒤(약 166개) 부족분은 삶/멘탈 관련 국내외 속담·격언(한국/중국/일본/영미/탈무드/아프리카/인도/아랍/독일/프랑스/스페인/러시아/아일랜드/북유럽/몽골/베트남/라틴/그리스 등)으로 보충**하는 방식으로 완성 (2026-07-03 사용자 확정)
  - 인용 정확도: 인물 어록은 널리 검증된 실제 어록만 사용, 출처 불확실한 인터넷 밈 명언 제외. 속담/격언은 문화권 단위로 널리 알려진 것만 사용. 번역 문구는 기존 번역을 베끼지 않고 사실관계만 참고해 새로 표현
  - 로테이션 로직: **24시간(KST, 하루) 단위**로 `seededRandom(dailyQuoteSeed())`(app.js)로 인덱스를 뽑는 방식(초기엔 12시간 단위였으나 v0.0.26에서 하루 단위로 변경) — 같은 날엔 모든 방문자에게 같은 문구가 노출됨. 365개 기준 정확히 1년 주기로 순환
  - 섹션 제목도 "오늘의 폼 미친 한마디"(장난스러운 톤) → **"오늘의 인생 한마디"**로 변경, 인용문 아래에 `— 인물명 (역할)` 표기 추가
  - **v0.0.27에서 365개 전량 완성** — 진행 이력은 `HISTORIC_LOG.md` 세션 #6 참고
