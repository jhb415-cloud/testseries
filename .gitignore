.wrangler/
node_modules/
.env

# 이상형 월드컵 이미지 파이프라인 원본(리사이즈 전 대용량 png/jpg) — 가공본만 worldcup/images/에 커밋
worldcup-images-raw/

# test-engine 이미지 생성 스타일 레퍼런스 원본 — 빌드 입력값일 뿐, 최종 결과물(assets/cover.webp,
# assets/result/*.webp)만 커밋. 대용량이라 리포에 안 올림(Codespaces 로컬에만 유지).
test-engine/tests/*/assets/reference/
