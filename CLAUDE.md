# CLAUDE.md — 5-in-1 Dashboard SPA 아키텍처 규칙

## 현재 버전: v0.8.2

## 프로젝트 개요
Tailwind CSS(CDN) + 순수 Vanilla JS 기반 5-in-1 종합 테스트 대시보드 SPA.
빌드 도구 없음, 단일 HTML 파일 구조 (외부 JS/CSS 로드 방식).

**사이트 브랜드명(v0.0.30~)**: 사용자에게 노출되는 사이트 제목/부제목은 "과몰입 연구소" / "재미로 시작했다가 뼈 맞고 공유하는 종합 테스트 모음"으로 변경됨(`index.html`의 `<title>`, 사이드바, 모바일 헤더, 푸터, meta description 전부 반영). 이 문서 제목과 각 파일 상단의 버전 코멘트(`5-in-1 Dashboard SPA`)는 내부 개발용 프로젝트명이라 별도 요청 전까지는 그대로 유지.

---

## 2. 기능 현황

| 기능 | 상태 | 버전 | 비고 |
|---|---|---|---|
| 🎨 Kinetic Logic 리디자인 후속 수정 7건(다크모드 잔재 확인/홈 카드 배색/홈 섹션 개편/FAQ/월드컵 톤/색상 리맵 감사/전체 크롤) → **v0.8.2: 폰트 뭉개짐/히어로 텍스트 무색투명화/로또 버튼 톤/글자크기 확대 6건** | ✅ 완료 | v0.8.0 → v0.8.2 | **v0.8.2 상세는 "4. 변경 이력" 표의 2026-07-11 v0.8.2 행 참고**(요약: `font-synthesis: none`으로 한글 합성볼드 뭉개짐 차단, 심리테스트존 히어로 배너 텍스트를 고정 hex 클래스로 분리해 무색투명화 수정, 로또 조합기 버튼에 `border` 클래스를 추가해 Phase 1 브루탈리즘 shim이 걸리도록 수정, test-engine 9개 테마의 질문/답/결과 텍스트를 `engine.css` 공용 수정 한 곳으로 15% 확대). 이하 v0.8.1(7건) 상세 → 사용자가 라이브 스크린샷 다수(홈/월드컵/두뇌테스트존/MBTI/ADHD)로 7가지를 한 번에 제보. **①다크/라이트 토글**: 코드에는 이미 완전히 제거돼 있음을 grep으로 재확인(v0.8.0 첫 커밋에서 이미 삭제됨) — 사용자가 본 화면은 Cloudflare/브라우저가 index.html은 구버전(v0.7.1, 토글 버튼 있음)을 캐싱한 채로 `?v=` 쿼리만 붙은 style.css는 최신 버전을 받아온 "옛 마크업+새 CSS" 혼종이었을 가능성이 높다고 진단(실제 로직 재현 안 됨), 코드 수정 없이 하드리프레시 안내만. **②홈 카드 배색**: "오늘의 인생 한마디"(`from-amber-900/40 to-amber-950/30`)와 "내 정보"(`from-indigo-900/60 to-indigo-950/40`)가 `amber-950`/`indigo-950`을 쓰고 있었는데 이 -950 shade들은 애초에 리맵 목록에 없어(950은 slate 전용) 라이트 팔레트에서 Tailwind 리터럴 다크값이 그대로 남아 탁하게 보였던 것 — 두 카드+오늘의 챌린지(`from-fuchsia-900/40`, fuchsia도 미리맵) 전부 그라데이션을 걷어내고 `--tertiary-container`(인생한마디)/`--surface-lowest`+ink 테두리(내정보)/`--secondary-container`(챌린지) 플랫 컬러+굵은 테두리로 재작성, XP 바도 커스텀 그라데이션 대신 공용 `.progress-bar-fill`(줄무늬) 재사용. **③홈 섹션 개편**: `sections.json`의 "지금 뜨는 신설존"+"MBTI존" 2개 섹션을 `dynamic:"psych-trending"`(신규) 1개로 통합해 "🆕 요즘 뜨는 심리테스트"로 명명, `externalTests`+`mbtiZoneTests`를 합쳐 인기순 상위 3개만 노출. "🔥 이번주 인기"는 limit 6→3, `buildPopularCandidates()`에 그동안 빠져있던 메뉴(몰입테스트/MBTI존 자체, 이상형 월드컵 6팩, 가족오락관 3게임, 꿈해몽/오늘의운세/로또조합기)를 신규 헬퍼(`buildPsychTrendingCandidates`/`buildWorldcupCandidates`/`buildFamilyCandidates`) 3개로 추가해 실제 "전체 메뉴" 기준 인기 후보 풀로 확장(전부 기존 `engagementCount` 로컬 카운터 재사용, 신규 원격 집계 없음). `homeCardHTML()`에 `image`/`thumbHTML` 필드 지원 추가 — 실제 커버 이미지가 있는 카드(몰입테스트/MBTI존은 `psyCoverImage()`, 이상형 월드컵은 기존 `wcThumbHTML()` 2분할 썸네일 재사용)는 큰 이모지 대신 실제 썸네일을 렌더링, 없는 카드는 기존처럼 이모지 유지. **④FAQ 아코디언**: "한 번에 안 열린다"는 제보를 Playwright로 실제 재현 시도했으나 정상 클릭 시 항상 즉시 열림을 확인(네이티브 `<details>`라 커스텀 JS 없음) — 재현은 안 됐지만 방어적으로 `<summary>`에 `block w-full` 추가해 클릭 가능 영역을 카드 전체 너비로 보강. **⑤이상형 월드컵 테마 미적용**: `.wc-card`/`.wc-btn-accent`/`.wc-btn-ghost`/`.wc-cat-chip`/`.wc-sort-btn`/`.wc-round-btn`/`.wc-match-card`/`.wc-vs-badge`/`.wc-thumb`(신규 `.wc-thumb-framed` 래퍼) 등 이 모듈 전용 커스텀 클래스들은 Tailwind 유틸리티 클래스명이 아니라서 Phase 1의 속성선택자 shim이 전혀 적용되지 않고 있었음(구버전 얇은 테두리 그대로 방치) — 전부 굵은 ink 테두리+하드 그림자로 직접 재작성. **⑥색상 리맵 전수 감사**: app.js/index.html 전체에서 실제 쓰이는 색상-shade 조합을 스크립트로 추출해 CSS 변수 정의와 diff한 결과 27개 조합이 리맵 누락 상태였음을 발견(`amber-700/800`, `violet-700/800`, `purple-700/800`, `pink-*`, `indigo-700`, `orange-*`, `cyan-*`, `fuchsia-*`, `emerald-200`, `rose-200`, `yellow-400/700` 등) — 이 때문에 형제 색상인 emerald/rose(800까지 리맵됨)는 멀쩡한데 amber(900/400/300만 리맵)는 유독 탁하게 보이는 등 "일부 버튼만 톤이 다른" 현상이 발생하고 있었음(두뇌나이 난이도 "보통", MBTI "정밀 모드", ADHD "정밀 모드" 버튼이 대표 사례). `style.css`의 `html{}` 변수 블록과 `index.html`의 `tailwind.config.colors`에 27개를 일괄 추가(red-400/900/950은 HELL 버튼 전용으로 항상 검정 배경 위 밝은 글자여야 해서 의도적으로 리맵 대상에서 제외, 그대로 둠). 근본 원인 하나를 CSS 변수 레벨에서 고쳐 마크업은 전혀 손대지 않고 두뇌나이/MBTI/ADHD 모드 버튼 전체가 한 번에 해결됨. **⑦전체 재검토**: 25개 섹션(두뇌테스트존 13개+심리/밸런스/가족오락관/로또/월드컵/약관 등) 전체를 Playwright로 순회하며 콘솔 에러 0건 확인, 홈/월드컵 탐색·인트로·매치 화면/브레인 난이도/MBTI·ADHD 모드 선택/밸런스게임/가족오락관/사주타로궁합/로또 직접뽑기/심리테스트존 화면을 스크린샷으로 육안 확인. **검증**: `node --check` 통과, `sections.json` JSON 유효성 확인, 25개 섹션 크롤 콘솔 에러 0건, 위 화면들 스크린샷 확인. 캐시버스팅 `?v=`·CLAUDE.md·사이드바 버전 표시 전부 v0.8.1로 갱신 |
| 🎨 전체 UI 리디자인 — "Kinetic Logic"(다크/라이트 토글 제거 → 단일 고정 라이트 테마, 굵은 검정 테두리+입체 그림자의 네오 브루탈리즘 톤) | 🔄 진행 중 — Phase 1(전역 팔레트+shim) 완료, Phase 2 일부(심리테스트존 질문카드) 완료 | v0.7.1 → v0.8.0 | 원본 기획(코드 주석상 `DESIGN1.md`)은 텍스트 파일이 아니라 사용자가 별도로 만든 참고 목업 이미지(PSYCH-LAB/NEURO-SYNC 두 콘셉트)로만 존재 — 저장소나 스크래치패드 어디에도 파일로 남아있지 않으니, 이어서 작업할 땐 사용자에게 그 이미지를 다시 첨부받아야 함(세션 컨텍스트가 끊기면 다시 사라짐 — `[[project_kinetic_logic_redesign]]` 메모리에 목업에서 관찰한 시각 언어를 텍스트로 최대한 남겨둠). **Phase 1(전역, 완료)**: `html.light` 별도 팔레트를 없애고 `html` 단일 팔레트로 통합(크림/화이트 배경+진한 잉크 텍스트 고정, slate 50~100은 이제 "진한 잉크 텍스트", 700~950은 "밝은 배경"을 의미하도록 정의가 뒤바뀜 — 기존 다크모드 우선 스키마와 정반대라 이 변수를 참조하는 코드를 읽을 때 주의). 사이드바/모바일 헤더의 다크·라이트 토글 버튼과 `setTheme`/`toggleTheme`/`applyThemeIcon` 전부 제거(변수 스키마 자체는 남겨둬서 나중에 다크 테마를 값 세트 하나 추가+토글 재도입만으로 복원 가능하게 설계). `style.css`에 속성선택자 기반 전역 shim 추가 — `border-{color}-*` 유틸리티를 전부 잉크색으로 강제하고, `rounded-*`+`border` 조합 엘리먼트에 일괄 3~6px 굵은 테두리+하드 오프셋 그림자(6px 6px 0 0)+눌림 액티브 상태를 부여(app.js의 약 150여 곳 인라인 카드 마크업을 하나하나 고치지 않고 기존 class 문자열을 그대로 재활용한 안전망, Phase 2~4에서 화면별로 정밀 대체 예정). Sunburst Yellow(`--primary-container`)/Electric Cyan(`--secondary-container`)/Punch Magenta(`--tertiary-container`) 3색 신규 토큰 도입. Google Fonts(Plus Jakarta Sans/Space Grotesk/Be Vietnam Pro — 한글 글리프 없어 한글은 시스템 폰트로 자동 폴백, Black Han Sans는 한글 헤드라인 전용으로 병행) 추가. **다크→라이트 단일화 과정에서 발견한 회귀 버그 2건 수정**: 홈 "오늘의 인생 한마디" 인용문이 `text-white` 하드코딩이라 이제 밝아진 amber 카드 위에서 안 보이던 것(`text-slate-100`으로 교체), 로또 공 색상 31~40 구간이 `bg-slate-700 text-white`였는데 `slate-700`이 이제 거의 흰색을 의미하게 돼 숫자가 안 보이던 것(테마와 무관한 고정 hex `bg-[#374151]`로 교체 — 실제 로또 공 색상 규칙은 사이트 테마와 무관해야 하므로). **Phase 2 착수(진행 중)**: 목업 스타일 중 위험이 낮은 첫 조각으로 ①공용 `.option-btn` 클래스에 옐로/시안/마젠타/그레이 4색 순환을 `nth-of-type` CSS만으로 추가(마크업 무변경 — MBTI/ADHD/인싸력/속담완성/물가퀴즈/논리력/숫자기억력 키패드/심리테스트존까지 이 클래스를 쓰는 모든 화면에 한 번에 적용됨) ②사용자가 "심리테스트존은 몰입테스트·MBTI존 두 카테고리만 빼고 전부 적용"으로 범위를 지정 — 이 두 카테고리는 애초에 `/test-engine/`으로 링크만 나가는 구조라 아래 질문카드 함수 자체를 안 타므로 자동으로 제외됨. 신규 `.quiz-question-card`(말풍선 카드: 굵은 테두리+하드 그림자)+`.quiz-question-avatar`(카드 하단에 매달리는 원형 캐릭터 아이콘, CSS 삼각형 트릭으로 말풍선 꼬리 구현)를 `renderPsychtestQuestion()` 한 곳에 적용해 심리테스트존 실콘텐츠 6개(전생동물/짠테크/혈액형/민초/부먹찍먹/칼답러) 전체에 반영, 아이콘은 각 테스트 데이터의 기존 `emoji` 필드 재사용(신규 자산 없음). Artifact(스크린샷 base64 임베드 비교 페이지)로 혈액형/민초 두 샘플을 먼저 보여주고 승인받은 뒤 확정. **아직 손 안 댄 것(다음 후보, 화면마다 개별 마크업 수정 필요)**: MBTI/ADHD 등 심리테스트존 밖의 질문 화면에 동일한 말풍선 카드 적용 여부(사용자 확인 필요), 결과 화면의 입체 그림자 대형 타이틀 처리, 상단 헤더의 하트/별 배지, 데스크톱 상단 내비게이션 바. 목업의 모바일 하단 탭바(홈/테스트/통계/프로필 4탭)는 이 사이트가 25개 이상 섹션을 사이드바로 관리하는 구조와 안 맞아 그대로 옮기지 않기로 함. **검증**: `node --check` 통과, Playwright로 홈/MBTI/ADHD/로또 조합기·통계기반 결과/두뇌나이 난이도 화면과 심리테스트존 혈액형·민초 문항 화면을 다크→라이트 전환 후 스크린샷 확인(콘솔 에러 0건). 커밋 전 상태에서 세션이 한 번 끊겼던 이력이 있어(메모리에 기록) 이번엔 매 단계 결과를 Artifact로 실제로 보여주고 승인받은 뒤에만 다음 단계로 진행 |
| 🧪 심리테스트 전면 재구축 — 신규 config 기반 테스트 엔진(`/test-engine/`) STEP 1(엔진+1호 콘텐츠) → STEP 1.5(AI 일러스트 교체) → STEP 2(공유카드+related+sitemap+사이트 연동) → STEP 3(홈 화면 큐레이션 시스템+몰입테스트/MBTI존 신설+test-engine 카카오공유) | ✅ STEP 3까지 완료 — **홈 화면 진열대가 `sections.json` 하나로 큐레이션되고, 심리테스트존에 몰입테스트/MBTI존 카테고리가 신설됨, 몰입테스트 #4~6까지 사이트 연동 완료, 몰입테스트/MBTI존 카드가 커버 이미지 포스터형으로 리디자인됨** | v0.6.4 → v0.6.8 | **STEP 3**: 사용자가 "심리테스트존을 홈 화면에서도 더 잘 보이게 하고 싶다"는 방향으로 STEP 2 이후 이어서 요청(프롬프트는 이전 세션에서 설계, 이번 세션은 그 작업을 이어받아 완료+검증). **①홈 화면 큐레이션 시스템**: 기존엔 `initHome()` 안에 "도구 그리드"/"인기 테스트 그리드" 2개가 하드코딩 고정이었던 것을, 신규 루트 `sections.json`(사용자가 편집하는 설정 파일 — id/title/highlight/test_ids 배열, 또는 `dynamic:'popularity'`+`limit`로 기존 인기순 자동계산 재사용, 또는 `placeholder`로 콘텐츠 없는 섹션에 "준비중" 티저 카드 노출) 하나로 진열대 구성을 바꿀 수 있게 일반화. `app.js`에 `renderHomeSections()`(fetch로 `sections.json` 로드 → 섹션별 카드 조회/정렬 → 빈 섹션은 placeholder 없으면 조용히 숨김 → `#home-sections-container`에 렌더)와 `buildTestIndex()`(내부 코어 14개+심리테스트존+밸런스게임+`AppData.externalTests`를 id 하나로 조회 가능하게 통합한 인덱스, 존재하지 않는 id는 조용히 무시)를 신규 추가, 기존 `initHome()`의 인라인 카드 배열(`CORE_TEST_CARDS`/`HOME_TOOL_CARDS`)과 정렬 로직(`buildPopularCandidates()`)은 그대로 함수로 분리 재사용. 카드 클릭 핸들러는 DOM 순서 의존 대신 렌더링 시점에 고유 id로 바인딩. index.html의 "🎯 서비스 바로가기"/"🔥 지금 인기있는 테스트" 고정 그리드 2개를 `#home-sections-container` 컨테이너 하나로 교체. **②몰입테스트/MBTI존 카테고리 신설**: `PSYCHTEST_CATEGORIES`에 `immersive`(몰입테스트, `/test-engine/` 프로토타입존 — STEP 2의 `externalTestsBannerHTML()`을 그대로 이 카테고리 전용으로 이동)와 `mbtizone`(MBTI존, 아직 콘텐츠 없어 "MBTI 정밀 유형 분석" 잠금 카드만 있는 준비중 상태) 2개를 최상단에 추가, 둘 다 `badge:'NEW'`로 카테고리 탭 자체에 배지 노출(사이드바 하위메뉴에도 동일한 `.badge-new-nav` 배지 추가). character/trait/taste/national 4개는 `[[project_psychtest_menu_consolidation]]` 메모대로 애드센스 승인 후 통합 예정이라 이번엔 손대지 않음. **③test-engine 카카오톡 공유+메인 링크**: `test-engine/engine.js`의 `init()` 시점에 메인 사이트와 동일한 Kakao 앱 키(같은 도메인이라 도메인 화이트리스트 추가 등록 불필요)로 SDK를 자동 로드하는 `loadKakaoSdk()`와, 모든 화면(질문 중간 포함)에서 메인 사이트로 빠져나갈 수 있는 고정 링크(`injectHomeLink()`, `document.body`에 직접 붙여 `rootEl.innerHTML` 교체와 무관하게 항상 노출)를 신규 주입 — 새 테스트를 추가해도 이 두 가지가 자동 적용되도록 설계. 결과 화면에 "💬 카카오톡으로 공유하기"(feed 템플릿) 버튼을 최우선 배치로 추가(버튼 4개로 늘어 하단 여백도 확대). **이번 세션에서 발견·수정한 버그**: `buildTestIndex()`가 `CORE_TEST_CARDS`(mbti/brain 등 14개 내부 테스트) 항목에 `id` 필드가 없어 `index[c.id]`가 전부 `index[undefined]`로 겹쳐 써지는 문제를 발견 — `sections.json`이 아직 이 id들을 직접 참조하지 않아 현재는 증상이 드러나지 않지만, 그대로 두면 나중에 `test_ids`에 `"mbti"` 같은 내부 테스트 id를 직접 쓰는 섹션을 추가할 때 조용히 실패했을 것이라 `index[c.id || c.section]`로 최소 수정. **검증**: `node --check`로 app.js/data.js/engine.js 문법 확인, Node로 `buildTestIndex()`를 직접 재현해 `mbti`/`dream` 등 id 조회가 정상 해결됨을 확인, 정적 서버+Playwright로 (a) 홈 화면이 `sections.json`대로 "🆕 지금 뜨는 신설존"/"🧬 MBTI존"(준비중 placeholder)/"🔥 이번주 인기"/"🎯 도구 모음" 4개 섹션만 렌더링(콘텐츠 없는 "☀️ 시즌 기획전"은 조용히 숨겨짐)되는지 (b) 신설존 카드 클릭 시 실제 `/test-engine/tests/mental-age/index.html`로 이동하는지 (c) 심리테스트존에서 몰입테스트 탭 진입 시 배너가 정상 노출되고 MBTI존 탭 진입 시 잠금 카드가 노출되는지 (d) 홈의 MBTI존 placeholder 카드를 클릭하면 실제로 `psychtest` 섹션의 `mbtizone` 카테고리로 정확히 이동하는지 (e) mental-age 테스트를 처음부터 끝까지 실제로 플레이해 결과 화면에 카카오/이미지저장/공유하기/다시하기 4개 버튼과 좌상단 "← 메인으로" 링크가 전부 정상 노출되는지까지 전부 콘솔 에러 없이 확인. **STEP 3 이후(2026-07-10 야간 세션, 버그수정+콘텐츠 3건 신규+기존 3건 리라이팅)**: 사용자가 스크린샷 7장으로 몰입테스트/MBTI존 문제를 일괄 제보 — ①mental-age 일부 문항에서 카드가 "반으로 나뉜" 것처럼 보이는 레이아웃 버그 ②#11~13(MBTI존 3종)의 문항 이미지가 내용과 무관 ③#11~13 질문/답변이 "본인 유형은?" 식 단답형이라 성의 없어 보임 ④#12는 설명 자체도 부족. **①레이아웃 버그**: 원인은 `themes/retro.css`의 가짜 타이틀바(`::before`)가 `.te-question-body`(engine.css가 이미지 없는 문항엔 `justify-content:center`로 세로 중앙정렬)의 flex 자식이라, 타이틀바까지 카드 가운데로 밀려 카드가 위/아래로 쪼개져 보이던 것 — 타이틀바를 `position:absolute`로 상단 고정 + 본문에 `padding-top:44px` 보정으로 수정(mental-age 전용 retro 테마만 영향, 다른 8개 테마는 영향 없음). ②③④(#11~13): 질문/답변 전체를 "OO할 때 나는 어떻게 할까?" 식 상황 서술형 완결 문장으로 전면 리라이팅(`choice.axis` 값은 그대로 두고 `text`/`label` 텍스트만 교체 — 채점 로직 무변경), #12는 설명(`description`)도 "평소엔 안 그러는데 연애만 하면 딴사람 된다?"는 후킹 문구로 보강. 문항 이미지는 사용자가 먼저 "API(images/edits)로 품질을 올려보자"고 요청해 #11/#13에 샘플 2장을 실제 생성(레퍼런스 스타일 전이로 품질 자체는 좋았음)했으나, 이어서 "20개 전체로 확장하면 비용이 꽤 든다"고 판단해 문항 이미지는 SVG 제작으로 원복 확정(API 생성 결과물은 폐기, `generate-assets.js`의 `question-` id 분기도 되돌림) — 대신 "SVG라도 그 문항 내용을 제대로 형상화하라"는 요구를 반영해 10장(#11 4+#12 3+#13 3) 전량을 문항별 맞춤 디자인으로 재제작(예: "갈등 상황에서 남들이 보는 나=판사 역할 vs 다독이는 역할" 문항엔 다투는 두 사람+중재자를, "취기가 오르면 감정이 터지는 방향=팩폭 vs 눈물고백" 문항엔 번개+눈물방울 아이콘을 직접 그림). **이어서 같은 기준으로 몰입테스트 #4(소비유형)/#5(빌런지수)/#6(MBTI 과몰입) 3개 신규 제작**: 세 테스트 모두 사용자가 이미 텍스트 초안(config.json)과 Gemini 레퍼런스 이미지를 준비해둔 상태였음(각각 빈티지 신문 콜라주/필름누아르 수배전단/스테인드글라스 성화 스타일) — 질문·답변을 동일하게 상황 서술형으로 리라이팅(type/sum 채점값은 무변경), 레퍼런스 3장 기반으로 신규 테마 3종(`vintage-collage.css`/`noir-comic.css`/`stained-glass.css`, 전부 fantasy.css 패턴을 따르되 각 매체에 맞는 카드 프레임·폰트·색감을 새로 설계) 신설, `images/edits`로 커버+결과 이미지 18장(7+5+6) 전량 1회 생성 성공(재시도 1건, OpenAI 세이프티 필터 일시 차단 후 자동 재시도로 통과). #4/#5/#6은 전부 선택지 4~6개짜리라 4-4-1 규칙상 애초에 문항 이미지 대상이 아니라(선택지 버튼 자체가 화면을 채움) 별도 제작 없음. **검증**: `node --check` 통과, Playwright로 7개 테스트(mental-age/#4/#5/#6/#11/#12/#13) 전체를 실제로 완주시켜 결과 화면 타이틀·이미지 로드·콘솔 에러 0건까지 확인(QA 스크립트에 문항→결과 전환 시 로딩 화면 구간을 놓쳐 오탐하던 버그가 있었는데, 이건 테스트 스크립트 자체의 버그였고 앱은 처음부터 정상 동작이었음 — Promise.race로 수정 후 재검증), 신규 문항 SVG 10장을 실제 화면 스크린샷으로 육안 확인(전부 해당 문항 내용과 명확히 연결됨). `test-engine/CLAUDE.md` 4-1(질문 자연문장화 규칙)·4-4-1(문항 이미지 SVG 유지 재확정)에 이번 방침을 명문화, `style-guide.md`에 3개 신규 매체+톤 기록 및 SVG 원복 경위 기록. **사이트 연동은 보류** — #4~6/#11~13 전부 아직 `sections.json`/`sitemap.xml`/`AppData.mbtiZoneTests` 등 실사이트 연결 전 단계(다음 세션에서 사용자 승인 후 진행). **2026-07-11 세션 — 몰입테스트 #4/#5/#6 사이트 연동 완료(v0.6.7)**: 지난 세션에서 "index.html 생성 + QA"가 미완 상태로 남아있었던 걸 이어받아, 먼저 Playwright로 #4/#5/#6 QA를 재검증(min/max 양극단 경로로 실제 플레이, 결과 화면 렌더링·이미지 로드·카카오공유/이미지저장/다시하기 버튼·villain-index의 `sum+tag` 채점과 `{tag}` 치환 전부 콘솔 에러 0건 확인). 검증 중 의심됐던 두 가지는 모두 실제 버그가 아님을 확인 — ①결과 화면 하단 안내 문구("이미지를 꼭 눌러 저장해보세요")가 고정 푸터에 가려 보이는 것처럼 보였으나, `syncFixedFooterHeight()`가 `padding-bottom`을 버튼바 높이에 맞춰 정확히 계산해두고 있어 실제로 스크롤하면 정상 노출됨(페이지가 뷰포트보다 길어 초기 스크롤 위치에서만 일부가 안 보이는 정상 동작) ②vintage-collage 테마의 타이틀 폰트(Black Han Sans, 24px)가 이 headless 테스트 환경에서 글자가 겹쳐 보이는 현상은 이미 사이트 연동까지 끝난 #1(k-office-type, 동일 폰트 사용)에서도 동일하게 재현되는 **테스트 샌드박스 전용 폰트 렌더링 아티팩트**(48px 에서는 정상 렌더링됨)로 확인, 실사용자 브라우저 이슈가 아니라 코드 수정 없이 넘어감. QA 통과 확인 후 사용자에게 연동 여부를 확인받아 진행 — `data.js`의 `AppData.externalTests`에 3개 항목 추가(#1/#2와 동일한 필드 구조), `sections.json`의 "new" 섹션 `test_ids`에 3개 id 추가, `sitemap.xml`에 URL 3줄 추가. `tests/index.json`의 #4~6 상태를 "완료(운영중)"으로 갱신하고 `concept_style`/`note` 필드도 #1/#2/#11~13과 동일한 형식으로 채워 일관성을 맞춤(부수적으로 #2의 상태 문구가 실제로는 이미 사이트 연동이 끝났는데 "연동 전"으로 방치돼있던 걸 발견해 함께 정정). **검증**: `node --check`로 data.js/app.js 문법 확인, `sections.json`/`sitemap.xml` 파싱 유효성 확인, 정적 서버+Playwright로 (a) 홈 화면 "🆕 지금 뜨는 신설존" 섹션에 신규 3개를 포함해 6개 카드 전부 렌더링되고 클릭 시 실제 test-engine 페이지로 이동 (b) 심리테스트존 몰입테스트 탭의 3열 그리드에도 신규 3개가 포함되어 인기순으로 정렬되고 클릭 시 정상 이동까지 콘솔 에러 없이 확인. **2026-07-11 후속 세션 — 몰입테스트/MBTI존 카드 UI 리디자인(v0.6.8)**: 사용자가 라이브 화면 스크린샷(poomang.com·방구석연구소 레퍼런스 첨부)으로 "카드가 이모지+회색배경이라 밋밋하고 AI스럽다"고 지적, "썸네일은 테스트 제작 시 같이 생성하는 게 좋겠다"는 방향을 제시. 코드 조사 결과 **이미 있는 걸 못 쓰고 있던 것**으로 확인 — 몰입테스트/MBTI존 9개 테스트 전부 `test-engine/CLAUDE.md` 4-4 파이프라인이 생성해둔 `assets/cover.webp`(테마별 AI 일러스트)가 있는데 카드 렌더러(`externalTestsFeedHTML()`)가 이모지 원형만 그리고 있었음. 실제 cover.webp를 base64로 임베드한 Artifact 목업(전/후 비교, 그리드 전체, 히어로 배너, 잠금카드, 보너스 아이디어 7종)을 먼저 만들어 확인받은 뒤(1차 배포 시 라이트모드에서 텍스트가 안 보이는 대비 버그를 사용자가 재확인해줘서 다크 전용 단일 테마로 고쳐 재배포) AskUserQuestion으로 범위(1단계=카드+배너+랭킹리본+조회수포맷만 / 잠금카드는 스타일만 통일)를 확정받아 진행. **구현**: `data.js`의 externalTests/mbtiZoneTests 9개 항목에 조회수 초기값 `baseCount`(200~300 사이 임의 배정, 실제 카운팅이 쌓이기 전 조회수가 0으로 안 보이게) 필드 추가 — 썸네일 경로는 별도 필드 없이 `url`에서 파생(`psyCoverImage()`, `index.html`→`assets/cover.webp` 치환, 4-4 파이프라인이 모든 테스트 폴더에 커버를 생성해두는 관례를 신뢰). `app.js`에 `formatCount()`(1만 이상만 "1.2만" 축약, 그 미만은 그대로 숫자) 신규, `externalTestsFeedHTML()`을 전면 재작성 — 이모지 원형 카드 → 3:4 포스터 타일(cover.webp 배경+하단 그라데이션 스크림 위에 제목/훅 오버레이), 인기순 1~3위엔 금/은/동 순위 리본, 대표 배너도 같은 원리의 와이드 히어로 이미지 배너로 확대. 제목/훅 텍스트를 이미지에 직접 굽지 않고 HTML 오버레이로 유지한 이유: gpt-image-1의 한글 텍스트 렌더링이 아직 불안정하고(획 깨짐 위험), 카피 수정 시 이미지 재생성이 불필요해짐. 잠금카드(캐릭터/성향/취향/국민테스트 4개 카테고리, 아직 커버 아트 없음)는 사용자가 "이번엔 스타일만 통일"로 확정한 대로, 실사진 대신 무드 그라데이션+이모지 워터마크+자물쇠로 같은 포스터 틀만 재사용(`renderPsychtestFeed()`의 locked 카드 블록 교체, 옆에 남는 "reals" 실콘텐츠 카드는 이번 범위 밖이라 무변경 — 다만 같은 그리드 행에서 키가 다른 카드끼리 늘어나 보이지 않게 `items-start` 추가). `style.css`에 `.psy-card-*`/`.psy-hero-*`/`.psy-locked-*` 신규(기존 `.wc-match-card`의 이미지+스크림 오버레이 패턴을 그대로 재사용, NEW 배지는 기존 `badge-new-sparkle` 키프레임 재사용, 이미지 로드 실패 시 기존 `wcImgFallback()` 재사용 — 신규 자산·신규 애니메이션 없이 기존 관례만으로 구현). **범위 밖(2단계 후보)**: 캐릭터/성향/취향/국민테스트 4개 카테고리의 실콘텐츠 카드에 커버 아트를 새로 만드는 것, poomang식 인기 TOP 미니리스트·테마별 가로 스크롤 큐레이션 — 사용자 승인 후 순서대로 진행 예정. **검증**: `node --check`로 app.js/data.js 문법 확인, 정적 서버+Playwright로 (a) 몰입테스트/MBTI존 그리드 9개 카드 전부 cover.webp 정상 로드+제목/훅 오버레이+금은동 리본(1~3위)+NEW 배지 노출 (b) 히어로 배너 이미지 배경 정상 렌더링 (c) 캐릭터/성향/취향 카테고리의 잠금카드가 무드그라데이션+워터마크+자물쇠로 정상 표시되고 옆 실콘텐츠 카드와 그리드가 깨지지 않는지 (d) 이미지 경로를 일부러 깨뜨려 `wcImgFallback` placeholder로 안전하게 폴백하는지까지 콘솔 에러 없이 확인. 이하 STEP 2 상세 → | **STEP 2**: 사용자가 "구현은 괜찮은데" 다음 단계로 결과 공유카드+관련테스트 배너+실사이트 연동+sitemap 반영을 요청. **작업1(Canvas 공유카드)**: 신규 `test-engine/result-card.js`(`window.TestEngineResultCard.renderCard/saveCanvasAsImage`) — 세로형 카드(540×675 논리 크기, 2x 스케일 렌더링, 로또 영수증 캔버스와 동일한 house 컨벤션)에 상단 청록 밴드(테스트 제목)+결과 이미지(cover-fit 라운드 사각형)+타이틀+서브타이틀+특징 요약(오렌지 불릿)+팁 박스+하단 워터마크(`gwamol-lab.xyz`)를 합성. 결과 화면에 3번째 버튼 "🖼️ 이미지 저장"(주황 accent) 추가, 클릭 시 카드 생성 후 모달(이미지 미리보기+"📱 길게 눌러서 저장" 안내+다운로드 버튼+닫기)을 띄우는 방식 채택 — 기존 로또 직접뽑기 영수증(`lottodrawSaveImage`, `canvas.toDataURL()`+`<a download>`) 패턴을 그대로 재사용해 house style 일관성 유지. **작업2(관련 테스트 배너)**: config.json에 `related: []` 필드 추가, 결과 화면에 `related`가 비어있지 않을 때만 `#te-related-container`를 렌더하고 각 관련 테스트의 `../{id}/config.json`을 fetch해 제목/커버 썸네일로 카드 노출(현재는 테스트가 1개뿐이라 빈 배열로 둠, 자기 자신을 넣어 렌더링/링크만 임시 검증 후 원복). **작업4(sitemap.xml)**: 이미 존재하는 루트 `sitemap.xml`(URL 1개만 등재)에 `https://gwamol-lab.xyz/test-engine/tests/mental-age/index.html`(changefreq monthly, priority 0.5) 1줄만 추가 — 이 URL은 해시 라우팅이 아닌 진짜 정적 경로라 kkum 사전 페이지들과 같은 근거로 sitemap 등재가 정당함. **작업3(기존 사이트 연동)**: 계획(파일/위치/코드)을 먼저 텍스트로 보여주고 승인받은 뒤 진행(사용자가 "뭔소린지 모르겠다"고 해서 쉬운 말로 재설명 후 승인) — 승인 시 사용자가 "나중에 인기순/최신순으로 바꿀 수 있게 확장성 열어두라"는 조건을 추가해 설계 변경. 단순히 카드 1개를 하드코딩하는 대신, `data.js`에 신규 배열 `AppData.externalTests`(`id/title/emoji/hook/url/engagementKey/addedAt/isNew` 필드)를 신설하고, `app.js`에 `sortExternalTests(list, sortMode)`(`'new'`=addedAt 내림차순 기본, `'popular'`=기존 `engagementCount()` 재사용해 참여수 내림차순 — 둘 다 이미 동작하는 정렬 로직으로 구현, UI 토글만 없음)와 `externalTestsBannerHTML(sortMode)`를 신설해 `renderPsychtestFeed()`가 카테고리 탭 렌더링 **바로 위**(어느 카테고리를 보든 항상 최상단)에 이 배너를 출력하도록 연결. 항목이 늘어나도 배열에 추가만 하면 되고, 정렬 기준 전환은 호출부의 `sortMode` 문자열만 바꾸면 되는 구조. 카드는 `<a href="/test-engine/tests/mental-age/index.html">`(SPA `onclick` 이동이 아닌 진짜 정적 페이지로의 일반 링크, 푸터의 `/kkum/` 링크와 동일 패턴) + 클릭 시 `bumpEngagement()`로 참여수 집계, 주황(`#D85A30`) 고정 hex 테두리+NEW 배지(라이트/다크 테마 리맵 대상 아닌 고정색이라 무관하게 안전, 기존 X 공유버튼과 동일 처리). 버전 주석을 관례상 "test-engine vN (STEP N)" 형태로 새로 도입해 이번에 수정/생성된 파일(engine.js/engine.css/themes/retro.css/index.html/result-card.js) 상단에 반영. **검증**: `node --check`로 engine.js/result-card.js/app.js/data.js 문법 확인, Playwright로 (a) 이미지 저장 버튼→모달→실제 `download` 이벤트 발생(파일명 확인)→닫기 (b) related 배너 non-empty/empty 두 경우 렌더링 여부 (c) sitemap.xml XML 유효성 (d) 심리테스트존 진입 시 배너가 최상단에 노출되고 카테고리 탭을 바꿔도 계속 유지되는지 (e) 배너 클릭 시 실제로 `/test-engine/tests/mental-age/index.html`로 정상 이동하고 인트로가 로드되는지 (f) 홈 화면·MBTI 섹션 등 기존 화면 회귀 없는지까지 전부 콘솔 에러 없이 통과. 이하 STEP 1.5: 사용자가 "구현은 나쁘지 않은데 이미지가 너무 허접하다"며 SVG placeholder를 AI 생성 이미지로 교체하는 프롬프트를 추가 전달. 이미 이 프로젝트에 OpenAI Images API(`gpt-image-1`) 연동 스크립트(`scripts/generateWorldcupImages.js`, 이상형 월드컵 AI팩용)와 `OPENAI_API_KEY`(Codespaces Secrets)가 있어 동일 패턴 재사용. **비용 발생 전 2단계 확인 절차**: ①일러스트 스타일 2종(플랫+굵은아웃라인 vs 손그림 러프라인) 샘플을 동일 장면(고민하는 마스코트)으로 medium 품질 생성 후, Read 도구로 보여준 이미지가 사용자 화면엔 실제로 안 보였던 문제를 발견 — Artifact(base64 데이터 URI로 이미지를 인라인 임베드한 자체완결 HTML 비교 페이지)로 전환해 실제로 보이게 함, 사용자가 "손그림 러프 라인(B)" 선택 ②동일 프롬프트를 medium vs high 품질로 한 번 더 Artifact 비교 페이지를 만들어 확인시킨 뒤 "medium"으로 최종 확정(이미지 7장 기준 약 $0.3). **캐릭터 설계**: 브랜드 컬러(청록 `#085041`/`#1D9E75` + 주황 `#D85A30`) + 로고의 "뇌" 모티프를 살려 정수리에 뇌 모양 안테나가 달린 동글동글한 블롭 마스코트를 신규 오리지널 캐릭터로 설계, 커버+결과 6종 전체에 표정·포즈·소품만 바꿔가며 재사용해 시리즈감 유지(예: 10세=파티모자+만세+반짝눈, 32세=넥타이+클립보드, 60세=명상+찻잔+후광). **구현**: `test-engine/tests/mental-age/assets/prompts.md`(월드컵 스크립트와 동일한 `## {id}` 헤딩 파싱 컨벤션, 베이스 캐릭터 묘사를 각 프롬프트에 인라인 포함) + 신규 `test-engine/scripts/generate-assets.js`(`generateWorldcupImages.js`와 동일한 재시도/스킵 로직 재사용, 생성된 1024px PNG를 Python/Pillow로 즉시 700px WebP로 리사이즈 변환 후 원본 PNG는 임시파일이라 삭제 — 최종 결과물만 저장, 장당 24~54KB로 경량화) — `node test-engine/scripts/generate-assets.js mental-age`로 실행, 7장(cover+age-10~60) 전량 1회 성공. `config.json`의 이미지 경로 7곳을 `.svg`→`.webp`로 갱신(SVG 파일 자체는 삭제하지 않고 보존). **검증**: 정적 서버+Playwright로 전체 플로우(전부 A선택→60세, 전부 B선택→10세) 재실행해 이미지 정상 로드+콘솔/네트워크 에러 없음 확인, 스크린샷으로 캐릭터 일관성과 포즈 표현 확인. **다음 단계**: 이미지 저장 기능 실제 구현(STEP 2), 2호 테스트(axis 채점 실사용) 콘텐츠 제작, 최종적으로 이 프로토타입을 실제 사이트(심리테스트존)에 통합할지/어떻게 통합할지 결정. 이하 STEP 1 상세 → | 사용자가 "지금 심리테스트존 콘텐츠가 너무 재미없다"며 전면 재구축을 요청, 다른 세션에서 미리 설계한 STEP 1 프롬프트(엔진+1호 콘텐츠 스펙)를 그대로 전달받아 진행. **이 프로토타입은 기존 사이트(`index.html`/`app.js`/`data.js`)를 전혀 건드리지 않고 신규 폴더 `/test-engine/`에서만 독립적으로 동작** — 아직 사이드바·심리테스트존 등 실제 사이트에는 연결되지 않은 상태(추후 STEP에서 통합 여부 논의 예정). **엔진(`engine.js`+`engine.css`+`themes/retro.css`)**: config.json 하나로 새 테스트를 찍어낼 수 있는 범용 구조 — `인트로→질문 루프→가짜 로딩 연출→결과` 4단계 SPA(해시 라우팅 없이 단일 컨테이너 innerHTML 전환), 화면 하단 큰 버튼은 `position:fixed`+`max-width:500px 중앙정렬`로 모바일 풀폭에서도 항상 폭 고정. **채점 엔진 4종 지원**: `sum`(선택지 점수 합산→구간 매칭)/`type`(최다 유형 매칭)/`quiz`(정답 수 구간 매칭)/`axis`(양극 비율→게이지, 2호 MBTI형 예정) — 1호는 `sum`만 실사용, 나머지 3종은 스키마·분기 로직만 미리 준비. 완료 카운터는 `config.seed_count + localStorage` 방식으로 "지금까지 N명이 확인했어요" 표시. 테마는 `document.currentScript.src` 기반으로 `themes/{theme}.css`를 동적 `<link>` 주입(레이아웃 골격 `engine.css`와 스킨 `themes/*.css` 완전 분리, 새 스킨 추가 시 config의 `theme` 값만 바꾸면 적용). **1호 콘텐츠 "진짜 정신연령 테스트"**: MZ 타깃 톤(라떼 드립·갓생 강박·홧김비용·탕진잼·할많하않 등 자기객관화 개그)으로 15문항 직접 창작, 6구간 결과(10/17/25/32/43/60세), 윈도우95 레트로 테마(회색 창+파란 타이틀바+베벨 버튼). SVG는 동작 검증용 임시 자산(cover 1개+결과 6개, 코드로 그린 심플 일러스트)으로 만들었으며, STEP 1.5에서 실제로 AI 이미지로 교체 완료(위 STEP 1.5 항목 참고) — config의 이미지 경로만 바꾸면 교체되도록 하드코딩 없이 설계해뒀던 덕에 코드 수정 없이 경로 7곳만 갱신. **검증**: `node --check`로 engine.js 문법 확인, Node 스크립트로 15문항 점수 합산 최소~최대(0~43)가 6개 결과 구간과 갭/중복 없이 정확히 매칭되는지 기계 검증, 정적 서버+Playwright로 전 구간 양극단(전부 A선택→60세, 전부 B선택→10세) 실제 플레이·완료 카운터 증가·"다시하기" 동작까지 콘솔 에러 없이 확인, 스크린샷으로 레트로 테마 렌더링 확인 |
| 검색엔진 최적화(SEO) 기본기 반영 — 구글 SEO 스타터 가이드 + 네이버 서치어드바이저 가이드 | ✅ 완료 → 네이버 소유확인까지 완료(사이트맵 제출은 다음 단계) | v0.5.1 → v0.5.2 | **v0.5.2**: 사용자가 네이버 서치어드바이저에 `gwamol-lab.xyz` 등록 후 발급받은 소유확인 코드(`5f45a9833d40e690e8cc4851df222f915e11b791`)를 전달 — v0.5.1에서 주석 처리해뒀던 `<meta name="naver-site-verification">`에 채워 넣고 주석 해제. **다음 단계**: 네이버 서치어드바이저에서 "확인" 버튼 클릭 → 소유 확인 완료 후 "요청 > 사이트맵 제출"에 `https://gwamol-lab.xyz/sitemap.xml` 제출, 구글 서치 콘솔도 별도 등록+sitemap 제출 필요. 이하 v0.5.1 상세 → | 사용자가 구글/네이버 두 공식 SEO 가이드 링크를 첨부하며 반영 요청. WebFetch로 구글 가이드 체크리스트(title/meta description/canonical/OG/구조화데이터/sitemap/robots.txt/이미지 alt 등) 전량 확보, 네이버는 접근 차단으로 WebSearch로 대체 확인(사이트 등록→소유확인(메타태그)→sitemap/RSS 제출 흐름, 소유확인 코드는 사용자가 서치어드바이저에 직접 등록해야 발급됨 — 코드에 미리 채워둘 수 없어 주석 placeholder만 남김). **이 프로젝트가 해시 라우팅 SPA(`index.html` 1개)라는 제약을 감안**: 섹션별 서로 다른 URL이 없어 sitemap/canonical/구조화데이터는 전부 루트 문서 1개 기준으로만 의미가 있음(기존 `functions/share/[section].js`처럼 쿼리스트링 기반 동적 랜딩은 안정적인 고유 URL이 아니라 sitemap 후보에서 제외). **반영 내역**: ①`<title>`을 "과몰입 연구소"에서 핵심 키워드(MBTI/두뇌 나이/오늘의 운세/꿈해몽) 포함 형태로 보강 ②`<link rel="canonical" href="https://gwamol-lab.xyz/">` 신규 ③OG 태그(`og:type/site_name/locale/url/title/description/image`)+Twitter 카드 태그 신규(기존엔 `functions/share/[section].js`의 동적 랜딩에만 OG가 있었고 루트 문서 자체엔 없었음) — 신규 대표 이미지 `share-cards/og-default.jpg`(1200×630)를 `scripts/generate-og-card.js`(기존 `generate-dream-generic-card.js`와 동일한 패턴+안전영역 검증 재사용, 청록/주황 브랜드 톤)로 생성 ④`<script type="application/ld+json">`으로 WebSite+Organization 구조화 데이터 신규(Node로 JSON 유효성 검증 완료) ⑤신규 `sitemap.xml`(루트 URL 1개만 등재, SPA 특성상 정직한 최소 구성) + `robots.txt`에 `Sitemap:` 줄 추가 ⑥네이버 소유확인용 `<meta name="naver-site-verification">` 주석 placeholder 추가(사용자가 서치어드바이저에서 사이트 등록 후 발급받는 코드를 직접 채워 넣어야 함, 값 자체는 비밀정보가 아니라 Claude가 대신 넣을 수 없는 게 아니라 발급 자체가 사용자 계정 행위라 코드 시점엔 존재하지 않음). 기존에 이미 잘 되어 있던 것 확인만 하고 손대지 않은 항목: `lang="ko"`, 이미지 3곳 전부 `alt` 속성 존재, `robots.txt`가 이미 전체 허용 상태, 반응형(Tailwind)이라 모바일 친화성 이미 확보. **다음에 사용자가 할 일**: ①네이버 서치어드바이저(searchadvisor.naver.com)에 `gwamol-lab.xyz` 사이트 등록 → 소유확인 코드 발급 → 위 주석 처리된 meta 태그에 코드 삽입 후 주석 해제 → sitemap.xml 제출 ②구글 서치 콘솔에도 동일 도메인이 이미 애드센스 심사용으로 확인된 상태이므로, 별도로 서치 콘솔에 sitemap.xml 제출 권장. **검증**: 정적 서버+Playwright로 canonical/og:image/JSON-LD 파싱과 콘솔 에러 없음 확인, `node --check` 대상 없음(신규 파일은 스크립트 1개, 정상 실행 확인) | 사용자가 구글/네이버 두 공식 SEO 가이드 링크를 첨부하며 반영 요청. WebFetch로 구글 가이드 체크리스트(title/meta description/canonical/OG/구조화데이터/sitemap/robots.txt/이미지 alt 등) 전량 확보, 네이버는 접근 차단으로 WebSearch로 대체 확인(사이트 등록→소유확인(메타태그)→sitemap/RSS 제출 흐름, 소유확인 코드는 사용자가 서치어드바이저에 직접 등록해야 발급됨 — 코드에 미리 채워둘 수 없어 주석 placeholder만 남김). **이 프로젝트가 해시 라우팅 SPA(`index.html` 1개)라는 제약을 감안**: 섹션별 서로 다른 URL이 없어 sitemap/canonical/구조화데이터는 전부 루트 문서 1개 기준으로만 의미가 있음(기존 `functions/share/[section].js`처럼 쿼리스트링 기반 동적 랜딩은 안정적인 고유 URL이 아니라 sitemap 후보에서 제외). **반영 내역**: ①`<title>`을 "과몰입 연구소"에서 핵심 키워드(MBTI/두뇌 나이/오늘의 운세/꿈해몽) 포함 형태로 보강 ②`<link rel="canonical" href="https://gwamol-lab.xyz/">` 신규 ③OG 태그(`og:type/site_name/locale/url/title/description/image`)+Twitter 카드 태그 신규(기존엔 `functions/share/[section].js`의 동적 랜딩에만 OG가 있었고 루트 문서 자체엔 없었음) — 신규 대표 이미지 `share-cards/og-default.jpg`(1200×630)를 `scripts/generate-og-card.js`(기존 `generate-dream-generic-card.js`와 동일한 패턴+안전영역 검증 재사용, 청록/주황 브랜드 톤)로 생성 ④`<script type="application/ld+json">`으로 WebSite+Organization 구조화 데이터 신규(Node로 JSON 유효성 검증 완료) ⑤신규 `sitemap.xml`(루트 URL 1개만 등재, SPA 특성상 정직한 최소 구성) + `robots.txt`에 `Sitemap:` 줄 추가 ⑥네이버 소유확인용 `<meta name="naver-site-verification">` 주석 placeholder 추가(사용자가 서치어드바이저에서 사이트 등록 후 발급받는 코드를 직접 채워 넣어야 함, 값 자체는 비밀정보가 아니라 Claude가 대신 넣을 수 없는 게 아니라 발급 자체가 사용자 계정 행위라 코드 시점엔 존재하지 않음). 기존에 이미 잘 되어 있던 것 확인만 하고 손대지 않은 항목: `lang="ko"`, 이미지 3곳 전부 `alt` 속성 존재, `robots.txt`가 이미 전체 허용 상태, 반응형(Tailwind)이라 모바일 친화성 이미 확보. **다음에 사용자가 할 일**: ①네이버 서치어드바이저(searchadvisor.naver.com)에 `gwamol-lab.xyz` 사이트 등록 → 소유확인 코드 발급 → 위 주석 처리된 meta 태그에 코드 삽입 후 주석 해제 → sitemap.xml 제출 ②구글 서치 콘솔에도 동일 도메인이 이미 애드센스 심사용으로 확인된 상태이므로, 별도로 서치 콘솔에 sitemap.xml 제출 권장. **검증**: 정적 서버+Playwright로 canonical/og:image/JSON-LD 파싱과 콘솔 에러 없음 확인, `node --check` 대상 없음(신규 파일은 스크립트 1개, 정상 실행 확인) |
| 구글 애널리틱스(GA4) 연동 → SPA 섹션 이동 가상 페이지뷰 추가 → PageSpeed Insights 성능 개선(1차: 로고 이미지, 2차: llms.txt, 3차: 색상 대비, 4차: 이미지 width/height 명시) | ✅ 완료 | v0.4.5 → v0.5.0 | 로드맵을 "검색노출/신규유입/퍼포먼스 마케팅" 단계로 전환하며 착수한 첫 작업. 사용자가 GA4 속성 생성 후 전달한 gtag.js 스니펫(측정 ID `G-W05KHWP4WY`)을 `index.html` `<head>` 최상단에 삽입, SPA라 파일 1개뿐이라 추가 삽입 지점 없음. **v0.4.6**: 해시 라우팅 특성상 최초 접속 1회만 페이지뷰가 잡히고 이후 섹션 간 이동(홈→MBTI→운세 등)은 GA가 전혀 못 잡는 문제를 사용자에게 설명 후 "지금 바로 해달라"는 확정을 받고 구현 — `App.navigate()`에 섹션이 실제로 바뀔 때만(중복 방지) `gtag('event', 'page_view', {page_title, page_location, page_path})`를 전송하도록 추가, gtag 함수가 `<head>`에서 먼저 정의되고 `app.js`는 body 끝에서 로드되는 순서라 문제 없음. **v0.4.7**: 사용자가 구글 PageSpeed Insights로 실제 사이트를 분석한 스크린샷 다수를 첨부 — Claude가 먼저 전체 항목(색상 대비/JS 축소/미사용 JS/preconnect/네트워크 체인/레거시 JS/캐시 수명/이미지 전송/렌더링 차단)을 제3자 스크립트(광고·GA·카카오·Supabase·Tailwind CDN, 전부 의도된 기능이라 삭제 대상 아님) vs 직접 고칠 수 있는 것으로 분류해 방향성 보고 후 AskUserQuestion으로 범위를 확인받음 — 1차로 **로고 이미지 크기 문제**(179KB 낭비, LCP에 직접 영향)만 진행 확정. nav/모바일헤더/푸터 3곳이 512×512 원본(`assets/brand/logo-icon.png`)을 32px/24px로 줄여서 표시하고 있던 것을 발견 — Python(Pillow)으로 96px 리사이즈본(`logo-icon-96.png`, 183KB→12.4KB)을 신규 생성해 3곳 전부 교체. preconnect/색상대비/defer/Tailwind CDN 빌드전환/JS minify 등 나머지 항목은 보류, 다음 확인 후 순차 진행 예정. **v0.4.8**: 사용자가 PageSpeed의 "에이전트 접근성 — llms.txt가 권장사항을 준수하지 않음"(H1 헤더 없음+링크 없음) 지적을 가져와 신규 `llms.txt`(repo 루트) 작성 — H1로 사이트 소개(오락 목적 명시) + 두뇌테스트존 13개/심리테스트존·투표소 3개/운명 관측소 2개/가족오락관·로또·약관 링크까지 전체 섹션을 마크다운 링크 목록으로 정리. 구조 변경도 제3자 스크립트도 아닌 루트 텍스트 파일 추가라 사용자가 즉시 확정한 안전한 작업. **v0.4.9**: 사용자가 PageSpeed의 "대비 — 백그라운드 및 포그라운드 색상의 대비율이 충분하지 않습니다" 항목(MBTI/꿈해몽/오늘의운세/두뇌나이/프로미루러/인싸력/속담완성/그시절물가 등 홈 화면 pill 배지, 푸터 전체, 홈 팝업/캡션 텍스트가 통과 못 함) 재지적 — WCAG 대비 계산기로 실측(`text-slate-500` on `bg-slate-900` = 3.75:1, `text-slate-500` on `bg-slate-700` 배지 = 2.18:1, `text-slate-600` on `bg-slate-900` = 2.36:1, 전부 AA 기준 4.5:1 미달)해 원인을 확인한 뒤, 공유 CSS 변수(`--slate-500`/`--slate-600` 자체)는 다른 용도(테두리·배경 등)로도 쓰이므로 건드리지 않고 **해당 텍스트 요소들의 클래스만** `text-slate-300`(대비 12.02:1/6.97:1)·`text-slate-400`(7.05:1)으로 개별 교체 — 푸터(`index.html:549,555`), 홈 인기순위 캡션(`index.html:306`), 완주현황 pill 배지·완주기록 빈 상태 문구·칭호 빈 상태 문구(`app.js` 3곳) 총 6곳. 라이트모드는 CSS 변수 remap 구조상 slate-300/400이 이미 어두운 톤으로 반전 매핑돼있어 별도 확인 결과 8.21:1/5.70:1로 오히려 더 여유 있게 통과, 회귀 없음. **v0.5.0**: 사용자가 데스크톱 PageSpeed 리포트에서 "에이전트형 브라우징"(구글이 "아직 개발 중이며 변경될 수 있음"이라 명시한 베타 카테고리) 아래 `Cumulative Layout Shift — 0.13`(모바일 리포트에선 0으로 문제없었던 지표)을 재확인 요청 — 로고 `<img>` 3곳(nav/모바일헤더/푸터)이 Tailwind 클래스(`w-8 h-8`/`w-6 h-6`)로만 크기를 지정하고 HTML `width`/`height` 속성이 없어, Tailwind CDN이 비동기로 스타일시트를 주입하기 전까지 브라우저가 이미지를 원본 크기(96px)로 잠깐 그렸다가 줄어드는 밀림이 생길 수 있는 구조를 발견 — 구조 변경이 아닌 HTML 속성 추가라 3곳 전부에 `width="32" height="32"`/`width="24" height="24"`(각 표시 크기와 일치)를 추가해 이미지 영역을 초기 렌더링부터 확정 |
| MBTI 12문항 테스트 | ✅ 완료 → 간단/정밀 2모드 | v0.0.1 → v0.0.28 | |
| 꿈 해몽 검색 (통합설명) | ✅ 완료 | v0.0.1 | |
| 오늘의 운세 (띠별) | ✅ 완료 → 카테고리별 다중 variant(336개) 완료 | v0.0.1 → v0.0.36 | |
| 두뇌 나이 측정기 (스트룹) | ✅ 완료 → 3단계 난이도 확장 | v0.0.1 → v0.0.28 | |
| ADHD 성향 진단 | ✅ 완료 → 간단/정밀 2모드 | v0.0.1 → v0.0.28 | |
| 꿈해몽 구조 개편 (테마+하위칩) | ✅ 완료 | v0.0.4 | "오늘의 행동" 잘림버그 동시 수정 |
| 꿈해몽 데이터 15개→59개 확장 | ✅ 완료 | v0.0.5 | 약 20개 테마에 하위variants 포함 |
| 띠별 운세 행운숫자 필드 | ✅ 완료 | v0.0.6 | 로또 연동용 |
| 공유하기 (Web Share API) | ✅ 완료 | v0.0.7 | MBTI/운세/두뇌나이/ADHD 적용 |
| 마이홈 대시보드 | ✅ 완료 → v0.0.10에서 홈에 통합 | v0.0.8 | 닉네임/완주현황/방문스트릭/최근기록. 별도 섹션 제거, 홈 화면에 병합 |
| 로또 번호 조합기 (랜덤/직접지정/운세연동/통계기반) | ✅ 완료 → UX 개선 | v0.0.9 → v0.1.3 | v0.1.3: 사용자가 실 사용 스크린샷을 레퍼런스로 6가지 개선 요청. ①공/번호 크기 전체 확대(조합 결과 `w-8→w-11`, 통계패널 공 `w-7→w-9`) ②통계기반 모드가 직전 회차 1개만 보여주던 것을 최근 5회차 카드로 확대(`functions/api/lotto-stats.js`에 `recentRounds` 필드 추가, 클라이언트 미러 직접조회 폴백도 동일 반영) ③운세·꿈 연동 모드에 "이번 조합에 반영된 행운숫자" 패널을 신설해 출처별(🔮 운세/🌙 꿈)로 실제 숫자를 나열하고, 조합 결과에서도 해당 번호에 금색 테두리(`ring-amber-300`)로 하이라이트 ④완전랜덤/직접지정/운세연동 3개 모드에 "🔄 다시 뽑기" 버튼 신설(직접지정은 입력값 유지한 채 재조합) ⑤4개 모드 전부에 "🖼️ 결과 이미지 공유"(Canvas로 조합 카드 렌더링 후 `navigator.share` files) + "💬 카카오톡으로 공유하기"(신규 공용 이미지 `share-cards/lotto-share.jpg` + `functions/share/[section].js`의 `lotto` 항목) 버튼 추가, FB/X/밴드/링크복사 아이콘 행도 동일 적용. |
| 라이트/다크 모드 토글 | ✅ 완료 | v0.0.10 | 사이드바(PC)·모바일 헤더에 토글 버튼. 기본값 다크, localStorage(`theme`)에 기억 |
| 마이홈 → 홈 화면 통합 | ✅ 완료 | v0.0.10 | 마이홈 섹션/메뉴 제거, 닉네임·스트릭·완주현황·최근기록을 홈 화면에 병합 |
| 카카오톡 공유 | ✅ 완료 (실사용 검증까지) → 개인 계정 앱으로 이전 | v0.0.41 → v0.2.1 | **v0.2.1**: 기존 앱이 사용자의 회사 계정에 잘못 생성돼있던 걸 발견 — 개인 계정으로 새 Kakao Developers 앱을 만들고 JavaScript 키를 재발급, `kakao-share.js`의 `Kakao.init()` 키를 교체(이 프로젝트에서 카카오 키를 쓰는 곳은 이 한 줄뿐). 마침 진행 중이던 도메인 이전(`www.gwamol-lab.xyz`, 아래 "도메인 이전" 항목 참고)과 시점이 겹쳐, 이중 작업을 피하려고 새 앱의 "JavaScript SDK 도메인"과 "제품 링크 관리 > 웹 도메인" 양쪽 모두에 기존 도메인(`testseries1.pages.dev`)과 이전 예정 도메인(`www.gwamol-lab.xyz`)을 **동시에** 등록해둠 — 도메인 실제 전환 시 카카오 쪽 추가 작업이 없도록 선제 처리. **검증**: 정적 서버+Playwright로 새 키로 `Kakao.isInitialized()`가 `true`로 정상 초기화됨을 확인(실제 도메인 화이트리스트 통과 여부는 라이브 배포 후 재확인 필요). 이하 v0.0.41 상세 → | Kakao Developers 앱("과몰입 연구소") 생성 + JavaScript 키 발급 후 구현. 신규 `kakao-share.js`(`Kakao.init()` + `shareToKakao(text)` 헬퍼, `Kakao.Share.sendDefault({objectType:'text', ...})` 사용) 추가. 기존 15개 테스트 결과 화면의 "📤 내 결과 공유하기"(Web Share API) 버튼 14곳 전부 바로 아래에 "💬 카카오톡 공유" 버튼을 동일한 `shareText` 변수로 추가(같은 텍스트를 두 채널로 공유 가능), 별도 문구 새로 작성 없이 재사용. **주의 — 도메인 등록은 2곳 모두 필요**: "플랫폼 키 > JavaScript SDK 도메인"만 등록하면 SDK 초기화·팝업까지는 되지만 공유 단계에서 4019 에러 발생, "제품 링크 관리 > 웹 도메인"에도 등록해야 함(설정 반영에 5~10분 지연 있음). 두 곳 등록 후 사용자가 실제 카카오톡 전송까지 성공 확인(2026-07-03 완료 확정) |
| 도메인 이전 (`testseries1.pages.dev` → `gwamol-lab.xyz`/`www.gwamol-lab.xyz`) | ✅ 완료 | v0.2.1~v0.2.8 | 사용자가 "test/dev 단어가 임시 페이지처럼 보이고 애드센스 심사에도 불리할 것 같다"는 판단으로 도메인 구매 후 이전 진행. 코드 조사 결과 실제로 도메인 하드코딩된 곳은 문서뿐, `og:url`은 요청 origin 기준 자동 생성이라 코드 수정 불필요함을 확인, Supabase도 익명 인증만 써서 도메인 변경과 무관. v0.2.7~v0.2.8에서 루트 도메인 DNS를 Cloudflare로 완전 이전 + Pages Custom domain으로 루트+www 둘 다 연결 완료(상세는 아래 "구글 애드센스 실 게재" 행 참고) |
| contact@gwamol-lab.xyz 이메일 수신+발신 설정 | ✅ 완료 | v0.3.6 | 개인 이메일(jhb415@gmail.com) 대신 도메인 이메일로 문의받고 싶다는 요청. 코드 변경 없는 순수 대시보드 설정 작업이라 Claude가 화면 스크린샷 기반으로 실시간 단계별 안내. **수신**: Cloudflare 대시보드 → Email Routing 활성화(도메인이 이미 Cloudflare에 있어 MX/TXT(SPF/DKIM) 레코드 자동 추가) → Destination address로 `jhb415@gmail.com` 인증 → Routing rule `contact@gwamol-lab.xyz → jhb415@gmail.com` 생성. **발신**: Google Workspace(유료) 없이 **Gmail 자체 "다른 주소에서 메일 보내기"** 기능으로 무료 구현 — SMTP 서버를 `smtp.gmail.com`(사용자 이름은 `jhb415@gmail.com` 본인 계정, 비밀번호는 Google 계정 2단계 인증 후 발급한 앱 비밀번호)으로 지정해 자기 자신의 Gmail을 발신 릴레이로 재사용하는 방식(별도 메일 서버 구축 불필요). **트러블슈팅**: ①Gmail의 확인 메일이 Cloudflare Email Routing이 아직 "Syncing"(DNS 전파 중) 상태일 때 발송되어 최초 시도가 유실됨 → Routing status가 "Enabled"로 바뀐 후 Gmail 설정에서 확인 메일 재전송 필요 ②재전송 후에도 안 왔던 건 위 타이밍 이슈였고, Cloudflare Activity Log로 "Cloudflare까지 도착했는지"를 먼저 확인하는 방식으로 원인 진단(수신단 문제 vs 발신단 문제 구분). 최종적으로 Gmail 확인 메일 수신 → 확인 버튼 클릭까지 완료, "보내는 사람" 목록에 `contact@gwamol-lab.xyz` 정상 추가 확인 |
| 공유 링크 클릭 시 결과 프리뷰 화면 | ✅ 완료 | v0.1.1 | 지금까지는 공유 링크(결과공유/도전장/대결결과)를 열면 곧바로 테스트 화면으로 리다이렉트돼 카카오 카드에서 본 캐릭터/결과를 다시 볼 수 없었음(도전장만 작은 텍스트 배너, 나머지 둘은 아무 정보 없이 빈 시작화면). `functions/share/[section].js`가 실제 방문자를 `#{section}`으로 바로 보내던 것을 신규 공용 화면 `#shared-preview`로 한 단계 거치도록 변경 — 카카오 og:image/title/description과 동일한 내용을 먼저 보여주고 버튼(결과공유·대결결과="나도 해보기", 도전장="⚔️ 도전하기")을 눌러야 실제 테스트로 이동. 15개 섹션×3개 타입(TIER 8개의 result/challenge/verdict + 성향형 7개) 전부 섹션별로 따로 만들지 않고 화면 1개로 재사용, 도전장 페이로드도 그대로 유지해 기존 대결 비교 로직 재사용. `app.js`에 `initSharedPreview()`/`sharedPreviewProceed()` 신설. **구현 중 발견한 함정**: `App.navigate()`가 항상 `location.hash = sectionId`로 덮어써 쿼리스트링이 사라지기 때문에, init 함수 안에서 `location.hash`를 다시 읽으면 이미 늦음 — DOMContentLoaded 초기 라우팅 단계(`App.navigate` 호출 **전**, 기존 vs/match 페이로드를 캡처하는 것과 같은 타이밍)에서 `App._sharedPreviewParams`에 미리 떼어두는 방식으로 해결. **검증**: `npx wrangler pages dev`로 실제 Cloudflare Function까지 구동, Playwright로 result/challenge/verdict/mbti 4가지 타입 전부 프리뷰 렌더링→버튼 클릭→실제 테스트 이동(도전장은 기존 배너까지 정상 복원)까지 확인, 콘솔 에러 없음 |
| 공유 카드 이미지 — 제목 텍스트 좌우 잘림 수정 | ✅ 완료 → v0.1.8에서 근본 재설계 | v0.1.1 → v0.1.8 | **v0.1.8 재설계**: v0.1.1의 760px 축소로도 여전히 잘린다는 사용자 스크린샷 재신고("산호초 색색깔 틈에 사는 만능색각 사마귀새우" 좌우 잘림) → 크롭 폭을 재실측하니 카카오톡은 1200×630을 **약 1:1(중앙 ~630px 폭)로 크롭**해서 보여주고 있었음(760px도 이보다 넓어서 잘렸던 것, 좌상단 Tier 배지는 크롭 밖이라 통째로 안 보였음). 모든 요소를 **중앙 540px "정사각 안전영역"** 안에 재배치: 배지를 좌상단→상단 중앙으로 이동, 제목/팁에 `word-break:keep-all`로 어절 단위 2~3줄 배치(제목 56→44px, 이모지 200→120px 등 세로 공간 재배분), 생성 스크립트에 안전영역 자동 검증(배지·제목·팁의 x범위 315~885px + 세로 넘침 체크, 실패 시 scale 0.9→0.8 자동 축소 재시도, 그래도 실패하면 생성 중단) 내장. `generate-lotto-share-card.js`도 동일 규칙 적용(공 96→74px로 6개 행이 크롭 안에 들어가게, 제목도 540px 제한). 320장 전체 재생성(전량 scale 1.0에서 검증 통과), 이미지 크기는 1200×630 유지라 og:image 소비처(FB/트위터) 영향 없음. **검증**: 1:1 object-fit:cover 크롭 시뮬레이션 페이지에 실제 신고 카드+최장 제목(brain-C-2, 25자)+최장 부제목(brain-S-0, 52자)+MBTI/운세/꿈/VS/로또 8장을 렌더링해 전부 잘림 없음을 스크린샷으로 확인, 원본 비율에서도 레이아웃 자연스러움 확인 | 이하 v0.1.1 상세 → | v0.0.51에서 고친 건 카카오 카드 **본문(하단 흰 글자)**의 "···" 잘림이었는데, 이번엔 **이미지 안에 그려 넣은 제목 텍스트**가 카카오톡 채팅창 미리보기에서 좌우로 물리적으로 잘리는 별개의 문제(사용자가 실제 스크린샷 2장으로 지적, "달빛 정원에 사는 영민한 부엉이"의 "달"이 잘리는 식). 원인: 1200×630 원본을 카카오톡이 더 좁게 크롭해서 보여주는데, 제목 텍스트 max-width가 이미지 폭의 83%(1000px)까지 채워 여유가 없었음. `scripts/generate-share-cards.js`의 `.title`/`.tip` max-width만 1000/920px→760/700px로 축소(레이아웃·폰트·색상 등 다른 로직은 그대로 유지한 최소 수정), 219장 전체 재생성 — 실제로 안전선에 걸쳐있던 81장만 픽셀이 바뀌고 나머지 138장은 이미 여유가 있어 그대로 유지됨(이번 재생성에 이번 세션에서 추가된 꿈해몽 신규 테마 100개의 공유 이미지 `dream-74~173.jpg`도 처음 포함됨). **검증**: 착수 전 Artifact로 진단 리포트(재현 스크린샷+안전선 오버레이+수정 전후 비교)를 만들어 사용자 컨펌을 받은 뒤 진행 — 실제 신고된 문구(부엉이/돌고래 카드)와 219장 중 제목+설명이 가장 긴 조합(색각 Tier S, 24자)까지 재현해 안전 여백 확보와 세로 넘침 없음을 확인, 실제 재생성된 이미지로 재확인 |
| 공유 UI 개선 — 카카오톡 버튼 분리 + 설명 잘림 수정 | ✅ 완료 | v0.0.51 | 사용자가 실제 라이브 스크린샷 2장을 보내며 지적: ①카카오톡 공유가 다른 채널과 똑같은 크기 원형 아이콘이라 눈에 안 띔 ②정량 테스트(예: 색각) 카카오톡 공유 시 설명이 "···"로 잘림. **①**: 신규 `shareKakaoButtonHTML()`을 아이콘 행(`shareIconRowHTML()`, 이제 페이스북/X/밴드/링크복사 4개만 남음)에서 분리해 노란색(#FEE500) 전체너비 대형 버튼("💬 카카오톡으로 공유하기")으로 독립 배치, "친구에게 도전장 보내기"와 같은 시각적 무게감을 갖도록 아이콘 행 위쪽에 최우선 배치. `renderShareRow`/`renderIdentityShareRow`/`renderChallengeCompareCard`(대결 결과 카드) 전부 동일하게 적용해 일관성 유지. **②**: 카카오 피드 카드는 설명이 실제 UI에서 대략 2줄(40~50자)이 넘으면 "···"로 잘리는데, 기존엔 Twitter/밴드용 긴 `shareText`를 카카오 description에도 그대로 재사용하고 있었던 게 원인 — 카카오 전용 `kakaoDesc` 파라미터를 신설해 Tier 8개 테스트는 이미 짧은 `resultLabel`(예: "92% (Tier S)")을, 성향형 7개 테스트는 등급/타입 정보만 담은 짧은 문자열을 쓰도록 분리(긴 동물카드 이름 등 재미 요소는 카카오 "제목" 필드로 옮겨 잘리지 않게 함). **부수 발견·수정**: v0.0.50에서 작성한 MBTI/ADHD/인싸력/속담/물가/운세 6개 테스트의 공유 문구에 강조용으로 넣은 큰따옴표(`"..."`)가 `onclick="..."` HTML 속성 델리미터와 충돌해 카카오/트위터/밴드 버튼이 깨지는 실제 버그를 발견 — 전부 한국어 낫표(「」)로 교체해 근본 수정(HTML 속성 파싱 자체가 깨지는 문제라 방치했다면 심각했을 버그). **검증**: `npx wrangler pages dev`로 색각 테스트 완주 후 카카오 버튼이 정확히 1개만 크게 뜨는지, onclick 파라미터에 담긴 설명이 실제로 짧은지 Playwright로 직접 파싱해 확인, MBTI 카카오 버튼 onclick이 낫표 수정 후 정상 파싱됨을 재확인, 도전장→VS 비교 카드 플로우까지 전체 콘솔 에러 없이 재검증 |
| 공유 카드 리디자인 확장 (성향형 7개 테스트: MBTI/ADHD/인싸력/속담/물가/운세/꿈해몽) | ✅ 완료 | v0.0.50 | v0.0.49에서 Tier 8개 테스트만 적용했던 이미지 공유 인프라를 나머지 7개 테스트 전부로 확장. 사용자가 "동물카드 없는 것들도 추천 이미지 셋트로 구축, 친구/가족에게 말하듯 멘트, 항상 재미요소" 요청. **이미지 매핑**: 테스트마다 결과 정체성 개수와 1:1로 이미지 제작(MBTI 16유형·ADHD/인싸력/속담/물가 각 4등급·운세 12띠·꿈해몽 74테마 = 118장 신규, 기존 100+1장과 합쳐 총 219장). MBTI는 16Personalities의 관례를 차용해 4기질군(분석가=보라/외교관=에메랄드/관리자=블루/탐험가=골드)별 색상 테마 적용, ADHD는 자극적이지 않은 차분한 teal 톤(등급이 좋고나쁨이 아니므로 등급별 색상 차등 없이 통일), 인싸력은 MZ 파티 톤(핑크·오렌지), 속담/물가는 각각 전통 원목톤/빈티지 세피아톤, 운세는 코스믹 인디고, 꿈해몽은 드리미한 나이트 바이올렛 — 74개 테마 각각에 어울리는 이모지를 제목·키워드에서 착안해 수작업 배정(예: 지진꿈=🫨, 알몸꿈=🙈로 순화). **말투**: 기존 "너도 확인해봐" 톤에서 친구·가족에게 실제로 말하듯 캐주얼체로 전면 교체 — 운세="나 오늘 운세 이렇대~", 꿈해몽="나 어제 이런 꿈 꿨어!", MBTI="나 방금 MBTI 해봤는데 ○○ 나왔어!" 등 사용자가 준 예시 그대로 반영. **공유 랜딩 페이지**: `functions/share/[section].js`를 `section` 자체로 분기하도록 재구성(Tier 8개는 기존 type=result/challenge/verdict 그대로, 성향형 7개는 section이 곧 타입 — mbti/adhd/insa/proverb/pricequiz/fortune/dream). **버튼**: `renderShareRow()`와 마크업을 공유하는 `shareIconRowHTML()`로 공통 부분을 추출하고, 성향형 7개 전용 `renderIdentityShareRow(section, landingParams, imageUrl, kakaoTitle, shareText)`를 신설해 8개 Tier 테스트와 똑같은 아이콘 행(카카오/페이스북/X/밴드/링크복사)이 나오도록 일관성 유지. 꿈해몽은 결과 화면이 없고 검색→모달 구조라 `dreamRenderModal()`에 새로 공유 행을 추가(74개 테마는 기본 이미지 재사용, variant는 캡션 텍스트로만 구분). **검증**: `npx wrangler pages dev`로 7개 섹션 전부 og 태그/이미지 curl 확인, Playwright로 MBTI(간단 모드 12문항 완주)·오늘의 운세·꿈해몽(검색→모달)·인싸력 4개를 실제로 완주/진입시켜 공유 행 렌더링과 링크 복사 값까지 검증, 콘솔 에러 없음. 배포 중 오늘의 운세/꿈해몽은 실제 닉네임 개념이 없어 "나 님의"처럼 어색했던 랜딩 페이지 문구를 닉네임 없는 표현으로 수정 |
| 공유 카드 리디자인 (이미지 있는 공유 + 아이콘 공유 행 + 도전장 재설계) | ✅ 완료 | v0.0.49 | 사용자가 실제 라이브 카카오톡 공유 화면 스크린샷을 보고 "글씨만 공유돼서 안 눌린다"고 지적, 참고 사이트(smore.im)와 레퍼런스 이미지를 분석해 기획 후 진행. **핵심 구조**: SPA가 해시 라우팅(`#brain`)이라 서버가 og:image를 못 읽는 문제를, 신규 `functions/share/[section].js`(공유 랜딩 페이지, `/share/{section}?type=...`)로 해결 — 실제 사람이 열면 즉시 `#{section}`(도전장은 `vs=` 페이로드까지 복원)으로 리다이렉트, 크롤러는 og:title/description/image를 그대로 읽어감. **공유 이미지**: Tier 채점 8개 테스트의 `AppData.animalCards` 100개 조합 전부를 `scripts/generate-share-cards.js`(Playwright, 티어별 그라데이션 5색 + 이모지 + 문구, 1200x630 JPEG)로 1회성 렌더링해 `share-cards/`에 정적 파일로 커밋(합 4.2MB), 카카오 feed 템플릿과 og:image 양쪽에 동일 이미지 재사용. 렌더링에 필요한 한글 폰트(fonts-noto-cjk)가 개발 환경에 없어 처음엔 글자가 깨졌던 것을 설치 후 재생성. 친구 대결 도전장/대결결과는 별도로 만든 공용 VS 이미지(`share-cards/vs.jpg`) 1장 재사용. **버튼 통폐합**: 기존 "결과 공유하기"(Web Share, 텍스트만)+"카카오톡 공유"(`objectType:'text'`, 이미지 없음) 두 버튼을 신규 `renderShareRow()` 아이콘 행(카카오/페이스북/X/밴드/링크복사, 원형 버튼 5개)으로 통합해 8개 테스트에 일괄 적용, 카카오는 `objectType:'feed'`로 전환해 이미지+제목+설명+버튼이 있는 카드 형태로 개선. **친구 대결 재설계**: 승패 판정은 기존 Tier 기준 그대로 유지(테스트별 방향성이 달라 원점수 직접 비교는 버그 위험이 커서 배제, 사용자도 이 방식에 동의), 대신 원점수(정답률/초 등)는 그대로 노출 유지. `renderChallengeCompareCard()`에 신규 파라미터(section/myNickname) 추가하고 승패 확인 후 "이 결과 자랑하기" 공유 버튼(카카오/X/링크복사) 신설 — VS 이미지 + "OO vs OO 대결결과, 92% · 78%, 승리!" 형태로 공유 가능. **범위**: 이번 라운드는 Tier 8개 테스트만 대상(성향형 MBTI/ADHD/인싸력 등 7개는 기존 텍스트 공유 그대로, 다음 확장 후보로 남겨둠). 홈화면 "아바타 만들기" 기능은 사용자가 완전 별개 작업으로 명시, 이번 범위에서 제외하고 백로그 기록만. **검증**: `npx wrangler pages dev`로 로컬에 Functions까지 포함해 구동(정적 서버로는 Functions 테스트 불가) — curl로 result/challenge/verdict 3가지 type 전부 og 태그·리다이렉트 URL 정상 확인, Playwright로 (1) 색각 테스트 완주 → 아이콘 행 렌더링 → 링크 복사 버튼으로 실제 클립보드 URL 검증 (2) 두뇌나이로 도전장 생성 → 새 컨텍스트(상대방 역할)로 링크 진입 → vs= 페이로드 정상 복원되어 배너 노출 → 완주 → VS 비교 카드+자랑하기 버튼까지 전체 플로우 콘솔 에러 없이 통과 |
| 홈·사이드바 UI 개편 (Phase 3 이후 신규 라운드) | ✅ 완료 → "오늘의 인생 한마디" 크기·색상 조정 | v0.1.0 → v0.2.0 | **v0.2.0**: 사용자가 "오늘의 인생 한마디가 칸을 너무 크게 차지하고, 바로 아래 '내 정보' 카드와 색이 너무 비슷하다"고 지적. 배경을 기존 violet/indigo 계열(바로 아래 `renderHomeMypage()` 카드와 동일 계열이라 구분이 안 갔음)에서 amber 계열(`bg-gradient-to-br from-amber-900/40 to-amber-950/30 border-amber-700/40`)로 교체, 인용문 폰트 `text-xl sm:text-2xl`→`text-base sm:text-lg`, 패딩 `p-6`→`p-4`, 모서리 `rounded-2xl`→`rounded-xl`, 여백(`mb-3/mb-2/mb-4`)도 전반적으로 축소해 세로 공간을 줄임. amber-900/300/400은 `tailwind.config`에 이미 라이트모드 remap이 돼있어 다크/라이트 양쪽에서 대비 확보. **검증**: 정적 서버+Playwright로 다크/라이트 양쪽 스크린샷을 찍어 아래 "내 정보" 카드(인디고 계열)와 색이 명확히 구분되고, 카드 높이가 줄어든 것을 확인 | 이하 v0.1.0 상세 → | 사용자가 Claude가 준 시안(Artifact)을 보고 항목별로 번호/수정사항을 답변하는 방식으로 6개 항목 확정 후 진행. **①정량 테스트 사용법 안내**: Tier 채점 8개 테스트(두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력) 시작화면에 접이식 `guideCardHTML(steps)` 카드 신설 — 기본 접힘 상태라 재방문자 방해 없음, 각 테스트 성격에 맞는 "어떻게 하나요"(3단계 이내) 문구를 새로 작성. 최초 시안엔 "왜 하나요"(효용 설명) 문단도 있었으나 사용자가 "그 부분은 삭제, 어떻게 하나요만 남기자"고 정리해 최종적으로는 단계 안내만 남음. **②홈 화면 재구성**: "내 정보"(마이홈 통합 영역)를 "오늘의 챌린지"보다 위로 순서 교체, 도구·테스트 그리드 16장 전부 원색 그라데이션에서 무채색(slate) 톤으로 통일(`renderGrid()`의 `color` 필드 제거하고 고정 스타일로 단순화), 그리드 제목을 "바로 써먹는 도구"→"서비스 바로가기"로 변경(v0.0.30에서 쓰던 이름을 재사용). **③홈 상단 구분 섹션 신설**: 사용자가 참고 사이트(BoardLife) 스크린샷을 보내며 "나중에 내정보/로그인을 볼 수 있는 구분된 섹션이 있으면 좋겠다"고 요청 → 전체 화면 공통 적용 여부는 Claude가 판단해 추천(다른 14개 테스트 시작화면은 이미 자체 헤더가 있어 전체 확장은 과함, 로그인 기능 자체가 아직 없어 지금은 홈 화면에만 적용하고 실제 로그인 구현 시 공통 헤더로 승격할지 재검토하기로 결정) → 홈 화면 최상단에 테두리로 구분된 컨테이너(`bg-slate-800/40 border border-slate-700`)를 신설해 날짜+"오늘의 인생 한마디"를 그 안에 배치(기존엔 "안녕하세요! 👋" 인사말 자리였던 곳, 인사말 자체는 삭제하고 그 자리를 한마디 카드가 대신함, 카드 크기는 기존 그대로 유지). **④사이드바 부제 가시성**: "재미로 시작했다가 뼈 맞고 / 공유하는 종합 테스트 모음" 지점에서 줄바꿈(사용자가 지정한 정확한 위치), `text-xs`→`text-sm`+`font-bold`+더 밝은 색(`text-slate-500`→`text-slate-300`)으로 가시성 개선. **⑤사이드바 그룹명 변경**: "테스트"→"두뇌 테스트존". **⑥다크/라이트 모드 UI 개편**: 사이드바 상단 아이콘 토글 1개를 제거하고, 하단(버전 표시 바로 위)에 "🌙 다크모드 / ☀️ 라이트모드" 두 버튼이 항상 같이 보이고 현재 선택된 쪽만 보라색으로 강조되는 방식으로 교체(`setTheme(mode)` 신설, 기존 `toggleTheme()`은 모바일 헤더 전용으로 유지 — 모바일은 공간이 좁아 기존 아이콘 토글 그대로 둠). **버전 규칙 전환**: 이 버전부터 CLAUDE.md에 2026-07-04 확정된 "자리올림 카운터" 방식 적용 — v0.0.54 다음이 v0.1.0. **검증**: `node --check`로 app.js 문법 확인, `npx wrangler pages dev`+Playwright로 홈 화면(다크/라이트)·사이드바 클로즈업·반응속도 테스트 시작화면(안내 카드 접힘/펼침) 실제 스크린샷을 촬영해 매 라운드 사용자에게 확인받으며 진행, 콘솔 에러 없음(Tailwind CDN 프로덕션 경고 제외) |
| 사운드 재설계 + 게임성 강화 | ✅ 완료 | v0.0.54 | PRD.md "10-2/10-3" 항목, Phase 3 로드맵 진행순서의 세 번째. 착수 전 "BGM 넣을지" 재확인(사용자가 이전에 50대 이상 거부감 우려로 비추천받은 부분) → **"BGM 없이 효과음만 개선"으로 확정**, 반복 재생 배경음악은 넣지 않음. **사운드**: `playSound()`의 단순 삐- 단일톤(정답880Hz/오답220Hz)을 퀴즈쇼에서 익숙한 "딩동"(2음 상승)/완만한 하강 2음으로 교체, Tier S 달성 시 짧은 축하 팡파레(4음 아르페지오) 신규 추가. 기존 15개 테스트 중 5개(순서기억력/충동억제/숏폼집중력/속담완성/물가퀴즈)에만 있던 사운드를 Tier 8개 테스트 전체(두뇌나이/반응속도/숫자기억력/색각/논리력 5곳 신규 연결)로 확대. 사이드바·모바일 헤더에 다크모드 토글과 같은 자리에 🔊/🔇 음소거 토글 버튼 신설(`localStorage.sound_enabled`로 기억, 기본값 켜짐). **게임성**: 콤보 카운터(연속 정답 3회부터만 "🔥N연속!" 표시 — 매번 뜨면 과하다는 사용자 우려를 반영해 절제) 신설, 이미 있던 `pulseElement()`(정답 pop/오답 shake 애니메이션)를 색각·논리력 테스트에도 확대 적용, 난이도 선택 직후 "3→2→1→시작!" 카운트다운 연출 신규 추가(브레인/반응속도/색각/논리력/충동억제/숏폼집중력 6개 — 숫자기억력/순서기억력은 이미 "잘 보고 기억하세요" 도입부가 있어 중복 방지 차원에서 제외). **부수 발견**: 사이드바 하단의 사용자 노출용 버전 표시(`v0.0.46 · 2026-07-04`)가 v0.0.46부터 8버전이나 업데이트를 빠뜨리고 방치돼있던 걸 발견해 수정, CLAUDE.md 버전관리 규칙에 앞으로 이 표시도 매번 같이 갱신하라는 항목 추가. **검증**: `npx wrangler pages dev`+Playwright로 카운트다운 프레임(3→2→1→시작!)이 정확한 타이밍에 전환되는지, 논리력 테스트에서 정답을 연속으로 맞혀 콤보 문구가 3연속부터 정확히 뜨는지, 음소거 토글 클릭 시 아이콘·localStorage가 정상 전환되는지, 전체 플레이(카운트다운→콤보→결과)까지 콘솔 에러 없이 통과 |
| HELL 난이도(도전 모드) 추가 | ✅ 완료 | v0.0.53 | PRD.md "10-1" 항목, Phase 3 로드맵 진행순서(5→1→2·3→6→4)의 두 번째. 정성적 테스트(MBTI 등)는 제외하고 Tier 채점 8개 테스트(두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력)에만 기존 쉬움/보통/어려움과 "비교도 안 되게" 어려운 4번째 난이도 추가(예: 색각 델타 8·6x6·2초, 숏폼집중력 No-Go비율 50%). **부수 발견**: seqmem의 그리드 클래스 로직이 3/4단만 처리하는 삼항연산자였고 colorvision 룩업 객체엔 6열이 없어서, HELL의 5x5/6x6 그리드를 그대로 켰으면 레이아웃이 깨졌을 버그를 사전에 발견해 먼저 수정. 시작 화면에 검정·빨강 톤의 별도 "🔥 HELL" 버튼을 추가하고 `confirm()` 경고("정말 어렵습니다, 십중팔구 실패") 통과해야 진입 가능. **B안(난이도별 완전 분리) 채택**: 사용자가 "기존 있는 데이터에 난이도 하나 추가하는 것뿐인데 왜 커지냐"고 지적 → 실제로 `test_results.payload`가 JSONB라 스키마 변경 없이 difficulty를 넣을 수 있다는 걸 재확인, A안(HELL을 통계·대결에서 제외)이 아닌 B안(난이도별 완전 분리)으로 진행 확정. `saveRanking()`/`syncResultToSupabase()`/`renderPercentileBadge()`/`renderChallengeButton()`/`renderChallengeCompareCard()` 8개 테스트 호출부 전부에 `difficulty` 인자 추가(기존 세션에서 반복해온 것과 동일한 패턴의 기계적 작업). `percentile-refresh.js`를 section×difficulty×tier 3중 집계로 재작성(난이도 필드는 `payload->>difficulty`로 조회, 레거시 row는 difficulty 없으면 'normal'로 간주). `percentile_cache`에 `difficulty` 컬럼 추가 + UNIQUE(section,tier)→UNIQUE(section,difficulty,tier) 제약 교체가 필요해 SQL을 사용자에게 안내(제약 이름이 환경마다 다를 수 있어 `pg_constraint`에서 자동 탐색 후 드롭하는 방어적 SQL로 작성), 사용자가 Supabase 대시보드에서 직접 실행. 친구 대결은 도전장 payload에 `d`(difficulty) 필드를 추가해 상대와 난이도가 다르면 승패 대신 "⚖️ 난이도가 달라 직접 비교는 어려워요" 메시지로 대체(같은 난이도끼리는 기존 승패 판정 그대로 유지), 도전장 배너에도 "이 난이도로 도전해야 정확히 비교돼요" 힌트 추가. **검증**: `npx wrangler pages dev`로 로컬 구동 — 8색 스트룹/5x5 순서기억력/6x6 색각 HELL 화면 스크린샷으로 레이아웃 확인, HELL 완주 시 티어·동물카드·공유행 정상 렌더링, 도전장을 어려움↔HELL 다른 난이도로 주고받았을 때 불일치 메시지 노출과 같은 난이도끼리는 기존 승패 판정 그대로 나오는 회귀 테스트까지 통과, 콘솔 에러 없음(단, `percentile_cache`에 difficulty 컬럼이 아직 없어 SQL 마이그레이션 전까지는 퍼센타일 배지 조회가 400으로 조용히 실패함 — try/catch로 감싸져 있어 화면 동작에는 영향 없음, 사용자가 SQL 실행 후 정상화) |
| 로또 직접 뽑기 게임 (물리 추첨기 + Canvas 영수증) | ✅ 완료 → 공유 개선 → 애니메이션·기계 외형 개편 → 물리 "분수 순환" 재설계 | v0.1.2 → v0.1.5 | **v0.1.5**: v0.1.4 바람 물리가 "공이 아래에 뭉치고 어떤 공이 나올지 예측된다"는 피드백 → 실제 에어믹스 추첨기 방식(분수 순환)으로 재설계. 바닥 중앙 노즐이 쉬지 않고 공기를 뿜고(가우시안 수평 폭, 세기 매 프레임 랜덤 요동), 노즐 위치가 비주기 이중 사인으로 좌우 스윙, 중력 강화(0.05→0.09)로 솟구친 공이 양옆 벽을 타고 떨어져 다시 빨려 들어가는 순환 구조 — 팝콘처럼 끓으며 뭉침 없음. 공 크기 0.052→0.041(−21%), 속도 상한 r×0.45→0.75. 옆바람/공별 위상 소용돌이 코드는 제거. Artifact 라이브 데모(현재 vs 개선안 비교→3단계 비교, 2라운드)로 사용자가 직접 보고 "2단계+공 0.041" 확정 후 적용. **v0.1.4**: 물리를 회전 패들→"바람"(하단 에어제트+옆바람+공별 위상 난기류) 방식으로 교체하고 속도 다운, 실제 추첨기 사진 참고한 2D 기계 외형(링 프레임/배출구/유리 튜브/경사 레일/받침대) 신설 — 뽑힌 공이 배출구→튜브→레일을 굴러 내려와 트레이 점선 슬롯에 안착하는 연출(`lottodrawAnimateExit`). 버튼 "6구 고속 추출"→"6개 한번에 뽑기"(1.5배 빠르게)+"5게임 한번에 다 뽑기" 신설(자동 연속 추첨→영수증 직행), 동작하지 않던 "결과 이미지 공유" 버튼은 제거. 이하 v0.1.2 상세 → | PRD 10-6, 사용자가 제공한 Gemini 레퍼런스 프롬프트 기반. 로또 조합기에 5번째 버튼("🎰 직접 뽑기 게임") 추가 → 신규 섹션 `#lottodraw`. **추첨기**: 유리 구체(glassmorphism, `style.css` `.lottodraw-sphere`) 안에서 실제 동행복권 색상 규칙의 45개 공(radial-gradient 입체감)이 rAF 물리 루프(중력/원형 벽 반사/회전 금속 패들 휘젓기/공끼리 탄성 충돌)로 상시 튀어다님. 물리 루프는 `session` 토큰+`currentSection` 체크로 섹션 이탈 시 자동 정지. **듀얼 컨트롤**: [⚪ 공 1개 뽑기](공이 하단으로 굴러 나가는 연출 후 트레이 안착, 손맛용) / [⚡ 6구 고속 추출](430ms 간격 연속 배출, 답답함 해소용). **5게임 시퀀스**: 6개 차면 A~E 보드에 기록 후 공 45개 재충전, E게임까지 반복. **클라이맥스**: 완료 시 추첨기 페이드아웃 → Canvas 2x 스케일로 분홍 감열지풍 영수증(LOTTO 6/45 헤더, 발행일/추첨일/회차 추정, A~E "수동" 마킹, 뽑은 번호 시드 기반 결정론 바코드, "가상" 표기로 정직성 유지) 렌더링. 트레이/보드/배너의 공 UI는 기존 `lottoBallClass()` 재사용, 사운드도 기존 `playSound('tick'/'correct'/'tierS')` 재사용. **v0.1.3 공유 개선**: 사용자가 "결과 이미지 공유가 제대로 작동 안 하는 것 같다"고 지적 → 원인은 `canvas.toBlob()`의 비동기 콜백 안에서 `navigator.share()`를 호출해 일부 브라우저(iOS Safari 등)가 user-activation을 잃는 문제로 진단, 영수증이 렌더링되는 시점에 미리 Blob/File을 만들어 캐싱해두고 클릭 시 즉시 사용하도록 수정(PNG→JPEG로도 전환, 파일 크기·호환성 개선). "웹 링크로 공유(링크 복사)" 버튼은 사용자 요청대로 색각 테스트 등과 동일한 "이미지+링크 카카오 공유" 방식으로 교체 — 신규 공용 이미지(`share-cards/lotto-share.jpg`)와 `functions/share/[section].js`의 `lottodraw` 항목(뽑은 번호를 `extra` 파라미터로 shared-preview까지 그대로 전달해 기존 친구 번호 배너 로직 재사용)을 신설, FB/X/밴드/링크복사 아이콘 행도 추가. 기존 `lottodrawShareLink()`/`lottodrawRefId()`(리퍼럴 UUID)는 새 방식으로 대체되며 제거. |
| 로또 조합기 — 공 색상 다양화 | ✅ 완료 | v0.0.52 | PRD.md "10-5" 항목. 조합 결과의 모든 번호 공이 `bg-violet-700` 하나로 고정돼 어떤 조합을 뽑아도 전부 같은 색 공만 보이던 문제 수정. 실제 동행복권 공식 볼 색상 규칙(1~10 노랑/11~20 파랑/21~30 빨강/31~40 검정·회색/41~45 초록)을 그대로 적용하는 `lottoBallClass(n)` 헬퍼 신설, 조합 결과(`lottoRenderGames()`)와 직전 회차 당첨번호 패널(`lottoRenderStatsPanel()`)의 공 전부에 적용. 보너스 볼은 구간별 색은 그대로 따르되 금색 테두리(`ring-2 ring-amber-400`)를 추가해 "보너스"라는 걸 시각적으로 계속 구분. 500/600대 원색 배지라 다크/라이트 테마 remap 대상이 아니라 별도 테마 작업 불필요(CLAUDE.md 테마 규칙 그대로 적용). Playwright로 완전랜덤 조합 5게임에서 5가지 색 전부 등장하는지, 실제 당첨 통계 패널(1230회차, 보너스 45=초록+금테두리)까지 스크린샷으로 시각 확인, 콘솔 에러 없음 |
| 로또 통계기반 추천 (실제 당첨번호) | ✅ 완료 (미러 데이터 소스로 해결) | v0.0.42~v0.0.46 | dhlottery.co.kr의 데이터센터/해외 IP 차단(v0.0.43~45)을 **발상 전환으로 해결**: 과거 당첨번호는 불변 공공 데이터라 실시간 크롤링 자체가 불필요 — 매주 토요일 추첨 직후 자동 갱신되는 **GitHub Pages 미러(`smok95.github.io/lotto/results/all.json`, 1회차~최신 전체 419KB, CORS 개방, 데이터센터 IP 차단 없음)** 를 1차 소스로 채택. `functions/api/lotto-stats.js`를 미러 1순위 → dhlottery 직접 2순위(차단 풀리면 자동 복귀) 폴백 체인으로 재작성, 클라이언트 `lottoFetchStats()`는 자체 프록시(`/api/lotto-stats`) 실패 시 브라우저가 미러를 직접 조회하는 3중 폴백. "고객이 과거 데이터를 확인하고 그에 따라 조합"이라는 요구에 맞춰 조합 결과 위에 **통계 패널**(직전 회차 당첨번호+보너스, 최근 30회 🔥최다 출현 TOP7 / 🧊뜸한 번호 7개, 번호별 출현 횟수 표기) 신설(`lottoRenderStatsPanel()`). 버튼 재활성화("📊 실제 당첨 통계 기반"). 응답에 `latestRound`/`latestDrawDate`를 항상 표기해 기준 시점을 정직하게 노출 |
| 구글 애드센스 실 게재 | 🔲 **심사 진행 중** (2026-07-05 검토 요청 완료, 결과 대기) | v0.2.2~v0.2.8 | 심사 전 필수 준비물(개인정보처리방침/이용약관/푸터)은 v0.2.2에서 완료(아래 상세 참고). **v0.2.6**: 사용자가 애드센스 가입+사이트 등록(`gwamol-lab.xyz`, 최상위 도메인 형식으로 등록 — 처음엔 `https://www.gwamol-lab.xyz/`로 시도해 "최상위 도메인이어야 함" 에러가 났었고, 프로토콜/www/슬래시를 뺀 `gwamol-lab.xyz`로 재시도해 통과)까지 마친 뒤 코드 스니펫(publisher ID `pub-4825324689294427`)을 전달, `index.html` `<head>` 최상단(다른 CDN 스크립트보다 먼저, Google 권장 위치)에 `adsbygoogle.js` 스크립트 태그 삽입 + 신규 `ads.txt`(repo 루트, `google.com, pub-4825324689294427, DIRECT, f08c47fec0942fa0`) 추가 — 정적 사이트라 루트에 두면 `/ads.txt`로 바로 서빙됨. **다음 단계**: git push로 배포 → 애드센스가 사이트를 크롤링해 소유권 확인 → 심사(보통 몇 시간~2주) → 승인 후 자동 광고 켜기 또는 수동 광고 단위(`<ins class="adsbygoogle">`) 배치. **실제 광고 노출(자동광고 on 또는 수동 슬롯 삽입)은 심사 승인 후 진행 예정**, 그때까지는 스크립트만 심어둔 상태. **참고(2012년 애드센스 공식 블로그 "고품질 사이트 도움말" 1·2부, 지금도 유효한 원칙)**: ①콘텐츠가 거의/아예 없는 페이지 지양(→ v0.2.8에서 준비중 페이지 3개 숨긴 조치와 정확히 일치) ②**콘텐츠 없는 페이지(감사 인사 페이지, 404 등)에는 광고 게재 금지** ③**광고가 콘텐츠보다 과하거나 더 눈에 띄면 안 됨**(광고 구현과 콘텐츠 사이 균형 유지) — ②③은 지금 당장 해당 없음(아직 광고 미게재)이나, **7단계(승인 후 실제 광고 배치) 시점에 반드시 참고할 것**. **v0.2.7~v0.2.8 도메인 이전 마무리 + 심사 신청 완료(2026-07-05)**: `gwamol-lab.xyz`(루트) DNS를 Cloudflare로 완전히 이전(등록업체 네임서버 → Cloudflare 네임서버)하고 Pages Custom domain으로 루트+www 둘 다 연결 완료(둘 다 Active, SSL 자동 발급, 상세 경위는 `[[project_stage_d_backend]]` 메모리 참고). 도메인 정상 응답 확인 후 애드센스에서 사이트 소유권 확인 재시도 → 통과 → **"검토 요청" 버튼 클릭 완료, 애드센스 프로그램 정책 준수 여부 심사가 실제로 시작됨**(대시보드 상태: "사이트의 광고 게재 가능 여부 검토 중"). **다음에 확인할 것**: 심사 결과(승인/거절) 통보 시 — 승인되면 `[[project_adsense_review_temp_hides]]`에 정리된 임시 조치(가짜 통계·죽은 링크 배너·준비중 메뉴 3개 숨김) 원복 + 실제 광고 단위 배치 논의, 거절되면 거절 사유 확인 후 보완 | 이하 v0.2.2 상세 → | 2026-07-05 사용자가 다른 사이트(달다구리) 푸터 스크린샷을 보여주며 "애드센스 하기 전에 먼저 해야 할 작업"으로 지적한 **심사 전 필수 준비물**을 완료: 신규 `#section-privacy`(개인정보처리방침)·`#section-terms`(서비스 이용약관) 정적 섹션 추가(수집 항목/이용 목적/제3자 제공(Supabase·카카오·OpenAI·AdSense)/쿠키 안내/문의처 등 실제 서비스 구조에 맞게 작성, `sectionInits`에 no-op로 등록해 직접 링크 진입도 정상 라우팅되게 함), 푸터를 로고+태그라인+저작권+약관 링크+문의 이메일(`jhb415@gmail.com`) 구조로 재구성(레퍼런스와 동일한 구성 요소, 우리 브랜드 톤으로). **주의**: 정책 문서는 일반적인 웹 서비스 기준의 초안이라 사업자 등록 여부 등 실제 운영 형태에 맞춰 검토·수정 필요함을 문서 하단에 명시해둠. **검증**: 정적 서버+Playwright로 푸터 링크 클릭→약관 페이지 렌더링→플로팅 뒤로가기 버튼 정상 동작까지 다크 테마 기준 확인 |
| 댓글 백엔드 연동 | 🔲 백로그(Stage D 범위에서 제외) → 로그인 필수화로 재검토 여지 | - | 현재 로컬스토리지 임시 저장. 2026-07-03 사용자가 "욕만 먹을 것 같다"며 모더레이션(신고/금칙어 필터) 설계 없이는 착수하지 않기로 결정 — Stage D 1단계 스키마에도 포함하지 않음. 2026-07-05 아래 "온라인 랭킹" 논의에서 "댓글은 로그인해야 작성 가능하게 하자"는 안이 나옴 — 로그인이 최소한의 책임 추적성은 주지만 완전한 모더레이션 해결책은 아니라 신고/삭제 등 최소 장치는 여전히 필요, 온라인 랭킹 착수 시 함께 재검토 예정 |
| 온라인 랭킹 (로그인 + 전체/주간/오늘 탭) | 📋 기획 중(요구사항만 정리, 코드 착수 없음) | - | 기존 "로컬 명예의 전당"(디바이스별 localStorage 기록, 홈 화면 내 정보 카드 하단)을 실제 온라인 랭킹으로 바꾸는 안. 2026-07-05 사용자가 레퍼런스 스크린샷(순위 배지+아바타+닉네임+기록(ms)+날짜 5줄 리스트, 상단 "전체"/"주간"/"오늘" 탭)을 제공하며 4가지 요구사항 정리, 이번 세션은 계획만 세워두고 구현은 다음으로 미루기로 함. **①로그인 검토**: 온라인 랭킹이 기기를 바꿔도 유지되고 어뷰징 추적이 최소한으로라도 되려면 지금의 순수 익명 인증만으로는 부족 — 로그인 수단은 **카카오 로그인 + 구글 로그인 + 익명(게스트)** 3가지로 하기로 함(2026-07-05 추가 논의). 카카오/구글로 로그인한 사용자만 정식 기능(온라인 랭킹 등재, 공유, 기록 영구 보존)을 쓸 수 있고, **익명(게스트)은 기능이 제한**됨 — 공유 불가, 그리고 다음에 재방문하면 그전 기록이 사라지는(영구 저장 안 됨) 방식으로 하기로 함. 이 정책은 로그인 유도 장치이자 어뷰징 방지 장치이기도 함. 랭킹에 공개되는 이름은 카카오/구글 실명·프로필명이 아니라 사용자가 직접 정하는 닉네임으로 분리해야 함(`profiles` 테이블에 공개용 닉네임 필드 별도 필요). **구현 시 참고할 비대칭성**: 구글 로그인은 Supabase Auth가 기본 제공하는 표준 OAuth 프로바이더라 대시보드에서 몇 클릭+Google Cloud Console OAuth 클라이언트 등록 정도로 비교적 간단하지만, 카카오는 Supabase의 1차 지원 프로바이더가 아니라서 커스텀 OAuth(OIDC) 연동이 필요해 상대적으로 손이 더 감(지금 쓰는 "카카오톡 공유하기"와는 완전히 다른 카카오 제품이라 별도 REST API 키+Redirect URI 등록도 새로 필요). **"게스트는 재방문 시 기록 소실" 구현 아이디어**: localStorage(현재 모든 기록의 기본 저장소)는 브라우저를 꺼도 남아있어 "다음에 오면 없어짐" 요건과 안 맞음 — 게스트 모드에서는 localStorage 대신 sessionStorage(탭/창을 닫으면 자동 소멸)를 쓰면 별도의 만료 처리 코드 없이 자연스럽게 요건이 충족됨(구현 시점에 검토). Stage D 1단계에서 Supabase Anonymous Auth 설정 시 "Allow manual linking"을 미리 켜둔 게 바로 이 시나리오(기존 익명 세션 → 카카오/구글 계정으로 연결)를 위한 준비였음. **②탭 구조**: 전체/주간/오늘 3개 탭 구성이 재밌겠다고 판단, 단 "전체" 탭에 날짜를 그대로 노출하면 순위 간 날짜 간격이 크게 벌어져(레퍼런스 스크린샷도 26.03~26.04로 넓게 흩어져 있음) 참여자가 적다는 게 티 나는 문제가 있어 표시 방식 고민 필요(날짜 숨기기/상대 시각 표기/표본이 충분해질 때까지 비공개 등 옵션, 방식 미확정). **③댓글은 로그인해야 작성 가능**하게 설계 — 자연스러운 로그인 유도 수단으로 활용, 위 "댓글 백엔드 연동" 항목과 맞물려 함께 재검토하기로 함(로그인이 완전한 모더레이션 해결책은 아님에 유의). **④테스트별 참여자 수(몇 명이 했는지) 노출**은 후순위 — 표본이 적으면 오히려 초라해 보이는 문제(②와 같은 맥락)가 있어, 집계 인프라는 미리 만들어두되 특정 임계치(예: 섹션당 1000회 이상) 이상일 때만 노출하는 방식을 검토(임계치 수치는 미확정). **다음 단계**: 로그인 UI/어뷰징 방지/랭킹 쿼리 설계 등 상세 스펙과 착수 여부는 다음 세션에서 논의 후 결정 |
| MBTI 16유형 상세 텍스트 검수 | ✅ 검토 완료(변경 없음) | v0.0.48 세션 | v0.0.32에서 `famous` 필드만 가볍게 검수했던 것과 별개로, 16유형 전체의 `desc`/`strength`/`challenge`/`tip` 본문 텍스트를 Node 스크립트로 기계 검증(필드 누락 없음, `title`/`emoji`/`famous` 중복 없음, `desc` 50~62자·`tip` 40~71자로 길이 균형 양호, 문장 시작 패턴도 다양해 반복감 없음) + 직접 정독. INTJ의 `famous`가 "일론 머스크"인 것이 v0.0.32에서 ESTP의 트럼프를 "정치적으로 민감한 현직 인물"이라는 이유로 교체했던 전례와 같은 기준에 해당할 수 있어 사용자에게 교체 여부 확인 → **"그대로 유지" 결정**(머스크는 대중문화에서 INTJ 대표 사례로 여전히 널리 통용됨). 결과적으로 콘텐츠 품질이 이미 양호해 코드/데이터 변경 없이 검토만 완료 |
| PWA 오프라인 지원 | ❌ 불필요 판단(제외) | - | 2026-07-04 사용자가 "필요없겠다"고 명시적으로 판단, MBTI 텍스트 검수와 범위를 저울질하는 과정에서 제외 확정 |
| 두뇌나이(스트룹) 모바일 터치 지연 보정 | 🔲 백로그 | - | PRD.md 학습 반영. touchstart 우선 처리로 ms 정밀도 확보 필요 |
| Phase 2 확장 로드맵 (신규 인지테스트 6종+α, 난이도/모드 체계, 메뉴 개편, 재미요소, 백엔드) | 📋 기획 완료·순차 구현 중 | - | 상세는 `PRD.md` "9. 확장 로드맵" 참고 |
| Phase 4 수익화 확장 로드맵 (가족오락관/포인트경제/제휴배너/심리테스트존/밸런스게임/사주타로궁합 등 13개 항목) | 📋 기획 완료 → Now 4건 완료(v0.2.3) → 심리테스트존 카테고리 구조 재편(v0.2.4) → **사이드바 그룹 재편(v0.2.5)** | v0.2.2~v0.2.5 | 2026-07-05 사용자가 8개 아이디어 제시(정식 수익화 대비 확장성) + Claude가 5개 아이디어 추가 제안, Now(지금 가능)/Later(트래픽·시스템 성숙 필요)로 분류(v0.2.2). **v0.2.3에서 Now 4건 구현 완료**: ①사이드바에 신규 메뉴 4개(심리테스트존/밸런스게임/가족오락관/사주·타로·궁합) 신설 — 가족오락관·사주타로궁합은 전체 "준비중"(가족오락관은 게임 10개 리스트업만), 심리테스트존·밸런스게임은 실제 샘플 1개씩 완전 구현 ②Tier·성향형 테스트 결과 화면 14곳이 공유 `renderPlaceholderUI()` 한 곳에 제휴 상품 배너 슬롯(쿠팡파트너스 등, `href="#"` placeholder) 추가 — 실제 링크는 가입 후 교체만 하면 됨 ③**핵심 원칙 반영**: 심리테스트존 샘플("카톡 답장 속도 테스트", 3등급)과 밸런스게임 샘플("평생 안 아프기 vs 지금 10억")에 기존 `renderIdentityShareRow()`/카카오공유 인프라를 그대로 적용해 완주 후 반드시 공유 가능하게 구현(신규 `scripts/generate-phase4-share-cards.js`로 이미지 4장 생성, `functions/share/[section].js`에 psychtest/balance 분기 추가). 댓글은 "로그인 후 작성 가능" 잠금 문구(온라인 랭킹 계획과 연동 예정), 참여수/공감수는 로컬 더미+증분 방식(다른 더미 위젯과 동일하게 "추후 실데이터 연동 예정" 명시). **검증**: `node --check` 통과, `npx wrangler pages dev`+Playwright로 심리테스트존(피드→포스트→퀴즈 5문항→결과→공유행)과 밸런스게임(피드→포스트→A/B선택→결과 퍼센트바→공유행) 전체 플로우, 가족오락관/사주타로궁합 준비중 화면, 사이드바 신규 메뉴 4개(모바일 포함) 다크/라이트 양쪽, `/share/psychtest`·`/share/balance` og 태그, 기존 테스트(반응속도) 결과화면에 제휴배너 정상 삽입까지 콘솔 에러 없이 확인. Later 9건은 PRD.md에 그대로 보류. **v0.2.4**: 사용자가 실제 라이브 화면을 보고 사이드바 메뉴 순서 재배치("홈→두뇌테스트존→심리테스트존→밸런스게임→오늘의운세→사주타로궁합→꿈해몽→로또", 가족오락관은 요청 목록에 없어 맨 끝으로 임의 배치 후 확인 요청)와 poomang.com 레퍼런스 기반 콘텐츠 고도화를 요청. ①**심리테스트존을 카테고리 그룹으로 재구성**: `character`(캐릭터)/`trait`(성향)/`taste`(취향)/`national`(국민테스트) 4개 카테고리를 사이드바 하위메뉴로 신설(두뇌테스트존과 동일한 `nav-group` 패턴 재사용, 그룹 토글 로직을 공용 루프로 일반화), 각 카테고리는 정원 3개(오픈 1+잠금 2)로 운영. `psychTests`에 `category` 필드 추가, 카테고리 피드 화면에 탭 4개(`.cat-tab-btn`) 신설. ②**문항별 커스텀 선택지 도입(버그 수정 겸 구조 개선)**: 기존엔 모든 심리테스트 문항이 "바로 답장한다/조금 뜸 들이다/한참 뒤에"라는 카톡 전용 문구를 하드코딩해서 공유했던 것을 발견 — 문항 데이터에 `options: [[라벨,점수],...]`를 직접 명시하는 구조로 변경, 혈액형 테스트처럼 문항 1개+선택지 4개로 직접 결과에 매핑하는 것도 동일 엔진으로 지원(range를 단일 구간으로 지정). ③**콘텐츠 4개 완성**: 기존 "카톡 답장 속도"를 poomang 스타일 코인 신조어 제목("칼답러 vs 잠수러 테스트")+FOMO 후킹으로 업그레이드(trait), 신규 "나의 전생 동물 테스트"(character, 4유형), "짠테크형 vs 플렉스형 테스트"(taste, 4유형), "혈액형 성격 테스트"(national, 4유형, 단일문항) 전체 작성. ④**AI 콘텐츠 생성 프롬프트**: 사용자가 "앞으로 게시물을 AI로 생성해서 올리고 싶다"고 요청해 신규 `CONTENT_PROMPTS.md` 작성 — 카테고리 분류 기준·문항 옵션 스키마·톤(FOMO/신조어)·금지사항을 포함한 프롬프트, 생성된 JSON을 전달받으면 검증 후 `data.js`에 추가하는 워크플로우로 합의. `scripts/generate-phase4-share-cards.js`를 `AppData.psychTests`/`balanceGames`를 직접 순회하도록 리팩터해 콘텐츠 이중 관리 방지(신규 테스트 추가 시 재실행만 하면 이미지 자동 생성). **버그 발견·수정**: `functions/share/[section].js`의 grade 검증 정규식이 `/^[ABC]$/`(katokspeed 3등급 기준)로 하드코딩돼 있어 신규 4등급 테스트의 공유 이미지가 전부 A로 강등되는 문제를 Playwright 검증 중 발견해 `/^[ABCD]$/`로 수정. **검증**: `node --check` 통과, wrangler+Playwright로 4개 카테고리 피드 전환, 전생동물(5문항 만점→D등급)/짠테크(5문항 최저점→A등급)/혈액형(1문항 AB형→D등급) 전체 플레이, "목록으로" 버튼이 방문 중이던 카테고리를 정확히 기억하는지, 신규 테스트들의 `/share/psychtest` og:image가 올바른 등급 이미지로 나오는지까지 콘솔 에러 없이 확인. **v0.2.5**: 사용자가 사이드바 레퍼런스 이미지를 첨부하며 메뉴 그룹 재편 요청 — 기존에 최상위에 흩어져 있던 밸런스 게임/오늘의 운세/꿈 해몽 검색/사주·타로·궁합을 신규 그룹 2개로 묶음. `🗳️ 과몰입 투표소`(밸런스 게임 + 신규 `🏆 이상형 월드컵` 준비중 placeholder 섹션)와 `🔭 운명 관측소`(오늘의 운세 + 꿈 해몽 검색 + 사주/타로/궁합)를 두뇌테스트존·심리테스트존과 동일한 `nav-group` 패턴으로 신설(그룹 토글 공용 루프에 두 그룹 ID만 추가), 그 아래 단독 항목으로 가족오락관 → 로또 번호 조합기 순서 배치. "사주 · 타로 · 궁합"은 하위메뉴로 들어가며 폭이 좁아져 줄바꿈되는 문제가 있어 구분자를 `·`→`/`로 변경했으나, 그것만으로는 부족해(기본 CJK 줄바꿈 규칙상 한글 글자 사이 어디서든 줄바꿈이 허용됨) 실제로는 여전히 2줄로 잘리는 걸 Playwright 검증 중 발견 — `.nav-item`/`.nav-group-header`에 `white-space: nowrap` 추가로 근본 수정(전체 29개 메뉴 항목 중 가로 overflow 발생 항목 없음을 실측 확인 후 적용). **주의**: 이 작업은 로컬 코드 수정 직후라 아직 git push 전 상태였는데, 사용자가 라이브 배포 사이트(Cloudflare Pages)에서 "운명 관측소가 안 보인다"고 재확인 요청 — 실제로는 버그가 아니라 배포 전 상태였을 뿐임을 확인, 커밋+푸시로 해결(이 프로젝트는 GitHub 연동 자동배포라 push 후에만 라이브에 반영됨). **검증**: `node --check` 통과, Playwright로 신규 그룹 순서·하위 항목 클릭 정상 동작(밸런스 게임/오늘의 운세/꿈 해몽 검색 기존 기능 회귀 없음)·이상형 월드컵 준비중 화면·그룹 접기펼치기+새로고침 후 상태 유지(`nav_vote_collapsed`/`nav_destiny_collapsed`)·사주/타로/궁합 한 줄 렌더링(47px, 다른 항목과 동일)까지 콘솔 에러 없이 확인. **v0.2.9 버그 수정**: 사용자가 "심리테스트존 하위메뉴에서 뭘 누르든 사이드바에 캐릭터 테스트만 계속 강조 표시된다"고 제보 — 원인은 4개 하위메뉴가 전부 `data-section="psychtest"`를 공유(카테고리만 `data-psych-category`로 구분)하는데, `App.navigate()`의 활성 메뉴 하이라이트 로직이 `document.querySelector('.nav-item[data-section="psychtest"]')`로만 찾아서 DOM상 항상 첫 번째 항목(캐릭터 테스트)만 골라버리던 것. `navigate()`가 `sectionId === 'psychtest'`일 때는 `App.state.psychtest.category`까지 같이 매칭하도록 수정 + 사이드바 클릭 핸들러에서 `psychtestNavCategory()`(상태 갱신)를 `App.navigate()`보다 먼저 호출하도록 순서 변경(반대 순서면 navigate가 갱신 전 카테고리를 참조하게 됨). **검증**: Playwright로 4개 카테고리를 순서대로 클릭하며 매번 정확히 그 카테고리의 사이드바 항목만 강조되는지 확인, 콘솔 에러 없음. **v0.3.1 — 밸런스게임/국민테스트 콘텐츠 퀄리티 개선(poomang류 어그로·댓글논쟁 지향)**: 사용자가 "밸런스게임/국민테스트 샘플 퀄리티가 너무 낮다, poomang 같은 사이트 참고해서 MZ 어그로 끌고 커뮤니티화(댓글논쟁)될 수 있는 내용으로 디벨롭해달라"고 요청. 착수 전 AskUserQuestion으로 3가지(밸런스게임 소재 방향/국민테스트 3번째 슬롯 소재/코드 구조를 "1개 오픈+가짜 잠금"에서 "전부 오픈"으로 바꿀지)를 확인받고 진행. **①밸런스게임**: 기존 "평생 안 아프기 vs 지금 10억" 1개(무난해서 논쟁 요소 없음)만 진짜였고 나머지 2칸은 가짜 "준비중" placeholder였던 것을, 전애인 SNS 차단여부/데이트비용 정산방식/워라밸-연봉/축의금 액수/딩크족 등 실제 온라인에서 자주 논쟁되는 가치관형 소재 5개를 신규 작성해 총 6개로 확장(성별 갈등처럼 위험한 소재는 배제, "정산 스타일"·"인생관 차이"처럼 중립적으로 프레이밍). `renderBalanceFeed()`가 `balanceGames[0]`만 보여주고 나머지는 하드코딩된 가짜 잠금 카드였던 구조를 `AppData.balanceGames` 전체를 순회해 전부 실제 플레이 가능한 카드로 노출하도록 변경(잠금 placeholder 완전 제거). **②국민테스트**: "혈액형 성격 테스트" 1개만 진짜였던 걸 톤을 순한 성격묘사에서 "국룰이잖아 ㅋㅋ" 식 티키타카 반말 톤 + 각 결과 끝에 "댓글로 검증/반박하고 가기" 유도 문구로 전면 재작성, 기존 백로그였던 "별자리/탄생월 성격 테스트"(순한 소재라 논쟁 유발력 낮음)는 빼고 실제 국민 밸런스 논쟁 최상위권 소재인 **민초 vs 반민초**(민트초코)와 **부먹 vs 찍먹**(탕수육)을 5문항·4등급 구조로 신규 작성해 카테고리 정원 3개를 전부 실제 콘텐츠로 채움(`PSYCHTEST_LOCKED.national`을 빈 배열로 변경). `renderPsychtestFeed()`가 카테고리당 `.find()`로 첫 매칭 1개만 노출하던 것을 `.filter()`로 바꿔 여러 개를 전부 노출하도록 수정(다른 카테고리는 여전히 1개 오픈+2개 잠금 그대로, 이번 범위는 밸런스게임·국민테스트 한정). **공유 이미지**: 신규 10개(밸런스게임 5+국민테스트 8등급) + 톤이 바뀐 기존 혈액형 4등급 재생성, `scripts/generate-phase4-share-cards.js`가 `AppData.psychTests`/`balanceGames`를 그대로 순회하는 구조라 코드 수정 없이 재실행만으로 전부 생성됨(안전영역 검증 전량 scale=1 통과). **검증**: Node 스크립트로 신규 데이터의 필드 누락·제목 중복·점수 range 공백 없음을 기계 검증, `node --check` 통과, Playwright로 밸런스게임 6개 카드 전부 노출+신규 게임(전애인 SNS) 실제 플레이, 국민테스트 3개 카드 전부 노출+민초 테스트 5문항 실제 플레이해 등급 산정까지 정상 동작, 콘솔 에러 없음 확인 | 상세는 `PRD.md` "11. Phase 4 수익화 확장 로드맵" 참고 |
| 이상형 월드컵 — "인생 공감 밈 월드컵" 1개 실구현 → 임계치 미만 순위 문구 숨김 → **팩 시스템 전면 재구축(6개 팩, 160개 아이템)** → **이미지형 3개 팩 실사진 소싱 완료** → **공유→"나도 해보기" 진입 시 빈 화면 버그 수정** → **cute-animals-32 오매칭 이미지 2장 교체** | ✅ 완료 | v0.4.0 → v0.6.4 | **v0.6.4 — 사용자가 라이브 화면에서 "치와와" 카드에 멕시코 치와와 시(市)의 건물 콜라주 사진이, "카피바라" 카드엔 카피바라가 프레임 구석에 작게 걸쳐 크롭되면 아예 안 보이는 온천 배경 사진이 나온다고 스크린샷으로 제보.** 원인 확인: `worldcup-sources/cute-animals-32.sources.json`의 검색 키워드가 `"chihuahua"`(개 품종과 멕시코 도시 이름이 동음이의라 검색 결과가 도시 사진으로 오매칭) / `"capybara bath"`(실제로는 카피바라가 맞았지만 프레임 왼쪽 구석에 작게 걸쳐있어 3:4 카드 크롭(`object-fit:cover`) 시 아예 잘려나감)였던 게 원인. Pexels(로열티프리, 상업이용 무료, 출처표시 불요 — 기존 CC0 원칙과 동일 선상)에서 검색 후보 여러 장을 다운로드해 실제 3:4 크롭 시뮬레이션(Python PIL로 카드와 동일한 `object-fit:cover` 크롭 재현)까지 거쳐 확인 후 교체: `an23`(치와와, 큰 귀+또렷한 눈빛의 단독 강아지 초상), `an32`(카피바라, 물속에서 정면을 응시하는 단독 근접샷 — "온천 마니아" 캡션과도 잘 어울림). `worldcup-images-raw/cute-animals-32/{an23,an32}.jpg` 교체 후 `python3 scripts/process_worldcup_images.py` 재실행으로 `worldcup/images/cute-animals-32/{an23,an32}.webp` 갱신(다른 94장은 원본 그대로라 무변화). `worldcup-sources/cute-animals-32.sources.json`에 두 항목 모두 "왜 문제였는지" 주석을 남겨 향후 이미지형 팩 소싱 시 같은 실수(모호한 검색어, 피사체가 작게 걸친 사진) 재발 방지. **검증**: 재생성된 webp를 PIL로 직접 열어 원본 시각 확인, 실제 카드 CSS 규칙(`.wc-match-image { aspect-ratio: 3/4 }` + `object-fit:cover`)과 동일한 크롭을 코드로 재현해 두 사진 모두 크롭 후에도 피사체가 중앙에 온전히 보임을 확인, `node --check` 통과. 이하 v0.6.3 상세 → | **v0.6.3 — 공유 링크로 들어와서 "나도 해보기"를 누르면 헤더/푸터만 보이고 본문이 완전히 빈 화면으로 멈추는 버그 수정.** 사용자가 실제 카카오톡 공유 링크로 접속해 재현한 스크린샷을 제보. Playwright로 직접 재현해보니 실제로는 `#worldcup-container` 안에 팩 인트로 화면(썸네일/제목/라운드 선택 버튼)이 전부 정상 렌더링돼 있었는데, 부모 `#section-worldcup`에 `hidden` 클래스가 다시 붙어있어 화면에 하나도 안 보이는 상태였음(콘텐츠는 있는데 컨테이너가 숨겨진 상태). **근본 원인**: `app.js` 맨 아래 `hashchange` 리스너(`location.hash.replace('#','') || 'home'`)가 쿼리스트링을 떼어내지 않고 있었음 — "나도 해보기" 클릭 시 `App.navigate('worldcup')`가 `location.hash`를 `'worldcup'`으로 바꾸며 비동기 `hashchange` 이벤트를 예약한 직후, 같은 동기 실행 흐름 안에서 `wcOpenIntro()`가 주소창만 갱신하려고 `history.replaceState(...,'#worldcup?play=lazy-hell-32')`를 호출 — 이 두 호출이 겹치는 유일한 조합(월드컵 공유 진입 전용, 다른 15개 테스트는 이런 조합이 없어 문제 없었음)이라, 뒤늦게 발화한 `hashchange` 이벤트가 이미 쿼리까지 붙어버린 해시(`'worldcup?play=lazy-hell-32'`)를 통째로 섹션 id로 오인 → `App.navigate('worldcup?play=lazy-hell-32')`를 호출 → 모든 섹션을 `hidden` 처리한 뒤 `document.getElementById('section-worldcup?play=lazy-hell-32')`(존재할 수 없는 id)를 못 찾아 그대로 `return` — 방금 정상 렌더링됐던 `#section-worldcup`이 다시 숨겨진 채로 멈춤. **수정**: `hashchange` 핸들러가 초기 라우팅 코드와 동일하게 `.split('?')[0]`로 쿼리스트링을 떼어내고 섹션 id만 비교하도록 한 줄 수정 — DOMContentLoaded의 초기 라우팅 파싱 로직과 일관되게 맞춤. **검증**: `node --check` 통과, `wrangler pages dev`+Playwright로 (a) 수정 전 상태를 먼저 재현해 실제로 `section-worldcup hidden:true`임을 확인 (b) 수정 후 동일 플로우(공유 랜딩→`#shared-preview`→"나도 해보기" 클릭)에서 `section-worldcup hidden:false`+인트로 화면 정상 노출 확인 (c) 회귀 없음 확인 — 일반 사이드바 내비게이션(`navigate('mbti')`/`navigate('home')`), 월드컵 탐색 화면에서 카드 "시작하기" 직접 클릭(다른 replaceState 경로), 브라우저 뒤로가기 버튼 전부 정상 동작. 이하 v0.6.2 상세 → | **v0.6.2 — 이미지형 팩 실사진 소싱 완료.** v0.6.1에서 placeholder 폴백 상태로 남겨뒀던 이미지형 3개 팩(`cute-animals-32`/`soul-food-32`/`korea-travel-16`)에 CC0 무료 상업이용 스톡사진을 실제로 채우고, AI 오리지널 캐릭터 팩(`gwamol-emotion-16`)은 신규 `scripts/generateWorldcupImages.js`(OpenAI Images API `gpt-image-1` 호출 → `process_worldcup_images.py` 파이프라인 자동 연계)로 생성 — **총 4팩 96장 전량 완료**(cute-animals-32 32장/soul-food-32 32장/korea-travel-16 16장/gwamol-emotion-16 16장), `worldcup/images/{packId}/{itemId}.webp`에 저장되고 `data.js`의 `imagePath` 필드가 전부 실제 파일을 정확히 가리킴. 신규 `worldcup/packs/README.md`에 "왜 팩마다 소싱 방식이 다른가"(동물/음식/여행=실사진이 재미 요소라 CC0 스톡사진, 과몰이 마스코트=오리지널 캐릭터라 AI 생성 외엔 방법이 없음)를 명시. `.gitignore`에 `.env` 추가(신규 `.env.example`로 `OPENAI_API_KEY` 필요 사실만 문서화, 실제 키는 Codespaces Secrets에서 주입). **검증**: `node --check` 통과, `data.js`의 imagePath 참조 96개 전부 디스크에 실재(0바이트 파일 없음), 4개 팩 각각 데이터 개수(32/32/16/16)와 파일 개수 정합성 확인, 정적 서버로 이미지 응답 200 확인. **다음**: 팩 3~6은 이제 전부 실사진 완료 상태 — 남은 후속 작업은 실사용 반응(역배 토스트/랭킹 100판 임계치 등) 모니터링 정도. 이하 v0.4.0~v0.6.1 상세 → | **v0.6.1 — 전면 재구축.** 사용자가 kravvme.com(매거진형 탐색)+zzicky.com(카테고리 칩/듀얼 버튼) 레퍼런스와 확정된 스펙(팩 JSON 스키마, 콘텐츠 6개 팩 전체, Supabase 테이블 설계)을 프롬프트로 전달, "질문 없이 진행하되 충돌 시만 보고"로 위임. **사전 분석에서 발견한 충돌 2건**: ①이 SPA는 해시 라우팅만 지원해 스펙의 `/worldcup/play/:packId` 같은 진짜 URL 경로가 없음 → `#worldcup?play={packId}`/`#worldcup?rank={packId}`(다른 기능의 `?vs=`/`?drawn=` 패턴과 동일)로 대체, 화면 전환은 `history.replaceState`로 주소창만 갱신(hashchange 미발생 — `App.navigate`가 모르는 sectionId를 만나면 화면을 비워버리는 기존 동작과 충돌 방지) ②레거시 `worldcup_votes`(테이블)/`worldcup_stats`(뷰) 스키마가 신규 스펙의 `worldcup_stats`(다른 컬럼의 테이블)/`worldcup_plays`와 이름이 겹쳐 SQL에서 drop 후 재생성. **레거시 처리**: 기존 밈 8개 단일토너먼트 코드(`app.js`의 `initWorldcup`~`worldcupRenderResult`, `data.js`의 `worldcupMemes`)는 git 히스토리가 곧 백업이라는 원칙으로 삭제 후 전면 재작성(다른 방식으로 파일에 죽은 코드로 남겨두지 않음). **콘텐츠**: `AppData.worldcupPacks`에 팩 6개(😇극한의귀찮음/🦸초능력=병맛·이모지형·32강, 🐶귀여운동물/🍗소울푸드=32강·✈️국내여행지=16강 CC0 이미지형, 🐾과몰이감정표현=16강 AI 이미지형) 총 160개 아이템 전량 반영(사용자 원문 그대로, lh14의 캡션 리터럴 "/"는 파싱 충돌 방지 위해 별도 처리). CC0 소싱 참고용 키워드는 앱이 읽지 않는 `worldcup-sources/{packId}.sources.json`으로 분리 보관. **모듈 구성**: 이 프로젝트는 번들러 없는 전역 스크립트 SPA라 실제 ES 모듈 대신 app.js 안에서 역할별 주석 블록으로 구분(①bracketEngine 순수함수 ②worldcupLoader 팩 검증/조회 ③statsEngine Supabase read/write ④explorePage ⑤ui/resultRenderer ⑥rank page ⑦shareCard). **탐색**: 검색바(입력마다 그리드만 부분 재렌더해 포커스 유지)+카테고리 칩(개수 동적표시)+인기순/최신순 토글+2분할 썸네일 카드(이모지형은 큰 이모지 좌우 배치, 이미지형은 실사진), 참여자수는 `worldcup_plays` 실제 row 개수 기반(100 미만 NEW 뱃지, 다른 곳의 로컬 더미와 달리 진짜 서버 집계). **플레이**: 인트로(커버+에디토리얼+라운드 선택+세션 이어하기 버튼)→매치(선택 시 확대/흐림 후 0.4초 뒤 진행, 다음 매치 이미지 프리로드, 4강 진입 시점 스냅샷으로 "나의 4강" 표시)→결과(우승/준우승/공유). **역배 토스트**: 방금 고른 아이템의 "이전까지 누적 승률"이 표본 10+에 20% 이하면 축하 토스트(매치업 단위 페어 데이터가 스키마에 없어 아이템 단위 승률로 실용적 해석). **세션 이어하기**: sessionStorage(탭 닫으면 소멸, 로그인 없는 익명 플레이 특성에 맞춤). **Supabase**: `worldcup_stats`(pack_id+item_id별 wins/losses/appearances/championships, 읽고-더해서-쓰는 upsert)+`worldcup_plays`(팩별 완주 기록), 둘 다 익명 insert/update 허용 RLS — 실패해도 화면엔 영향 없도록 항상 fire-and-forget+catch. **공유**: 이모지형(64개 아이템)은 `scripts/generate-worldcup-share-cards.js`로 카드 이미지 사전 생성, 이미지형은 실제 사진 경로를 og:image로 직접 재사용(별도 생성 불필요, 기존 원칙 그대로). `functions/share/[section].js`는 팩 데이터를 복제하는 대신 클라이언트가 계산한 팩제목/우승자이름/이미지URL을 쿼리로 그대로 전달받아 사용(성향형 섹션들의 `result` 텍스트 전달 패턴과 동일, 6개 팩 160개 아이템을 서버에 중복 보관 안 해도 됨 — `image` 파라미터는 우리 origin의 `share-cards/`/`worldcup/images/` 경로일 때만 신뢰). "나도 해보기"는 `#shared-preview`에서 `extra=packId`로 전달돼 곧장 그 팩 인트로로 진입(다른 섹션의 나도 해보기와 동일 사상). **이미지 파이프라인**: `worldcup-images-raw/{packId}/{itemId}.(png|jpg)`(`.gitignore` 처리) → `scripts/process_worldcup_images.py`(Python/Pillow, 최대 800px+WebP)로 `worldcup/images/{packId}/{itemId}.webp`에 생성(스펙 원문의 `/public/worldcup/...`은 이 프로젝트에 `/public` 폴더가 없어 다른 정적 자산과 동일하게 루트 경로로 조정). 실제 파일이 아직 없어도 `<img onerror>`→`wcImgFallback()`이 "🖼️ 이미지 준비중" placeholder로 안전하게 폴백해 게임이 끊기지 않음. **버그 발견·수정**: 팩 제목에 이미 이모지가 포함돼있는데(`"😇 극한의 귀찮음 월드컵"`) 카드/인트로/랭킹 3곳에서 `pack.emoji`를 앞에 또 붙여 이모지가 중복 출력되던 걸 Playwright 검증 중 발견해 제거. **검증**: `node --check` 4개 파일 통과, Node로 6개 팩 아이템 수(32/32/32/32/16/16) 정합성 확인, Playwright로 탐색(카드 6개/카테고리 필터/검색/정렬)·이모지형 8강 완주(세션 이어하기 포함)·이미지형 16강 완주(placeholder 폴백, Supabase 테이블 미생성 400/404가 나도 결과 화면 정상 렌더링)·직접 URL 진입(`#worldcup?play=`/`?rank=`)·`wrangler pages dev`로 실제 공유 랜딩(og 태그)→`#shared-preview`(팩 실제 이미지/제목 재현)→"나도 해보기"(해당 팩 인트로 정확히 진입)까지 전체 플로우 콘솔 에러 0건 확인. **다음에 사용자가 할 일**: ①Supabase SQL Editor에서 아래 마이그레이션 실행(레거시 `worldcup_votes`/`worldcup_stats` 뷰 drop 포함) ②`worldcup-images-raw/{packId}/{itemId}.png`에 실제 사진 배치 후 `python3 scripts/process_worldcup_images.py` 실행(CC0 소싱 키워드는 `worldcup-sources/*.sources.json` 참고, 팩6 과몰이는 AI 생성 이미지 투입) — 코드 변경 없이 자동 반영됨. 이하 v0.4.0~v0.4.4 상세 → | 사용자가 피쿠(piku.co.kr)·인벤·펨코 월드컵 스크린샷 12장을 제공, Claude가 핵심 메커니즘(①토너먼트 브래킷 ②라운드 수 ③"내 픽 전체 순위" ④랭킹 페이지 ⑤댓글=픽 귀속 ⑥공유)을 분석해 보고. **연예인/캐릭터 사진은 저작권+애드센스 심사 리스크로 배제** — 대신 "MZ 밈 공감형" 소재로 방향 확정(AskUserQuestion). 처음엔 이모지 카드로 제안했으나 사용자가 "이모지는 노잼, 실사진 써야 재미있다"고 명시적으로 반려 → **무료 상업이용 스톡사진**(Unsplash/Pexels/Pixabay, 저작권료·출처표시 불요)으로 방향 전환. 후보를 감정형(웃프러 등, 사진화 어려움)에서 사물/장면 중심 8개로 재선정: 눕눕/텅장/카페인수혈/야식/스크롤중독/영수증플렉스/택배쌓기/소파귀차니즘. **사진 소싱**: 백그라운드 에이전트에게 8장 검색·다운로드 위임 — 에이전트가 자체적으로 애플 로고가 찍힌 아이폰 사진, 나이키·아디다스 등 브랜드 로고 박스 사진 2건을 트레이드마크 리스크로 판단해 스스로 교체하는 등 신중하게 소싱(`assets/worldcup/*.jpg`, 8장). 다운로드 후 Python(Pillow)으로 EXIF 메타데이터 일괄 제거(일부 파일에 "Hak cipta..."(저작권) 문구가 EXIF에 남아있어 찜찜함을 없애기 위한 안전 조치, 사이트 라이선스 자체는 무관하게 이미 자유 이용 가능). **랭킹 설계**: 사용자가 "사람 없을 때 순위 보여주면 역효과 아니냐"고 우려 → Claude 판단으로 "투표는 1일차부터 Supabase에 실제 익명 기록(데이터 손실 없음), 노출은 누적 100판 넘을 때까지 숨김"으로 절충(기존 `percentile_cache`의 `MIN_SAMPLE_SIZE` 표본 게이트와 동일 사상, 임계치는 사용자가 30→100판으로 상향 확정). `worldcup_votes` 테이블(익명 INSERT) + `worldcup_stats` 뷰(meme_id별 count 집계, SELECT 공개)로 설계 — SQL은 사용자가 Supabase SQL Editor에서 직접 실행 필요(다른 시크릿과 동일 원칙). **구현**: `AppData.worldcupMemes`(8개 후보) 신규, `app.js`에 `initWorldcup`/`worldcupStart`/`worldcupRenderMatch`/`worldcupPick`/`worldcupFinish`/`worldcupRenderResult` 등 엔진 전체 신규 작성(8강→4강→결승 단일 토너먼트). 결과 화면은 챔피언 실사진+타이틀+순위(임계치 이하면 "아직 데이터가 적어서 비공개") + 기존 `shareKakaoButtonHTML`/`shareIconRowHTML` 재사용(챔피언의 실사진을 그대로 카카오 카드 이미지로 사용 — 다른 15개 테스트처럼 별도 이미지 생성 스크립트 불필요). `functions/share/[section].js`에 `worldcup` 분기 추가(별도 런타임이라 data.js를 import할 수 없어 title/desc만 최소 복제). 사이드바 `nav-item`의 `hidden` 클래스와 "준비중" 배지 제거(v0.2.8에서 애드센스 심사용으로 숨겼던 3개 중 하나였으나, 이제 실제 콘텐츠가 생겨 "빈 페이지 지양" 원칙에 따라 오히려 노출이 맞음 — 가족오락관/사주타로궁합 2개는 여전히 준비중이라 계속 숨김 유지, `[[project_adsense_review_temp_hides]]` 메모리도 갱신 필요). **검증**: `node --check` 통과, Playwright로 인트로→8강→4강→결승 전체 플레이(선택자를 컨테이너로 스코프하지 않으면 다른 숨김 섹션의 카드를 잘못 집는 함정 발견·수정)→결과 화면(챔피언 사진/순위 placeholder/공유 버튼) 렌더링, Supabase 테이블 미생성 상태에서 404가 나도 앱이 안 깨지고 "집계 중" 문구로 우아하게 처리되는지 확인. **다음에 사용자가 할 일**: Supabase SQL Editor에서 `worldcup_votes` 테이블 + `worldcup_stats` 뷰 생성 SQL 실행 — 실행 전까지는 순위가 항상 "집계 중"으로만 표시됨(에러는 안 남). **v0.4.4**: 사용자가 실제로 SQL을 실행하고 1판 플레이해보니 "아직 데이터가 적어서 전체 순위는 비공개예요 (누적 1판, 100판부터 공개)" 문구가 그대로 노출되는 걸 보고 "고객한테 보여주지 말고 실제로 숨겨달라"고 요청 — 표본 부족을 인정하는 placeholder 문구 자체가 사용자 경험상 안 좋다고 판단, `worldcupRankingHTML()`이 `stats`가 없거나 임계치(100판) 미만이면 아무 문구도 없이 완전히 빈 문자열을 반환하도록 수정(100판을 넘어야만 "전체 N판 중 M위" 문구가 처음으로 나타남). `node --check` 통과 |
| 밸런스게임 스페셜 (여러 문항 → 유형 결과, 피쿠 "중급" 레퍼런스 기반) → 결과유형 8단계 세분화 + 실시간 서버 집계 | ✅ 완료 (2개, 8등급+실데이터) | v0.4.1 → v0.6.0 | **v0.6.0**: 사용자가 클로드(다른 세션)와 논의한 밸런스게임 고도화 기획서(테마팩 구조/실시간 응답비율/결과유형 확장/공유카드/마이크로인터랙션/모듈화)를 프롬프트로 전달 — 착수 전 Explore 에이전트로 현황(balanceGames 6개=단문항 A/B, balanceSpecials 2개=다문항 유형결과, 실시간 응답비율은 전부 로컬+더미, 서버 집계 인프라 없음)을 조사해 보고한 뒤 AskUserQuestion으로 범위(①콘텐츠+결과유형 확장만 vs ①+②실시간 서버집계까지 vs 전체 7개 요구사항 / balanceGames 구조변경 여부 / 결과유형 확장 시 톤)를 확인받아 **"①+② 진행, balanceGames는 구조 그대로 유지하고 balanceSpecials 확장에 집중, 톤은 기존 v0.3.1 선(심사 리스크 없는 선의 병맛+가벼운 논쟁) 유지"로 확정. 실시간 서버집계는 방식(이상형 월드컵의 `worldcup_votes`/`worldcup_stats` 패턴 재사용 — 익명 insert 테이블 + 집계 뷰)을 사용자에게 먼저 설명하고 컨펌받은 뒤 착수. **①결과유형 확장**: `lifepick`/`lovepick` 둘 다 4등급(A~D, score 2~3구간 단위)에서 8등급(A~H, score 거의 1점 단위)으로 세분화 — 기존 최상위/최하위 등급 문구는 최소 수정으로 보존, 중간 4개 등급은 캐릭터(동물)를 신규로 채워 넣어 촘촘한 스펙트럼 구성(예: lifepick은 백조→공작→부엉이→너구리→다람쥐→수달→곰→모아이). **②실시간 서버 집계**: 신규 Supabase 테이블 `balance_responses`(익명 insert, `pack_id`/`question_id`/`choice`)+집계 뷰 `balance_stats`(SELECT 공개) — SQL은 사용자가 직접 실행 필요(다른 시크릿과 동일 원칙, 미실행 시 404가 나도 앱은 안 깨지고 더미값만 보임, Playwright로 확인). `balanceGames`(스피드 6개)는 `dummySplitA`를 "가상 표본 100개"로 두고 실제 집계와 블렌딩하는 `balanceBlendPercent()`로 즉시 %를 보여주면서 점점 정확해지는 구조(기존 로컬스토리지 전용 카운터는 제거), 선택 직후 내 선택이 소수의견(≤35%)이면 리액션 문구 노출. `balanceSpecials`는 문항마다 실제 투표를 기록하고, 결과 화면에서 `Promise.all`로 전체 문항의 집계를 조회해 "몇 문항이 소수의견이었는지" 요약(표본 5개 미만 문항은 `percentile_cache`/월드컵과 동일한 표본 게이트로 비교 제외, 화면엔 조용히 생략). **부수 수정**: `functions/share/[section].js`의 balance grade 정규식이 `/^[ABCD]$/`로 하드코딩돼 있어 신규 E~H 등급 공유 시 이미지가 깨질 뻔한 걸 사전에 발견해 `/^[A-H]$/`로 수정(v0.2.4의 psychtest 4등급 버그와 동일 패턴). **검증**: `node --check` 3개 파일 통과, Node 스크립트로 8등급 range가 score 0~8/0~9 전 구간을 빈틈없이 커버하는지 기계 검증, `scripts/generate-phase4-share-cards.js` 재실행으로 신규 등급 공유 이미지 16장 생성, Playwright로 정적 서버(Supabase 테이블 미생성 상태)에서 balanceGames 즉시 더미% 노출→balanceSpecials 8문항 전부 최고점 선택→3초 로더→그레이드 H 결과 정확히 매핑, 404 응답이 콘솔에 남아도 화면은 정상 렌더링(minority 문구는 조용히 빈 상태)되는 것까지 확인. **다음에 사용자가 할 일**: Supabase SQL Editor에서 `balance_responses` 테이블 + `balance_stats` 뷰 생성 SQL 실행 — 실행 전까지는 지금처럼 더미 위주 % + 소수의견 리액션 미노출로 정상 동작, 실행 후 표본이 쌓이면 자동으로 실데이터가 반영됨. 이하 v0.4.1 상세 → | 전날 세션에서 피쿠(piku) 레퍼런스 스크린샷 4단계(간단/중간/고급/현 사이트 수준)를 분석해 확정한 기획의 실구현. **고급(방구석연구소식 문항별 전용 일러스트+부적/자격증 결과 아트웍)은 이미지 제작 파이프라인이 없어 배제, "중급 수준"(CSS+이모지 그라데이션 카드)을 목표로 확정**(사용자가 "솔직히 중급 이상으로 만들고 싶다"고 했고 Claude가 실제 제작 가능선인 중급을 추천, 승인됨). 구조는 완전 신규 엔진이 아니라 **심리테스트존 엔진과 같은 채점 사상(여러 문항 → pt 합산 → range로 유형 매핑)을 밸런스게임 고유 UI(문항마다 A/B 카드 2개: 이모지+제목+짧은 설명)로 감싼 것** — `data.js`에 신규 `AppData.balanceSpecials`(문항 `options:[{emoji,label,desc,pt}]`, 결과 `{grade,range,emoji,title,catch,detail,hashtags[3],traits[4]}`), `app.js`에 `balanceSpOpen`/`balanceSpStart`/`renderBalanceSpQuestion`/`balanceSpAnswer`/`renderBalanceSpResult` 신설. 1차 게임 2개: **"인생 밸런스 게임 — 그나마 나은 지옥 고르기"**(8문항, A=신체 불편 감수/B=사회적 굴욕 감수 축, 백조/너구리/수달/모아이 4유형)와 **"연애 밸런스 게임 — 최악 피하기 편"**(9문항, A=무심함 감수/B=과잉밀착 감수 축, 고양이/수달/강아지/코알라 4유형). 결과 화면 = 유형 카드(캐치프레이즈+해시태그 칩+특징 체크리스트 2단 그리드) + 기존 공유 인프라(`renderIdentityShareRow('balance', ...)`) 재사용 + "지금까지 N명 플레이"(`engagementCount`, base 180) + 다시하기 버튼, 결과 직전 `App.showLoader()` 3초 광고 프리로더 재사용. 밸런스게임 피드 상단에 "🎯 스페셜" 배너 리스트 신설(기존 A/B 6개는 "⚡ 스피드 선택" 라벨 아래 그대로 유지). 공유 이미지는 `scripts/generate-phase4-share-cards.js`에 balanceSpecials 루프를 추가해 유형별 8장(`balance-sp-{id}-{grade}.jpg`) 생성(전량 scale=1 안전영역 통과), `functions/share/[section].js` balance 분기에 `grade` 파라미터가 있으면 유형별 이미지를 쓰도록 확장(기존 A/B 게임 공유는 무변화). **검증**: `node --check` 4개 파일 통과, Playwright로 피드(스페셜 2+스피드 6 노출)→인트로→8/9문항 진행바→로더→결과(극단 점수로 모아이/고양이, 중간 점수로 수달 정확 매핑)→카카오 버튼 onclick의 유형별 이미지 경로→다시하기→기존 A/B 게임·심리테스트존 회귀까지 콘솔 에러 0건. **다음 확장**: 반응 보고 3번째 게임 추가 여부 결정, 이후 가족오락관으로 이동하기로 전날 합의 |
| 가족오락관 1차 오픈 — 스피드 퀴즈/몸으로 말해요 (진행 도우미 엔진) → 라이어 게임 추가 | ✅ 완료 (3/10) | v0.4.2 → v0.4.3 | **v0.4.3 라이어 게임**: "폰 돌려보기" 방식 오프라인 진행 도우미 — 설정(인원 3~8명 + 카테고리, 카테고리는 라이어에게도 공개됨을 명시) → 한 명씩 "📵 N번 플레이어 차례" 화면에서 혼자 확인(무작위 1명만 "🤥 당신이 라이어!"+카테고리만 안내, 나머지는 동일 제시어) → 토론 화면(진행 순서 3단계 안내 + 선택식 1/2/3분 토론 타이머, 마지막 5초 틱 사운드) → "라이어 공개"(N번 플레이어+제시어 공개+역전승 룰 판정 가이드) → 같은 설정 한 판 더/설정 바꾸기. 제시어 풀은 스피드 퀴즈 카테고리 5개(128개 단어)를 그대로 재사용(`reuseCategoriesFrom` 필드, 신규 데이터 없음), `familyOpenGame()`이 liar일 때만 별도 플로우(`liarRenderSetup` 계열 함수)로 분기. 공유는 기존 family 분기에 game=liar만 추가(공용 이미지 `family-liar.jpg` 생성). 토론 타이머도 session 토큰+currentSection 체크로 섹션 이탈 시 자동 정지. **검증**: Playwright로 3명 설정 전체 플로우(라이어 정확히 1명 무작위 배정, 시민 2명은 동일 제시어, 타이머 1분 카운트다운, 결과 공개·역전승 가이드·카카오 이미지 경로, 한 판 더 시 1번부터 재시작, 스피드퀴즈 회귀) 콘솔 에러 0건 + wrangler curl로 `/share/family?game=liar` og 태그 확인 | 이하 v0.4.2 상세 → v0.2.3부터 "전체 준비중"(게임 10개 리스트업만)이던 가족오락관에 실제 게임 2개를 오픈. 리스트업된 10개는 전부 오프라인 파티게임이라 사이트의 역할을 "게임 자체"가 아닌 **진행 도우미**(폰 하나로 출제·타이머·채점 대행)로 정의. AskUserQuestion으로 1차 범위 확인 — 사용자가 "스피드 퀴즈/몸으로 말해요 먼저, 라이어게임은 준비중으로"를 선택(같은 엔진 2개 게임, 라이어게임은 다음 작업 후보). **데이터**: `data.js`에 `AppData.familyGames` 신설 — 게임별 5개 카테고리(스피드퀴즈: 음식/동물/사물·생활/직업/장소·여행, 몸으로말해요: 동물/스포츠·운동/일상 행동/직업/악기·놀이), 제시어 총 252개(온 가족이 아는 보편 단어만, 시사·특정인 배제, 몸으로말해요는 동작 표현 가능한 단어 위주로 별도 선정). **엔진**(`app.js`): `familyOpenGame`(설정: "어떻게 하나요" 3단계 안내+카테고리 칩+제한시간 60/90/120초) → `familyStart`(3→2→1 카운트다운, "출제자님/표현할 사람 폰 잡으세요") → 플레이(제시어 대형 카드+타이머 바+⭕정답/⏭️패스 버튼, 정답 시 pop·사운드, 마지막 5초 틱 사운드, 제시어 풀 소진 시 재셔플로 무한 공급) → 결과(점수+유머 코멘트+제시어 리캡 칩(정답=초록/패스=취소선)+같은 설정 한판 더/설정 바꾸기). 타이머는 session 토큰+`currentSection` 체크로 섹션 이탈 시 자동 정지(lottodraw 물리 루프 패턴 재사용). 점수는 팀 기록이라 `saveRanking` 미사용(개인 랭킹 오염 방지), 참여수는 `engagementCount('family-{id}-plays', 95)`. **공유**: `renderIdentityShareRow('family', {game,score,cat,time,...})` 재사용, `functions/share/[section].js`에 family 분기 신설(게임별 공용 이미지 1장, 기록 숫자는 카카오 카드 텍스트가 담당 — 잘못된 game 파라미터는 speedquiz로 안전 폴백), `generate-phase4-share-cards.js`에 FAMILY_THEME(웜 오렌지) 루프 추가로 `family-speedquiz.jpg`/`family-charades.jpg` 2장 생성. **사이드바 숨김 해제**: v0.2.8 애드센스 심사용 임시 숨김 3개 중 family를 실콘텐츠 완성으로 해제(worldcup과 동일 논리, 준비중 배지도 제거) — 이제 남은 숨김은 fortuneext(사주·타로·궁합) 1개뿐. **검증**: `node --check` 4개 파일, Playwright로 사이드바 노출→피드(오픈 2+잠금 8)→설정(카테고리/시간 선택 반영)→카운트다운→플레이(단어 전환·점수 증가·타이머 감소)→타임업→결과(점수/리캡/카카오 이미지 경로/한판 더)→섹션 이탈 시 타이머 무해 정지→몸으로말해요 안내문까지 콘솔 에러 0건, wrangler+curl로 `/share/family` og 태그 3종·charades·잘못된 파라미터 폴백 확인 |
| 2026-07-05 | v0.2.9 | **버그 수정 — 심리테스트존 사이드바 하위메뉴 활성 표시 오류.** 상세는 위 "Phase 4 수익화 확장 로드맵" 행의 v0.2.9 항목 참고 |
| ㄴ Stage A: 메뉴 구조 개편 (사이드바 "테스트" 그룹화) | ✅ 완료 | v0.0.11 | MBTI/두뇌나이/ADHD를 접이식 그룹으로, 꿈해몽/운세/로또는 최상위 유지 |
| ㄴ Stage B: 신규 인지테스트 10종 | ✅ 완료 (10/10) | - | 순서 확정(PRD.md 9-1 참고): 반응속도→숫자기억→순서기억→색각→논리력→충동억제→숏폼집중력→인싸력→속담완성→그시절물가 |
| ㄴㄴ 반응속도 테스트 | ✅ 완료 | v0.0.12 | 쉬움(5회)/보통(7회,반칙페널티)/어려움(10회,가짜신호 30%). 티어 S~D + 유머 코멘트, 공유 |
| ㄴㄴ 숫자 기억력 테스트 | ✅ 완료 → 첫 문제 카운트다운 추가 | v0.0.13 → v0.1.7 | 적응형 자릿수(맞히면 +1/틀리면 -1). 쉬움 3자리~/보통 4자리~/어려움 5자리~+3초 방해단계. 탭 키패드 UI. v0.1.7: 첫 라운드 시작 시 숫자가 너무 빨리 나온다는 피드백 → 첫 라운드에서만 "3→2→1→시작!"(0.5초 간격) 카운트다운 + 0.3초 여유 후 첫 숫자 노출(2라운드부터는 매번 카운트다운하면 지루해서 제외) |
| ㄴㄴ 순서 기억력 테스트 | ✅ 완료 → 첫 문제 카운트다운 추가 | v0.0.14 → v0.1.7 | 격자 타일 순서 암기(3x3, 어려움은 4x4). 탭마다 즉시 정오답 판정(Simon-says류 UX). 적응형 칸수. v0.1.7: 숫자 기억력과 동일하게 첫 라운드에만 카운트다운 추가(피드백 텍스트 영역 재사용) |
| ㄴㄴ 색각 테스트 | ✅ 완료 | v0.0.15 | "미묘하게 다른 색 타일 찾기" 게임(임상 색맹검사 아님, 안내문구 명시). 격자 3x3~5x5+시간제한, 정확도·반응속도 기반 티어 |
| ㄴㄴ 논리력 테스트 | ✅ 완료 | v0.0.16 | 숫자 규칙(등차/등비/피보나치식) 다음 값 4지선다. 시간제한+정확도 기반 티어(색각 테스트와 동일 공식) |
| ㄴㄴ 충동억제 테스트 | ✅ 완료 | v0.0.17 | Go/No-Go 방식. 초록=빠르게 탭, 빨강=참기. 성급한 반응(commission error) 추적, 정확도 기반 티어 |
| ㄴㄴ 숏폼 집중력 테스트 | ✅ 완료 → 메커니즘 전면 재설계 | v0.0.18 → v0.1.7 | v0.1.7 이전까지는 충동억제(Go/No-Go)와 완전히 동일한 단일 원 자극 엔진을 리스킨만 한 상태(🔥꿀잼=탭/📢광고=참기)라 "두 테스트가 사실상 똑같다"는 사용자 피드백을 받음. **v0.1.7 재설계**: 착수 전 WebSearch로 실제 근거 조사 — "숏폼 노출 후 평균 집중 지속시간 47초"·"팝콘브레인" 통계(사이언스타임즈)와, 실제 임상 주의력 검사 CCPT(좌우에 무작위로 나타나는 버섯=타겟/꽃=방해자극 중 타겟만 탭하는 방식)를 참고해 리서치 결과와 샘플 라운드를 먼저 사용자에게 제시하고 승인받은 후 구현. 단일 원 Go/No-Go 대신 **여러 콘텐츠 카드가 격자로 동시에 뜨고, 라운드마다 미리 제시되는 목표 카테고리(예: "🐶 강아지 콘텐츠 찾기")와 일치하는 카드만 빠르게 찾아 탭**하는 시각 탐색+선택적 주의 방식으로 전환(`SHORTFOCUS_CATEGORIES` 15개 카테고리 신설). 목표가 이번 라운드에 아예 없는 "target-absent" 트라이얼(난이도별 25~45%)도 포함해 CCPT의 타겟/논타겟 구조 그대로 재현. 난이도가 격자 크기(2x2~3x3)뿐 아니라 "가짜 광고" 트랩 카드(타겟과 같은 이모지지만 우측 상단에 작은 "AD" 배지, 탭하면 오답)의 등장 비율로도 차등화(쉬움 0%→HELL 45%) — 어려움/HELL엔 실제 숏폼의 스폰서 콘텐츠 낚시를 재현. **신규 지표**: 결과 화면에 전반부/후반부 평균 반응속도를 비교하는 "집중력 저하도"(예: "후반부에 반응이 23% 느려졌어요 — 팝콘브레인 주의! 🍿")를 추가해 충동억제엔 없는 vigilance decrement 개념 반영. 정확도 기반 티어 공식과 등급 문구 톤은 유지하되 "가짜 광고에 낚임/다른 콘텐츠 탭" 등 새 오답 케이스에 맞게 문구 세분화. **검증**: `node --check` 통과, Playwright로 easy(2x2)/hard(2x4, AD 트랩)/HELL(3x3) 격자 크기와 카드 수 확인, 상태를 직접 조작해 정답/오답 3가지 분기(다른 콘텐츠 탭·광고 트랩 탭·타겟부재 라운드에 탭) 전부 정상 동작 확인, 8라운드 전량 정답 처리 시 정확도 100%+집중력 저하도 문구까지 콘솔 에러 없이 통과 |
| ㄴㄴ 인싸력 테스트 | ✅ 완료 | v0.0.19 | 10문항 사교성 성향 퀴즈(MZ향). MBTI/ADHD와 동일한 "문항→점수 누적→등급" 패턴 재사용(Go/No-Go 계열 아님). 점수 0~30점, D~S 4등급, 인싸/아싸 모두 긍정적으로 프레이밍(우열 없음 명시) |
| ㄴㄴ 속담 완성 퀴즈 | ✅ 완료 → 풀 100개로 확장 | v0.0.20 → v0.0.38 | 어르신향 지혜 테스트. 4지선다(정답+decoy 3개, shuffleArray 재사용), 시간 제한 없음. 정답 개수 기반 채점(D~S 4등급), 결과 화면에 "정답 확인" 리캡 추가해 가족 대화소재로 활용 가능하게 구성. 오답도 긍정적으로 프레이밍(정답 시험이 아닌 지혜 나눔 소재로 안내). v0.0.38: 콘텐츠 갭 분석 후속 ③번 — 풀을 10→100개로 확장하고, 한 판(10문항)은 유지하되 `proverbStart()`가 매번 풀에서 랜덤 10개를 뽑아 쓰도록 변경해 재도전할 때마다 다른 조합이 나오게 함 |
| ㄴㄴ 그 시절 물가 맞히기 | ✅ 완료 → 풀 50개로 확장 | v0.0.21 → v0.0.38 | 어르신향 향수 트리비아, Stage B 신규 인지테스트 10종 마지막 항목. 속담완성 구조 재사용(4지선다, shuffleArray, 시간 제한 없음, 정답 개수 기반 D~S 등급, "정답 확인" 리캡). 문항은 실제 물가 통계 웹 검색으로 확보, 복수 기사 교차 확인 후 확정. 리캡에 문항별 트리비아(note) 문구 추가해 가족 대화소재 강화. v0.0.38: 콘텐츠 갭 분석 후속 ③번 — 풀을 10→50개로 확장(짜장면/버스/라면/소주/담배/영화관람료/택시/지하철 기존 카테고리 연도 추가 + 커피자판기/새우깡/초코파이/롯데리아/우표 5개 신규 카테고리, 1971~2023년), 전부 웹 검색으로 재검증한 실제 통계만 사용. `pricequizStart()`가 매번 풀에서 랜덤 10개를 뽑아 쓰도록 변경 |
| 오늘의 한마디 → 유명인 명언+속담 365개 교체 | ✅ 완료 | v0.0.22~v0.0.27 | Stage B 완료 후 착수. `{text,author,role}` 구조, 24시간(KST, 하루) 단위 순환(초기 12시간→v0.0.26에서 하루 단위로 조정). 목표를 700→365개(하루 1개×1년)로 현실적으로 하향 조정(v0.0.26, 사용자 결정). 20개(v0.0.22)→56개(v0.0.23)→116개(v0.0.24)→145개(v0.0.25)→166개(인물, v0.0.26)→365개(속담·격언으로 보충 완성, v0.0.27). 인물 어록(철학자/사상가/작가/과학자/투자자 등, 1인당 최대 2개) 약 166개 + 국내외 속담·격언(한국/중국/일본/영미/탈무드/아프리카/인도/아랍/독일/프랑스/스페인/러시아/아일랜드/북유럽/몽골/베트남/라틴/그리스) 약 199개로 구성. 섹션명 "오늘의 폼 미친 한마디"→"오늘의 인생 한마디"로 변경. 출처 불확실한 오귀속 명언은 확인 후 배제 |
| ㄴ Stage C: 난이도/모드 체계 개편 | ✅ 완료 | v0.0.28 | PRD.md 9-2 기준. 상세는 아래 3개 항목 참고 |
| ㄴㄴ 두뇌 나이(스트룹) 2단계→3단계 | ✅ 완료 | v0.0.28 | 기존엔 "쉬움/어려움" 버튼이 있어도 색상 개수(4/5색)만 다르고 문항수·시간은 고정이었던 걸 개선. 쉬움(4색·15문항·5.0초)/보통(5색·20문항·4.0초, 기존 "어려움" 자리를 대체)/어려움(6색·주황 추가·25문항·3.0초, 신규 강화). 채점 공식은 기존 그대로(정확도-평균반응시간/100) |
| ㄴㄴ MBTI 간단/정밀 2모드 | ✅ 완료 | v0.0.28 | 간단(기존 유지, 12문항)/정밀(신규, 24문항=축당 6개, 기존 12문항+신규 `mbtiQuestionsExtra` 12문항 합산). 정밀 모드 결과 화면에 축별 비율 바(E73%/I27% 등) 추가 — 답변 개수만으로 계산해 16개 타입 결과 데이터는 그대로 재사용(강점/도전과제/팁/유명인 신규 작성 없음) |
| ㄴㄴ ADHD 간단/정밀 2모드 | ✅ 완료 | v0.0.28 | 간단(기존 유지, 10문항 단일 총점)/정밀(신규, `adhdQuestionsPrecise` 20문항을 부주의10+과잉행동-충동성10 두 영역으로 분리, DSM-5 구조 참고). 응답 척도도 정밀 모드에선 진짜 4단계(전혀0/가끔1/자주2/항상3)로 세분화(간단 모드는 기존 척도 유지). 정밀 모드 결과의 종합 등급은 합산점수(최대60점)를 3으로 나눠 기존 20점 만점 등급표에 그대로 대입하는 방식으로 새 등급 데이터 작성 없이 재사용. 임상 진단 도구 아님 안내문구 계속 유지 |
| ㄴ Stage F: 종합 인지 프로필(레이더 차트) | ✅ 완료 | v0.0.29 | Stage D(백엔드)·E(동물비유카드)는 아직 "실제 운영 단계" 전이라 계속 보류, 백엔드 불필요한 F를 먼저 진행. 두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제 7개 인지테스트의 최근 기록에서 Tier(S~D) 문자만 파싱해 100/80/60/40/20점으로 환산(신규 데이터·채점 로직 변경 없이 구현). 외부 차트 라이브러리 없이 순수 SVG로 7각형 레이더 차트 직접 렌더링. 7개 중 5개 이상 완료해야 노출(미만이면 진행률 티저 카드), 홈 화면 "테스트 완주 현황"과 "최근 테스트 기록" 카드 사이에 배치. 숏폼집중력은 충동억제와 동일 엔진이라 축에서 제외(중복 측정 방지) |
| ㄴ Stage E: 동물 비유 결과 카드 | ✅ 완료 | v0.0.48 | PRD상 선행조건(D의 퍼센타일 집계)은 v0.0.42에서 이미 끝나있었으나 카카오공유·로또통계·퍼센타일UI가 먼저 진행되며 순서가 밀려있었음. 사용자가 "①퍼센타일UI → ②Stage E → ③MBTI텍스트검수" 순서를 확정하며 착수. 착수 전 AskUserQuestion으로 적용 범위(Tier 8개 테스트만, 추천안 채택 — 성향형 테스트 확장이나 전체 15개 확장은 매핑 규칙이 더 필요해 제외)와 조합 규모(100개, PRD 최소치)를 확인. `data.js`에 `AppData.animalCards` 신설 — 테스트별로 2~3개의 고정 "동물 정체성"(예: 두뇌나이=부엉이/코끼리/돌고래, 반응속도=치타/매/캥거루, 색각=사마귀새우/카멜레온 등, 테스트 성격에 맞는 실제 동물 특성에서 착안)을 두고 등급(S~D)별로 서식지·수식어·행동팁 톤만 달라지는 구조로 100개 조합 작성(brain/reaction/memdigit/seqmem 4개는 등급당 3개=60개, colorvision/logic/impulse/shortfocus 4개는 등급당 2개=40개). `app.js`에 `pickAnimalCard(section, tier)`(등급별 풀에서 `pickOne()`으로 랜덤 선택)와 `animalCardHTML(card)`(카드 UI 렌더링) 신규 추가, Tier 8개 테스트 결과 화면의 Tier 배지 바로 아래에 카드 삽입 + 기존 `shareText`에 "🐾 나는 {title}!" 문구를 붙여 Web Share/카카오톡 공유 시 함께 전송(신규 공유 버튼 없이 기존 인프라 재사용, 카카오 공유는 이미 `location.href`를 포함하고 있어 별도 유입 링크 로직 불필요). **검증**: Node로 8개 섹션×5등급 구조·필드 완결성과 정확히 100개임을 기계 검증, Playwright로 두뇌나이(3개 슬롯 테스트)와 논리력(2개 슬롯 테스트) 둘 다 실제 완주시켜 카드가 정상 렌더링되고 콘솔 에러 없음을 확인 |
| 사이트 브랜드명 변경 (제목/부제목) → 실제 로고 이미지 삽입 | ✅ 완료 | v0.0.30 → v0.3.4 | "5-in-1 대시보드/테스트 대시보드" → "과몰입 연구소" / "재미로 시작했다가 뼈 맞고 공유하는 종합 테스트 모음"으로 변경. `<title>`·사이드바·모바일 헤더·푸터·meta description 전부 반영. D(백엔드) 착수 전 사용자가 먼저 요청한 프론트엔드 개선 항목. **v0.3.4**: 사용자가 디자이너에게 의뢰한 실제 로고(청록 헥사곤 뇌 실루엣 + 주황 번개, 하단에 "관몰입 연구소" 워드마크 포함 PNG)를 전달, 채팅에 붙여넣은 이미지는 파일로 접근이 안 돼 처리 불가함을 안내 → 사용자가 VS Code 탐색기에 드래그로 `assets/brand/`에 실제 파일(`logo-source.png`, 2180×1920)을 올려줘서 진행. Python(Pillow+numpy)으로 ①흰 배경 기준 알파값 계산 후 un-premultiply로 흰 테두리(fringe) 없이 투명화 ②텍스트 워드마크 영역(하단, 안전하게 gap row 검출로 경계 특정)은 제외하고 아이콘(헥사곤+번개)만 크롭 ③정사각 캔버스로 패딩 후 512px(`logo-icon.png`)·180px(`apple-touch-icon.png`)·32px(`favicon-32.png`) 3종 생성. 기존에 이모지(🎯)로 임시 대체돼있던 사이드바 로고·모바일 헤더·푸터 3곳의 아이콘을 전부 `logo-icon.png`로 교체(워드마크 텍스트 "과몰입 연구소"는 이미지가 아닌 기존처럼 실제 텍스트로 유지), `<head>`에 파비콘·apple-touch-icon 링크 신규 추가(그 전까지 파비콘 자체가 없었음). **검증**: Playwright로 다크/라이트 모드 사이드바·모바일 헤더·푸터 스크린샷 촬영해 흰 테두리 없이 다크 배경에도 깔끔하게 합성되는지 확인(축소된 크롭으로 직접 픽셀 확대 검증 포함) |
| 제휴문의 폼 (푸터, Slack 웹훅 연동) → 실 배포 검증 완료 + 의견보내기 폼 추가 | ✅ 완료 | v0.3.4 → v0.3.5 | 사용자가 참고 스크린샷 2장(네이버/줌 뉴스제휴 문의 폼 스타일)을 제시하며 "중요한 건 아니다"고 해서 최소 필드(이름 선택/이메일·내용 필수)로 심플하게 구현. 알림 방식은 AskUserQuestion으로 확인해 **Slack 인커밍 웹훅**으로 결정. 푸터의 문의 이메일 왼쪽에 "제휴문의" 링크 신설 → 클릭 시 꿈해몽 모달과 동일한 오버레이 패턴(`#partnership-modal`)의 폼 모달 오픈. 신규 `functions/api/partnership-inquiry.js`(Cloudflare Pages Function)가 `SLACK_WEBHOOK_URL`(Cloudflare Pages 환경변수, 코드에는 안 둠)로 메시지 전달, 이메일 형식 검증 + 허니팟 히든필드(`website`)로 기초 스팸 방지. **Slack 앱 생성 과정에서 발견한 이슈**: ①Incoming Webhooks 페이지에서 스코프만 켜고 "Add New Webhook to Workspace"를 누르면 "설치할 봇 사용자가 없습니다" 에러가 나는 Slack 플랫폼 자체의 알려진 버그 발생(스코프 수동 추가로도 미해결) → **"From an app manifest"로 앱을 처음부터 재생성**(`features.bot_user`를 매니페스트에 명시)하는 방식으로 우회 ②매니페스트의 한글 `display_name`(예: "과몰입연구소 알림봇")이 Slack 매니페스트 편집기의 검증 버그로 "We can't translate a manifest with errors" 에러를 유발 → 영문 이름(`gwamollab-notifier`)으로 대체해 해결(봇 표시 이름은 실사용에 영향 없음). ③사용자가 "제휴문의랑 의견보내기 둘 다 채널 하나로 쓰자"고 판단 — 웹훅 1개(`#과몰입연구소-제휴문의` 채널)만 만들고 두 폼이 공유, 메시지 접두어(📮 제휴문의 / 💬 의견보내기)로만 구분. 사용자가 Cloudflare에 `SLACK_WEBHOOK_URL` Secret 등록 후 실제 배포 사이트(`testseries1.pages.dev`)에 curl로 테스트 요청 → Slack 채널에 정상 수신 확인 완료. **v0.3.5 — 의견보내기 폼 추가**: 제휴문의와 동일한 패턴(`#feedback-modal`, `functions/api/feedback.js`)으로 신규 구현, 같은 `SLACK_WEBHOOK_URL` 재사용(신규 웹훅 불필요). 이름/이메일 둘 다 선택 입력(제휴문의와 달리 이메일도 필수 아님 — 답장 원할 때만 남기게), 의견 내용만 필수. **사진 첨부는 아직 미구현** — Slack 인커밍 웹훅은 텍스트/링크만 가능해 바이너리 이미지를 받으려면 Cloudflare R2(오브젝트 스토리지) 버킷이 필요, 사용자 확인 후 추가 예정(다음 세션 또는 후속 작업으로 보류). **검증**: `node --check` 통과, Playwright로 두 모달 오픈/빈값 검증 확인, 실 배포 사이트에 curl로 제휴문의 실전송 테스트(`{"ok":true}` 200 응답 + Slack 채널 수신) 완료 |
| 홈 "서비스 바로가기" 그리드 개편 | ✅ 완료 | v0.0.30 | 기존엔 5개 카드(MBTI/꿈해몽/운세/두뇌나이/ADHD)만 있어 Stage B 신규테스트 10종+로또가 누락돼 있던 걸 발견해 전체 15개로 확장. "🎯 바로 써먹는 도구"(꿈해몽/운세/로또 3개)와 "🧪 테스트 모음"(MBTI/두뇌나이/ADHD+신규 10종, 13개) 두 그리드로 분리 |
| 홈 "테스트 모음" → "🔥 지금 인기있는 테스트"(사이트 전체 인기 TOP 6)로 개편 | ✅ 완료 | v0.3.2 | 사용자가 "13개 카드를 다 꺼내놓을 필요는 없다"고 지적, 대안으로 "많이 찾는 테스트를 놓든지, 아니면 없애버리든지"를 직접 제시 — AskUserQuestion으로 3안(완전삭제/인기TOP로 축소/에디토리얼 큐레이션)을 확인받아 **인기 TOP 6 축소(추천안)**로 확정. 착수 중 사용자가 "그건 전체메뉴 다 포함인거 알지? 로또추첨기가 많다면 그걸 노출하는거야"라고 추가 지적 — 처음엔 기존 13개 테스트만 후보 풀로 잡았던 걸, 사이드바에 있는 **사이트 전체 콘텐츠**(심리테스트존 개별 테스트 6개·밸런스게임 개별 6개·로또 직접 뽑기 게임까지 포함)가 전부 경쟁하도록 후보 풀을 확장. **실제 인기 데이터의 한계**: 전체 유저의 `test_results`는 RLS로 본인만 조회 가능해 클라이언트가 전 사이트 실사용 순위를 직접 집계할 수 없음(진짜 순위를 내려면 `percentile-refresh.js`처럼 서버 배치 집계 인프라가 새로 필요, 이번 범위 밖) — 대신 심리테스트존/밸런스게임에서 이미 쓰던 "로컬 dummy base + 이 기기 클릭 시 로컬스토리지 증분"(`engagementCount`/`bumpEngagement`) 컨벤션을 그대로 재사용, 화면에 "인기 순위는 추후 실데이터 연동 예정" 캡션을 항상 노출해 정직성 유지(다른 더미 위젯들과 동일 원칙). **구현**: 13개 핵심 테스트 각각의 실제 "시작" 함수(`mbtiStart`/`brainSelectDifficulty`/`adhdStart`/`reactionStart`/`memdigitStart`/`seqmemStart`/`colorvisionStart`/`logicStart`/`impulseStart`/`shortfocusStart`/`insaStart`/`proverbStart`/`pricequizStart`, 닉네임 검증 통과 직후)에 `bumpEngagement('site-{section}-plays')`를 신규 삽입해 어느 진입경로(홈/사이드바/딥링크)로 들어와도 실제 플레이가 집계되게 함(기존엔 홈 카드 클릭 시에만 카운트하려 했다가, "홈에서만 세면 안 된다"는 지적을 반영해 각 테스트의 진짜 시작 시점으로 옮김). `dreamShowModal`/`fortuneSubmit`/로또 4개 실행 함수(`lottoRunRandom`/`lottoRunCustom`/`lottoRunFortunePick`/`lottoRunStats`)/`initLottodraw`에도 동일 패턴으로 `site-{section}-plays` 신규 추가(단, 꿈해몽/오늘의운세/로또 조합기 3개는 위 "도구" 그리드에 항상 고정 노출되고 있어 인기 후보 풀에서는 제외 — 카운트는 하되 홈에 중복 카드로 노출되지 않게 함). 심리테스트존 6개는 기존 `psychtestStart`가 이미 찍고 있던 `psychtest-{id}-plays`(base 128), 밸런스게임 6개는 기존 `balancePick`이 찍던 `balance-{id}-plays`(base 203)를 그대로 재사용해 홈과 각 피드 화면에 동일한 숫자가 보이도록 일관성 유지. 후보들을 점수(=`engagementCount`) 내림차순 정렬해 상위 6개만 `home-service-grid`에 렌더링, 카드 클릭 시 단순 섹션 이동이 아니라 항목별 `run` 클로저(심리테스트존은 `psychtestNavCategory()`→`App.navigate('psychtest')`→`psychtestOpenPost()` 순서, 밸런스게임은 `App.navigate('balance')`→`balanceOpenPost()`)로 정확한 하위 화면까지 바로 진입시킴. 초기 base 값은 실사용 데이터가 없어 mbti(980)~물가퀴즈(150)까지 임의 추정치로 매겨 기존 상위 6개(MBTI/두뇌나이/ADHD/반응속도/인싸력/숏폼집중력)가 그대로 유지되도록 설계, 신규 콘텐츠(로또추첨기 220/심리테스트존 128/밸런스게임 203)는 낮게 잡아 실사용이 쌓여야만 순위에 올라오게 함. **검증**: `node --check` 통과, Playwright로 (a) 초기 상태에서 기존 상위 6개가 그대로 나오는지 (b) `site-lottodraw-plays`를 인위적으로 대량 증분시키면 실제로 "로또 직접 뽑기 게임"이 1위로 올라오고 클릭 시 해당 섹션으로 정상 이동하는지 (c) `psychtest-mintchoco-plays`를 증분시키면 "민초 vs 반민초 테스트"가 상위에 노출되고 클릭 시 심리테스트존의 정확한 포스트 화면까지 진입하는지 전부 확인, 콘솔 에러 없음 |
| 닉네임 전역 유지 | ✅ 완료 | v0.0.30 | "매번 닉네임을 다시 입력하는 게 귀찮다"는 요청 — DB/백엔드 불필요, 순수 로컬스토리지(`app_nickname`, 기존 홈 화면용 키 재사용)로 해결. 닉네임을 쓰는 13개 테스트 전부의 시작 화면 입력창에 `getNickname()`으로 마지막 저장값을 미리 채워두고, 테스트 시작 시 `setNickname()`으로 저장 — 한 곳에서 바꾸면 다른 모든 테스트에 자동 반영, 안 바꾸면 계속 유지 |
| 다크모드 기본값 재확인 | ✅ 확인 완료(변경 없음) | v0.0.30 | "접속하면 무조건 다크모드로 시작하게" 요청 → 코드 확인 결과 로컬스토리지에 테마 기록이 없는 최초 방문은 이미 항상 다크로 시작하고 있었음(OS `prefers-color-scheme` 감지 코드 자체가 없음). 사용자에게 현재 동작(신규 방문=항상 다크, 라이트로 바꾼 뒤 재방문=저장된 라이트 유지) 확인받고 코드 변경 없이 종료 |
| 친구 대결 모드 | ✅ 완료 | v0.0.31 | D(백엔드) 무관 재미요소 백로그 중 2순위. Tier(S~D) 채점을 쓰는 8개 테스트(두뇌나이/반응속도/숫자기억/순서기억/색각/논리력/충동억제/숏폼집중력) 대상. 결과 화면 "친구에게 도전장 보내기" 버튼 → 닉네임+결과문자열을 URL(`#{section}?vs=<JSON>`)에 담아 공유 → 상대가 링크로 들어와 배너 확인 후 같은 테스트를 마치면 Tier 점수(S=100~D=20, 레이더차트와 동일 기준)로 승/패/무 자동 판정. 백엔드·계정 없이 URL만으로 구현, 해시 라우팅에 쿼리스트링 파싱을 추가(`App.pendingChallenge`) |
| 오늘의 챌린지 | ✅ 완료 | v0.0.31 | D 무관 재미요소 백로그 2순위. 오늘의 한마디와 동일한 `todaySeed()` 기반 날짜 시드로 13개 테스트(MBTI/두뇌나이/ADHD+신규10종) 중 매일 다른 3개를 홈 화면에 노출, 재방문 유도. 완료한 항목은 체크 표시 |
| MBTI famous 필드 검수 | ✅ 완료 | v0.0.32 | D 무관 백로그 3순위(콘텐츠 보강). 스티브 잡스가 ENTP/ENTJ 두 유형에 중복 기재돼 있던 버그 수정(ENTP→토마스 에디슨, ENTJ→나폴레옹 보나파르트), ESTP의 도널드 트럼프(정치적으로 민감한 현직 인물)를 마돈나로 교체. 나머지 텍스트(desc/strength/challenge/tip)는 품질 균일해 그대로 유지 |
| 꿈해몽 데이터 59개→74개 확장 + variants 확대 | ✅ 완료 → 전체 74개 테마 variants 보유로 완료 | v0.0.32 → v0.0.39 | D 무관 백로그 3순위. 로또당첨/거울/무지개/나비/추락/곰/거북이/좀비/태풍/화산/시체/연예인/폭포/코끼리/UFO 15개 신규 테마 추가(59→74개). 기존 8개 테마(학교/전애인/짝사랑/키스/싸움/알몸/이사/지갑분실)에 variants 2개씩 신규 추가(23→46개 테마가 variants 보유). 꿈해몽 전용 무료 오픈API는 존재하지 않음을 웹 검색으로 확인 후, 명언·속담·물가 트리비아 때와 동일하게 웹 검색 기반 수작업 조사·집필 방식으로 진행. v0.0.32에서는 나머지 28개 테마 variants 확장을 "정식 배포 전 콘텐츠 보강 단계"로 순위를 뒤로 미뤘으나(사용자 결정), v0.0.39에서 그 28개 전부에 variants 2개씩 추가해 **74개 테마 전체가 variants를 보유하도록 완료**(총 variant 146개) |
| 꿈해몽 외부 API 연동 — 74개→174개 데이터 확장 + 로또 연동 버튼 + AI 폴백 (PRD 10-4) | ✅ 완료(3단계 전부, 라이브 검증까지) → 공유 기능 확장 → 공유 카드 이미지 통일 | v0.1.1 → v0.1.9 | **v0.1.9**: 사용자가 "무슨 꿈을 공유하든 항상 '하늘을 나는 꿈' 이미지로 나온다"고 지적 — 원인은 AI 해몽(테마 인덱스가 없음) 공유 시 폴백으로 쓰던 `dream-0.jpg`가 하필 인덱스 0번 테마("하늘을 나는 꿈")였던 것. 꿈 내용은 검색어마다 계속 달라져 매번 전용 이미지를 만들 수 없다는 한계를 인정하고, 정적 테마 해몽까지 포함해 **모든 꿈 공유를 범용 티저 카드 1장으로 통일**하는 방향으로 재설계(사용자 지시) — 신규 `share-cards/dream-share.jpg`(달+말풍선 이모지, 기존 `DREAM_THEME` 색상 재사용, `scripts/generate-dream-generic-card.js`로 안전영역 검증까지 거쳐 생성)와 고정 카피(제목 "나 이런 꿈 꿨어" / 설명 "너도 꿈 꾼거 있으면 찾아볼래?")로 `dreamRenderModal()`/`dreamRenderAiModal()`의 카카오 카드 파라미터와 `functions/share/[section].js`의 dream og:image/title/description 분기를 교체(테마별 174장 이미지는 이제 미사용이나 삭제하지 않고 보존). 공유 링크 클릭 후의 `#shared-preview` 화면은 기존처럼 `extra` 파라미터로 실제 꿈 제목/본문/행운색/행운숫자/행동을 그대로 재현해 변경 없음 — "친구가 궁금해서 눌러보게 하는 미끼 카드"와 "클릭 후 진짜 결과 확인"의 역할을 명확히 분리. **검증**: `node --check` 통과, `npx wrangler pages dev`로 정적 테마/AI 두 경로 모두 og:image가 `dream-share.jpg`, og:title/description이 고정 카피로 나오는지 curl로 확인, Playwright로 `#shared-preview` 진입 시 실제 꿈 상세(제목/요약/본문/행운의 색·숫자/오늘의 행동)가 여전히 정확히 렌더링됨을 스크린샷으로 재확인. 이하 v0.1.6 상세 → | **v0.1.6**: 사용자가 스크린샷(AI 해몽 모달)을 첨부하며 ①AI 해몽 결과에도 카카오톡 공유 버튼이 없다 ②꿈해몽 공유 링크를 열면 다른 테스트처럼 "공유자가 실제로 본 결과"를 그대로 봐야 하는데 그렇지 않다 2가지를 지적. **①AI 해몽 공유 버튼**: `dreamRenderAiModal()`에 `renderIdentityShareRow('dream', {dreamAi:1, ...})` 추가 — 정적 해몽과 동일한 카카오/FB/X/밴드/링크복사 행 노출, 이미지는 테마 인덱스가 없어 범용 `dream-0.jpg` 재사용. AI 응답(OpenAI, 사용자 검색어 기반 프롬프트 인젝션 가능)을 카카오 공유 버튼 onclick의 백틱 템플릿 리터럴에 그대로 넣으면 `escapeHtml()`(HTML 이스케이프)과는 별개로 백틱/`${...}`로 JS 템플릿을 탈출할 수 있는 취약점을 발견해, 신규 `sanitizeForJsTemplate()`(백틱→작은따옴표, `${`→`$ {` 치환)로 kakaoTitle/kakaoDesc/shareText에만 추가 방어 적용. **②실제 결과 재현**: 기존엔 꿈 공유 링크를 열어도 제목+요약 한 줄과 범용 이미지만 보여주고 "나도 해보기"를 눌러야 검색 화면이 나왔음(로또의 "실제 뽑은 번호 표시"와 달리 진짜 해몽 내용은 안 보임) — `dreamRenderModal()`/`dreamRenderAiModal()`의 공유 landingParams에 제목뿐 아니라 본문(detail)·행운의 색·행운의 숫자·오늘의 행동까지 전부 담아 `functions/share/[section].js`가 이를 그대로 JSON `extra` 파라미터로 `#shared-preview`까지 전달하도록 변경, 신규 `sharedPreviewDreamCardHTML()`이 실제 꿈 모달과 동일한 레이아웃(제목/요약 인용구/본문/행운의 색·숫자 그리드/오늘의 행동 박스, AI생성이면 배지 표시)으로 재현하고 그 아래 기존과 동일한 "나도 해보기" 버튼 배치. **검증**: `node --check` 통과, wrangler+Playwright로 (a)정적 꿈(하늘 높이 솟아 날아오르는 꿈) 공유 URL 생성→실제 랜딩 접속→`shared-preview`에 본문/행운색/행운숫자/오늘의 행동까지 전부 정확히 렌더링 (b)AI 해몽 목업 데이터로 동일 플로우 재검증 (c)백틱+`${alert(2)}`가 섞인 악의적 제목/요약으로 공유 버튼을 실제 클릭해도 alert 미실행·콘솔 에러 없음(살균 정상 동작) 확인. **PRD.md 10-4 구도(①데이터 확장 ②로또 연동 ③AI 폴백)는 그대로 유지, 이번은 그 위에 공유 기능만 보강**. 이하 v0.1.1 상세 → | PRD.md 10-4 3단계 구도(①데이터 대량 확장 → ②로또 연동 버튼 → ③AI 폴백)를 전부 이번 세션에서 진행. 원래 "Fable(상위 모델) 담당"으로 정해져 있었으나 사용자가 "Fable까지는 필요없다"고 재판단해 Sonnet 세션에서 진행하기로 변경(PRD.md에 반영), AI 폴백에 쓸 API도 Claude 대신 **OpenAI**(이미 결제수단 등록됨)로 확정. **①데이터 확장**: 목표 규모를 AskUserQuestion으로 확인 — 사용자가 "최적으로 할 수 있는 만큼 먼저 진행(100~150개 신규), 이후 AI 폴백 비용을 보고 재판단"으로 확정. 군대/도둑/경찰/피/금/소/닭/쥐/여우/원숭이/부처님/태양/구름/배/열쇠/전쟁/발표/옛친구/가위눌림/별똥별/사자/독수리/까마귀/개구리/지렁이/무당굿/바람/빙판길/기차지하철/안경분실/문잠김/폭발/총/칼/도둑질(1인칭)/고향/외국/보물/손톱/선물/파티/안개/무인도/우주여행/로봇/낚시/시계/해일/미로/달걀/빵케이크/생일/포옹/분노/노래/춤/그림/책/은하수/학백조/비/우산/눈사람/키변화/초능력/시간여행/유체이탈/천사/악마/지옥/천국/화장/새옷/사진/기기고장/통신장애/은행/시장/이름변화/목소리변화/졸업/입학/이혼/장례식/제사/세배/떡/국수/고기/부자/유명해지다/시상식/벌쏘임/화상/감기/치통/성형수술/문신/흰머리/실명 등 신규 테마 100개를 5개 배치로 나눠 추가(74→174개), 각 테마당 4개 variants 신규 작성(신규 variant 400개, 총 variants 146→546개). 기존 74개 테마와 동일한 웹 지식 기반 수작업 집필 방식(전통 한국 꿈해몽 관례 + 상징 해석)으로 진행, 명언/속담/물가처럼 사실 검증이 필요한 콘텐츠가 아니라 상징 해석 콘텐츠라 매 항목 개별 웹 검색 없이 진행. 배치마다 Node 스크립트로 필드 완결성·제목 중복 없음을 기계 검증 후 커밋+푸시(배치1~5, 각 20개 테마=100개 데이터 단위). 검증 중 신규 "눈사람을 만드는 꿈"이 기존 "눈이 내리는 꿈" variant 제목과 정확히 겹치는 걸 발견해 "눈사람을 정성껏 완성하는 꿈"으로 수정. **②로또 연동 버튼**: `dreamRenderModal()`에 "🎰 이 행운숫자로 로또 조합하기" 버튼 신설, 기존 `last_dream_luckynum` 저장 인프라와 로또 조합기 "오늘의 운세·꿈 연동" 모드(`lottoRunFortunePick`)를 그대로 재사용해 신규 `dreamGoToLotto()` 헬퍼 하나로 모달 닫기+섹션 이동+실행까지 원클릭 처리. **③AI 폴백**: 검색 결과 0건일 때만 "🤖 AI 해몽으로 찾아보기" 버튼 노출(자동 호출 없음, 비용·남용 통제). 신규 `functions/api/dream-ai.js` — Supabase `dream_ai_cache` 캐시 우선 조회 → 없으면 OpenAI(`gpt-4o-mini`, Structured Outputs)로 title/summary/detail/lucky/luckyNum/action JSON 생성 후 캐시 저장(동일 키워드는 전세계 사용자 통틀어 1회만 과금). 클라이언트에 `dreamAiSearch()`/`dreamRenderAiModal()` 신설, "🤖 AI 생성 해몽" 배지로 정직하게 구분 표시. **보안**: OpenAI 응답은 외부 입력(사용자 검색어) 기반이라 프롬프트 인젝션으로 임의 HTML이 섞여 들어올 수 있어, 신규 `escapeHtml()` 헬퍼로 렌더링 전 이스케이프(다른 모든 텍스트는 직접 작성한 정적 데이터라 이 경로에만 필요). **검증**: 배치마다 Node로 74개 기존 테마 포함 전체 대상 중복·필드 검증, Playwright로 로또 연동 버튼(검색→모달→클릭→로또 결과 렌더링)과 AI 폴백(목업 서버로 XSS 페이로드 응답을 흘려보내 스크립트 미실행+태그 리터럴 표시 확인, 검색→AI버튼→로딩→모달→로또연동까지 전체 플로우) 콘솔 에러 없음 확인. **배포 준비물 등록 완료(2026-07-04)**: 사용자가 ①Supabase SQL Editor에서 `dream_ai_cache` 테이블 생성 ②Cloudflare Pages "Variables and secrets"(옛 "Environment variables" 명칭 변경분)에 `OPENAI_API_KEY` Secret 등록 → "Retry deployment"로 재배포까지 직접 완료(키 값은 Claude와 공유하지 않는 기존 보안 원칙 유지). 라이브 `curl` 검증: 신규 키워드 첫 호출은 `source:"ai"`(OpenAI 실제 생성), 동일 키워드 재호출은 `source:"cache"`(Supabase 캐시 즉시 반환)로 캐싱까지 정확히 동작 확인. Playwright로도 라이브 사이트에서 검색 0건 → AI 버튼 → 로딩 → "AI 생성 해몽" 배지 모달 → 로또 연동 버튼까지 전체 플로우 콘솔 에러 없이 재확인. **PRD 10-4 항목 최종 완료** |
| 꿈해몽 검색 랭킹 엔진 개편 (조사/불용어 제거 + 희소성 가중치 + 컷오프) | ✅ 완료 | v0.3.0 | **⚠️ 실험적 설계 — 나중에 되돌리거나 방향을 바꿀 수 있으니 이 항목은 자세히 남겨둠.** 사용자가 라이브 스크린샷 4장으로 버그 신고: ①"외계인과 싸우는 꿈" 검색 시 "연인이나 배우자와 싸우는 꿈"이 나옴(여러 단어 중 하나만 겹쳐도 노출되는 OR 매칭 때문 — "싸우는"만 걸림) ②"외계인" 검색 시 변형 1개만 노출(테마당 첫 매칭 변형에서 멈추는 로직) ③"외계인과 싸우는 꿈" 검색 시 전체 174건이 다 나옴(모든 테마 제목이 "~꿈"으로 끝나 "꿈"이라는 단어 자체가 사실상 전체 매칭되는 불용어 처리 누락) ④"외게"(오타) 검색 시 결과 없음 → AI 버튼. AskUserQuestion 없이 먼저 원인 분석과 2가지 설계안(①엄격 AND 매칭 ②단어 희소성 기반 랭킹)을 제시했고, 사용자가 "①안은 애매하게 안 맞으면 바로 AI API를 호출하게 돼서 비용이 늘어난다"고 정확히 지적 — **②랭킹 방식(추천안)으로 확정**. **핵심 알고리즘**: 검색어를 공백 분리 후 각 토큰에서 조사(을/를/이/가/은/는/과/와/에게/에서/으로 등 30개, 긴 것부터 매칭)를 제거하고, `꿈/해몽/나오다/보다/꾸다/관하다/대하다/거/것` 등 거의 모든 테마 제목에 등장해 변별력이 없는 불용어를 제거(신규 `dreamTokenizeQuery()`). 남은 토큰마다 전체 174개 테마+546개 변형 문서 중 몇 건에 등장하는지(df) 세어 가중치 `1/df`를 매김(신규 `dreamComputeWeights()`) — 즉 "외계인"처럼 희소한 단어는 가중치가 높고 "싸우다"처럼 흔한 단어는 낮음. 각 테마의 점수 = 매칭된 토큰 가중치 합, 테마 자체 텍스트와 각 변형 텍스트 중 더 높은 쪽을 그 테마의 대표 점수로 사용(변형이 테마보다 "확실히" 높을 때만 변형을 노출, 동점이면 테마를 노출해 모달에서 변형 칩을 마저 보게 함). 전체 후보 중 최고점의 50% 이상인 것만(컷오프) 통과, 점수 0(토큰이 하나도 안 걸림)이면 결과 없음으로 판정해 AI 버튼으로 유도. **AI 호출 비용 관점**: 이 방식은 "관련 키워드가 조금이라도 있으면" 최대한 기존 데이터로 답하려 시도하고, 정말 매칭되는 단어가 하나도 없을 때만(신종 꿈/오타) AI를 호출하도록 설계되어 사용자의 비용 우려에 부합. **Option A(사용자 요청)**: 검색 결과가 있어도 목록 하단에 "찾는 꿈이 아닌가요? 🤖 AI 해몽으로 찾아보기" 버튼을 눈에 띄게(작게 숨기지 않고 기존 무결과 버튼과 동일한 크기로) 항상 노출 — AI 캐시(`dream_ai_cache`)가 있어 실제 호출량이 많지 않을 거라는 사용자 판단. **결과 없음 화면 축소**: 기존에 있던 "또는 다른 키워드로 검색해보세요"+추천 칩 5개 블록은 제거(사용자 지적대로 화면 하단에 이미 있는 "추천 검색어" 칩과 내용이 겹치고, Option A로 AI 버튼도 항상 노출되니 불필요) — 결과 없음 화면은 이제 안내 문구 + AI 버튼 단 둘로 단순화. **알려진 한계(참고용)**: 조사 제거는 규칙 기반 접미사 스트리핑이라 완벽한 형태소 분석이 아님(예: 흔치 않은 단어가 우연히 조사와 같은 글자로 끝나면 과도하게 잘릴 수 있음, 동사 활용형은 어간까지만 대충 잘라내는 수준). 컷오프 비율(50%)과 조사/불용어 목록은 임의로 정한 값이라 실사용 중 이상 사례가 나오면 조정 필요. **검증**: Node 스크립트로 신고된 4가지 사례+추가 케이스(돈이 나오는 꿈/뱀에게 물리는 꿈/이빨이 빠지다/꿈만 입력 등) 전부 기대대로 동작함을 실제 `dreamData` 174개 테마 대상으로 시뮬레이션 확인, `node --check` 통과, Playwright로 정적 서버에서 4가지 신고 검색어 전부 실제 UI 렌더링까지 재확인(콘솔 에러 없음) |
| 사이드바 "테스트" 그룹 아이콘 가시성 수정 | ✅ 완료 | v0.0.33 | 접기/펼치기 삼각형(chevron)이 font-size 0.7rem으로 너무 작아 안 보인다는 피드백 → 1.1rem+bold로 확대 |
| 테스트 화면 뒤로가기/홈 버튼 | ✅ 완료 | v0.0.34 | v0.0.33에서 후순위로 미뤄뒀던 요청. 홈 화면을 제외한 모든 섹션에서 우하단 고정 플로팅 버튼(◀ 뒤로가기 / 🏠 홈) 노출. `App.navigate()`에 `_sectionHistory` 스택을 추가해 "직전에 있던 화면"으로 돌아가도록 구현(항상 홈으로 가는 게 아니라 테스트→테스트 직접 이동 시에도 원래 왔던 화면으로 복귀). 딥링크로 테스트 화면에 바로 진입한 경우(스택 비어있음)는 뒤로가기 시 홈으로 폴백. 15개 이상 섹션 HTML을 개별 수정하지 않고 `navigate()` 한 곳에만 로직을 추가해 유지보수 부담 없앰 |
| Tier 등급 코멘트뱅크 확장 (8개 게임형 테스트) | ✅ 완료 | v0.0.37 | 콘텐츠 갭 분석 후속 조치 ②번. 두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력 8개 테스트의 S~D 등급별 코멘트가 각 1개씩(총 40개)만 있어 재도전해도 같은 등급이면 토씨 하나 안 틀리고 반복되던 문제 해결. 등급당 1개→3개(기존 문구는 항상 첫 번째로 보존)로 늘려 총 120개, 결과 렌더링 시 신규 공용 헬퍼 `pickOne(arr)`로 매번 랜덤 하나를 골라 노출. 두뇌나이는 `tierMsg` 객체(S/A/B/C/D 키) 값을 배열로, 나머지 7개는 if/else 분기별 `tierMsg` 변수를 배열로 바꾸고 분기 종료 직후 `pickOne()`으로 최종 문자열을 재대입하는 방식이라 `shareText`/화면 표시 등 하위 코드는 변경 없이 그대로 재사용 |
| 칭호 시스템 | ✅ 완료 | v0.0.33 | D 무관 백로그 4순위(사용자가 순서 재조정: 칭호→레벨→궁합→사운드, 시즌테마는 제외, 미스터리박스는 보류). 방문 스트릭·완주 개수·Tier 성적 등 이미 쌓인 로컬 기록만으로 5종 뱃지(개근왕/올라운더/천재 과몰입러/인싸력 만렙/지혜로운 어른) 계산, 신규 저장소 없이 홈 화면 닉네임 카드에 표시 |
| 레벨/경험치 시스템 | ✅ 완료 | v0.0.33 | 모든 테스트가 공통으로 호출하는 `saveRanking()` 한 곳에만 XP 적립 로직 추가(Tier 테스트는 등급별 차등 30~10XP, 그 외는 완료 시 고정 15XP) — 15개 테스트 개별 수정 불필요. 100XP당 1레벨, 홈 화면에 레벨/XP 바 표시 |
| 결과 궁합 보기 (일치율 %) | ✅ 완료 | v0.0.33 | 친구 대결과 동일한 URL 공유 인프라(`#{section}?match=...`) 재사용. MBTI는 4축 일치 개수 기반 %, 인싸력은 점수 차이 기반 % 계산. 상위 % 랭킹 표시는 Stage D(백엔드) 필요해 이번 범위에서 제외(PRD 기존 방침 그대로) |
| 정답/오답 사운드 + 마이크로 애니메이션 | ✅ 완료 | v0.0.33 | 외부 음원 파일 없이 Web Audio API 오실레이터로 정답(880Hz)/오답(220Hz) 비프음 직접 생성(용량 0, 라이선스 문제 없음). CSS keyframe(pop/shake)으로 즉시 판정형 테스트(순서기억력/충동억제/숏폼집중력/속담완성/물가퀴즈)에 마이크로 애니메이션 적용 |
| ㄴ Stage D: 백엔드(Supabase) 1단계 — 익명 인증 + 결과 이중 기록 | ✅ 완료 | v0.0.40 | 콘텐츠가 충분히 쌓여 "실제 운영 단계" 조건 충족 판단, 사용자가 착수 확정. Supabase 프로젝트(`testseries`, Seoul 인접 리전) 생성 후 `profiles`/`test_results`/`percentile_cache` 3개 테이블 + RLS 정책 적용(댓글 테이블은 제외, 위 항목 참고). 인증은 로그인 UI 없는 **Anonymous Auth**(`signInAnonymously()`) 채택 — 클라이언트가 UUID를 자체 생성하는 방식 대신 Supabase가 발급하는 진짜 JWT 세션을 써서 RLS의 `auth.uid()`가 위조 불가능하도록 함. "Allow manual linking"도 함께 켜둬서 나중에 카카오 소셜 로그인과 연결하는 하이브리드 확장이 가능하게 준비. `index.html`에 `@supabase/supabase-js` CDN 스크립트 + 신규 파일 `supabase-client.js` 추가(`window.sb` 클라이언트, 페이지 로드 시 자동 익명 로그인, `syncResultToSupabase()` 헬퍼). 기존 15개 테스트가 공통 호출하는 `app.js`의 `saveRanking()` 맨 끝에 딱 한 줄만 추가해 로컬스토리지는 그대로 유지한 채 Supabase에도 결과를 이중 기록(마이그레이션 전략 1단계, PRD 방침대로 기존 테스트 로직은 전혀 건드리지 않음). `test_results` 원본은 본인만 select 가능하도록 잠그고, 향후 퍼센타일은 서버가 배치 집계해 `percentile_cache`에만 공개하는 구조로 설계. anon(publishable) 키는 RLS로 보호되는 공개 키라 코드에 그대로 포함, service_role 키는 절대 클라이언트에 두지 않음. Playwright로 익명 세션 자동 수립 → `saveRanking()` 호출 → `profiles`/`test_results`에 실제 row가 정상 insert되고 로컬스토리지 기록도 그대로 유지됨을 실제 Supabase 프로젝트 대상으로 검증, 콘솔 에러 없음 |
| ㄴ Stage D: 백엔드(Supabase) 2단계 — 퍼센타일 배치 집계 | ✅ 완료 | v0.0.42 | 사이트가 이미 Cloudflare Pages(`testseries1.pages.dev`, GitHub 연동 자동배포)에 배포돼있음을 확인 후 착수. 신규 `functions/api/percentile-refresh.js`(Cloudflare Pages Function, POST) — Tier 채점 8개 섹션의 `test_results`를 `service_role` 키로 집계해 등급별 "해당 등급 이상 비율"을 퍼센타일로 계산, `percentile_cache`에 upsert. 이 엔드포인트가 프로젝트에서 `service_role` 키를 쓰는 유일한 지점이라 `X-Cron-Secret` 커스텀 헤더로 보호(아무나 호출 불가). 별도 Cloudflare Worker+Cron Trigger를 새로 구축하지 않고, 이미 쓰고 있는 GitHub 저장소에 `.github/workflows/percentile-cron.yml`(매일 UTC 18:00=KST 03:00) 신설해 스케줄링 — 인프라를 새로 늘리지 않고 기존 파이프라인 재사용. 클라이언트는 `percentile_cache`가 이미 공개 읽기 RLS라 별도 API 없이 기존 `window.sb`로 직접 조회 가능(신규 읽기 엔드포인트 불필요). `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`CRON_SECRET`은 Cloudflare Pages 환경변수(암호화)로, `CRON_SECRET`은 GitHub Actions 시크릿으로 동일하게 등록. **2026-07-03 사용자가 시크릿 등록 완료 → 워크플로우 수동 실행(workflow_dispatch)으로 전체 파이프라인 라이브 검증 성공**: 401 인증 통과 → `test_results` 집계 → `percentile_cache` upsert → 공개 키 읽기까지 end-to-end 확인, **매일 KST 03:00 자동 집계가 실제로 가동 중인 상태**. 검증 시점에 이미 실제 플레이 기록(brain/reaction/seqmem/colorvision 등)이 쌓이기 시작한 것도 확인. 시크릿 값은 Claude와 공유하지 않고 사용자가 각 대시보드에 직접 입력(보안 원칙 준수). **클라이언트 "상위 %" UI는 v0.0.47에서 완료** — `percentile-refresh.js`에 `MIN_SAMPLE_SIZE=5` 게이트 추가(섹션당 표본 5개 미만이면 해당 section+tier는 `percentile_cache`에 아예 upsert하지 않아, row 존재 여부 자체가 "표본 충분" 신호가 됨). 클라이언트는 신규 `renderPercentileBadge(section, tier)`가 Tier 채점 8개 테스트 결과 화면(두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력)의 Tier 배지 바로 아래에 `percentile_cache`를 직접 조회해 "📊 상위 N% (Tier X 이상 기록 기준)"을 비동기로 채워넣음(row 없으면 조용히 미표시, 신규 API 불필요). 기존 `percentile_cache`에 이미 쌓인 row는 5개 미만 표본이어도 다음 크론 실행 전까지는 남아있을 수 있음(정상 — 다음 KST 03:00 자동 집계 또는 수동 workflow_dispatch 시 새 게이트가 적용되며 정리됨) |

---

## 3. 데이터 구조 요약
- `dreamData`: 테마(keywords/title/summary/detail/lucky/luckyNum/action) + `variants[]`(하위 세부꿈)
- `fortuneData`: 띠별 운세(v0.0.35~v0.0.36 `categories[].variants[]` 구조 개편 완료, 카테고리당 7벌 날짜 시드 순환) + `luckyNum`
- localStorage 키: `ranking_*`, `done_*`, `app_nickname`, `visit_streak`, `last_fortune_luckynum`, `last_dream_luckynum` 등

---

## 4. 변경 이력 (요약)
| 일자 | 버전 | 내용 |
|---|---|---|
| 2026-06-28 | v0.0.1 | 최초 생성, 5개 테스트 구조 구현 |
| 2026-06-29 | v0.0.3 | 애드센스 승인 전 광고 플레이스홀더 제거 |
| 2026-07-02 | v0.0.4~0.0.9 | 꿈해몽 구조개편/확장, 공유하기, 마이홈, 로또조합기 추가 |
| 2026-07-02 | v0.0.10 | 라이트/다크 모드 토글 추가, 마이홈을 홈 화면으로 통합 |
| 2026-07-02 | v0.0.11 | Phase 2 확장 로드맵 기획 확정 (PRD.md 9번 섹션). Stage A: 사이드바에 "테스트" 그룹(접기/펼치기) 신설 |
| 2026-07-02 | v0.0.12 | Stage B 착수: 반응속도 테스트 신설 (난이도 3단계, pointerdown 기반 ms 정밀 측정, 가짜신호 반칙 로직) |
| 2026-07-02 | v0.0.13 | Stage B 계속: 숫자 기억력 테스트 신설 (적응형 자릿수, 탭 키패드 UI, 어려움 모드 방해단계) |
| 2026-07-02 | v0.0.14 | Stage B 계속: 순서 기억력 테스트 신설 (격자 타일, 즉시 판정 UX). 사이드바 "테스트" 그룹 높이 고정값 버그 수정(항목 늘어나며 위쪽이 잘리던 문제) |
| 2026-07-02 | v0.0.15 | Stage B 계속: 색각 테스트 신설 (색 구별 감각 게임, 임상 검사 아님을 명시) |
| 2026-07-02 | v0.0.16 | Stage B 계속: 논리력 테스트 신설 (숫자 규칙 4지선다) |
| 2026-07-02 | v0.0.17 | Stage B 계속: 충동억제(Go/No-Go) 테스트 신설 |
| 2026-07-03 | v0.0.18 | Stage B 계속: 숏폼 집중력 테스트 신설 (충동억제 Go/No-Go 엔진 재활용, MZ향 리스킨) |
| 2026-07-03 | v0.0.19 | Stage B 계속: 인싸력 테스트 신설 (MBTI/ADHD식 문항 기반 사교성 성향 퀴즈) |
| 2026-07-03 | v0.0.20 | Stage B 계속: 속담 완성 퀴즈 신설 (어르신향, 시간압박 없는 4지선다 지혜 테스트) |
| 2026-07-03 | v0.0.21 | Stage B 완료: 그 시절 물가 맞히기 신설 (어르신향, 실제 물가 통계 기반 4지선다 향수 트리비아). Stage B 신규 인지테스트 10종 전체 완료 |
| 2026-07-03 | v0.0.22 | 오늘의 한마디 개편 착수(1단계): `quotes` 데이터를 문자열 배열 → `{text,author,role}` 객체 배열로 구조 변경, 철학자/사상가/작가/과학자/투자자 실제 검증 어록 20개 샘플 반영. 로테이션 로직을 날짜 기반 `todaySeed()`에서 12시간(KST) 단위 `halfDaySeed()`로 변경. 섹션명 "오늘의 폼 미친 한마디"→"오늘의 인생 한마디", 인용문 아래 `— 인물명 (역할)` 표기 추가. 최종 목표 700개, 2단계(50~100개 단위 확장)부터 계속 진행 예정 |
| 2026-07-03 | v0.0.23 | 오늘의 한마디 개편 2단계 배치 #1: 명언 36개 추가(20→56개). 서양 철학자(플라톤·데카르트·칸트·사르트르·카뮈 등)·동양 사상가(장자·왕양명·손자·간디)·작가(카프카·오웰·헤밍웨이·괴테 등)·과학자(뉴턴·보어·다윈·호킹)·심리학자(융)·투자자(그레이엄·템플턴·린치) 추가. 검증 과정에서 오귀속으로 알려진 유명 "명언"들(아인슈타인 "미친 짓" 명언, 다윈 "가장 강한 종" 정확 문구, 간디 "네가 보고 싶은 변화가 되어라" 정확 문구, 아리스토텔레스 "우리는 반복하는 대로 된다"(실제로는 윌 듀런트의 요약문), 괴테의 "결단의 순간" 명언, 갈릴레오의 "그래도 지구는 돈다"/"가르칠 수 없다" 두 명언, 마크 트웨인 "20년 후" 명언, 프로이트 "표현되지 않은 감정" 명언)은 확인 후 제외 |
| 2026-07-03 | v0.0.24 | 오늘의 한마디 개편 2단계 배치 #2: 명언 60개 추가(56→116개), 사용자 요청으로 배치 크기를 키워 진행 속도 향상. 서양 철학자·경제학자(몽테뉴/파스칼/볼테르/베이컨/홉스/루소/헤겔/포퍼/쿤/소로/에머슨/케인즈/프리드먼/슘페터), 동양 사상가·한국 인물(순자/묵자/한비자/이백/두보/원효대사/법정스님/신영복/정약용/이황/이이/최제우/김구/안창호/함석헌), 작가·시인(셰익스피어/와일드/디킨슨/휘트먼/포/릴케/체호프/마야안젤루/토니모리슨/보니것/보르헤스/마르케스/안네프랑크/프로스트/프랭클린), 사상가로 분류한 인권/독립운동가(마틴루터킹/넬슨만델라), 과학자(슈바이처/파스퇴르/테슬라/에디슨/하이젠베르크/플랑크/폴링/갈릴레오/카너먼), 투자자(소로스/보글/하워드막스/리버모어) 추가. 볼테르의 "나는 당신 말에 동의 안 하지만 목숨걸고 지키겠다"(실제로는 전기작가 이블린 홀의 창작 문구), 오스카 와일드 "자기 자신이 되어라"(출처 불명), 에머슨 "성공이란 자주 웃는 것"과 대체안이었던 "자기신뢰" 관련 인용 일부(오귀속 다수 확인), 조지 엘리엇 "될 수 있었던 존재가 되기에 늦은 때는 없다"(출처 불명), 앨런 튜링 "아무도 상상 못한 사람들이"(영화 대사, 실제 발언 아님), 넬슨 만델라 "나는 결코 지지 않는다" 버전(출처 불명, 자서전에서 확인되는 다른 문구로 대체)은 확인 후 제외 |
| 2026-07-03 | v0.0.25 | 오늘의 한마디 개편 2단계 배치 #3: 29개 추가(116→145개). 새 인물 21명(헤라클레이토스/제논/디오게네스/시몬베유/비트겐슈타인/아우구스티누스, 찰스디킨스/제인오스틴, 윤동주/한용운/김소월/이육사/이상 등 한국 시인·작가, 무라카미하루키/솔제니친/프리모레비/T.S.엘리엇, 오펜하이머/암스트롱/그레이스호퍼, 앙드레코스톨라니)+기존 검증된 인물의 2번째 어록 8개(괴테/아리스토텔레스/오웰/아인슈타인/버핏/세이건/템플턴/케인즈). 새 인물 풀이 좁아짐에 따라 이번 배치부터 1인당 최대 2개 어록 원칙 적용 시작 |
| 2026-07-03 | v0.0.26 | 오늘의 한마디 개편 방향 재조정 + 배치 #4: 사용자가 "700개는 비현실적으로 오래 걸린다"고 판단해 ①목표를 700→365개(하루 1개×1년)로 하향 ②로테이션을 12시간→24시간(하루) 단위로 변경(`app.js` `halfDaySeed()`→`dailyQuoteSeed()`) ③인물 소재가 고갈되면 삶/멘탈 관련 속담·격언도 포함하기로 결정. 실행: 인물 21명 추가(데즈먼드투투/치누아아체베/칼릴지브란/루미/오마르하이얌/파울로코엘료/비베카난다/미야모토무사시/후쿠자와유키치/조너스소크/나이팅게일/매슬로/칙센트미하이/칼로저스/안중근/갤브레이스 등, 145→166) + 삶/멘탈 관련 국내외 속담·격언 144개(한국50/중국21/일본3/영미15/탈무드8/아프리카10/인도8/아랍8/독일7/프랑스7/스페인7)를 처음으로 대량 투입(166→310) |
| 2026-07-03 | v0.0.27 | 오늘의 한마디 개편 완료: 마지막 배치로 속담·격언 55개 추가(러시아10/아일랜드8/북유럽7/몽골6/베트남6/라틴8/그리스10)해 목표치 **365개 정확히 달성**(310→365). 전체 구성은 검증된 인물 어록 약 166개(1인당 최대 2개) + 세계 각국 속담·격언 약 199개. Node 스크립트로 전체 365개의 text 필드 중복 없음·필드 누락 없음을 기계 검증, Playwright로 홈 화면 렌더링 재확인. **Stage B 이후 착수했던 "오늘의 한마디 개편" 작업이 이번 버전으로 전체 완료됨** |
| 2026-07-03 | v0.0.28 | **Stage C(난이도/모드 체계 개편) 완료**. 착수 전 사용자에게 세부 스펙(스트룹 색상 확장 여부/MBTI 정밀 문항수/ADHD 하위영역 분리 여부)을 먼저 설명하고 확인받은 뒤 진행. ①두뇌나이(스트룹): 색상 개수만 다르고 문항수·시간은 고정이던 기존 2단계를, 색상·문항수·제한시간이 모두 강화되는 진짜 3단계(쉬움4색15문항5초/보통5색20문항4초/어려움6색25문항3초)로 개편. ②MBTI: 간단(12문항 기존유지)/정밀(24문항, `mbtiQuestionsExtra` 12문항 신설) 2모드 추가, 정밀 결과에 축별 비율 바(계산만으로 구현, 신규 데이터 불필요) 추가. ③ADHD: 간단(10문항 기존유지)/정밀(`adhdQuestionsPrecise` 20문항, 부주의10+과잉행동-충동성10 분리, 응답척도도 진짜 4단계로 세분화) 2모드 추가, 정밀 결과에 영역별 점수 바 추가하고 종합등급은 점수/3 환산으로 기존 20점 등급표 재사용. Playwright로 3개 기능 각각 시작→진행→결과 화면까지 실제 플로우 구동해 콘솔 에러 없음 확인 |
| 2026-07-03 | v0.0.29 | **Stage F(종합 인지 프로필 레이더차트) 완료**. Stage D/E/F/G 우선순위를 사용자에게 위임받아 Claude가 결정 — Stage D(백엔드)는 "실제 운영 단계 진입 시 착수"로 합의돼 있어 사용자에게 시점 재확인(아직 아님) 후 계속 보류, D에 의존하는 E도 자연히 보류, 백엔드 불필요한 F를 먼저 착수. 두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제 7개 인지테스트를 레이더 축으로 선정(숏폼집중력은 충동억제와 동일 엔진이라 제외), 각 테스트 최근 랭킹 기록에서 "Tier (S~D)" 문자열을 정규식으로 파싱해 100/80/60/40/20점으로 환산 — 기존 채점 로직·데이터는 전혀 손대지 않고 구현. 외부 차트 라이브러리 없이 순수 SVG(`<polygon>`/`<line>`/`<text>`)로 7각형 레이더 직접 렌더링, 7개 중 5개 이상 완료해야 노출(미만이면 진행률 티저 카드). 구현 중 라벨 텍스트가 SVG viewBox 밖으로 밀려 잘리는 문제를 Playwright 스크린샷으로 발견해 viewBox/중심좌표/반지름 비율을 조정해 수정 |
| 2026-07-03 | v0.0.30 | D(백엔드) 착수 전 사용자가 먼저 요청한 프론트엔드 개선 4건 처리. ①사이트 브랜드명을 "5-in-1 대시보드"→"과몰입 연구소"(부제: "재미로 시작했다가 뼈 맞고 공유하는 종합 테스트 모음")로 전면 변경(title/사이드바/모바일헤더/푸터/description). ②홈 "서비스 바로가기"가 5개 카드짜리 옛날 버전으로 방치돼 있던 걸 발견해 15개 전체로 확장, "도구"(3개)/"테스트 모음"(13개) 두 그리드로 분리. ③닉네임을 매번 재입력해야 하는 불편함을 DB 없이 로컬스토리지만으로 해결 — 닉네임 쓰는 13개 테스트 시작화면에 `getNickname()` 프리필 + 시작 시 `setNickname()` 저장을 공통 패턴화(guard 문 뒤에 `replace_all`로 일괄 삽입). ④"무조건 다크모드로 시작" 요청은 코드 확인 결과 이미 만족하고 있음을 확인(OS 다크/라이트 감지 코드 자체가 없어 최초 방문은 항상 다크) — 변경 없이 사용자에게 현재 동작 설명 후 종료 |
| 2026-07-03 | v0.0.31 | D(백엔드) 무관 재미요소 백로그 2순위(친구 대결 모드, 오늘의 챌린지) 완료. **친구 대결**: Tier(S~D) 채점 8개 테스트(두뇌나이/반응속도/숫자기억/순서기억/색각/논리력/충동억제/숏폼집중력) 대상, 결과 화면에 "친구에게 도전장 보내기" 버튼 신설 → 닉네임+결과문자열을 `#{section}?vs=<JSON>` 형태로 URL 인코딩해 공유 → 상대가 링크로 들어오면 시작화면에 도전장 배너 표시, 같은 테스트 완료 시 Tier 점수(레이더차트와 동일한 S=100~D=20 기준) 비교로 승/패/무 자동 판정하는 VS 카드 노출. 해시 기반 라우팅에 쿼리스트링 파싱(`App.pendingChallenge`)을 추가해 백엔드 없이 URL만으로 구현. 8개 테스트 각각의 init/시작화면/결과화면에 공통 헬퍼(`renderChallengeBanner`/`renderChallengeCompareCard`/`renderChallengeButton`)를 적용하는 반복 작업(24개 지점). **오늘의 챌린지**: 오늘의 한마디와 동일한 `todaySeed()` 시드로 13개 테스트 중 매일 다른 3개를 홈 화면에 노출, 완료 여부 체크 표시. Playwright로 도전 링크 생성→새 컨텍스트에서 링크 진입→배너 확인→테스트 완료→Tier 비교 승패 판정까지 전체 플로우 검증 |
| 2026-07-03 | v0.0.32 | D(백엔드) 무관 백로그 3순위(콘텐츠 보강) 완료. 착수 전 MBTI·꿈해몽 현황을 Explore 에이전트로 조사하고, 진행 방식(꿈해몽 API 존재 여부, variants 확장 범위)을 사용자와 확인 후 진행. **MBTI**: famous 필드에서 스티브 잡스 ENTP/ENTJ 중복 기재 버그 수정, ESTP의 도널드 트럼프를 마돈나로 교체(민감한 현직 정치인 배제). **꿈해몽**: 꿈해몽 전용 무료 오픈API가 존재하지 않음을 웹 검색으로 확인(네이버 등 어디에도 없음, 있는 건 실시간 AI 챗봇형 유료 서비스뿐이라 정적 데이터로 가져올 수 없음) → 명언/속담/물가 트리비아와 동일한 "웹 검색 기반 수작업 조사·집필" 방식 채택. 로또당첨/거울/무지개/나비/추락/곰/거북이/좀비/태풍/화산/시체/연예인/폭포/코끼리/UFO 15개 신규 테마를 각각 웹 검색으로 조사해 추가(59→74개), 기존 8개 인기 테마(학교/전애인/짝사랑/키스/싸움/알몸/이사/지갑분실)에 variants 2개씩 추가(variants 보유 테마 23→46개). Node로 데이터 구조 기계 검증, Playwright로 신규/확장 테마 검색+모달+변형칩 네비게이션까지 실제 동작 확인 |
| 2026-07-03 | v0.0.33 | D(백엔드) 무관 백로그 4순위(재미요소) 완료 + 소소한 UI 수정 2건. 사용자가 남은 variants 28개는 "정식 배포 전 콘텐츠 보강 단계"로 순위를 완전히 뒤로 미루기로 결정, 4순위는 칭호→레벨→궁합→사운드 순으로 재배열(시즌테마 제외, 미스터리박스 보류)하고 진행 전 계획을 사용자에게 보고 후 착수. ①사이드바 "테스트" 그룹 접기/펼치기 삼각형이 너무 작아 안 보이던 문제를 font-size 확대(0.7rem→1.1rem)+bold로 수정. ②**칭호 시스템**: 로컬 기록(방문 스트릭/완주 개수/Tier 성적)만으로 뱃지 5종(개근왕/올라운더/천재 과몰입러/인싸력 만렙/지혜로운 어른) 계산, 홈 화면에 표시. ③**레벨/경험치**: 모든 테스트 공통 호출 지점인 `saveRanking()`에 XP 적립 로직을 한 곳에만 추가(Tier 등급별 30~10XP, 그 외 완료 시 고정 15XP), 100XP당 1레벨, 홈에 레벨/XP 바 표시. ④**결과 궁합**: 친구 대결과 동일한 URL 공유 인프라(`#{section}?match=...`)를 재사용해 MBTI(4축 일치율)·인싸력(점수차 기반 일치율) 궁합 기능 추가, 상위% 랭킹은 계속 D 이후로 보류. ⑤**사운드/애니메이션**: Web Audio API 오실레이터로 정답/오답 비프음을 직접 생성(외부 음원 파일 없음), CSS keyframe(pop/shake)으로 순서기억력·충동억제·숏폼집중력·속담완성·물가퀴즈에 마이크로 애니메이션 적용. Playwright로 배지 5종 동시 충족 시나리오, 레벨업 표시, MBTI 궁합 0%/100% 양극단 케이스, 사운드/애니메이션 콘솔 에러 없음까지 전부 검증. 뒤로가기/홈 버튼 추가 요청은 사용자가 후순위로 명시적으로 미룸(백로그 등록만) |
| 2026-07-03 | v0.0.34 | v0.0.33에서 미룬 "테스트 화면 뒤로가기/홈 버튼" 진행. 배치(고정 플로팅 vs 화면 상단 인라인)와 뒤로가기 동작(이전 화면 이동 vs 무조건 홈) 두 가지를 사용자에게 확인받아 둘 다 추천안(고정 플로팅 버튼 + 이전 화면으로 이동)으로 결정. 홈 화면을 제외한 모든 섹션 우하단에 ◀ 뒤로가기 / 🏠 홈 두 개의 원형 플로팅 버튼 노출. `App.navigate()`에 `_sectionHistory` 스택을 추가해 뒤로가기가 "무조건 홈"이 아니라 "직전에 있던 화면"으로 돌아가도록 구현(테스트→테스트로 바로 이동한 경우도 왔던 화면으로 복귀), 딥링크로 테스트 화면에 바로 진입해 스택이 비어있으면 홈으로 폴백. 15개 이상 섹션의 HTML을 개별 수정하는 대신 `navigate()`/`goBack()` 두 함수에만 로직을 추가해 구현(칭호/레벨 시스템 때와 동일하게 "공통 진입점 한 곳만 수정" 패턴 재사용). Playwright로 홈 화면 숨김/테스트 진입 시 노출/홈 버튼 동작/테스트→테스트 이동 후 뒤로가기(직전 화면 복귀)/딥링크 진입 시 폴백까지 6가지 시나리오 전부 검증, 콘솔 에러 없음 확인 |
| 2026-07-03 | v0.0.35 | 사용자가 콘텐츠 갭 분석 중 가장 임팩트 크다고 짚었던 "오늘의 운세" 실제 일별 로테이션 착수(①번 항목). 착수 전 목표치(띠×카테고리 조합당 variant 개수)와 진행 방식을 AskUserQuestion으로 확인 — 7개(추천, 총 336문장)·배치로 나눠 샘플 먼저 승인받는 방식(추천) 둘 다 채택. `fortuneData`의 `base`(4개 카테고리 고정 문장) 구조를 `categories: [{title, variants:[]}]`로 전면 개편(기존 문구는 항상 variants[0]으로 보존해 무손실 마이그레이션), `app.js`의 `renderFortuneView('result')`가 `dailyQuoteSeed()`(오늘의 한마디와 동일한 24시간 KST 시드) + 띠 인덱스 + 카테고리 인덱스로 매일 다른 variant를 결정론적으로 선택하도록 변경(별점 계산도 기존 `todaySeed()`+로컬 타임존 방식에서 동일한 KST 시드 체계로 통일해 자정 경계 오차를 없앰). 샘플 배치로 12띠 중 쥐·소 2개만 먼저 7벌씩(4카테고리×7=28문장×2띠=56문장) 완성해 톤을 승인받고, 나머지 10띠는 이번 버전에서 구조만 변환(각 1벌, 기존 문구 그대로) — 다음 배치에서 순차 확장 예정. Node 스크립트로 12띠×4카테고리 구조·필드 무결성 기계 검증, Playwright로 쥐띠(variants 7개) 결과 화면 정상 렌더링과 호랑이띠(variants 1개, 아직 미확장) 결과 화면도 인덱스 아웃오브바운드 없이 정상 동작함을 확인, 콘솔 에러 없음 |
| 2026-07-03 | v0.0.36 | 사용자가 "계속 진행해줘"로 배치 승인 → 오늘의 운세 나머지 10띠(호랑이/토끼/용/뱀/말/양/원숭이/닭/개/돼지)를 v0.0.35 샘플(쥐·소)과 동일한 방식으로 전부 7벌씩 확장, **336개 목표 완전 달성**. 띠별로 기존 v1 문구에 담겨 있던 캐릭터성을 유지해서 확장(호랑이=대담함/결단력, 토끼=섬세함/감수성, 용=리더십/존재감, 뱀=통찰력/신중함, 말=자유로움/즉흥성, 양=평화/배려, 원숭이=재치/유머, 닭=꼼꼼함/완벽주의, 개=충성심/신뢰, 돼지=풍요/너그러움). Node 스크립트로 12띠×4카테고리×7벌(총 336개) 구조·필드 무결성과 띠별 카테고리 내 `positive` 문구 중복 없음까지 재검증(전부 통과), Playwright로 용띠/닭띠/말띠/돼지띠 4개를 추가로 실제 렌더링해 undefined/NaN 없이 정상 동작함을 확인. **"오늘의 운세 실제 일별 로테이션" 작업이 이번 버전으로 전체 완료됨** |
| 2026-07-03 | v0.0.37 | 콘텐츠 갭 분석 후속 조치 ②번(Tier 등급 코멘트뱅크 확장) 완료. 사용자에게 "②번이 뭔말이야"라는 질문을 받아 먼저 기존 코드(`tierMsg = '...'` 패턴이 8개 테스트에 반복됨)를 예시로 들어 문제를 설명한 뒤 "진행하자" 승인받고 착수. `app.js`에 공용 헬퍼 `pickOne(arr)`를 `seededRandom()` 바로 아래에 신설. 두뇌나이(브레인)는 `tierMsg` 가 `{S:'...',A:'...',...}` 객체라서 각 값을 배열로 바꾸고 `pickOne(tierMsgBank[tier])`로 변환, 나머지 7개(반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력)는 if/else 분기마다 대입되는 `tierMsg` 변수 자체를 배열로 바꾸고 분기 종료 직후 `tierMsg = pickOne(tierMsg);` 한 줄만 추가 — `shareText`나 화면 표시 부분(`${tierMsg}`)은 변수명이 그대로라 손댈 필요 없이 재사용됨. 등급별 1개→3개(기존 문구는 항상 배열의 첫 번째로 보존)로 늘려 8테스트×5등급×3개 = 총 120개 문구 완성, 각 테스트 고유의 유머 소재(반응속도=교통/스포츠, 숫자기억력=번호/암기, 순서기억력=댄스/패턴, 색각=디자인/톤, 논리력=수학/암산, 충동억제=자제력/유혹, 숏폼집중력=알고리즘/도파민, 두뇌나이=뇌나이/워밍업)를 유지해서 확장. Node로 8개 테스트 전부 `tierMsg = [` 배열 형태(35개 분기) + 두뇌나이 `tierMsgBank` 객체(1개)로 정상 치환됐는지, 문자열 단독 대입이 남아있지 않은지 검증, app.js 파싱 에러 없음 확인. Playwright로 `logic`/`reaction` 두 테스트는 동일 조건으로 결과 화면을 15회 반복 렌더링해 실제로 서로 다른 코멘트가 노출됨을 확인(고정 코멘트가 아님을 증명), 8개 테스트 전체 섹션 진입까지 콘솔 에러 없음 확인 |
| 2026-07-03 | v0.0.38 | 콘텐츠 갭 분석 마지막 후보 ③번(속담완성/물가퀴즈)을 사용자가 "지금의 5배 이상으로 늘리자, 재미를 줘야 한다, 계속 하고 싶게 해야 한다"고 확장 요청 — 단순 "순서 랜덤화"보다 강력한 방식으로 방향 전환. AskUserQuestion으로 목표 풀 크기(속담 100개/물가 50개, 물가는 실제 통계 확인이 관건이라 보수적으로)와 진행 방식(한 판 10문항 유지, 매번 풀에서 랜덤 추출)을 확인받고 착수. **코드**: `app.js`의 `proverbStart()`/`pricequizStart()`에 `shuffleArray(AppData.xxxQuestions).slice(0, 10)`로 매번 새 10문항 세트를 뽑아 `state.questions`에 저장하도록 변경, `renderXxxView`/`xxxAnswer`/`xxxAdvance`의 모든 `AppData.xxxQuestions` 직접 참조를 `state.questions`로 교체(총 4곳×2테스트). **콘텐츠**: 속담은 90개 신규 작성(기존 10개+90=100개, 초등 교과서 수준의 널리 알려진 속담만 선정해 정확성 리스크 최소화). 물가퀴즈는 "추측 금지" 원칙(HISTORIC_LOG 기존 방침)에 따라 WebSearch로 13개 카테고리(짜장면/버스/라면/소주/담배/영화관람료/택시/지하철/커피자판기/새우깡/초코파이/롯데리아/우표) 각각 실제 통계를 조사해 40개 신규 작성(기존 10개+40=50개, 1971~2023년) — 나무위키·경향신문·아시아경제·서울시 공식 물가정보·우정사업본부 공식 우편요금표 등 복수 출처 교차 확인 후 확정, 범위가 넓거나(예: "1990년대 500원") 단일 출처만 있어 불확실한 항목은 채택하지 않고 연도가 명시된 확실한 데이터만 사용. Node로 속담 100개/물가 50개 전부 구조·필드 완결성과 문항 간 중복 없음 기계 검증, Playwright로 두 테스트 모두 시작할 때마다 서로 다른 10문항 조합이 나오는지(3회 재시작 비교)와 물가퀴즈 전체 플로우(10문항 완료→결과화면→정답확인 리캡 10개)까지 검증 |
| 2026-07-03 | v0.0.39 | 사용자 요청으로 v0.0.32에서 "정식 배포 전 콘텐츠 보강 단계"로 미뤄뒀던 꿈해몽 variants 잔여 28개 테마를 빠르게 완료. 호랑이/용/개/고양이/새(동물 5종), 다리/산/계단/엘리베이터/지진/번개천둥/눈(자연·이동 7종), 꽃/나무/과일/음식/술(성장·소비 5종), 우는꿈/웃는꿈(감정 2종), 옷·신발·휴대폰 분실/길잃음/감옥/병원/수술(불안 7종), 별/달(천체 2종) 등 28개 테마 각각에 variants 2개씩 신규 작성(총 56개 추가) — 기존 테마의 summary/detail 톤(예: 호랑이=권위·리더십, 용=최고의 길몽, 뱀=변화·재생)을 그대로 이어받아 하위 세부 꿈(예: "호랑이에게 쫓기는 꿈", "용과 싸우는 꿈")을 확장하는 방식으로 진행, 기존 46개 테마가 이미 확립해둔 {keywords/title/detail/lucky/luckyNum/action} 포맷 그대로 재사용. **74개 테마 전체가 variants를 보유하게 되어(테마당 최소 2개, 총 146개) v0.0.32에서 미뤘던 작업이 이번 버전으로 완전히 마무리됨.** Node로 74개 테마 전부 variants 존재 여부·필드 완결성·테마+variant 전체 제목 중복 없음까지 기계 검증(전부 통과), Playwright로 이번에 새로 추가한 테마 3개(호랑이/달/별)와 기존에 이미 variants가 있던 테마 1개(뱀, 회귀 확인용)를 실제 검색→모달 열기→변형 칩 클릭까지 구동해 정상 동작 확인, 콘솔 에러 없음 |
| 2026-07-03 | v0.0.40 | **Stage D(백엔드) 착수 — 1단계(익명 인증 + 결과 이중 기록) 완료.** 콘텐츠가 충분히 쌓여 "실제 운영 단계" 조건을 충족했다고 판단, 사용자가 Supabase 사용 확정 후 착수(HISTORIC_LOG.md 세션#7 마지막 메모가 이번 세션 시작점). 착수 전 Claude가 스키마 방향/인증 방식/보안 고려사항/마이그레이션 전략을 먼저 설명 → 사용자가 인증은 "익명 UID만(추천)", 진행은 "스키마·보안정책 구체화 후 프로젝트 생성 안내부터"를 선택. 중간에 "댓글 기능은 나중에, 욕만 먹을 것 같다"는 요청으로 `comments` 테이블/RLS를 최종 스키마에서 제외. 사용자가 직접 Supabase 대시보드에서 프로젝트(`testseries`, Asia-Pacific) 생성 + SQL Editor에서 스키마 실행 + Authentication에서 Anonymous/Manual linking 활성화까지 진행, Claude는 화면 스크린샷 기반으로 각 단계(Organization/GitHub 연동 스킵/비밀번호 생성/리전/Security 체크박스 3개 중 "Enable automatic RLS" 추가 체크 권장, Publishable key 위치, Data API URL 위치)를 실시간 안내. 완료 후 `index.html`에 `@supabase/supabase-js` CDN + 신규 `supabase-client.js` 추가, `app.js`의 공용 `saveRanking()` 끝에 한 줄만 추가해 로컬스토리지 유지 + Supabase 이중 기록 구현(마이그레이션 1단계, 기존 15개 테스트 로직 무변경). Playwright로 실제 Supabase 프로젝트를 대상으로 익명 세션 자동 수립 → `saveRanking()` 호출 → `profiles`/`test_results` row 정상 insert(RLS 통과) → 로컬스토리지 기록도 그대로 유지됨을 end-to-end 검증, 콘솔 에러 없음 |
| 2026-07-03 | v0.0.41 | **카카오톡 공유 완료.** "다음 단계" 우선순위를 사용자가 직접 정리 — 카카오톡 공유부터, Stage D 2단계(퍼센타일 집계)와 로또 통계기반은 나중에 같이, 두뇌나이 모바일 터치 지연 보정은 백로그에서 제외("나중에 문제되면"). Kakao Developers에서 앱("과몰입 연구소", ID 1503406) 생성 → JavaScript 키 발급 → Web 플랫폼 도메인(`https://testseries1.pages.dev`) 등록까지 진행. 최근 개편된 Kakao Developers UI(옛 "내 애플리케이션"→"앱", "플랫폼 등록" 위치 변경 등) 때문에 몇 차례 헤맸고, WebSearch/WebFetch로 공식 문서를 재확인해 정확한 경로("앱 > 플랫폼 키 > JavaScript 키" 박스를 클릭하면 나오는 "JavaScript SDK 도메인" 필드)를 찾아 안내. 코드: `index.html`에 Kakao JS SDK CDN 스크립트(v2.8.1, `curl`+`openssl`로 SRI `integrity` 해시 직접 계산해 삽입) 추가, 신규 `kakao-share.js`(`Kakao.init()` + `shareToKakao(text)` — `Kakao.Share.sendDefault({objectType:'text', link:{webUrl,mobileWebUrl}})`, 실패 시 콘솔 로그+토스트로 조용히 처리). `app.js`의 기존 "📤 내 결과 공유하기"(Web Share API) 버튼 14곳(MBTI/운세/두뇌나이/ADHD/반응속도/숫자기억/순서기억/색각/논리력/충동억제/숏폼집중력/인싸력/속담완성/물가퀴즈) 전부 바로 아래에 동일한 `shareText` 변수를 재사용하는 "💬 카카오톡 공유" 버튼을 Node 스크립트로 일괄 삽입(각 버튼 텍스트/문구를 새로 작성하지 않고 기존 공유 문구 재사용). Playwright로 SDK 로드·`Kakao.isInitialized()` true 확인, MBTI·속담완성(서로 다른 UI 구조) 두 테스트에서 실제 결과 화면까지 진행해 버튼이 정상 렌더링되고 클릭 시 콘솔 에러 없음을 검증(단, 실제 카카오톡 전송까지의 end-to-end는 로컬/Codespaces 도메인이 화이트리스트에 없어 배포 후에만 완전히 확인 가능). **후속 확인**: 사용자가 Cloudflare Pages 대시보드 스크린샷으로 사이트가 이미 `testseries1.pages.dev`(GitHub 연동 자동배포)에 배포돼있음을 알려줘서, 실제 그 라이브 도메인에서 Playwright로 재검증 — 카카오톡 공유 버튼 클릭 시 `accounts.kakao.com` 로그인 팝업이 `sharer.kakao.com/picker/link?app_key=...&short_key=...` 형태로 정상 오픈되는 것까지 확인해 도메인 화이트리스트 통과와 공유 콘텐츠 등록이 실제로 동작함을 증명 |
| 2026-07-03 | v0.0.42 | **Stage D 2단계(퍼센타일 배치 집계) + 로또 통계기반 추천 완료.** 사용자가 "다음단계로 가보자 구도 잡아줘봐"라고 요청 → 두 기능을 하나의 인프라(Cloudflare Pages Functions)로 묶는 구조를 먼저 설계해 제시하고, AskUserQuestion으로 스케줄링 방식(GitHub Actions cron, 추천안 채택 — 별도 Workers+Cron Trigger 신규 구축 없이 기존 GitHub 저장소 재사용)과 로또 데이터 소스(동행복권 비공식 엔드포인트, PRD 기존 방침대로 진행) 확정 후 착수. 신규 `functions/api/percentile-refresh.js`(POST, `X-Cron-Secret` 헤더로 보호, `service_role` 키로 Tier 8개 섹션 `test_results` 집계해 "해당 등급 이상 비율"을 퍼센타일로 계산 후 `percentile_cache`에 upsert) + `functions/api/lotto-stats.js`(GET, 동행복권 `getLottoNumber` 엔드포인트에서 최근 30회차를 서버사이드로 가져와 CORS 우회, 번호별 출현 빈도 계산, Cloudflare Cache API로 24시간 엣지 캐싱, 첫 회차 2002-12-07 기준 날짜 계산으로 최신 회차 추정 후 보정). `.github/workflows/percentile-cron.yml` 신설(매일 UTC 18:00=KST 03:00 `percentile-refresh` 호출). 클라이언트: 로또 조합기의 기존 "준비중" 비활성 버튼을 `lottoRunStats()`로 교체, `lottoWeightedPick()`이 빈도 가중 랜덤(빈도 0 번호도 최소 확률 보장)으로 5게임 생성 — 로또 조합기 4개 모드(랜덤/직접지정/운세연동/통계기반) 전부 완료. `percentile_cache`는 이미 공개 읽기 RLS라 클라이언트가 신규 API 없이 기존 `window.sb`로 직접 조회 가능하게 설계(읽기용 서버 엔드포인트 불필요). `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`CRON_SECRET`은 Cloudflare Pages 환경변수로, `CRON_SECRET`은 GitHub Actions 시크릿으로 동일하게 등록(코드에는 없음) |
| 2026-07-03 | v0.0.43 | **로또 통계기반 실데이터 소스 차단 발견 + 방어코드.** 배포 직후 `/api/lotto-stats`를 curl로 직접 호출해보니 500(Cloudflare "worker threw exception") 에러 → 원인 조사 결과 `dhlottery.co.kr`이 데이터센터/해외 IP 대역을 차단하고 있어 API 엔드포인트든 일반 결과 페이지든 전부 "문서가 이동되었습니다" HTML 차단 페이지로 리다이렉트됨을 개발 샌드박스에서 직접 재현 확인(WebSearch로 2026-01경 동행복권 측 정책 변경 정황도 확인). `fetchRound()`에 User-Agent 헤더 추가, `content-type`이 JSON이 아니면 즉시 null 반환, try/catch로 감싸 예외가 나도 조용히 실패하도록 방어 코드 추가 — 이제 500 크래시 대신 502 에러로 안전하게 응답. 클라이언트는 이미 API 실패 시 랜덤 모드로 자동 대체하도록 설계돼있어 사용자 경험은 깨지지 않지만, **실제 당첨 통계 데이터 자체는 여전히 확보 못한 상태** — 데이터 소스 대안(네이버 파싱 등, 신뢰성·법적 리스크 있음)을 진행할지는 사용자 확인 필요해 CLAUDE.md 기능현황에 "코드 완료·데이터소스 차단" 상태로 정확히 기록 |
| 2026-07-03 | v0.0.44 | **로또 통계 프록시 크래시 완전 근절.** v0.0.43 배포 후에도 라이브 확인 결과 여전히 500 "error code 1101"이 떠서, `fetchRound()`가 아닌 다른 지점(Cache API 등)에서 예외가 나는지 진단하려고 `onRequestGet` 전체를 try/catch로 감싸고 에러 상세를 임시로 응답에 노출하는 디버그 커밋을 먼저 배포. 라이브 재확인 결과 이번엔 정상적으로 502 JSON 에러("원본 사이트가 일시적으로 접근을 막고 있을 수 있습니다")가 돌아옴을 확인 — 즉 v0.0.43의 수정 자체는 맞았고, 직전 확인 때는 배포 전파가 아직 덜 된 상태에서 테스트했던 것으로 판명. Cache API 접근 자체도 실패할 가능성을 대비해 `caches.default` 호출도 별도 try/catch로 한 번 더 감싸 캐시가 안 되는 환경에서도 기능은 계속되도록 보강. 디버그용 에러 상세(`detail` 필드) 노출은 확인 끝나서 제거. **최종 상태**: 크래시는 완전히 사라짐(항상 502 또는 정상 JSON만 응답), 하지만 dhlottery 실데이터 확보는 여전히 미해결 — 이 상태를 사용자에게 있는 그대로 보고하고 다음 방향(대안 데이터소스 탐색 여부, 문구 수정 여부, 백로그 전환 여부) 결정 요청 예정 |
| 2026-07-03 | v0.0.45 | **로또 통계기반 UI를 다시 "준비중"으로 되돌림.** Stage D 2단계 환경변수 등록 안내와 함께 로또 통계기반 처리 방향을 AskUserQuestion으로 확인 — 사용자가 "일단 통계기반은 준비중으로 두고, 내일 다시 작업하자" 선택(문구 수정이나 대안 데이터소스 탐색 대신 원상복구 후 보류 선택). `initLotto()`의 로또 조합기 버튼을 `lottoRunStats()` 활성 상태에서 다시 비활성 "📊 통계 기반 추천 (준비중)"으로 변경(툴팁: "데이터 소스 접근 이슈로 점검 중") — 실제로는 랜덤인데 "통계 기반"이라고 표시해 사용자를 오해시키지 않기 위한 정직성 우선 결정. `lottoRunStats()`/`lottoWeightedPick()` 함수와 `functions/api/lotto-stats.js` 서버 코드는 삭제하지 않고 그대로 남겨둠 — 데이터 소스 문제만 풀리면 버튼 재활성화 한 줄로 바로 복구 가능 |
| 2026-07-04 | v0.0.46 | **로또 통계기반 추천 완성 — 데이터 소스 문제를 발상 전환으로 해결.** 사용자가 "완전히 다른 시각에서도 바라봐달라"고 재작업 요청 → 핵심 통찰: 과거 당첨번호는 불변 공공 데이터라 차단된 dhlottery를 실시간으로 뚫을 필요가 없음. 샌드박스(데이터센터 IP, dhlottery 차단 재현 환경)에서 대안 소스를 실측 테스트해 **GitHub Pages 미러(`smok95.github.io/lotto`)** 를 발굴 — `results/all.json`(1~1230회차 전체 419KB)과 `results/latest.json` 제공, 커밋 로그로 매주 토요일 추첨 직후(21시대 KST) 자동 갱신 확인, CORS `*` 개방, 최신 회차(1230회, 2026-06-27)까지 반영 확인. **서버**: `functions/api/lotto-stats.js`를 `fetchFromMirror()`(1차) → `fetchFromDhlottery()`(2차, 기존 로직 보존 — 차단 풀리면 자동 복귀) 폴백 체인으로 재작성, 응답에 `latestNumbers`/`latestBonus`/`source` 필드 추가, 24시간 엣지 캐싱 유지. **클라이언트**: 신규 `lottoFetchStats()`가 자체 프록시 실패 시 미러를 직접 조회(CORS 개방 덕분에 가능 — 로컬 개발이나 Functions 장애 시에도 동작하는 3중 폴백), "고객이 과거 데이터를 확인"할 수 있도록 신규 `lottoRenderStatsPanel()`이 조합 결과 위에 직전 회차 당첨번호(보너스 포함)+최근 30회 최다/최소 출현 번호(출현 횟수 표기)를 렌더링, 버튼을 "📊 실제 당첨 통계 기반"으로 재활성화. 테마 규칙 준수(blue-800은 remap에 없어 blue-700 테두리 사용). **검증**: Node로 서버 함수를 실제 미러 대상 구동(frequency 합계=180=30회×6개 정합성, 45개 키) + 미러 다운/전체 다운 시뮬레이션 시 502 JSON 안전 응답까지 3개 시나리오 통과, Playwright로 정적 서버(=/api 없음, 미러 직접 폴백 경로)에서 버튼 클릭→통계 패널+5게임(각 6개 번호 1~45 오름차순) 렌더링→콘솔 에러 없음(의도된 /api 404 제외) 확인 |
| 2026-07-04 | v0.0.47 | **퍼센타일 "상위 %" 표시 UI 완료.** 사용자가 잔여 백로그 우선순위를 "①퍼센타일 UI(표본 5개 기준) → ②Stage E 동물비유카드 → ③MBTI 텍스트 검수(PWA 제외)" 순서로 확정. `functions/api/percentile-refresh.js`에 `MIN_SAMPLE_SIZE = 5` 상수 추가, 기존 `if (total === 0) continue`를 `if (total < MIN_SAMPLE_SIZE) continue`로 변경해 표본 5개 미만 section+tier는 `percentile_cache`에 아예 upsert하지 않도록 함 — 별도 `sample_size` 컬럼 추가(스키마 변경·서비스 롤 키 재사용 필요)나 클라이언트 카운트 조회 없이, "row가 존재하면 이미 표본 검증된 것"이라는 불변식만으로 표본 게이트를 구현(서버 로직 3줄 변경). `app.js`에 신규 `renderPercentileBadge(section, tier)` 추가 — `window.sb`로 `percentile_cache`를 직접 조회(신규 API 불필요, 기존 공개 읽기 RLS 재사용), row가 있으면 "📊 상위 N% (Tier X 이상 기록 기준)"을 비동기로 채워넣고 없으면 조용히 아무것도 표시 안 함(try/catch로 실패해도 결과 화면 전체에 영향 없음). Tier 채점 8개 테스트(두뇌나이/반응속도/숫자기억력/순서기억력/색각/논리력/충동억제/숏폼집중력) 결과 화면의 Tier 배지 바로 아래에 `id="percentile-badge-{section}"` placeholder div를 Python 스크립트로 8곳 일괄 삽입(줄 번호 기준, 역순 삽입으로 오프셋 문제 방지), `saveRanking()` 호출 직후 `renderPercentileBadge(...)` 호출도 동일하게 8곳 일괄 삽입. **검증**: `node --check`로 두 파일 문법 확인, Playwright로 로컬 정적 서버에서 두뇌나이 테스트를 실제로 완주시켜 결과 화면까지 도달 — 라이브 Supabase 프로젝트의 실제 `percentile_cache`(brain/D tier, percentile=100)를 조회해 배지가 정상 렌더링되고 콘솔 에러 없음을 end-to-end 확인. **주의**: 기존에 이미 쌓여있던 `percentile_cache` row는 5개 미만 표본이어도 다음 크론(KST 03:00) 또는 수동 workflow_dispatch 전까지는 그대로 남아있을 수 있음(정상 동작, 다음 집계 때 새 게이트가 적용되며 자연 정리됨) |
| 2026-07-04 | v0.1.0 | **홈·사이드바 UI 개편 완료 (버전 규칙 v0.1.0으로 전환).** Claude가 준 시안(Artifact)에 사용자가 항목별 번호/수정으로 답변하는 방식으로 6개 항목 확정 후 진행 — ①Tier 8개 테스트 시작화면에 접이식 "어떻게 하나요" 안내 카드(`guideCardHTML`) 신설(1차 시안엔 "왜 하나요" 문단도 있었으나 사용자가 삭제 요청해 단계 안내만 남김) ②홈 화면 내정보↔챌린지 순서 교체, 도구·테스트 그리드 16장 무채색 톤 통일, "바로 써먹는 도구"→"서비스 바로가기" ③홈 상단에 테두리로 구분된 섹션 신설해 날짜+오늘의 인생 한마디 배치("안녕하세요!" 인사말 자리를 대신함, 추후 내정보/로그인 확장 자리로 예약 — 전체 화면 적용은 과하다고 판단해 홈에만 적용) ④사이드바 부제 2줄+굵게+가시성 개선 ⑤"테스트"→"두뇌 테스트존" ⑥다크/라이트 아이콘 토글→사이드바 하단 두 버튼 선택 방식. `npx wrangler pages dev`+Playwright로 매 라운드 실제 스크린샷을 촬영해 확인받으며 진행, 콘솔 에러 없음. |
| 2026-07-04 | v0.1.1 | **PRD 10-4(꿈해몽 외부 API 연동) 3단계 전체 완료, 버전 규칙 v0.1.1.** 사용자가 다른 세션(Fable)에서 논의하던 3단계 구도(①데이터 확장 → ②로또 연동 버튼 → ③AI 폴백)를 이어와 "Fable까지는 필요없다"며 Sonnet 세션 진행으로 결정 변경(PRD.md 반영). AI 폴백에 쓸 API도 Claude 대신 **OpenAI**(이미 결제수단 등록됨, `gpt-4o-mini`+Structured Outputs)로 확정. **①데이터 확장**: AskUserQuestion으로 규모("최적으로 할 수 있는 만큼 먼저, 100~150개 신규, 이후 AI 비용 보고 재판단")와 순서(제안대로 데이터→로또버튼→AI)를 확인 후, 웹 지식 기반 수작업 집필로 신규 테마 100개를 5개 배치(각 20개 테마×4 variants)로 나눠 추가하고 배치마다 Node 스크립트 검증+커밋+푸시. 결과: dreamData 74→174개 테마, variants 146→546개. 검증 중 신규 테마 제목이 기존 variant 제목과 겹치는 사례 1건("눈사람을 만드는 꿈") 발견해 수정. **②로또 연동 버튼**: 꿈 모달에 `dreamGoToLotto()` 원클릭 버튼 신설, 기존 인프라(`last_dream_luckynum`/`lottoRunFortunePick`) 재사용. **③AI 폴백**: 검색 0건 시에만 노출되는 버튼으로 `functions/api/dream-ai.js`(Supabase 캐시 우선 → OpenAI 생성) 호출, `escapeHtml()` 신설로 프롬프트 인젝션발 XSS 방지. Playwright로 로또 연동과 AI 폴백(목업 서버 XSS 페이로드 테스트 포함) 전체 플로우 콘솔 에러 없음 검증. **3단계 배포 준비물(Supabase `dream_ai_cache` 테이블 + Cloudflare Pages `OPENAI_API_KEY`)도 같은 세션에서 사용자가 등록 완료** → 재배포 후 라이브 `curl`로 신규 키워드 `source:"ai"`/재호출 `source:"cache"` 정상 확인, Playwright로 라이브 전체 플로우까지 재검증. **PRD 10-4 완전 완료.** |
| 2026-07-04 | v0.1.1 | **도전장 공유 방식 변경 + 공유 카드 제목 잘림 수정.** ①`challengeFriend()`/`shareCompatibility()`가 쓰던 `shareResult()`가 `navigator.share()`를 우선 시도해 데스크톱(Windows 등)에서 연락처 공유 유도 화면이 뜨는 게 거부감을 준다는 사용자 스크린샷 피드백 → 항상 클립보드 복사로 통일(앱의 다른 공유 버튼과 동일 방식). ②카카오 공유 이미지의 제목 텍스트가 채팅창 미리보기에서 좌우로 잘리는 문제(v0.0.51의 설명 "···" 잘림과는 다른 지점) — `scripts/generate-share-cards.js`의 `.title`/`.tip` max-width를 760/700px로 축소, 착수 전 Artifact 진단 리포트로 재현·검증·사용자 컨펌 후 219장 전체 재생성. ③공유 링크를 열면 곧바로 테스트 화면으로 가던 것을 신규 공용 화면 `#shared-preview`로 한 단계 거치도록 변경, 카카오 카드와 동일한 이미지/제목/설명을 먼저 보여주고 버튼을 눌러야 실제 테스트로 이동. 상세는 위 "2. 기능 현황" 표 참고. |
| 2026-07-05 | v0.1.6 | **꿈해몽 공유 기능 확장: AI 해몽 카카오 공유 버튼 + 공유 링크에 실제 해몽 결과 재현.** 사용자가 AI 해몽 결과 스크린샷을 첨부하며 "AI 결과도 카카오톡 공유 버튼 만들어달라" + "공유 링크를 누른 사람이 실제 그 사람 꿈 결과값을 보게 해달라(다른 테스트처럼)"고 요청. ①`dreamRenderAiModal()`에 정적 해몽과 동일한 `renderIdentityShareRow` 공유 행 추가(이미지는 테마 인덱스가 없어 범용 `dream-0.jpg`). AI 응답을 카카오 버튼 onclick의 백틱 템플릿에 그대로 넣으면 기존 `escapeHtml()`(HTML 이스케이프)과는 별개로 백틱/`${...}`로 JS 템플릿을 탈출하는 신규 취약점이 생김을 발견 — `sanitizeForJsTemplate()` 신설(백틱→작은따옴표, `${`→`$ {`)로 kakaoTitle/kakaoDesc/shareText만 추가 방어. ②`dreamRenderModal()`/`dreamRenderAiModal()`의 공유 landingParams에 제목뿐 아니라 본문·행운의 색·행운의 숫자·오늘의 행동까지 전부 담고, `functions/share/[section].js`가 이를 JSON `extra`로 `#shared-preview`까지 전달 → 신규 `sharedPreviewDreamCardHTML()`이 실제 꿈 모달과 동일한 레이아웃으로 재현하고 그 아래 기존과 동일한 "나도 해보기" 버튼 배치(로또의 "실제 뽑은 번호 표시"와 같은 접근을 꿈해몽에도 적용). **검증**: `node --check` 통과, wrangler+Playwright로 정적 꿈/AI 해몽 양쪽 공유 URL 생성→랜딩 접속→shared-preview에 본문·행운색·행운숫자·오늘의 행동까지 정확히 렌더링 확인, 백틱+`${alert(2)}` 섞인 악의적 제목으로 공유 버튼을 실제 클릭해도 alert 미실행·콘솔 에러 없음(살균 정상 동작) 확인 |
| 2026-07-05 | v0.1.5 | **로또 직접 뽑기 물리 "분수 순환(에어믹스)" 재설계.** v0.1.4 바람 물리가 "공이 아래에 뭉치고, 어떤 공이 나올지 너무 예측된다"는 피드백 → Artifact 라이브 데모 2라운드(①현재 vs 균등증속 vs 돌풍카오스 → 사용자가 "뭉침" 지적+"바람은 계속, 공 더 작게" 재요청 ②연속 하단 송풍 3단계)로 사용자가 직접 보고 "2단계+공 크기 0.041" 확정 후 적용. **물리 구조 교체**: 바닥 중앙 노즐이 쉬지 않고 공기를 뿜는 연속 제트(수평 가우시안 폭 R×0.5, 세기 매 프레임 0.4~1.6배 랜덤 요동, 아래쪽일수록 강함) + 노즐 위치가 비주기 이중 사인으로 좌우 스윙(패턴 고착 방지) + 중력 강화(0.05→0.09, 옆으로 밀려난 공이 벽을 타고 떨어지게) + 상시 난기류(±0.08) + 감쇠 완화(0.99→0.995) — 공이 가운데로 솟구쳤다 양옆으로 쏟아져 다시 빨려 들어가는 팝콘식 순환이라 바닥 뭉침이 없어짐. 옆바람(crossWind)/공별 위상(phase) 소용돌이 코드는 제거. 공 크기 비율 0.052→0.041(−21%), 속도 상한 r×0.45→0.75. **검증**: `node --check` + wrangler+Playwright로 공 45개·크기 26px(310px 구체×0.041) 정합, 1초간 평균 이동 65px(활발), 상반부 공 31/45(뭉침 해소), 1개 뽑기→트레이 안착, 6개 한번에 뽑기→게임 완료→다음 게임 전환까지 콘솔 에러 없이 통과 |
| 2026-07-04 | v0.1.4 | **로또 직접 뽑기 애니메이션·기계 외형 전면 개편.** 사용자가 실제 동행복권 추첨기 사진 3장을 레퍼런스로 첨부하며 4가지 요청. ①**물리 방식 교체**: 회전 패들이 공을 휘젓던 방식(너무 일관되고 빠름)을 실제 추첨기 같은 "바람" 방식으로 교체 — 하단 에어제트(아래쪽일수록 강한 상승기류, 세기 매 프레임 랜덤) + 천천히 방향이 바뀌는 옆바람(sin 이중 합성) + 공마다 위상(phase)이 다른 소용돌이 + 미세 난기류 + 공기저항 감쇠. 속도 상한도 r×0.75→r×0.45로 낮춰 차분하게. 패들 관련 코드(상태 필드/HTML/CSS/충돌 로직) 전부 제거. ②**2D 기계 외형**: 사진 참고해 원형 프레임(ring)+구체 하단 배출구(hole)+유리 배출 튜브(neck)+16° 경사 레일(ramp)+받침대(stand, clip-path 사다리꼴)로 구성한 `#lottodraw-machine` 컨테이너 신설, 공이 뽑히면 구체(overflow hidden) 밖으로 엘리먼트를 기계 컨테이너로 옮겨 심고 배출구→튜브 낙하→레일을 회전하며 굴러 내려감→트레이 점선 슬롯 안착 순서로 CSS transition 체이닝(`lottodrawAnimateExit`, 슬롯 좌표는 getBoundingClientRect로 실측). ③**버튼 개편**: "6구 고속 추출"→"⚡ 6개 한번에 뽑기"(구간 40% 단축+간격 430→280ms로 약 1.5배 빠르게, 통로에 여러 공이 줄지어 굴러가도록 `pending` 카운터로 슬롯 선예약), "🎯 5게임 한번에 다 뽑기" 신설(게임 완료 시 자동 재충전 350ms 후 다음 게임 연속 추첨, 영수증까지 직행 — 전체 약 11.5초). ④**"결과 이미지 공유" 버튼 제거**: 여전히 동작하지 않는다는 피드백으로 영수증 화면에서 삭제(`lottodrawShareImage()` 함수째 제거, "이미지 저장"+카카오 공유는 유지. 조합기 쪽 이미지 공유 버튼은 다운로드 폴백이 있어 유지). **검증**: wrangler+Playwright로 기계 부품 6종 렌더링, 공 45개 랜덤 부유(transform 변화 실측), 1개 뽑기 시 레일 위 굴러가는 중간 프레임 캡처, 5게임 자동 완주(11.5초)→영수증 도달, 영수증 버튼에서 "결과 이미지 공유" 부재까지 콘솔 에러 없이 통과 |
| 2026-07-04 | v0.1.3 | **로또 번호 조합기/직접 뽑기 UX 개선 6건.** 사용자가 실제 사용 스크린샷(동행복권 공식 사이트의 "최근 5회" 당첨결과 화면)을 레퍼런스로 첨부하며 6가지 요청. ①**공/번호 크기 확대**: 조합 결과 공 `w-8→w-11`(+37%), 통계패널·직접뽑기 트레이(`w-9→w-12`)/보드(`w-7→w-9`)/친구배너(`w-6→w-7`) 전부 확대, 물리 구체 공 반지름 비율도 0.043→0.052로 확대. ②**통계기반 최근 5회차 표시**: `functions/api/lotto-stats.js`와 클라이언트 미러 직접조회 폴백 양쪽에 `recentRounds`(최근 5회, 최신순) 필드 추가, `lottoRenderStatsPanel()`→`lottoStatsPanelHTML()`로 리팩터해 회차 카드 5장을 나열(레퍼런스 스크린샷과 동일 레이아웃). ③**운세·꿈 연동 숫자 표기 강화**: `lottoRunFortunePick()`이 운세/꿈 출처를 분리 파싱해 "이번 조합에 반영된 행운숫자" 패널에 출처별(🔮/🌙)로 나열하고, 실제 조합 결과에서도 해당 번호에 금색 테두리(`ring-amber-300`)로 하이라이트. ④**다시 뽑기 버튼**: 완전랜덤/직접지정/운세연동 3개 모드(통계기반 제외)에 재조합 버튼 추가. ⑤**이미지 공유를 4개 모드 전체로 확대**: 신규 `lottoBuildResultCanvas()`(조합 카드 Canvas 렌더링)+`lottoShareResultImage()`를 4개 모드 공용 `lottoRenderGames()`에 통합, `navigator.share` files로 사진 공유. ⑥**웹 링크 공유를 카카오 이미지+링크 공유로 전환**: 신규 공용 이미지 `share-cards/lotto-share.jpg`(범용 홍보 카드, 숫자는 매번 달라 개인화 대신 카카오 카드 텍스트에 실제 번호 표기) 제작, `functions/share/[section].js`에 `lotto`(조합기, 일반 랜딩)/`lottodraw`(직접뽑기, 뽑은 번호를 `extra` 파라미터로 shared-preview까지 전달 — `sharedPreviewProceed()`가 `App._lottodrawSharedDrawn`에 복원해 기존 친구 배너 로직 그대로 재사용) 두 항목 신설, 색각 테스트 등 기존 15개 테스트와 동일한 "이미지+제목+설명 미리보기 → 버튼 클릭 → 실제 이용" 플로우로 통일. **버그 수정**: 직접 뽑기 게임의 "결과 이미지 공유"가 일부 브라우저에서 안 되는 문제를 진단 — `canvas.toBlob()` 비동기 콜백 안에서 `navigator.share()`를 호출하면 iOS Safari 등이 user-activation을 잃는 게 원인으로 판단, 영수증 렌더링 시점에 미리 Blob/File을 만들어 캐싱해두고 클릭 시 즉시 사용하도록 수정(PNG→JPEG 전환). 기존 `lottodrawShareLink()`/`lottodrawRefId()`(링크 복사 전용, 리퍼럴 UUID)는 새 카카오 공유 방식으로 대체되며 제거. **배포 후속 수정 3건(같은 버전)**: ⓐ`/api/lotto-stats`가 배포 전 24시간 엣지 캐시(recentRounds 없는 옛 응답)를 계속 서빙 → 캐시 키를 `lotto-stats-v2`로 버전 업해 우회(앞으로 응답 스키마 바뀔 때마다 키 버전 올릴 것) ⓑ공유 카드 이미지의 장식 공이 5개뿐이라 "로또인데 왜 5개냐"는 지적 → 6개(7·14·21·27·33·41)로 재생성 ⓒ카톡 공유 링크를 열면 예시 그림(범용 홍보 이미지)만 보이고 실제 뽑은 번호가 안 보인다는 지적 → 조합기 공유 URL에 `drawn` 파라미터를 추가하고(직접뽑기는 이미 있었음) 프리뷰 화면(`initSharedPreview`)이 lotto/lottodraw 공유일 때 범용 이미지 대신 실제 뽑은 번호 5게임을 공 UI로 렌더링(`sharedPreviewLottoBallsHTML`, 직접뽑기는 A~E 라벨), 조합기에도 클릭 후 진입 시 "친구가 조합기로 뽑은 번호예요" 배너 신설(`lottoSharedBannerHTML`, `App._lottoSharedDrawn`) — og:image 자체는 서버에서 동적 이미지 생성이 필요해 범용 이미지 유지(실제 번호는 카카오 카드 텍스트+프리뷰 화면이 담당). **검증**: `npx wrangler pages dev`+Playwright로 4개 조합 모드(공 크기·하이라이트·다시뽑기·이미지공유·카카오버튼 렌더링)와 통계기반 5회차 카드, `/share/lotto?drawn=...`·`/share/lottodraw?drawn=...` 랜딩→미리보기에 실제 번호 공 UI 표시→클릭 시 각각 조합기(친구 배너 포함)/직접뽑기 친구 배너 정상 복원까지 전체 플로우 콘솔 에러 없이 통과. **영수증 이미지 법적 리스크 완화(같은 버전 후속)**: 사용자가 실제 복권처럼 보일 수 있다는 우려로 3가지 수정 요청 — ①제목 "LOTTO 6/45"→"행운번호뽑기 6/45" ②회차 표기 삭제(발행일/추첨일만 유지) ③시드 기반 결정론 바코드+숫자열 완전 삭제, 그 자리에 있던 오락용 고지 문구(`본 영수증은 오락용 이미지이며 실제 복권이 아닙니다 / 행운을 빌어요! 과몰입 연구소`)를 11px→22px(2배)로 키워 대신 배치. Canvas 높이(H)를 컨텐츠 감소분에 맞춰 575→540으로 재조정(첫 시도 500은 마지막 줄이 캔버스 경계에 잘려 Playwright 스크린샷으로 발견, 540으로 재조정 후 정상 확인) |
| 2026-07-04 | v0.1.2 | **로또 직접 뽑기 게임 완료 (PRD 10-6).** 사용자가 대기 중이던 레퍼런스(Gemini 작성 프롬프트, Vibe 1~4)를 제공하며 착수 — "꼭 똑같이 따를 필요 없이 적절히 참고" 지시에 따라 4개 Vibe 전부 반영하되 프로젝트 컨벤션에 맞게 조정. 로또 조합기에 5번째 버튼 추가 → 신규 섹션 `#lottodraw`(index.html `section-lottodraw` + `sectionInits`에 `initLottodraw` 등록). 유리 구체 물리 추첨기(45개 공, 중력·벽·패들·공끼리 충돌을 rAF 루프로 직접 연산, 외부 라이브러리 없음), 듀얼 컨트롤(1개 뽑기/6구 고속 추출), 5게임 A~E 시퀀스(게임마다 45개 재충전), 완료 시 Canvas 영수증(분홍 감열지풍, 시드 기반 바코드, "가상" 표기) + 이미지 저장/`navigator.share` files 이미지 공유(미지원 시 저장 폴백)/`?drawn=...&ref=UUID` 링크 복사 3종 CTA. 공유 링크 수신 측은 친구 번호 배너 표시(쿼리스트링 선캡처는 v0.1.1 shared-preview에서 확립한 패턴 재사용). 기존 인프라(`lottoBallClass`/`playSound`/`copyToClipboard`/`showToast`) 최대 재사용. **검증**: Playwright로 진입 버튼→추첨기 렌더링(공 45개, 패들 회전, 공 움직임 확인)→1개 뽑기→고속 추출로 5게임 완주→영수증 이미지·CTA 4종 렌더링→링크 복사 클립보드 포맷 검증→복사된 링크로 새 페이지 진입 시 친구 번호 배너(30개 공) 표시→이미지 저장 다운로드 이벤트까지 전체 플로우 콘솔 에러 없이 통과 |
| 2026-07-04 | v0.0.54 | **Phase 3 로드맵 10-2/10-3(사운드 재설계+게임성 강화) 완료.** BGM 여부를 AskUserQuestion으로 재확인 → "BGM 없이 효과음만 개선" 확정. `playSound()`를 단순 삐- 단일톤에서 퀴즈쇼풍 "딩동"/완만한 하강음으로 교체, Tier S 전용 축하 팡파레 신규 추가, 사운드 커버리지를 15개 테스트 중 5개에서 Tier 8개 테스트 전체로 확대. 사이드바/모바일 헤더에 🔊/🔇 음소거 토글 신설(`localStorage.sound_enabled`). 콤보 카운터(3연속부터 "🔥N연속!")와 3→2→1→시작! 카운트다운(6개 테스트, 이미 도입부 있는 숫자·순서기억력은 제외)을 신규 추가해 "과하지 않은 선"의 재미 요소 확보. **부수 발견**: 사이드바 하단 버전 표시가 v0.0.46에서 8버전째 멈춰있던 걸 발견해 수정, 앞으로 버전 관리 규칙에 반영. **검증**: wrangler+Playwright로 카운트다운 프레임 타이밍, 콤보 문구 3연속 기준 정확성, 음소거 토글 아이콘/localStorage 동기화, 전체 플레이 콘솔 에러 없음까지 통과 |
| 2026-07-04 | v0.0.53 | **Phase 3 로드맵 10-1(HELL 난이도) 완료 + B안(난이도별 완전 분리) 채택.** 사용자가 A/B안 중 B안(퍼센타일/친구대결을 난이도별로 완전 분리)을 선택하며 "원래 있는 데이터에 난이도 하나 추가하는 건데 왜 커지냐"고 지적 → `test_results.payload`가 JSONB라 스키마 변경 없이 difficulty를 넣을 수 있음을 재확인하고 B안으로 확정. Tier 8개 테스트에만 기존 3단계보다 압도적으로 어려운 HELL 난이도 추가(색각 델타8·6x6·2초, 숏폼집중력 No-Go 50% 등), MBTI 등 정성적 테스트는 제외. 시작화면에 검정·빨강 톤 전용 버튼+`confirm()` 경고 통과해야 진입. **부수 발견·수정**: seqmem 그리드클래스가 3/4단만 처리하는 삼항연산자, colorvision 룩업엔 6열이 없어 HELL의 5x5/6x6이 그대로 깨졌을 버그를 사전에 발견해 수정. `saveRanking`/`syncResultToSupabase`/`renderPercentileBadge`/`renderChallengeButton`/`renderChallengeCompareCard` 8개 호출부 전부에 difficulty 인자 추가, `percentile-refresh.js`를 section×difficulty×tier 3중 집계로 재작성, `percentile_cache`에 difficulty 컬럼+복합 UNIQUE 제약 SQL을 사용자에게 안내(제약명 자동탐색하는 방어적 SQL). 친구 대결은 도전장에 난이도(`d`) 필드를 추가해 다른 난이도끼리는 승패 대신 "난이도가 달라 직접 비교는 어려워요" 메시지로 대체, 배너에도 난이도 힌트 추가. **검증**: wrangler로 8색 스트룹/5x5/6x6 그리드 스크린샷 확인, HELL 완주 플로우, 어려움↔HELL 불일치 대결과 같은 난이도 정상 대결 둘 다 Playwright로 통과, 콘솔 에러 없음(SQL 마이그레이션 전까지 퍼센타일 조회만 try/catch로 안전하게 실패) |
| 2026-07-04 | v0.0.52 | **Phase 3 로드맵 10-5(로또 공 색상 다양화) 완료.** PRD.md "10. Phase 3 확장 로드맵"에서 사용자가 확정한 진행 순서(5→1→2·3→6→4)의 첫 항목. 조합 결과 공이 전부 `bg-violet-700`으로 고정돼 색이 안 다채롭던 문제를, 실제 동행복권 공식 볼 색상 규칙(1~10노랑/11~20파랑/21~30빨강/31~40회색/41~45초록)을 그대로 반영하는 `lottoBallClass(n)` 신설로 해결. 조합 결과·직전 회차 당첨번호 패널 양쪽에 적용, 보너스 볼은 구간색 유지하며 금색 테두리로 구분. Playwright로 완전랜덤 5게임 전색상 등장 확인과 실제 통계 패널 스크린샷 검증, 콘솔 에러 없음 |
| 2026-07-04 | v0.0.51 | **공유 UI 개선: 카카오톡 버튼 분리 + 설명 잘림 버그 수정.** 사용자가 실제 라이브 스크린샷(색각 결과 화면 아이콘 행, 카카오톡 채팅창의 잘린 미리보기)을 보내며 지적. 카카오 버튼을 아이콘 행에서 분리해 노란색 전체너비 대형 버튼("💬 카카오톡으로 공유하기")으로 독립시켜 "친구에게 도전장 보내기"와 같은 무게감으로 최우선 배치(신규 `shareKakaoButtonHTML()`, 15개 테스트+친구대결 VS 카드까지 전부 일관 적용). 카카오 피드 설명이 길면 "···"로 잘리는 문제를 신규 `kakaoDesc` 파라미터로 해결 — Tier 8개는 이미 짧은 `resultLabel`을, 성향형 7개는 등급/타입만 담은 짧은 문자열을 쓰고, 재미 요소(동물카드 이름 등)는 잘리지 않는 카카오 "제목" 필드로 이동. **부수 발견**: v0.0.50에서 작성한 6개 테스트 공유 문구의 큰따옴표가 `onclick="..."` HTML 속성과 충돌해 버튼이 깨지는 버그를 발견해 한국어 낫표(「」)로 전량 교체 수정. Playwright로 색각/MBTI 카카오 버튼의 실제 onclick 파라미터를 파싱해 짧고 정상 작동함을 확인, 도전장 플로우까지 재검증, 콘솔 에러 없음 |
| 2026-07-04 | v0.0.50 | **공유 카드 리디자인을 성향형 7개 테스트(MBTI/ADHD/인싸력/속담/물가/운세/꿈해몽)로 확장 완료.** 사용자가 "동물카드 없는 것들도 추천 이미지 셋트 구축, 친구/가족에게 말하듯 멘트, 항상 재미요소" 요청. 결과 정체성 개수만큼 1:1 공유 이미지 118장 추가(MBTI 16+ADHD 4+인싸력 4+속담 4+물가 4+운세 12+꿈해몽 74=총 219장, MBTI는 16Personalities 4기질군 색상 관례 차용, 나머지는 테스트 성격에 맞는 고유 컬러 테마, 꿈해몽 74개는 제목/키워드 기반으로 이모지 수작업 배정). 공유 문구를 "너도 확인해봐" 톤에서 "나 오늘 운세 이렇대~", "나 어제 이런 꿈 꿨어!" 같은 친구 말투로 전면 교체(사용자 예시 그대로 반영). `functions/share/[section].js`를 section 자체로 분기하도록 재구성, `app.js`에 `shareIconRowHTML()`(공통 마크업 추출)/`renderIdentityShareRow()`(성향형 7개 전용) 신설해 Tier 8개와 동일한 아이콘 행 일관성 유지. 꿈해몽은 결과화면이 없어 `dreamRenderModal()`에 새로 공유 행 추가. **검증**: wrangler로 7개 섹션 og 태그 confirm, Playwright로 MBTI/운세/꿈해몽/인싸력 실제 완주+공유행+링크복사 검증, 콘솔 에러 없음. 배포 중 운세/꿈해몽의 "OO 님의" 문구가 닉네임 개념이 없어 어색했던 것을 발견해 자연스러운 표현으로 수정 |
| 2026-07-04 | v0.0.49 | **공유 카드 리디자인 완료 (이미지 있는 공유 + 아이콘 행 + 도전장 재설계).** 사용자가 라이브 카카오톡 공유 스크린샷을 보고 "글씨만 나가서 안 눌린다"고 지적, 참고 사이트(smore.im)와 레퍼런스 이미지 분석 후 AskUserQuestion으로 이미지 스타일(그라데이션+이모지+워드마크, 100장으로 확대)과 도전장 재설계 방향(Tier로 승패 유지+원점수 텍스트 노출+공용 VS 이미지)을 확인받고 진행. `functions/share/[section].js` 신설(공유 랜딩 페이지, og:image/title/description 동적 생성 후 실제 사용자는 `#{section}`으로 즉시 리다이렉트 — 도전장은 `vs=` 페이로드까지 복원). `scripts/generate-share-cards.js`(Playwright, 개발환경에 `fonts-noto-cjk` 설치 후 사용)로 `AppData.animalCards` 100개 조합 전부를 티어별 그라데이션 5색 카드 이미지로 렌더링해 `share-cards/`에 커밋(총 4.2MB) + 공용 VS 이미지 1장. `app.js`에 `buildShareLandingUrl()`/`shareToKakaoCard()`(feed 템플릿)/`shareFacebook()`/`shareTwitter()`/`shareBand()`/`renderShareRow()` 신설, 기존 "결과 공유하기"+"카카오톡 공유" 두 버튼을 아이콘 행 하나로 통합(8개 Tier 테스트). `renderChallengeCompareCard()`에 section/닉네임 파라미터 추가해 대결 결과를 이미지로 공유하는 버튼 신설. 이번 라운드는 Tier 8개 테스트만 대상(성향형 7개는 다음 확장 후보), 홈 아바타 기능은 완전 별개 작업으로 백로그만 기록. **검증**: `npx wrangler pages dev`로 Functions 포함 로컬 구동, curl로 result/challenge/verdict 3개 타입 og 태그 확인, Playwright로 색각 테스트 완주+링크복사 검증과 두뇌나이 도전장 생성→새 컨텍스트 진입→vs 페이로드 복원→완주→VS 카드+공유버튼까지 전체 플로우 콘솔 에러 없이 통과 |
| 2026-07-04 | v0.0.48 | **Stage E(동물 비유 결과 카드) 완료.** 착수 전 AskUserQuestion으로 적용 범위(Tier 채점 8개 테스트만, 추천안)와 조합 규모(100개, PRD 최소치)를 확인. `data.js`에 `AppData.animalCards` 신설 — 테스트별 실제 동물 습성에서 착안한 2~3개의 고정 "동물 정체성"(두뇌나이=부엉이/코끼리/돌고래, 반응속도=치타/매/캥거루, 숫자기억력=까마귀/다람쥐/문어, 순서기억력=꿀벌/앵무새/침팬지, 색각=사마귀새우/카멜레온, 논리력=여우/개미, 충동억제=곰/늑대, 숏폼집중력=개구리/고양이)를 두고 S~D 등급별로 서식지·수식어·행동팁 톤만 달라지는 구조로 정확히 100개 조합 작성(4개 테스트는 등급당 3개=60개, 나머지 4개는 등급당 2개=40개). `app.js`에 `pickAnimalCard(section, tier)`(등급별 풀에서 `pickOne()` 재사용해 랜덤 선택)와 `animalCardHTML(card)`(카드 UI) 신규 추가, 8개 테스트 결과 화면의 Tier 배지 바로 아래에 카드를 삽입하고 기존 `shareText` 맨 앞에 "🐾 나는 {title}!" 한 줄만 붙여 Web Share/카카오톡 공유에 자동 포함(신규 공유 버튼·URL 로직 없이 기존 인프라 그대로 재사용 — 카카오 공유는 이미 `location.href`를 포함하고 있어 "유입 링크" 요건은 별도 작업 불필요). **검증**: Node로 8개 섹션×5등급 구조·필드 완결성과 총 100개 정확히 일치함을 기계 검증, Playwright로 두뇌나이(3개 슬롯 테스트)와 논리력(2개 슬롯 테스트)을 실제로 완주시켜 카드가 정상 렌더링되고 콘솔 에러 없음을 확인 |
| 2026-07-05 | v0.1.7 | **숫자/순서 기억력 첫 문제 카운트다운 추가 + 숏폼 집중력 테스트 메커니즘 전면 재설계.** 사용자가 "숫자/순서 기억력 첫 문제가 너무 빨리 나온다"와 "충동억제와 숏폼 집중력이 너무 똑같다"는 2가지를 지적. **①카운트다운**: `memdigit`/`seqmem` 둘 다 첫 라운드(`state.round === 0`)에서만 신규 `memdigitCountdown()`/`seqmemCountdown()`으로 "3→2→1→시작!"(0.5초 간격, 기존 `showCountdownThenStart()`와 같은 톤)을 보여준 뒤 0.3초 여유를 두고 첫 문제를 노출하도록 수정, 2라운드부터는 기존처럼 바로 진행(매 라운드 카운트다운은 지루해서 제외). **②숏폼 집중력 재설계**: 구현 전 WebSearch로 근거 조사 — "숏폼 노출 후 평균 집중 지속시간 47초"·"팝콘브레인" 통계(사이언스타임즈)와 실제 임상 주의력 검사 CCPT(좌우 무작위 위치의 버섯=타겟/꽃=방해자극 중 타겟만 탭)를 참고해 샘플 설계안을 먼저 사용자에게 제시하고 승인받은 후 진행. 기존엔 충동억제(Go/No-Go)와 완전히 동일한 단일 원 자극 엔진의 리스킨이었던 것을, `SHORTFOCUS_CATEGORIES`(15개 카테고리) 기반의 **격자 카드 시각 탐색**(라운드마다 목표 카테고리 제시 → 여러 카드 중 일치하는 것만 탭, 목표 부재 트라이얼 포함, 난이도별 "가짜 광고" 트랩 카드 등장)으로 전면 교체 — `app.js`의 `initShortfocus`/`renderShortfocusView`/`shortfocusStart`/`shortfocusBeginRound`/`shortfocusTap`/`shortfocusTimeUp`을 전부 재작성, 신규 `shortfocusCardHTML()` 추가. 결과 화면에 전반부/후반부 반응속도를 비교하는 신규 "집중력 저하도"(vigilance decrement) 지표를 추가해 충동억제와 차별화. **검증**: `node --check` 통과, Playwright로 카운트다운 타이밍 프레임 단위 확인(memdigit/seqmem 둘 다), 숏폼 집중력은 easy/hard/HELL 격자 크기·카드 수 확인과 상태 직접 조작으로 정답/오답 3가지 분기(다른 콘텐츠 탭/광고 트랩 탭/타겟부재 라운드 오탭) 전부 콘솔 에러 없이 통과 |
| 2026-07-05 | v0.1.8 | **공유 카드 이미지 잘림 근본 해결 — 카카오 1:1 크롭 기준 "정사각 안전영역" 재설계.** v0.1.1의 760px 축소로도 여전히 잘린다는 사용자 스크린샷 재신고 → 카카오톡이 1200×630 원본을 약 1:1(중앙 ~630px)로 크롭해 보여준다는 걸 실측으로 확인(좌상단 Tier 배지가 통째로 안 보였던 것도 같은 원인). `scripts/generate-share-cards.js`의 카드 레이아웃을 중앙 540px 안전영역 기준으로 재설계 — 배지 상단 중앙 이동, 제목/팁 `word-break:keep-all` 2~3줄 배치, 폰트·이모지 크기 재배분, 안전영역 자동 검증+scale 자동 축소 로직 내장. `generate-lotto-share-card.js`도 동일 규칙(공 6개 행 폭 축소). 320장 전체 재생성(전량 검증 통과, 1200×630 유지라 og:image 소비처 영향 없음). **검증**: 1:1 크롭 시뮬레이션(object-fit:cover)으로 신고 카드+최장 제목/부제목+MBTI/운세/꿈/VS/로또 8장 전부 잘림 없음을 스크린샷으로 확인 |
| 2026-07-05 | v0.1.9 | **꿈해몽 공유 카드 이미지를 범용 티저 1장 + 고정 카피로 통일.** 사용자가 "무슨 꿈을 공유하든 항상 '하늘을 나는 꿈'으로 나온다"고 지적 — 원인은 AI 해몽(테마 인덱스 없음) 공유가 항상 하필 인덱스 0번 테마 이미지(`dream-0.jpg`="하늘을 나는 꿈")로 폴백하던 것. "꿈 내용은 계속 바뀌니 매번 그림을 바꿀 수 없다"는 사용자 판단에 따라 정적 테마 해몽까지 포함해 전체를 범용 카드 1장으로 통일하기로 결정. 신규 `share-cards/dream-share.jpg`(달+말풍선, 기존 `DREAM_THEME` 색상 재사용)를 `scripts/generate-dream-generic-card.js`(안전영역 검증 로직 포함)로 생성, `dreamRenderModal()`/`dreamRenderAiModal()`의 카카오 카드와 `functions/share/[section].js`의 dream 분기를 고정 카피(제목 "나 이런 꿈 꿨어" / 설명 "너도 꿈 꾼거 있으면 찾아볼래?")+이 이미지로 교체(테마별 174장은 삭제하지 않고 보존, 현재는 미사용). 공유 클릭 후 `#shared-preview` 화면은 그대로 실제 꿈 상세를 재현(로직 변경 없음) — "미끼 카드"와 "클릭 후 진짜 결과"의 역할 분리가 명확해짐. **검증**: `node --check` 통과, wrangler+curl로 정적/AI 두 경로 모두 og:image가 `dream-share.jpg`로 통일되고 og:title/description이 고정 카피로 나옴을 확인, Playwright로 `#shared-preview`가 여전히 실제 꿈 상세를 정확히 재현함을 스크린샷으로 재확인 |
| 2026-07-05 | v0.2.0 | **홈 화면 "오늘의 인생 한마디" 카드 크기·색상 조정.** 사용자가 "칸을 너무 크게 차지하고 바로 아래 '내 정보' 카드와 색이 너무 비슷하다"고 지적 — 둘 다 violet/indigo 계열 그라데이션을 쓰고 있어 구분이 안 갔던 것을 amber 계열로 교체해 시각적으로 분리, 폰트·패딩·여백을 축소해 세로 공간도 줄임(`index.html` 홈 섹션 상단 카드 1곳만 수정). amber 900/300/400은 기존에 이미 `tailwind.config`에 라이트모드 remap이 돼있어 별도 테마 작업 불필요. **검증**: 정적 서버+Playwright로 다크/라이트 양쪽 스크린샷을 찍어 "내 정보" 카드와 색이 명확히 구분되고 카드 높이가 줄어든 것을 확인 |
| 2026-07-05 | v0.2.1 | **카카오 앱 개인 계정 이전 + 도메인 이전 착수.** 사용자가 기존 카카오 앱이 회사 계정에 잘못 연결돼있는 것과, `www.gwamol-lab.xyz` 도메인 구매(진행 중)를 알려와 함께 진행. 개인 계정으로 새 Kakao Developers 앱 생성 절차(앱 만들기 → 플랫폼 키 > JavaScript 키 → JavaScript SDK 도메인 + 제품 링크 관리 > 웹 도메인 등록, 최신 공식 문서로 재확인)를 안내, 사용자가 새 앱 생성 후 전달한 JavaScript 키로 `kakao-share.js`의 `Kakao.init()` 교체(이 프로젝트에서 카카오 키를 쓰는 유일한 지점). 도메인 이전과 시점이 겹쳐 이중 작업을 피하려고, 새 앱의 두 도메인 등록란 모두에 기존 `testseries1.pages.dev`와 이전 예정 `www.gwamol-lab.xyz`를 동시에 등록하도록 안내(도메인 전환 시 카카오 쪽 재작업 없음). 도메인 전환 자체에 필요한 작업을 조사해 정리 — 코드에는 하드코딩된 도메인이 없고(`og:url`은 origin 기준 자동 생성), Supabase도 익명 인증이라 도메인 변경과 무관함을 확인, 실제로 남는 작업은 Cloudflare Pages 커스텀 도메인 연결뿐임을 CLAUDE.md에 기록. **검증**: 정적 서버+Playwright로 새 키가 `Kakao.isInitialized() === true`로 정상 초기화됨을 확인 |
| 2026-07-05 | v0.2.2 | **애드센스 심사 전 필수 준비물(개인정보처리방침/이용약관/푸터) + 비즈니스 이메일 서식 완료.** 사용자가 다른 사이트(달다구리) 푸터 스크린샷을 보여주며 "이건 애드센스 하기 전에 먼저 해야 될 작업"이라고 지적. 신규 `#section-privacy`/`#section-terms` 정적 섹션을 실제 서비스 구조(Supabase/카카오/OpenAI/AdSense 등 제3자 서비스 목록, 쿠키·광고 안내, 문의처)에 맞게 작성해 추가, `sectionInits`에 no-op로 등록(직접 링크 진입 시 home으로 안 튕기게). 푸터를 로고+태그라인+저작권+약관 링크+문의 이메일 구조로 재구성. 별개로 요청받은 **비즈니스 이메일 서식**은 코드와 무관한 운영 문서라 신규 `BUSINESS_TEMPLATES.md`(제휴 문의용/콘텐츠 협업 요청용 2종, 대괄호 채워쓰기 방식)로 분리 작성, 애드센스 심사 전 체크리스트도 같은 문서에 정리. **주의**: 정책 문서는 일반적 웹서비스 기준 초안이라 사업자 등록 여부 등에 따라 검토 필요함을 문서 내 명시. **검증**: `node --check` 통과, 정적 서버+Playwright로 푸터 링크→약관 페이지 렌더링→플로팅 뒤로가기 버튼까지 확인 |
| 2026-07-05 | v0.2.3 | **Phase 4 수익화 로드맵 Now 4건 구현 완료.** PRD.md "11. Phase 4"에서 분류해둔 Now 항목(메뉴 스캐폴드/제휴배너/심리테스트존 샘플/밸런스게임 샘플)을 착수 전 MD 파일부터 정리(PRD.md 11장에 Claude 제안 5개도 Later로 포함, "신규 콘텐츠는 공유 필수" 원칙 명시)한 뒤 진행. `data.js`에 `psychTests`/`balanceGames` 신규 추가(심리테스트존 샘플 "카톡 답장 속도 테스트" 5문항 3등급, 밸런스게임 샘플 "평생 안 아프기 vs 지금 10억"), `app.js`에 `initPsychtest`/`initBalance`/`initFamily`/`initFortuneExt` 등 신규 섹션 로직 추가, 사이드바에 4개 메뉴 신설(`badge-soon-nav` 배지 CSS 신규). 심리테스트존/밸런스게임은 기존 `renderIdentityShareRow()`·카카오공유 인프라를 그대로 적용(신규 `scripts/generate-phase4-share-cards.js`로 공유 이미지 4장 생성, `functions/share/[section].js`에 psychtest/balance 분기 추가) — 사용자가 재확인한 "바이럴 없이는 의미 없다" 원칙을 코드로 반영. 제휴 배너는 14개 테스트가 공유하는 `renderPlaceholderUI()` 한 곳에 슬롯 추가(placeholder href, 실 링크는 쿠팡파트너스 가입 후 교체). 참여수/공감수는 다른 곳의 "더미 표시, 추후 실데이터 연동" 관례를 그대로 재사용(로컬 증분 + 고정 base). **검증**: `node --check` 통과, `npx wrangler pages dev`+Playwright로 심리테스트존/밸런스게임 전체 플로우(피드→포스트→진행→결과→공유), 가족오락관(10개 전부 준비중)/사주타로궁합(3개 준비중) 화면, 모바일 사이드바 신규 메뉴, `/share/psychtest`·`/share/balance` og 태그, 기존 반응속도 테스트에 제휴배너 정상 삽입까지 다크/라이트 양쪽 콘솔 에러 없이 확인 |
| 2026-07-05 | v0.2.4 | **심리테스트존 카테고리 구조 재편 + 사이드바 순서 재배치 + AI 콘텐츠 생성 프롬프트.** 사용자가 라이브 화면 스크린샷을 보며 메뉴 순서가 뒤섞였다고 지적, 새 순서(홈→두뇌테스트존→심리테스트존→밸런스게임→오늘의운세→사주타로궁합→꿈해몽→로또, 가족오락관은 목록에 없어 맨 끝 배치 후 확인 요청)로 재배치. 심리테스트존을 poomang.com 실제 분석 기반으로 `character`/`trait`/`taste`/`national` 4개 카테고리 그룹(정원 3개=오픈 1+잠금 2)으로 재구성, 두뇌테스트존과 동일한 `nav-group` 패턴 재사용(그룹 토글 로직은 공용 루프로 일반화). 문항마다 `options` 필드를 직접 명시하도록 스키마 변경(기존엔 모든 문항이 카톡 전용 선택지를 하드코딩 공유하던 문제를 발견해 근본 수정), 혈액형처럼 "선택=결과"인 문항도 같은 엔진으로 지원. 콘텐츠 4개 완성(기존 "카톡 답장 속도"→"칼답러 vs 잠수러 테스트"로 신조어+FOMO 톤 업그레이드, 신규 "나의 전생 동물 테스트"/"짠테크형 vs 플렉스형 테스트"/"혈액형 성격 테스트"). 사용자가 "앞으로 AI로 게시물 생성해서 올리고 싶다"고 요청해 신규 `CONTENT_PROMPTS.md`(카테고리 분류·문항 옵션 스키마·톤·금지사항 포함 프롬프트) 작성, `generate-phase4-share-cards.js`를 데이터 직접 순회 방식으로 리팩터. **버그 발견**: 신규 4등급 테스트의 공유 이미지가 전부 A로 나오는 문제 — `functions/share/[section].js`의 grade 정규식이 katokspeed 3등급 기준(`/^[ABC]$/`)으로 하드코딩돼 있던 것이 원인, `/^[ABCD]$/`로 수정. **검증**: `node --check` 통과, wrangler+Playwright로 4개 카테고리 피드 전환, 전생동물/짠테크/혈액형 전체 플레이+등급 정확성, "목록으로" 버튼의 카테고리 기억, 신규 테스트 og:image 정상화까지 콘솔 에러 없이 확인 |
| 2026-07-05 | v0.2.5 | **사이드바 메뉴 그룹 재편 — 과몰입 투표소/운명 관측소 신설.** 사용자가 사이드바 레퍼런스 이미지를 첨부하며 최상위에 흩어져 있던 밸런스 게임/오늘의 운세/꿈 해몽 검색/사주·타로·궁합을 그룹으로 묶어달라 요청. 두뇌테스트존·심리테스트존과 동일한 `nav-group` 패턴으로 `🗳️ 과몰입 투표소`(밸런스 게임 + 신규 `🏆 이상형 월드컵` 준비중 placeholder 섹션)와 `🔭 운명 관측소`(오늘의 운세 + 꿈 해몽 검색 + 사주/타로/궁합)를 신설(그룹 토글 공용 루프에 두 그룹 ID만 추가), 그 아래 단독 항목으로 가족오락관 → 로또 번호 조합기 배치. "사주 · 타로 · 궁합"이 하위메뉴로 들어가며 폭이 좁아져 줄바꿈되는 문제 — 구분자를 `·`→`/`로 바꿔봤으나 기본 CJK 줄바꿈 규칙상 한글 글자 사이 어디서든 줄바꿈이 허용돼 여전히 2줄로 잘리는 걸 Playwright 검증 중 발견, `.nav-item`/`.nav-group-header`에 `white-space: nowrap` 추가로 근본 수정(전체 29개 메뉴 항목 가로 overflow 없음을 실측 확인 후 적용). **후속 확인**: 코드 수정 직후 아직 push 전 상태에서 사용자가 라이브 사이트를 보고 "운명 관측소가 안 보인다"고 재문의 — 실제 버그가 아니라 이 프로젝트가 GitHub 연동 자동배포(Cloudflare Pages)라 push 전까지는 라이브에 반영 안 되는 것이었음을 확인, 커밋+푸시로 해결. **검증**: `node --check` 통과, Playwright로 신규 그룹 순서·하위 항목 클릭 정상 동작(밸런스 게임/오늘의 운세/꿈 해몽 검색 기존 기능 회귀 없음)·이상형 월드컵 준비중 화면·그룹 접기펼치기+새로고침 후 상태 유지(`nav_vote_collapsed`/`nav_destiny_collapsed`)·사주/타로/궁합 한 줄 렌더링(47px, 다른 항목과 동일)까지 콘솔 에러 없이 확인 |
| 2026-07-05 | v0.2.6 | **구글 애드센스 사이트 소유권 확인 코드 삽입.** 사용자가 애드센스 가입+사이트 등록(`gwamol-lab.xyz`)까지 마치고 코드 스니펫 전달, 처음부터 끝까지 단계별로 안내하며 진행하기로 함. 사이트 등록 시 `https://www.gwamol-lab.xyz/`(프로토콜+www+슬래시 포함)로 입력해 "최상위 도메인이어야 함" 에러가 났던 것을 `gwamol-lab.xyz`(도메인만)로 정정 안내해 해결. 코드 반영: `index.html` `<head>` 최상단(Google 권장대로 다른 CDN 스크립트보다 먼저)에 `adsbygoogle.js` 스크립트(publisher ID `pub-4825324689294427`) 삽입, 신규 `ads.txt`(repo 루트 — 정적 사이트라 별도 라우팅 설정 없이 `/ads.txt`로 바로 서빙됨)에 표준 한 줄(`google.com, pub-4825324689294427, DIRECT, f08c47fec0942fa0`) 추가. **다음 단계(순서대로)**: ①git push로 배포 ②애드센스가 크롤링해 소유권 확인 ③심사 대기(보통 몇 시간~2주, "검토 중"→"승인됨") ④승인 후 자동 광고 켜기 또는 수동 광고 단위(`<ins class="adsbygoogle">`) 배치 위치 결정 ⑤실제 노출 확인. 이번 버전은 ①만 해당, 실제 광고 노출은 아직 없음(심사 승인 후 진행) |
| 2026-07-05 | v0.2.7 | **애드센스 소유권 확인 강화(메타태그 추가) + 루트 도메인 DNS를 Cloudflare로 이전 진행.** ①`index.html` `<head>`에 `<meta name="google-adsense-account" content="ca-pub-4825324689294427">` 추가 — 애드센스가 확인 방법으로 스크립트 태그(v0.2.6에서 이미 삽입)와 메타태그 두 가지를 제공하는데, 사용자가 "둘 다 넣어보자"고 해서 추가(상호 배타적이지 않고 병행 가능). ②`gwamol-lab.xyz`로 애드센스 사이트 소유권 확인을 시도했으나 실패("사이트를 확인할 수 없습니다") — 원인을 DNS 조회로 진단한 결과 **루트(네이키드) 도메인 `gwamol-lab.xyz` 자체가 DNS에 전혀 연결되어 있지 않았음**(`www.gwamol-lab.xyz`만 Cloudflare Pages 커스텀 도메인으로 연결돼 있었고, 루트 도메인은 등록업체(호스팅케이알) 기본 네임서버만 걸려있어 애드센스 크롤러가 접속 자체를 못함). 해결을 위해 Cloudflare Pages에서 `gwamol-lab.xyz`(루트)를 Custom domain으로 추가 시도 → 루트 도메인은 CNAME을 못 걸어 Cloudflare가 네임서버 전체를 관리해야 해서 "Connect a domain" 플로우로 진행(도메인 등록 자체는 이전하지 않고 DNS 관리 권한만 이전, 이 도메인은 사이트 전용이라 이메일 등 다른 서비스 없음을 사용자에게 확인 후 진행) — 기존 `www` CNAME 레코드 자동 스캔 확인, Cloudflare Free 플랜 선택, 등록업체(호스팅케이알)의 네임서버를 `ns1~4.hosting.co.kr`에서 `abby.ns.cloudflare.com`/`marvin.ns.cloudflare.com`으로 교체 진행 중(전파 대기, 최대 24~48시간). **DNS 전파 완료 후**: 루트 도메인이 Cloudflare Pages Custom domain으로 정상 연결되면 애드센스 소유권 확인 재시도 예정 |
| 2026-07-05 | v0.2.8 | **애드센스 심사 통과율을 높이기 위한 사전 점검 및 임시 조치.** 사용자가 첨부한 애드센스 정책 링크 3개 중 2개(2012년 블로거 글)는 본문이 소실돼 참고 불가, 공식 정책 문서 1개+기존 코드베이스 직접 점검으로 리스크를 찾아 사용자 확인 후 진행(AskUserQuestion으로 3가지 항목 확인). **①제휴 상품 배너 숨김**: 결과화면 14곳에 있던 `renderPlaceholderUI()`의 "④ 제휴 상품 추천 배너"가 `href="#"`로 실제로는 아무 곳도 연결 안 된 죽은 링크 상태였음 — 심사 시 "기능하지 않는 광고성 요소"로 오인될 위험. **②가짜 통계 그래프 숨김**: 같은 함수의 "① 전체 통계 비교" 블록이 모든 사용자에게 동일한 고정값("상위 약 30% 추정")을 보여주는 하드코딩된 더미 데이터라 조작된 통계로 오인될 위험. 두 블록 모두 신규 상수 `ADSENSE_REVIEW_MODE = true`(app.js 상단)로 조건부 렌더링 처리 — 코드 삭제 없이 심사 승인 후 `false`로 한 줄만 바꾸면 원상복구됨. **③준비중 메뉴 3개 임시 숨김**: 완전히 빈 "준비중" 상태인 최상위 메뉴 3개(가족오락관/사주·타로·궁합/이상형 월드컵)를 사이드바에서 `hidden` 클래스로 숨김(섹션·초기화 코드는 그대로 유지, nav-item에 `hidden` 클래스만 추가) — 3개 다 정식 콘텐츠로 완성하려면 며칠 이상 걸리는 규모라, "지금 숨기고 오늘 심사 신청" vs "다 만들고 나중에 신청" 중 Claude가 전자를 추천(승인 확률에 큰 영향 없는 반면 심사만 불필요하게 지연됨)해 사용자가 채택. **④`robots.txt` 신설**(repo 루트, `User-agent: * / Allow: /` — 전체 허용 명시). **⚠️ 매우 중요 — 승인 후 반드시 원복할 것**: `ADSENSE_REVIEW_MODE`를 `false`로 변경하고, `index.html`의 `nav-item hidden` 3곳(`worldcup`/`fortuneext`/`family`)에서 `hidden` 클래스를 제거해야 원래 상태로 돌아옴(이 셋을 잊지 말 것 — 잊으면 승인 후에도 계속 숨겨진 상태로 방치됨). **검증**: `node --check` 통과, Playwright로 3개 nav-item이 실제로 `display:none`(숨김 확인)인지, 결과 화면 어디에도 "보러가기 →"/"상위 약 30% 추정" 문자열이 안 나오는지, 콘솔 에러 없음까지 확인 |
| 2026-07-06 | v0.3.0 | **꿈해몽 검색 알고리즘 개편 — OR 매칭/전체노출/변형 1개 제한 버그를 랭킹 엔진으로 해결.** 상세는 위 "2. 기능 현황" 표의 "꿈해몽 검색 랭킹 엔진 개편" 행 참고. 사용자가 실사용 스크린샷 4장으로 신고한 검색 버그(①단어 하나만 겹쳐도 엉뚱한 결과 노출 ②"꿈"이라는 단어가 모든 제목에 있어 전체 174건이 노출 ③테마당 첫 변형에서만 매칭 ④오타는 결과 없음)를 조사 후 두 가지 설계안(엄격 AND 매칭 vs 단어 희소성 기반 랭킹)을 사용자에게 제시, 사용자가 "AND 방식은 매칭 실패 시 곧바로 AI API를 호출하게 돼 비용이 는다"고 지적해 **랭킹 방식**으로 확정 — 조사·불용어(꿈/해몽/나오다 등) 제거 후 남은 단어의 희소성(`1/등장문서수`)으로 가중치를 매겨 최고점의 50% 이상만 통과시키는 방식(`dreamTokenizeQuery`/`dreamComputeWeights` 신설). 매칭 단어가 하나도 없을 때만 결과 없음으로 판정해 AI 폴백으로 유도하므로 기존 대비 API 호출이 늘지 않음. 사용자 요청으로 **옵션 A**(검색 결과가 있어도 하단에 "찾는 꿈이 아닌가요? 🤖 AI 해몽으로 찾아보기" 버튼을 눈에 띄게 상시 노출)도 함께 추가, 결과 없음 화면의 중복된 추천 키워드 칩 블록은 제거해 단순화. **실험적 설계라 되돌리거나 방향을 바꿀 수 있음을 CLAUDE.md에 자세히 기록**(컷오프 50%, 조사/불용어 목록은 임의값). **검증**: Node로 신고된 4가지 사례 전부 실제 `dreamData`(174개 테마) 대상 시뮬레이션 확인, `node --check` 통과, Playwright로 정적 서버에서 동일 검색어 4개를 실제 UI로 재현해 기대 결과와 콘솔 에러 없음 확인 |
| 2026-07-06 | v0.3.1 | **밸런스게임/국민테스트 콘텐츠 퀄리티 개선 — poomang류 어그로·댓글논쟁 지향으로 디벨롭.** 상세는 위 "2. 기능 현황" 표의 "Phase 4 수익화 확장 로드맵" 행 v0.3.1 항목 참고. 사용자가 두 카테고리의 오픈 샘플이 너무 무난하다고 지적, 코드를 짜기 전에 먼저 콘텐츠 초안을 텍스트로 보고하고 AskUserQuestion으로 방향(소재/국민테스트 3번째 슬롯/코드 구조 변경 여부)을 전부 확인받은 뒤 진행. 밸런스게임을 1개(가짜 잠금 2개 포함)→6개 전부 실플레이 가능하게, 국민테스트를 1개→3개(카테고리 정원 전부)로 확장하고 혈액형 테스트 톤도 티키타카 반말+댓글유도형으로 재작성. `renderBalanceFeed`/`renderPsychtestFeed`가 배열 첫 항목만 보여주던 것을 전체 순회로 변경(다른 심리테스트존 카테고리는 영향 없음). 신규 공유 이미지 10장+톤 변경된 혈액형 4장을 `scripts/generate-phase4-share-cards.js` 재실행으로 생성. **검증**: Node로 데이터 무결성(필드/중복/range) 확인, `node --check` 통과, Playwright로 밸런스게임 6장 전부 노출+신규 게임 실플레이, 국민테스트 3장 전부 노출+민초 테스트 5문항 실플레이까지 콘솔 에러 없이 확인 |
| 2026-07-06 | v0.3.2 | **홈 "테스트 모음" → "지금 인기있는 테스트"(사이트 전체 인기 TOP 6) 개편.** 상세는 위 "2. 기능 현황" 표 해당 행 참고. 사용자가 "13개 카드를 다 꺼내놓을 필요 없다"며 인기 TOP 6 축소안을 채택했고, 착수 중 "전체 메뉴가 다 포함되는 거 맞지? 로또추첨기가 인기면 그것도 노출돼야 한다"고 지적해 후보 풀을 기존 13개 테스트에서 **사이드바의 전체 콘텐츠**(심리테스트존 6개·밸런스게임 6개·로또 직접 뽑기 게임)로 확장. 전 유저 실사용 순위는 `test_results`가 RLS로 잠겨있어 클라이언트 집계가 불가능해 서버 배치 집계 없이, 기존 심리테스트존/밸런스게임이 쓰던 "로컬 dummy base + 이 기기 증분" 컨벤션을 그대로 재사용(화면에 "추후 실데이터 연동 예정" 캡션 필수 노출). 13개 핵심 테스트의 실제 시작 함수+꿈해몽/오늘의운세/로또 4개 실행 함수+로또 직접뽑기 진입점에 `bumpEngagement('site-{section}-plays')`를 신규 삽입해 진입 경로 무관하게 실제 사용을 집계, 심리테스트존/밸런스게임은 기존 `psychtest-{id}-plays`/`balance-{id}-plays` 키를 그대로 재사용. 상위 6개 카드 클릭 시 항목별 정확한 하위 화면까지 진입하는 `run` 클로저 적용. **검증**: `node --check` 통과, Playwright로 기본 상태 상위 6개 확인 + `site-lottodraw-plays`/`psychtest-mintchoco-plays`를 인위 증분시켜 실제로 해당 카드가 상위로 올라오고 클릭 시 정확한 화면까지 진입하는지 확인, 콘솔 에러 없음 |
| 2026-07-06 | v0.3.3 | **화이트모드(라이트모드) 톤 점검 및 수정 (3건) + 사이트 전반 QA.** 사용자가 "전체적으로 화이트모드 톤 다듬어달라, 작업량 크게 하지 말고 어색한 부분만" + "사이트 전반 QA해달라(없으면 없다고)"를 함께 요청 — 코드 grep+Playwright 스크린샷으로 눈에 띄는 문제 3건만 찾아 최소 범위로 수정, QA는 이 3건 외 추가로 발견된 버그 없음. ①`style.css`의 `.option-btn:hover`/`.option-btn.selected`(MBTI/ADHD 문항 선택 버튼)가 텍스트 색을 `#e2e8f0`/`#a5b4fc`로 하드코딩해둬서, 라이트모드에서 배경도 밝아지며 거의 안 보이던 문제 — 각각 `rgb(var(--slate-200))`/`rgb(var(--indigo-300))`로 교체(다크모드 기존 값과 정확히 일치하는 값이라 다크모드는 변화 없음). ②공유 아이콘 행의 X(트위터) 버튼이 `bg-slate-900 hover:bg-slate-800 border border-slate-600`(테마 remap 대상) + `text-white`를 쓰고 있어 라이트모드에서 배경이 밝아지며 흰 아이콘이 묻히던 문제 — X 브랜드 컬러는 테마와 무관하게 항상 어두워야 하므로 `bg-[#0f172a] hover:bg-[#1e293b] border border-[#475569]`(다크모드 slate-900/800/600 값을 고정 hex로 치환)로 수정, `app.js` 2곳(공용 아이콘 행/친구 대결 VS 카드) 동일 적용. ③MBTI 시작화면의 "정밀 모드" 버튼이 `purple` 계열(다크/라이트 테마 remap 목록에 없는 색상)을 써서, 라이트모드에서 `text-purple-300`이 계속 밝은 값으로 고정돼 배경과 거의 같은 밝기가 되던 문제 — `tailwind.config`에 `purple.300`을 신규 remap 추가(`--purple-300` CSS 변수, 다크값은 기존 `#d8b4fe`와 동일해 다크모드 무변화, 라이트값은 `126 34 206`로 진한 보라). **QA**: 전체 25개 섹션(로또 직접뽑기 포함) Playwright 크롤로 콘솔 에러 없음 확인(네트워크 실패류 제외), 꿈해몽 검색 랭킹 엔진(v0.3.0) 회귀 확인(오타/단일단어/복합검색 5종 정상), `App.navigate()` 대상 섹션 ID 전부 실제 존재, 함수명 중복 없음. **검증**: `node --check` 통과, Playwright로 홈/MBTI(호버·선택 상태 포함)/ADHD/두뇌나이/운세/로또/심리테스트존/밸런스게임/약관 페이지를 라이트모드로 스크린샷 촬영해 위 3건 수정 확인 |
| 2026-07-06 | v0.3.4 | **실제 브랜드 로고 이미지 삽입 + 제휴문의 폼(Slack 웹훅) 신설.** 상세는 위 "2. 기능 현황" 표의 "사이트 브랜드명 변경 → 실제 로고 이미지 삽입"·"제휴문의 폼" 두 행 참고. ①사용자가 디자이너 의뢰 로고 파일을 `assets/brand/logo-source.png`로 직접 업로드 → Python(Pillow)으로 흰 배경 제거(un-premultiply로 fringe 방지)+아이콘만 크롭+512/180/32px 3종 생성, 기존 🎯 이모지 임시 로고를 사이드바·모바일헤더·푸터 3곳 전부 교체 + 파비콘/apple-touch-icon 신규 추가. ②푸터에 "제휴문의" 링크+모달 폼 신설, `functions/api/partnership-inquiry.js`가 Slack 웹훅으로 전달(`SLACK_WEBHOOK_URL` 환경변수 등록 필요, 아직 미등록). **검증**: `node --check` 통과, Playwright로 다크/라이트 로고 렌더링(픽셀 확대 확인 포함)과 제휴문의 폼 검증/제출 플로우 확인 |
| 2026-07-06 | v0.3.5 | **제휴문의 Slack 연동 실전 배포 검증 완료 + 의견보내기 폼 신규 추가.** 상세는 위 "2. 기능 현황" 표 참고. Slack 앱 생성 중 겪은 2가지 플랫폼 버그(Incoming Webhooks 단독 활성화 시 "봇 사용자 없음" 에러 → 매니페스트 방식 재생성으로 해결, 한글 bot display_name의 매니페스트 검증 에러 → 영문명으로 해결)를 사용자와 함께 스크린샷 기반으로 실시간 트러블슈팅. 사용자 판단으로 제휴문의/의견보내기 두 폼이 웹훅 1개(채널 1개)를 공유하도록 단순화. `SLACK_WEBHOOK_URL` 등록 후 실 배포 사이트에 curl로 테스트 전송 → Slack 채널 수신까지 end-to-end 확인. 이어서 동일 패턴으로 `functions/api/feedback.js`+`#feedback-modal` 신규 구현(사진 첨부는 R2 필요해 이번 범위 제외, 텍스트만). **검증**: `node --check` 통과, Playwright로 의견보내기 모달 렌더링/빈값 검증 확인 |
| 2026-07-06 | v0.3.6 | **contact@gwamol-lab.xyz 이메일 수신+발신 설정 완료 (문서 전용, 코드 변경 없음).** 상세는 위 "2. 기능 현황" 표 참고. Cloudflare Email Routing으로 수신 설정(도메인이 이미 Cloudflare에 있어 MX/TXT 자동 추가), Gmail의 "다른 주소에서 메일 보내기"(SMTP: smtp.gmail.com + 앱 비밀번호)로 Workspace 없이 무료로 발신까지 설정. 확인 메일이 Cloudflare Email Routing "Syncing" 상태일 때 발송돼 최초 1회 유실됐던 것을 Activity Log로 진단 후 재전송으로 해결. 겸사겸사 CLAUDE.md의 "도메인 이전" 행이 실제로는 v0.2.8에 이미 완료됐는데 "진행 중"으로 방치돼있던 걸 발견해 함께 정정 |
| 2026-07-06 | v0.3.7 | **사이트 노출 문의 이메일을 개인 주소 → contact@gwamol-lab.xyz로 교체.** v0.3.6에서 이메일 수신/발신 설정이 끝나자 사용자가 바로 요청. `index.html` 내 `jhb415@gmail.com` 전체 3곳(개인정보처리방침 문의처, 서비스 이용약관 문의처, 푸터 문의 링크)을 `contact@gwamol-lab.xyz`로 일괄 교체(href의 `mailto:`와 표시 텍스트 양쪽 다). **검증**: Playwright로 푸터 렌더링 확인 |
| 2026-07-06 | v0.4.0 | **이상형 월드컵 "인생 공감 밈 월드컵" 1개 실구현 (자리올림, v0.3.x→v0.4.0).** 상세는 위 "2. 기능 현황" 표 참고. 스톡사진 8장(무료 상업이용, EXIF 제거) 기반 8강→4강→결승 토너먼트 엔진 신규 구현, Supabase 익명 투표(100판 임계치까지 순위 비공개) + 기존 공유 인프라 재사용. 사이드바 3개 임시숨김(v0.2.8) 중 worldcup만 실콘텐츠 완성으로 해제, family/fortuneext는 계속 숨김 유지 |
| 2026-07-07 | v0.4.1 | **밸런스게임 스페셜 2개 실구현 (여러 문항→유형 결과).** 상세는 위 "2. 기능 현황" 표의 "밸런스게임 스페셜" 행 참고. 전날 세션의 피쿠 레퍼런스 분석·"중급 목표" 합의를 이어받아 진행 — 심리테스트존 채점 사상 재사용, 인생/연애 밸런스 2개 게임(8/9문항, 각 4유형), 유형 카드(캐치프레이즈+해시태그+특징 체크리스트), 광고 프리로더·공유 인프라·참여수 카운트 전부 기존 재사용, 공유 이미지 8장 신규 생성, share 랜딩 balance 분기에 grade 파라미터 확장 |
| 2026-07-07 | v0.4.2 | **가족오락관 1차 오픈 — 스피드 퀴즈/몸으로 말해요 2개 (진행 도우미 엔진) + 사이드바 숨김 해제.** 상세는 위 "2. 기능 현황" 표의 "가족오락관 1차 오픈" 행 참고. 제시어 252개(카테고리 10개), 공용 엔진(카테고리/시간 설정→카운트다운→제시어+타이머+정답/패스→리캡), 공유 인프라·이미지 2장, v0.2.8 애드센스 임시숨김 중 family 해제(잔여: fortuneext 1개) |
| 2026-07-07 | v0.4.3 | **가족오락관 라이어 게임 추가 (3/10).** 상세는 위 "2. 기능 현황" 표의 가족오락관 행 v0.4.3 항목 참고. 폰 돌려보기 역할 배정+토론 타이머+역전승 룰 안내, 제시어는 스피드 퀴즈 풀 재사용, `/share/family`에 game=liar 확장+이미지 1장 |
| 2026-07-07 | v0.4.4 | **이상형 월드컵 순위 표본부족 문구 완전 숨김.** 사용자가 Supabase SQL(`worldcup_votes`/`worldcup_stats`) 실행 후 실제 플레이해보니 "누적 1판, 100판부터 공개" 문구가 그대로 노출돼 "고객한테 보여주지 말고 숨겨달라"고 요청 — `worldcupRankingHTML()`이 표본 부족 시 빈 문자열을 반환하도록 수정, 100판 이상부터만 순위 문구가 나타남 |
| 2026-07-07 | v0.4.5 | **구글 애널리틱스(GA4) 태그 삽입.** 사용자가 로드맵을 "검색노출/신규유입/퍼포먼스 마케팅" 단계로 전환하며 데이터 수집부터 착수 — GA4 속성 생성 후 전달받은 gtag.js 스니펫(측정 ID `G-W05KHWP4WY`)을 `index.html` `<head>` 최상단(Google 권장 위치, AdSense 스크립트보다도 앞)에 삽입. 이 프로젝트는 SPA라 실제 `.html` 파일이 `index.html` 하나뿐이라 다른 파일에는 삽입 불필요(공유 랜딩 `functions/share/[section].js`는 실제 방문자를 즉시 SPA로 리다이렉트하므로 별도 계측 불필요, 크롤러는 JS를 실행하지 않아 영향 없음) |
| 2026-07-07 | v0.4.6 | **SPA 섹션 이동 GA4 가상 페이지뷰 추가.** 해시 라우팅 SPA 특성상 최초 접속 1회만 페이지뷰가 잡히고 이후 섹션 간 이동은 GA가 못 잡는 문제를 사용자에게 설명, "지금 바로 해달라"는 결정을 받고 구현 — `App.navigate()`에서 섹션이 실제로 바뀔 때만 `gtag('event','page_view',...)`를 전송하도록 추가(같은 섹션 재클릭 시 중복 전송 없음) |
| 2026-07-07 | v0.4.7 | **PageSpeed Insights 성능 개선 1차 — 로고 이미지 크기.** 사용자가 PageSpeed 분석 스크린샷 다수를 첨부, Claude가 제3자 스크립트(삭제 불가) vs 직접 수정 가능 항목으로 분류해 방향 보고 후 로고 이미지만 우선 진행 확정 — nav/모바일헤더/푸터 3곳이 512×512 원본을 32px/24px로 축소 표시하던 것을 96px 리사이즈본(`logo-icon-96.png`, 183KB→12.4KB)으로 교체. 나머지 항목(preconnect/색상대비/defer/Tailwind CDN/JS minify)은 보류 |
| 2026-07-07 | v0.4.8 | **PageSpeed Insights 성능 개선 2차 — llms.txt 신규 작성.** "에이전트 접근성 — llms.txt가 권장사항을 준수하지 않음"(H1 헤더+링크 필요) 지적 반영, repo 루트에 사이트 소개+전체 섹션 링크 목록을 담은 `llms.txt` 신규 작성 |
| 2026-07-07 | v0.4.9 | **PageSpeed Insights 성능 개선 3차 — 색상 대비 개선.** 푸터/홈 인기순위 캡션/완주현황 pill 배지/빈 상태 문구 등에 쓰인 `text-slate-500`/`text-slate-600`이 어두운 배경(`bg-slate-900`/`bg-slate-700`)과 WCAG AA 기준(4.5:1) 미달인 것을 실측 확인 — 공유 CSS 변수는 그대로 두고 해당 텍스트 요소 6곳의 클래스만 `text-slate-300`/`text-slate-400`으로 교체, 라이트모드도 회귀 없음 확인 |
| 2026-07-07 | v0.5.0 | **PageSpeed Insights 성능 개선 4차 — 로고 이미지 width/height 명시.** 데스크톱 리포트의 "에이전트형 브라우징"(베타 카테고리) CLS 0.13 지적 — 로고 `<img>` 3곳이 Tailwind 클래스로만 크기 지정돼있어 CDN 스타일시트 적용 전 원본 크기로 잠깐 그려지는 밀림 가능성을 발견, `width`/`height` HTML 속성 추가로 초기 렌더링부터 크기 확정 |
| 2026-07-08 | v0.5.1 | **검색엔진 최적화(SEO) 기본기 반영 — 구글/네이버 공식 가이드 기반.** 상세는 위 "2. 기능 현황" 표 참고. `<title>` 키워드 보강, canonical, OG/Twitter 카드 태그(신규 `share-cards/og-default.jpg`), WebSite+Organization JSON-LD, `sitemap.xml`(루트 1개), `robots.txt`에 Sitemap 줄 추가, 네이버 소유확인 meta 주석 placeholder(발급은 사용자가 서치어드바이저에서 직접 진행 필요) |
| 2026-07-08 | v0.5.2 | **네이버 서치어드바이저 사이트 소유 확인 완료.** 사용자가 서치어드바이저에서 `gwamol-lab.xyz` 등록 후 발급받은 코드를 전달, v0.5.1에서 주석 처리해둔 `<meta name="naver-site-verification">`에 채워 넣고 활성화. 다음 단계(서치어드바이저 소유확인 버튼 클릭 + sitemap.xml 제출, 구글 서치 콘솔 등록)는 사용자가 진행 예정 |
| 2026-07-08 | v0.5.3 | **외부 SEO 체크업 도구(seositecheckup.com) 지적사항 반영.** 사용자가 실제 사이트를 스캔한 결과 이슈 목록(HIGH 5건+MEDIUM 4건)을 텍스트로 전달, 코드로 고칠 수 있는 것과 Cloudflare 계정 설정이 필요한 것을 구분해 진행. **코드 반영**: ①`<h1>`이 사이트 전체에 하나도 없던 것을 발견(디자인상 브랜드 워드마크가 `<span>`이었음) — 시각 디자인은 그대로 두고 `<body>` 최상단에 `class="sr-only"` H1(title/meta description과 동일 키워드)만 신규 추가 ②Tailwind CDN/Supabase JS/Kakao SDK/`data.js`/`supabase-client.js`/`kakao-share.js`/`app.js` 등 `<head>`와 body 끝 스크립트들이 전부 동기 로드라 렌더링을 막던 것 — Tailwind CDN은 렌더 전에 유틸리티를 주입해야 FOUC가 안 생겨 동기 유지, 나머지 6개 스크립트엔 `defer` 추가(defer는 문서 순서대로 실행되므로 body 끝 스크립트들의 실행 순서·의존관계는 그대로 유지됨) ③Cloudflare Pages가 `404.html`이 없으면 존재하지 않는 경로도 index.html을 200으로 서빙하던 것(SPA 폴백)을 발견 — 신규 `404.html`(브랜드 톤 + 홈/주요 섹션 링크) 추가로 진짜 존재하지 않는 경로는 실제 404 상태코드+안내 화면이 뜨도록 함(해시 라우팅 특성상 유효 경로는 원래 `/` 하나뿐이라 사이드 이펙트 없음) ④이상형 월드컵 스톡사진 8장이 전부 1600px 원본을 그대로 쓰고 있어(실제 표시는 카드 그리드 폭 기준 최대 ~330px) "이미지 크기 최적화" 항목에 걸림 — Pillow로 최대 1200px+품질82로 리사이즈, 총 용량 1.77MB→0.72MB(약 59% 절감), 화질 체감 차이 없음. **이미 v0.5.1~v0.5.2에서 해결돼 재스캔하면 사라질 항목**: sitemap 누락, 구조화 데이터 누락, 소셜 메타태그 누락 — 스캔 시점에 아직 반영 전이었거나 도구가 캐시를 본 것으로 추정. **코드로 못 고치는 항목(Cloudflare 계정 설정 필요)**: URL 정규화(canonicalization) — `gwamol-lab.xyz`와 `www.gwamol-lab.xyz`가 리다이렉트 없이 둘 다 200으로 동시 응답 중인 것을 실측 확인(canonical 태그는 이미 apex를 가리키지만, 실제 리다이렉트가 없으면 중복 콘텐츠로 인식될 수 있음) — Cloudflare 대시보드에서 Redirect Rule(www→apex 301)을 설정해야 하는 계정 작업이라 사용자에게 안내만 하고 코드 변경은 하지 않음. **검증**: 정적 서버+Playwright로 h1 텍스트 확인, MBTI 섹션 이동+Kakao SDK 초기화+Supabase 클라이언트 준비가 defer 적용 후에도 정상 동작함을 확인, 이상형 월드컵 섹션 진입 및 404.html 렌더링까지 콘솔 에러 없이 확인 |
| 2026-07-08 | v0.5.4 | **Meta Description 실제 태그 보강.** 사용자가 seositecheckup.com에서 "Meta Title Test" 확대 캡처를 전달 — 확인해보니 스캔 시점이 v0.5.1 배포 전이라 `<title>`은 이미 해결된 옛 이슈(캐시)였지만, 그 옆의 **Meta Description Test는 실제로 남아있던 진짜 문제**였음: v0.5.1에서 `og:description`/`twitter:description`은 길게 새로 썼는데 정작 실제 `<meta name="description">` 태그 본체는 옛날 39자짜리("과몰입 연구소 — 재미로 시작했다가...")를 그대로 두고 있었음(권장 150~220자에 크게 못 미침). 실제 서비스 콘텐츠(MBTI/두뇌 나이/심리테스트/밸런스 게임/오늘의 운세/꿈 해몽/로또)를 나열한 156자 설명으로 교체 |
| 2026-07-08 | v0.5.5 | **네이버 서치어드바이저 www 속성 추가 등록.** 사용자가 `gwamol-lab.xyz`(apex)만 등록했었다는 걸 뒤늦게 깨닫고 `www.gwamol-lab.xyz`도 별도 속성으로 등록, 발급받은 소유확인 코드(`86367a01ec31b5a81697eb1a01a7f785fe492cad`)를 전달 — 같은 `index.html`이 두 호스트 모두에서 서빙되므로 `<meta name="naver-site-verification">` 태그 2개(apex용/www용)를 나란히 추가. `[[project_stage_d_backend]]` 메모에 기록된 "www→apex 리다이렉트는 SEO 고도화 라운드까지 보류" 결정과 일관되게, 지금은 두 호스트가 모두 독립적으로 살아있는 상태를 그대로 인정하고 양쪽 다 검색엔진에 등록해두는 방향으로 진행 |
| 2026-07-08 | v0.5.6 | **GEO(생성형 AI 답변엔진 최적화) 착수 — 1단계(AI 크롤러 정책) + 2단계(llms.txt 고도화) 완료.** 사용자가 구글 AI 검색 가이드/토스페이먼츠 GEO 아티클/Anthropic·OpenAI 크롤러 문서 4개를 첨부하며 전략 제시를 요청 — Claude가 6단계 우선순위(①AI 크롤러 정책 ②llms.txt 고도화 ③FAQ/구조화데이터 ④사용자 관점 서술 ⑤최신화 신호 ⑥SPA에 숨은 콘텐츠를 정적으로 노출)로 정리해 보고, 사용자가 ①②는 필수로 즉시 진행, ③④⑤는 후순위, ⑥은 최후순위(사용자가 "30개씩 더보기+페이지네이션" 아이디어 제시, 추가 설계 필요)로 확정. **①AI 크롤러 정책**: 실측 결과 Cloudflare가 계정 레벨에서 이미 GPTBot/ClaudeBot/Google-Extended/Bytespider/CCBot/Amazonbot/Applebot-Extended/meta-externalagent(전부 "학습용" 크롤러)만 차단하고 OAI-SearchBot/ChatGPT-User/Claude-SearchBot/Claude-User(전부 "실시간 답변/검색용" 크롤러)는 차단하지 않고 있음을 확인(curl로 각 User-Agent 응답 200 재확인) — 이미 원하는 정책과 일치하는 상태였으나, Cloudflare 대시보드 설정 하나에만 의존하면 나중에 설정이 바뀔 때 무방비가 되므로 **이 저장소 자체의 `robots.txt`에도 동일한 학습차단/답변허용 정책을 명시적으로 이중화**(Cloudflare 설정과 무관하게 정책이 유지되도록). **②llms.txt 고도화**: 기존엔 섹션 링크 목록뿐이었던 걸 각 항목에 "무엇을 측정/제공하는지" 1줄 설명 추가 + 마지막 업데이트 날짜 명시. 신규 **`llms-full.txt`**(llms.txt 생태계의 확장 파일 컨벤션) 추가 — MBTI 16유형 전체(title/emoji/desc, `mbtiResults`에서 스크립트로 정확히 추출), 오늘의 운세 구조 설명, 꿈해몽 대표 예시 5개(`dreamData`에서 실제 title/summary 추출), 두뇌 인지 테스트 채점 방식, 심리테스트존/밸런스게임 대표 콘텐츠 목록을 실제 텍스트로 담아 AI가 인용할 "실체"를 제공(전체 데이터 아닌 발췌본임을 명시). **다음 순서(사용자 확정)**: ③FAQ+구조화데이터, ④사용자 관점 서술, ⑤최신화 신호는 후순위로 진행 예정, ⑥SPA 콘텐츠의 정적 노출(페이지네이션 아이디어 포함)은 최후순위 — 데이터가 대부분 `data.js`의 JS 객체 안에 있어 상호작용 없이는 크롤러가 못 보는 근본 구조 문제, 추가 설계 필요 |
| 2026-07-08 | v0.5.7 | **GEO 3·4·5단계 완료 — FAQ 구조화 데이터, 사용자 관점 서술, 최신화 신호.** 사용자가 "특이사항 있으면 보고, 없으면 계속 진행"으로 위임해 순차 완료. **③FAQ+FAQPage**: 홈 화면 서비스 그리드 아래에 "❓ 자주 묻는 질문" 섹션 신설 — 네이티브 `<details>/<summary>` 아코디언 6개(무료 여부/결과 저장/진단 여부/카카오 공유/운세·꿈해몽 갱신 주기/로또 당첨확률)를 실제 서비스 사실 기반으로 작성, `<head>`에 동일 문구로 1:1 매칭되는 `FAQPage` JSON-LD 추가(구글 가이드라인상 화면에 없는 내용을 구조화 데이터에만 넣으면 리치 스니펫 자격이 박탈되므로 반드시 일치시킴). `<details>`는 접혀있어도 텍스트가 DOM에 그대로 남는 네이티브 HTML이라 크롤러가 온전히 읽을 수 있음. **④사용자 관점 서술**: 홈 화면 카드 캡션(도구 그리드 2개 + 인기 테스트 그리드 14개, 총 16곳)을 기능 나열형("스트룹 테스트, 3단계 난이도")에서 상황·관점형("요즘 머리 굳은 것 같을 때 해보는 뇌 나이 체크")으로 리라이팅 — 심리테스트존/밸런스게임 카드는 데이터 기반 동적 문구라 이번 범위에서 제외. **⑤최신화 신호**: 푸터(섹션 토글과 무관하게 항상 DOM에 존재해 크롤러가 항상 읽는 위치)에 "콘텐츠 최근 업데이트: 2026-07-08" 문구 추가(`text-slate-500`는 `bg-slate-900` 배경 대비 WCAG AA 미달이라는 v0.4.9의 기존 발견을 그대로 적용해 `text-slate-400`으로 처음부터 작성). **검증**: `node --check` 통과, 정적 서버+Playwright로 FAQ 6개 렌더링, JSON-LD 2개(WebSite/FAQPage) 파싱, 리라이팅된 카드 문구 노출, 푸터 최신화 문구 노출까지 콘솔 에러 없이 확인 |
| 2026-07-08 | v0.5.8 (미리보기) | **GEO 6단계 파일럿 — 꿈해몽 사전 정적 콘텐츠 페이지(미리보기/noindex 배포).** 사용자가 "⑥번 아이디어를 애드센스 승인까지 고려해 제안해달라"고 요청 → Claude가 핵심 트레이드오프를 먼저 보고: 지금 SPA 구조에선 실제 콘텐츠(꿈해몽 174개 등)가 `data.js` JS 객체 안에만 있어 크롤러·AI봇·애드센스 심사봇이 못 봄(=thin content 리스크) → ⑥을 잘 하면 이 약점을 정면 해소하지만, **얄팍한 페이지를 대량 자동생성하면 구글의 "scaled content abuse" 정책에 걸려 오히려 애드센스 승인에 독**이 되므로 "많이"가 아니라 "각 페이지가 실제로 읽을 가치 있게"가 핵심임을 강조. **해결책**: 해시(`#dream`)가 아닌 진짜 URL을 가진 정적 콘텐츠 페이지를 신규 스크립트 `scripts/generate-content-pages.js`(기존 `generate-*.js`와 동일하게 data.js를 Node로 eval해 읽는 수동 파이프라인, 빌드도구 없음, SPA 런타임 무변경 순수 추가)로 생성 — `/kkum/`(사전 목록, 20개씩 페이지네이션), `/kkum/page/{N}/`, `/kkum/{한글슬러그}/`(테마별 상세: 요약·상세·행운색·행운숫자·오늘의행동 + 하위꿈 1~4개 전문 + 관련 꿈 6개 내부링크 + `#dream`으로 유입 CTA). 자체 완결 HTML(무거운 SPA/Tailwind 의존 없이 인라인 CSS, 다크 브랜드톤)이라 빠르고 크롤러 친화적, 애드센스 스크립트·GA4·canonical·OG도 각 페이지에 포함. 사용자 요청 반영: 페이지당 **20개**(30→20), 최종 개수는 "전체 174 vs 인기 큐레이션"을 **샘플 보고 결정**하기로 함(스크립트에 `LIMIT` 상수로 조절 가능, 174개 전체 생성도 스크립트라 작업량 동일하며 손으로 쓴 실제 해몽이라 scaled-abuse 아님을 설명). **안전장치**: 스크립트에 `PUBLISH` 플래그 — 현재 `false`라 모든 페이지에 `<meta name="robots" content="noindex">` 삽입 + sitemap/푸터 미연결 상태로 배포(사용자가 품질·범위 확정 전까지 크롤러·애드센스에 노출 안 됨). **다음 단계(사용자 확정 후)**: `PUBLISH=true`로 전환(noindex 제거) + sitemap.xml에 URL 추가 + SPA 푸터/꿈해몽 섹션에 "꿈해몽 사전" 링크를 걸어 크롤러가 발견하게 하는 "정식 공개" 작업 → 이때 v0.5.8 정식 확정 + index.html 버전표시 갱신. 검증되면 MBTI 16유형/오늘의 운세로 확장(게임형 테스트는 읽을 텍스트가 없어 thin이라 제외). **검증**: 스크립트 실행으로 상세 174개+목록 9페이지 생성, 정적 서버+Playwright로 목록(카드 20개+페이지네이션 1~9+이전/다음)·상세(뱀 꿈: 요약/상세/행운정보/하위꿈 4개/관련링크 6개) 렌더링, canonical/noindex/OG 정상, 콘솔 에러 없음, 모바일 스크린샷으로 시각 품질 확인. **후속(같은 버전)**: 사용자가 실제 스크린샷을 비교하며 "원래 앱(#dream 모달)엔 있던 로또연동/카카오공유 버튼이 정적 페이지엔 없다"고 지적 — Claude가 로또 연동(앱 상태 조작이 필요해 정적 페이지에 억지로 넣으면 어색함)과 공유 버튼(실제 검색 유입 방문자가 공유하고 싶어할 수 있어 넣을 가치 있음)을 구분해 보고, 사용자가 "로또는 지금처럼 앱 유도 방식 유지(로또 조합기 안에도 꿈연동 버튼이 있어 순환 가능), 공유 버튼만 추가"로 확정. 신규 `assets/kkum-share.js`(174개 페이지가 공유하는 독립 공유 스크립트 — app.js를 로드하지 않는 정적 페이지 특성상 `shareToKakaoCard`/`shareFacebook`/`shareTwitter`/`shareBand`/`copyToClipboard`와 동일한 로직을 최소 의존성으로 재구현) 추가, `generate-content-pages.js`의 `pageShell()`에 `shareData` 파라미터 신설(상세 페이지만 카카오 SDK+`kkum-share.js`를 defer로 로드, 목록 페이지는 불필요해 제외) — 각 상세 페이지 `<head>`에 `window.__kkumShare = {url,title,desc,image}` 인라인 주입 후 본문에 카카오톡 대형 버튼+FB/X/밴드/링크복사 아이콘 행(앱과 동일 톤) 삽입. **검증**: 재생성 후 Playwright로 `window.__kkumShare` 값 정확성, 카카오 버튼·아이콘 4개 렌더링, 콘솔 에러 없음, 스크린샷으로 앱과 일관된 톤 확인 |
| 2026-07-08 | v0.5.9 | **GEO 6단계 정식 공개 — 꿈해몽 사전(`/kkum/`) 174개 전체.** 사용자가 "진행해줘"로 확정(큐레이션 없이 174개 전체 유지) + "뒤로가기 살아있나" 확인 요청 — 이 페이지들은 해시가 아닌 진짜 URL이라 브라우저 히스토리가 기본으로 정상 동작함을 Playwright로 재확인(목록→상세→뒤로가기 시 정확히 목록으로 복귀). `scripts/generate-content-pages.js`의 `PUBLISH`를 `true`로 전환해 전 페이지의 `noindex` 제거, 스크립트 말미에 신규 로직 추가해 **`sitemap-kkum.xml`**(183개 URL: 목록 1+8페이지, 상세 174개) 생성 — 루트 `sitemap.xml`은 손으로 관리하는 별도 파일이라 건드리지 않고, `robots.txt`에 `Sitemap:` 라인 하나를 추가해 병행 등록. **발견 경로(크롤러가 링크를 타고 찾아올 수 있도록) 2곳 신설**: ①SPA `#dream` 검색 화면 하단에 "📖 174가지 꿈해몽 전체 목록 보기" 링크(`renderDreamSearch()`) ②사이트 전역 푸터에 "꿈해몽 사전" 링크. 둘 다 실제 사람에게도 노출되는 진입점이라 SEO뿐 아니라 실사용 유입 경로로도 기능. **다음 확장 후보(검증 후 순차 진행)**: MBTI 16유형 → 오늘의 운세 순, 게임형 테스트(반응속도 등)는 읽을 텍스트가 없어 정적 페이지화 대상에서 제외. **검증**: 재생성 후 noindex 제거 확인, `sitemap-kkum.xml` 183개 URL 검증, Playwright로 목록→상세→브라우저 뒤로가기(목록 페이지로 정상 복귀)·상세 breadcrumb "홈" 클릭 시 SPA(`#home`) 정상 진입·`#dream` 섹션과 푸터의 신규 링크 노출까지 확인(콘솔에 뜬 Supabase 익명로그인 실패는 로컬 테스트 환경이 실제 프로젝트 네트워크에 접근 못 해서 나는 기존 현상이며 이번 변경과 무관) |
| 2026-07-09 | v0.6.0 | **밸런스게임 스페셜 결과유형 8단계 세분화 + 실시간 서버 집계.** 상세는 위 "2. 기능 현황" 표의 "밸런스게임 스페셜" 행 v0.6.0 항목 참고. 사용자가 다른 세션에서 논의한 고도화 기획서(테마팩/실시간응답비율/결과유형확장/공유카드/마이크로인터랙션/모듈화)를 전달 — Explore 에이전트로 현황 조사 후 AskUserQuestion으로 범위(콘텐츠 확장+서버집계까지 진행, balanceGames 구조는 유지, 톤은 기존 선 유지)를 확정하고, 서버 집계 방식(월드컵과 동일한 익명 insert 테이블+집계 뷰 패턴)을 설명해 컨펌받은 뒤 착수. `lifepick`/`lovepick` 결과 4→8등급 세분화, 신규 `balance_responses`/`balance_stats`(Supabase, SQL은 사용자가 직접 실행)로 balanceGames/balanceSpecials 양쪽에 실제 응답 비율+소수의견 리액션 연결(표본 부족 시 percentile_cache/월드컵과 동일한 게이트로 조용히 생략). `functions/share/[section].js`의 grade 정규식(`/^[ABCD]$/`→`/^[A-H]$/`) 버그도 사전 발견해 수정. **검증**: `node --check` 3개 파일, Node로 8등급 range 전 구간 커버 확인, 공유 이미지 16장 재생성, Playwright로 Supabase 테이블 미생성 상태(404)에서도 화면이 안 깨지고 더미 기반으로 정상 동작함을 확인 |
| 2026-07-09 | v0.6.1 | **이상형 월드컵 팩 시스템 전면 재구축(6개 팩, 160개 아이템).** 상세는 위 "2. 기능 현황" 표의 "이상형 월드컵" 행 v0.6.1 항목 참고. 사용자가 kravvme.com/zzicky.com 레퍼런스와 확정 스펙(팩 JSON 스키마, 콘텐츠 6개 팩, Supabase 설계)을 전달, 사전 분석에서 해시 라우팅 한계(진짜 URL 경로 없음)와 레거시 Supabase 스키마 이름 충돌 2건을 보고 후 진행. 레거시(밈 8개 단일토너먼트)는 git 히스토리 백업 원칙으로 삭제, `AppData.worldcupPacks`로 완전 재작성. 탐색(검색/카테고리 칩/정렬/2분할 썸네일)→플레이(인트로/매치/역배토스트/세션이어하기)→결과→랭킹 전체 신규 구현, `worldcup_stats`/`worldcup_plays` 신규 테이블, 공유는 서버에 팩 데이터 중복 없이 클라이언트가 계산한 텍스트/이미지 URL을 그대로 전달받는 방식으로 설계. 이모지형 팩 공유카드 64장 생성, 이미지형 팩은 placeholder 폴백+파이프라인 스크립트만 준비(실제 사진은 다음 단계) |
| 2026-07-09 | v0.6.4 | **이상형 월드컵 — cute-animals-32 팩 오매칭 이미지 2장 교체(치와와/카피바라).** 상세는 위 "2. 기능 현황" 표의 "이상형 월드컵" 행 v0.6.4 항목 참고. 사용자가 실 화면 스크린샷으로 제보 — "치와와" 카드가 멕시코 치와와 시(市) 사진으로, "카피바라" 카드는 카피바라가 카드 크롭에서 잘려 안 보임. Pexels에서 새 사진을 골라 3:4 카드 크롭 시뮬레이션까지 검증 후 교체, `process_worldcup_images.py` 재실행으로 webp 갱신, 소싱 키워드 파일에 재발 방지 주석 추가 |
| 2026-07-09 | v0.6.3 | **이상형 월드컵 — 공유 링크→"나도 해보기" 클릭 시 빈 화면 버그 수정.** 상세는 위 "2. 기능 현황" 표의 "이상형 월드컵" 행 v0.6.3 항목 참고. 사용자가 실제 카톡 공유 스크린샷으로 제보한 버그를 Playwright로 재현 — `#worldcup-container`엔 콘텐츠가 정상 렌더링돼 있는데 부모 `#section-worldcup`이 `hidden` 상태로 멈춰있는 게 원인. 근본 원인은 `hashchange` 리스너가 쿼리스트링을 안 떼고 비교해, `App.navigate('worldcup')` 직후 `wcOpenIntro()`의 `history.replaceState(...,'#worldcup?play=...')`와 타이밍이 겹치면 비동기로 뒤늦게 온 `hashchange`가 `'worldcup?play=...'`를 통째로 섹션 id로 오인해 모든 섹션을 숨긴 뒤 잘못된 id를 못 찾아 그대로 멈추는 것. `hashchange` 핸들러에 `.split('?')[0]` 한 줄 추가로 수정, Playwright로 버그 재현→수정 확인→일반 내비게이션/카드 클릭/브라우저 뒤로가기 회귀 없음까지 검증 |
| 2026-07-09 | v0.6.2 | **이상형 월드컵 이미지형 3개 팩 실사진 소싱 완료 + AI 오리지널 팩 생성 스크립트 신설.** 상세는 위 "2. 기능 현황" 표의 "이상형 월드컵" 행 v0.6.2 항목 참고. v0.6.1에서 placeholder 상태였던 `cute-animals-32`(32장)/`soul-food-32`(32장)/`korea-travel-16`(16장)에 CC0 무료 상업이용 스톡사진을 수급해 `process_worldcup_images.py`로 변환·배치, `gwamol-emotion-16`(16장)은 신규 `scripts/generateWorldcupImages.js`(OpenAI `gpt-image-1` 호출 → 동일 파이프라인 자동 연계)로 생성 — 4개 팩 96장 전량 완료, `data.js`의 `imagePath` 필드가 실제 파일을 모두 정확히 가리킴. 신규 `worldcup/packs/README.md`로 팩별 소싱 방식 차이(실사진 3팩 vs AI 1팩)와 이유를 문서화, `.gitignore`에 `.env` 추가+`.env.example`로 필요 환경변수만 명시(키 자체는 미커밋). `node --check` 통과, imagePath 참조 96개 전부 디스크 실재 확인, 정적 서버로 이미지 응답 200 확인 |
| 2026-07-10 | 버전 변경 없음(별도 폴더) | **심리테스트 전면 재구축 착수 — 신규 config 기반 테스트 엔진 STEP 1 프로토타입.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 참고. 사용자가 "지금 심리테스트존이 너무 재미없다"며 다른 세션에서 미리 설계해둔 STEP 1 프롬프트(엔진+1호 콘텐츠 스펙)를 전달, 그대로 검토 후 진행. 신규 폴더 `/test-engine/`에 기존 사이트와 완전히 분리된 config 기반 엔진(`인트로→질문→로딩연출→결과` 4단계, `sum`/`type`/`quiz`/`axis` 4가지 채점 방식 지원, `themes/*.css`로 스킨 분리)을 구축하고, 1호 콘텐츠 "진짜 정신연령 테스트"(MZ 톤 15문항, 6구간 결과, 윈도우95 레트로 테마)를 직접 창작. Node 스크립트로 15문항 점수 합산 범위(0~43)와 6개 결과 구간이 갭·중복 없이 정확히 매칭됨을 기계 검증, 정적 서버+Playwright로 양극단 시나리오(전부 A→60세, 전부 B→10세) 실제 플레이와 완료 카운터 증가까지 확인. 아직 실제 사이트(심리테스트존)에는 연결하지 않은 순수 프로토타입 단계 |
| 2026-07-10 | 버전 변경 없음(별도 폴더) | **심리테스트 전면 재구축 STEP 1.5 — SVG placeholder를 AI 일러스트로 교체.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 STEP 1.5 항목 참고. 사용자가 "구현은 괜찮은데 이미지가 허접하다"고 지적, 기존 `scripts/generateWorldcupImages.js`와 동일한 OpenAI Images API(`gpt-image-1`) 패턴을 재사용해 진행. 비용 발생 전 스타일 2종(플랫+굵은아웃라인 vs 손그림 러프라인)과 품질 2종(medium vs high)을 각각 Artifact(base64 인라인 이미지 비교 페이지)로 보여주고 확인받아 "손그림 러프 라인 + medium"으로 확정(Read 도구로 보여준 이미지가 사용자에게 실제로 안 보였던 문제를 겪은 뒤 Artifact 방식으로 전환). 브랜드 컬러(청록/주황) 기반 오리지널 블롭 마스코트 캐릭터를 신규 설계해 커버+결과 6종에 표정·포즈만 바꿔 재사용, 신규 `test-engine/scripts/generate-assets.js`(월드컵 스크립트와 동일한 스킵/재시도 로직 + Python/Pillow로 700px WebP 변환)로 7장 전량 1회 생성 성공(장당 24~54KB), config.json 이미지 경로 갱신. Playwright로 전체 플로우 재검증, 콘솔/네트워크 에러 없음 |
| 2026-07-10 | 버전 변경 없음(별도 폴더 + app.js/data.js 소규모 추가) | **심리테스트 전면 재구축 STEP 2 완료 — 결과 공유카드(Canvas)+관련테스트 배너+sitemap 반영+심리테스트존 실제 연동.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 STEP 2 항목 참고. 신규 `test-engine/result-card.js`로 로또 영수증 캔버스와 동일한 house 컨벤션(2x 스케일 렌더링, `toDataURL`+`<a download>`)을 재사용해 세로형 공유카드 합성, 결과 화면에 "🖼️ 이미지 저장" 버튼+모달(길게 눌러 저장 안내+다운로드) 추가. config.json에 `related: []` 필드와 결과 화면 관련테스트 배너(비어있으면 렌더 자체 생략) 추가. 기존 `sitemap.xml`(URL 1개)에 mental-age 테스트 URL 1줄 추가. **사이트 연동(작업3)**: 계획을 먼저 텍스트로 보여주고 승인받는 절차대로 진행 — 사용자가 처음엔 "뭔소린지 모르겠다"고 해서 쉬운 말로 재설명 후 승인, 승인 시 "나중에 인기순/최신순으로 바꿀 수 있게 확장성 열어두라"는 조건이 추가돼 설계를 카드 1개 하드코딩에서 `data.js`의 배열(`AppData.externalTests`) + `app.js`의 정렬 가능한 렌더 함수(`sortExternalTests`/`externalTestsBannerHTML`, 이미 `'new'`/`'popular'` 두 정렬 모드가 실제로 동작하도록 구현, UI 토글만 아직 없음) 조합으로 변경, `renderPsychtestFeed()`가 카테고리 탭 위 최상단에 항상 노출하도록 연결. 심리테스트존이 이제 **격리된 프로토타입이 아니라 실제 사이트에서 도달 가능한 상태**가 됨(더 이상 사이트 미반영). Playwright로 공유카드 저장(실제 download 이벤트)·related 배너 노출/숨김·심리테스트존 배너 최상단 고정(카테고리 전환 후에도 유지)·배너 클릭 시 실제 test-engine 페이지로 이동·홈/MBTI 등 기존 화면 회귀까지 전부 콘솔 에러 없이 확인 |
| 2026-07-10 | v0.6.5 | **심리테스트 전면 재구축 STEP 3 완료 — 홈 화면 `sections.json` 큐레이션 시스템 + 몰입테스트/MBTI존 카테고리 신설 + test-engine 카카오공유·메인 링크.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 STEP 3 항목 참고. 이전 세션이 미완성으로 남긴 작업(스크린샷으로 제보받음)을 이어받아 마무리 — 신규 루트 `sections.json`으로 홈 화면 진열대 구성(제목/하이라이트/카드 id 목록, 또는 기존 인기순 자동계산 재사용, 또는 콘텐츠 없을 때 준비중 placeholder)을 코드 수정 없이 바꿀 수 있게 일반화(`renderHomeSections()`/`buildTestIndex()` 신규, 기존 하드코딩 그리드 2개를 `#home-sections-container` 하나로 교체), 심리테스트존에 몰입테스트(`/test-engine/` 노출 배너)/MBTI존(준비중) 카테고리 신설, `test-engine/engine.js`에 메인 사이트와 같은 Kakao 앱 키를 재사용하는 결과화면 카카오 공유 버튼과 모든 화면 공통 "← 메인으로" 고정 링크를 자동 주입하도록 추가. **이번 세션 작업**: 코드 리뷰 결과 `buildTestIndex()`가 내부 코어 테스트 카드(`CORE_TEST_CARDS`)에 `id` 필드가 없어 조회가 전부 `index[undefined]`로 겹쳐 쓰이는 잠재 버그를 발견해 `index[c.id || c.section]`로 수정(현재 `sections.json`은 이 id들을 직접 참조하지 않아 증상은 없었지만, 이 시스템의 설계 의도상 나중에 내부 테스트 id를 직접 참조하는 섹션을 추가하면 조용히 실패했을 것). `node --check`로 app.js/data.js/engine.js 문법 확인, `buildTestIndex()`를 Node로 직접 재현해 id 조회 정상화 확인, 정적 서버+Playwright로 홈 화면 4개 섹션(신설존/MBTI존 placeholder/인기/도구, 빈 "시즌 기획전"은 조용히 숨겨짐) 렌더링·신설존 카드 클릭 시 실제 test-engine 페이지 이동·몰입테스트/MBTI존 탭 진입·홈의 MBTI존 카드 클릭 시 정확한 카테고리로 이동·mental-age 테스트 전체 플레이(카카오/이미지저장/공유하기/다시하기 4버튼+메인 링크 노출)까지 전부 콘솔 에러 없이 확인 후 `sections.json`을 커밋에 포함, 버전 표시(index.html 헤더 주석+사이드바)를 v0.6.5로 갱신 |
| 2026-07-10 | v0.6.5 | **사이드바 NEW 배지 골드 반짝임 스타일 교체 + 정적 자산 캐시버스팅 쿼리스트링 추가.** ①사용자가 몰입테스트/MBTI존 NEW 배지(주황 고정색 칩)가 눈에 안 띈다고 지적 — 글씨 크기 축소(0.65rem→0.5rem), 금색 글씨+금색 테두리+어두운 반투명 배경으로 교체, `box-shadow`/`text-shadow`가 1.6초 주기로 밝아졌다 어두워지는 펄스 애니메이션 추가(`style.css` `.badge-new-nav`/`@keyframes badge-new-sparkle`). ②사용자가 라이브 사이트 스크린샷으로 "사이드바엔 새 메뉴가 보이는데 탭은 4개만 뜬다"고 제보 — `curl`로 라이브 `app.js`/`style.css`를 직접 조회해 이미 최신 STEP 3 코드가 정상 배포돼 있음을 확인, 원인은 배포 문제가 아니라 Cloudflare Pages 정적 자산 캐시(`max-age=14400`, 4시간)로 브라우저가 구버전 `app.js`를 계속 쓰고 있던 것으로 진단(파일명이 버전 없이 고정돼 `index.html`만 새로 받고 JS/CSS는 캐시에서 구버전을 서빙하는 불일치). 재발 방지로 `index.html`이 로드하는 자체 자산 5개(`style.css`/`data.js`/`supabase-client.js`/`kakao-share.js`/`app.js`)에 `?v=0.6.5` 쿼리스트링 추가, 앞으로 버전 올릴 때마다 이 값도 같이 갱신하도록 CLAUDE.md 버전 관리 규칙에 명시. **검증**: Playwright로 다크/라이트 사이드바 스크린샷(배지 반짝임 확인), 버전 쿼리스트링 붙은 5개 스크립트 정상 로드+psychtest 탭 6개 정상 렌더링까지 콘솔 에러 없이 확인 |
| 2026-07-10 | 버전 변경 없음(별도 폴더, 신규 md/json 3개 저장) | **심리테스트 콘텐츠 자동 생성 시스템 1회성 셋업.** 사용자가 앞으로 "새 심리테스트 추가해줘: {주제+문항}" 또는 md로 여러 개(예: 20개)를 한 번에 던지고 "다음 n개 만들어줘"라고만 요청하면 config 작성→이미지 컨셉 제안→샘플 승인→전체 제작→배포 연동까지 항상 같은 절차로 자동 진행되도록 요청 — 이번 세션은 그 절차 자체를 문서로 저장만 함(실제 신규 테스트 제작 없음). **첫 시도에서 사용자가 첨부한 md 파일이 인코딩 손상 상태(UTF-8 바이트 중 0x80~0x8F 범위가 통째로 유실)로 들어와, 문맥 추측 대신 원문을 채팅에 직접 붙여넣어 달라고 요청해 정확한 텍스트를 다시 받은 뒤 진행** — 향후 20개 콘텐츠 제작을 좌우할 규칙 문서라 추측 복원의 위험을 설명하고 원문 확보를 우선함. 신규 `test-engine/CLAUDE.md`(입력 형식/사전 체크/폴더 생성/이미지 샘플 우선 승인 절차/배포 연동/QA 체크리스트/배치 진행 보고 규칙 4-1~4-7 전체), `test-engine/tests/index.json`(콘텐츠 레지스트리, 기존 mental-age 1건 시드 등록 — 신규 테스트 작업 전 항상 먼저 읽어 id 중복·톤 반복 여부 체크하는 용도), `test-engine/style-guide.md`(톤 스타일 가이드 4종: 병맛/장르 미스매치·진지감성형·깔끔위트형·밈드립형, 브랜드 컬러·타깃·금지사항, mental-age의 톤 기록 시드 포함) 3개 파일 신규 저장. `test-engine/README.md` 최상단에 "신규 콘텐츠 작업은 이 3개 파일을 먼저 읽을 것" 포인터 한 줄 추가(README는 엔진 기술 구조 설명용으로 역할 분리 유지). **핵심 원칙**: 이미지는 항상 커버+결과 1~2컷 샘플만 먼저 만들어 승인받은 뒤 전체 생성(전량 선생성 금지), 여러 개를 배치 제작할 때는 배치마다 "전체 N개 중 M개 완료" 진행 현황을 매번 보고, 톤은 병맛 하나로 고정하지 않고 4종 중 주제에 맞는 것을 선택하며 최근 3회 이상 반복된 톤·컨셉은 피함 |
| 2026-07-10 | v0.6.6 | **MBTI존 콘텐츠 #11/12/13 제작 완료 + 사이트 전면 연동.** 사용자가 채팅에 3장의 Gemini 레퍼런스 이미지(neon 라인아트=취중 MBTI, 스크랩북/폴라로이드=찐vs겉 MBTI, 페이퍼컷 마스크=연애 MBTI)를 첨부하며 `test-engine/CLAUDE.md` 4-1~4-7 절차대로 11~13번 제작 요청. 채팅 붙여넣기 이미지는 파일로 접근 불가해(로고 이미지 때와 동일한 제약) 사용자가 VS Code 탐색기로 각 테스트 `assets/reference/`에 직접 업로드하도록 안내 후 진행. **콘텐츠 제작**: `prompts.md` 3세트(cover+generic, 레퍼런스의 2패널 구성을 그대로 반영) 작성 → `generate-assets.js`(OpenAI `images/edits`, 레퍼런스 스타일 전이)로 6장 전량 1회 성공 → 신규 테마 3종(`themes/scrapbook.css`/`themes/papercut.css`/`themes/neon.css`, 이미지 매체별로 배경 질감·폰트·버튼 스타일 전부 새로 설계, retro 재사용 없음) → 문항 이미지 SVG 10개(4-4-1 규칙, API 비용 없이 직접 제작) → `index.json`/`style-guide.md` 갱신(원래 style-guide 계획은 real-vs-fake=페이퍼컷/love-mode=스크랩북이었으나 실제 레퍼런스가 반대 조합에 더 잘 맞아 배정을 바꿔 채택, 사유를 기록해둠) → Playwright로 3개 전체 플로우(인트로→8~16문항→결과) 콘솔 에러 0건 확인. **사이트 연동**: 사용자가 이전 세션에서 "MBTI존이 아직 안 열려있다"는 설명을 요청 — Read 도구로 보여준 스크린샷이 안 보인다는 피드백을 받아(다시 한번 동일 패턴 발생) Artifact(base64 인라인) 페이지로 전환해 재설명, 승인받아 진행. `data.js`에 `AppData.mbtiZoneTests` 신설(`externalTests`와 동일 구조), `app.js`의 `PSYCHTEST_LOCKED.mbtizone` 가짜 잠금 카드 제거, `externalTestsBannerHTML()`(전체너비 배너 1줄)을 `externalTestsFeedHTML()`(인기순 1위 강조 카드 + 3열 그리드, 사용자가 스크린샷으로 보여준 character 카테고리 레이아웃과 동일 패턴)로 교체해 몰입테스트/MBTI존 두 카테고리 모두에 적용, `buildTestIndex()`에 `mbtiZoneTests` 병합, `sections.json`의 MBTI존 섹션을 준비중 placeholder에서 실제 `test_ids` 3개로 교체, `sitemap.xml`에 URL 3개 추가. **검증**: Playwright로 인위적으로 클릭수를 조작해 인기순 정렬이 실제로 바뀌는지, 잠금 카드 잔재가 없는지(0건), 그리드/피처드 카드 클릭 시 실제 test-engine 페이지로 이동하는지, 홈 화면 MBTI존 섹션 카드 클릭까지 콘솔 에러 없이 확인. `index.html` 버전 표시(헤더 주석+사이드바)와 정적 자산 캐시버스팅 `?v=` 5곳을 v0.6.6으로 갱신 |
| 2026-07-10 | 버전 변경 없음(별도 폴더) | **몰입테스트/MBTI존 버그수정 + 콘텐츠 리라이팅·신규 3건 — 사이트 미연결 상태.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 "STEP 3 이후" 항목 참고. 사용자가 스크린샷 7장으로 제보한 문제(①mental-age 문항 카드가 반으로 나뉘어 보이는 레이아웃 버그 ②#11~13 문항 이미지가 내용과 무관 ③단답형 질문/답변 ④#12 설명 부족)를 전부 해결. `themes/retro.css`의 가짜 타이틀바 flex 정렬 버그 수정, #11~13 질문/답변을 상황 서술형 완결 문장으로 전면 리라이팅(채점값 무변경), 문항 이미지는 API 생성을 잠깐 시도했다가 비용 문제로 SVG 제작으로 원복하되 내용에 맞춰 10장 전량 재제작. 이어서 몰입테스트 #4(소비유형)/#5(빌런지수)/#6(MBTI 과몰입) 3개를 동일 기준(자연문장화)으로 신규 제작 — 사용자가 미리 준비해둔 config.json 초안+Gemini 레퍼런스 3장을 바탕으로 신규 테마 3종(`vintage-collage.css`/`noir-comic.css`/`stained-glass.css`) 신설 후 커버+결과 이미지 18장 전량 생성. `test-engine/CLAUDE.md`(4-1 자연문장화 규칙, 4-4-1 SVG 유지 재확정)·`style-guide.md`(신규 매체 3종+SVG 원복 경위) 갱신. Playwright로 7개 테스트(mental-age/#4/#5/#6/#11/#12/#13) 전체 완주+콘솔 에러 0건 확인. **사이트 연동(sections.json/sitemap/data.js)은 사용자 승인 전이라 보류** — 다음 세션에서 진행 |
| 2026-07-11 | v0.6.7 | **몰입테스트 #4/#5/#6(소비유형/빌런지수/MBTI과몰입) 사이트 연동 완료.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 참고. 지난 세션에서 미완으로 남았던 #4~6 QA를 Playwright로 재검증(콘솔 에러 0, 이미지 로드·공유/저장 버튼·villain-index의 `sum+tag` 채점 정상 확인, 검증 중 의심됐던 푸터 겹침·폰트 글자겹침 2건은 실제로는 버그가 아님을 확인) 후 사용자 승인을 받아 `data.js`(`AppData.externalTests` 3건)/`sections.json`("new" 섹션 test_ids)/`sitemap.xml`(URL 3줄)에 연동. `tests/index.json` 상태값을 "완료(운영중)"으로 갱신(#2의 방치된 상태 문구도 함께 정정). Playwright로 홈 화면 신설존 6개 카드·심리테스트존 몰입테스트 탭 그리드 양쪽에서 신규 3개 정상 노출+클릭 시 실제 test-engine 페이지 이동까지 콘솔 에러 없이 확인 |
| 2026-07-11 | v0.7.1 | **test-engine 하단 고정 버튼바 불투명 배경 — 스크롤 시 본문이 버튼 사이로 비쳐 겹쳐 보이던 문제 수정.** v0.6.9(padding-bottom 보정)·v0.7.0(캐시버스팅)과는 또 별개의 세 번째 겹침 이슈 — 버튼바(`.te-choices-fixed`)는 `position:fixed`인데 배경이 투명이라, 본문이 뷰포트보다 길어 스크롤하면 결과 카드 텍스트/이미지가 버튼과 버튼 사이 틈으로 그대로 비쳐 보였음(padding 보정은 "끝까지 스크롤했을 때 마지막 내용이 가려지지 않게" 하는 장치라 스크롤 중간 상태의 비침은 못 막음). `engine.css`의 버튼바에 상단 22px만 투명→불투명으로 페이드되는 `linear-gradient` 배경(`var(--te-footer-bg)`) 추가 + 상단 패딩 12→26px(페이드 구간 확보), 테마마다 페이지 배경색이 달라 테마 10종(`retro`/`office`/`journal`/`fantasy`/`scrapbook`/`papercut`/`neon`/`vintage-collage`/`noir-comic`/`stained-glass`) 전부 `.te-app`에 자기 페이지 하단 배경과 같은 톤의 `--te-footer-bg`를 정의(미정의 시 `#0f172a` 폴백). v0.7.0 규칙대로 캐시버스팅 갱신 — `ENGINE_ASSET_VERSION` 6→7(themes), 10개 테스트 index.html의 `engine.css?v=4`/`engine.js?v=7`(result-card.js는 무변경이라 v=2 유지). **검증**: `node --check` 통과, Playwright로 mental-age(retro, 다크)/4-spending-type(vintage-collage, 라이트)/5-villain-index(noir-comic, 다크) 3개를 결과 화면까지 실제 완주 — 테마 CSS `?v=7` 로드, `--te-footer-bg` 정상 해석, padding-bottom=버튼바 높이(296px) 일치, 스크롤 중간 지점 스크린샷으로 본문이 버튼바 뒤에서 깔끔하게 가려짐을 육안 확인, 콘솔 에러 0건 |
| 2026-07-11 | v0.8.2 | **Kinetic Logic 리디자인 실사용 버그 6건 수정(폰트 뭉개짐/히어로 텍스트 무색투명화/로또 버튼 톤/글자크기 확대).** 사용자가 실기기 스크린샷 다수(심리테스트존 몰입테스트·MBTI존, 로또 조합기, MBTI존 3개 테스트 모바일 화면, code1.html 레퍼런스)로 6가지를 한 번에 제보. **①한글 글씨 뭉개짐**: 원인은 구글 폰트 Black Han Sans/Do Hyeon/Song Myung이 전부 단일 굵기(400)만 제공하는데, 메인 사이트의 `font-black` 유틸리티나 test-engine의 `<h1>`/`<h2>` 브라우저 기본 bold가 900/700 굵기를 요청하면서 브라우저가 없는 굵기를 "합성 볼드"(글자 윤곽을 겹쳐 그려 두껍게 만드는 렌더링)로 흉내내 한글 획이 뭉개져 보였던 것 — `style.css`의 `html{}`과 `test-engine/engine.css`의 `.te-app`에 각각 `font-synthesis: none;` 한 줄씩 추가해 합성 자체를 차단(실제 폰트에 있는 가장 가까운 굵기로 깔끔하게 렌더링, Plus Jakarta Sans/Gaegu/Baloo 2처럼 요청한 굵기가 진짜 로드된 경우는 무관하게 그대로 정상 동작). **②히어로 카드 텍스트 무색투명화**: 심리테스트존 몰입테스트/MBTI존 최상단 히어로 배너(`externalTestsFeedHTML()`)가 `text-slate-100`/`text-slate-300`/`text-orange-300` Tailwind 클래스를 쓰고 있었는데, Kinetic Logic(v0.8.0)에서 slate 50~300이 "밝은 텍스트"에서 "진한 잉크 텍스트"로 의미가 뒤바뀐 걸 이 컴포넌트만 놓쳤던 것 — 어두운 스크림(`.psy-hero-scrim`) 위에 어두운 잉크색 텍스트가 얹혀 사실상 안 보였음(v0.8.0의 "오늘의 인생 한마디"/로또 공 회귀 버그와 동일 계열, 이번엔 `.psy-card-title`처럼 테마 리맵과 무관한 고정 hex 클래스(`.psy-hero-eyebrow`/`.psy-hero-title`/`.psy-hero-subtitle` 신규)로 분리해 근본 수정. **③로또 번호 조합기 버튼 톤 불일치**: 5개 버튼이 `rounded-xl`만 있고 `border` 클래스가 없어 Phase 1 전역 브루탈리즘 shim(`[class*="rounded-xl"][class*="border"]`)이 전혀 안 걸리고 있었음(구버전 그라데이션/파스텔 필 그대로 방치) — `border border-{color}-600` 클래스를 추가해 두뇌나이 난이도 버튼과 동일한 기존 검증된 패턴(shim이 자동으로 굵은 잉크 테두리+하드 그림자+눌림 효과 부여)에 올라타도록 수정, "직접 뽑기 게임" CTA는 잉크색 배경+primary-container(옐로) 글자로 별도 강조. **④~⑥ 글자 크기 15% 확대**: test-engine의 질문/답/결과 텍스트가 작아 잘 안 보인다는 지적 — 9개 테마(retro/office/journal/vintage-collage/noir-comic/stained-glass 6개=몰입테스트 전체, scrapbook/papercut/neon 3개=MBTI존 real-vs-fake/love-mode/drunk)가 전부 `test-engine/engine.css`의 공용 크기 클래스(`.te-title`/`.te-desc`/`.te-question-text`/`.te-btn`/`.te-result-*` 등)를 공유하므로, 이 파일 한 곳만 수정해 전체에 자동 반영(22→25px, 20→23px, 24→28px, 16→18px 등 항목별 약 15% 확대, `min-height`/`padding`도 커진 글자에 맞춰 소폭 보정 — 하단 고정 버튼바 높이는 `syncFixedFooterHeight()`가 런타임에 자동 재측정해 별도 상수 수정 불필요). **캐시버스팅**: `engine.css` 자체 버전(v3→v4)과 10개 테스트 index.html의 `engine.css?v=4`→`?v=5` 일괄 갱신(engine.js/result-card.js는 이번에 변경 없어 그대로), 메인 사이트 `index.html`의 `style.css`/`data.js`/`supabase-client.js`/`kakao-share.js`/`app.js` `?v=`도 0.8.2로 갱신. **검증**: `node --check` 대상 없음(CSS/HTML 변경 위주), 정적 파일 grep으로 shim 매칭 조건(rounded-xl+border 동시 포함) 확인, Playwright로 심리테스트존 히어로 배너 텍스트 대비·로또 버튼 렌더링·mental-age/drunk-mbti/real-vs-fake-mbti 실제 완주 시 글자 크기·폰트 렌더링(합성 볼드 사라짐) 스크린샷 확인 |
| 2026-07-11 | v0.8.1 | **Kinetic Logic 리디자인 후속 수정 7건.** 상세는 위 "2. 기능 현황" 표의 해당 행 참고. 다크/라이트 토글 잔재 확인(코드엔 없음, 캐시 문제로 진단)/홈 카드 배색(amber-950·indigo-950·fuchsia-900 미리맵 문제)/홈 섹션 개편(신설존+MBTI존→"요즘 뜨는 심리테스트" 통합 3개, 이번주 인기 전체메뉴 포함 3개)/FAQ 클릭영역 보강/이상형 월드컵 전용 클래스 톤 적용/색상 리맵 27개 일괄 감사·추가(두뇌·MBTI·ADHD 모드버튼 포함)/25개 섹션 전체 크롤 검증. v0.8.0에서 이미 한 차례(커밋 `0a880cc`) 월드컵 카드 테두리 일부·순위 배지 플랫화·홈 버튼 색상·푸터 갱신일도 소소하게 손봤었음(그때는 changelog 누락, 여기 함께 기록) |
| 2026-07-11 | v0.8.0 | **전체 UI 리디자인 "Kinetic Logic" 착수 — Phase 1(전역 팔레트+shim) 완료, Phase 2 일부(심리테스트존 질문카드) 완료.** 상세는 위 "2. 기능 현황" 표의 "🎨 전체 UI 리디자인" 행 참고. 세션이 한 번 끊긴 뒤 재개하며 `git status`/`git diff`로 미완 작업 상태를 파악(다크/라이트 토글 제거+단일 라이트 팔레트로 전환된 v0.8.0 변경이 커밋 전 상태로 남아있었음). 원본 기획 문서(`DESIGN1.md`)는 실존한 적 없고 사용자가 만든 참고 목업 이미지로만 존재해 다시 첨부받아 확인, 발견된 회귀 버그 2건(홈 인용문 `text-white` 무색투명화, 로또 공 31~40 구간 `text-white` 무색투명화) 수정, `.option-btn` 4색 순환 CSS 추가, 심리테스트존(몰입테스트·MBTI존 제외) 질문 화면에 말풍선 카드+캐릭터 아이콘 신규 적용. Artifact로 실제 스크린샷을 보여주고 매 단계 승인받은 뒤 진행 |
| 2026-07-11 | v0.7.0 | **test-engine 캐시버스팅 도입 — 배포해도 반영 안 되는 것처럼 보이던 근본 원인 발견·수정.** 사용자가 v0.6.9 배포 후 새로고침까지 했는데도 여전히 버튼-본문 겹침이 재현된다고 재제보(스크린샷 2장) — "배포 문제인가?"라는 정확한 의심을 계기로 조사한 결과, 메인 사이트 `index.html`은 v0.6.5부터 `?v=` 캐시버스팅이 있는데 **`/test-engine/`은 `engine.js`/`engine.css`/`result-card.js` 전부 처음부터 캐시버스팅 쿼리스트링이 아예 없었음**을 발견 — Cloudflare/브라우저가 파일명이 그대로면 몇 시간씩 구버전을 계속 서빙할 수 있어, v0.6.9에서 고친 engine.js가 실제로는 배포됐어도 사용자 브라우저엔 반영이 안 됐을 가능성이 높음. 완성된 테스트 10개의 `index.html` 전부에 `engine.js?v=6`/`engine.css?v=3`/`result-card.js?v=2` 추가, 동적 주입되는 `themes/*.css`는 `engine.js` 내부 `ENGINE_ASSET_VERSION` 상수 하나로 중앙 관리(themes 파일이 바뀔 땐 이 상수만 올리면 10개 index.html을 안 건드려도 자동 캐시버스팅). engine.js 헤더 주석에 "앞으로 이 세 파일을 고칠 때마다 버전 번호를 올리고 10개 index.html의 `?v=`도 같이 교체할 것"을 명문화, `test-engine/README.md`에도 동일 규칙 기록(재발 방지). **검증**: `node --check` 통과, Playwright로 mental-age 전체 플레이 — 테마 CSS가 `?v=6` 쿼리로 정상 로드되고, 결과 화면 padding-bottom(282px)이 버튼바 실제 높이(282px)와 정확히 일치해 겹침 없음을 스크린샷으로 재확인, 404/콘솔 에러 없음. 이하 v0.6.9 상세 → |
| 2026-07-11 | v0.6.9 | **test-engine 버그 3건 수정 — 완료자수 과장 수치 정상화, 버튼바-본문 겹침 방어, 카드 조회수 새로고침 안내.** 사용자가 새로 리디자인된 몰입테스트 카드를 실사용하며 실제 화면 스크린샷 4장으로 제보. ①카드 그리드 조회수가 "▷ 0"으로만 보이고 안 올라가는 것처럼 보인다고 제보 — Playwright로 재현해보니 코드 자체(`engagementCount(t.engagementKey, t.baseCount)`)는 정상 작동(297/268/256/... 정상 표시+클릭 시 누적)함을 확인, 원인은 사용자의 브라우저 탭이 이번 세션 코드 변경 전에 이미 열려 있어 SPA 특성상(섹션 이동 시 `app.js` 재로드 없음) 예전 스크립트가 메모리에 그대로 남아있던 것으로 진단 — 탭을 새로고침하면 해결됨을 안내. ②villain-index 등 test-engine 테스트 진입 시 "지금까지 241,671명이 확인했어요"처럼 갓 만든 신규 콘텐츠치고 터무니없이 큰 완료자수가 뜬다고 지적 — 이 문제는 이번 세션 변경과 무관한 기존(STEP 1부터의) 문제로, `test-engine/tests/*/config.json`의 `seed_count`가 전부 15만~29만대의 비현실적인 값으로 세팅돼 있던 것을 발견. 21개 테스트 전부(연동 완료 10개는 카드 조회수보다 낮은 자연스러운 완료율로, 미연동 11개는 40~90대의 낮은 값으로) 재설정 — 처음엔 Python `json.dump`로 재작성해 포맷팅(줄바꿈 방식)까지 전부 바뀌며 파일당 수백 줄짜리 diff가 생기는 실수를 저질러, `git show HEAD:path`로 원본을 읽어와 `seed_count` 숫자 하나만 정규식 치환하는 방식으로 다시 작업해 파일당 1줄짜리 diff로 수정(git checkout으로 되돌리는 건 이미 완료한 수정 작업을 날릴 위험이 있어 세이프가드에 의해 차단됨 — 대신 원본 blob을 읽어와 재구성하는 방식으로 우회). ③결과 화면에서 스크롤하면 하단 고정 버튼바(카카오공유/이미지저장/공유하기/다시하기 4개)가 결과 카드 내용과 겹쳐 보인다고 스크린샷으로 제보 — `syncFixedFooterHeight()`가 버튼바 실제 높이를 측정해 본문에 정확히 그만큼 padding-bottom을 주는 기존 메커니즘(STEP 3에서 이미 한 번 고친 것)은 이번 세션 코드 변경 대상이 아니었고 Playwright로도 정상 동작(padding 282px=footer 282px 정확히 일치)해 헤드리스 환경에서는 재현 안 됨 — 대부분의 테마가 Google Fonts를 `@import`로 비동기 로드하는데, 최초 측정(`requestAnimationFrame`)은 폴백 폰트 기준으로 이뤄지고 실제 웹폰트가 나중에 적용되며 버튼 텍스트 줄바꿈/높이가 커지면 padding이 모자라 겹치는 것으로 추정(느린 네트워크/모바일에서 더 잘 재현되는 것과 일치) — `syncFixedFooterHeight()`에 `document.fonts.ready` 완료 후 한 번 더 재측정하는 로직과, 화면 회전 등 뷰포트 변화에도 대응하도록 `window.resize` 리스너를 `init()`에 전역 1회 추가해 방어(모든 테마·모든 테스트에 공통 적용되는 엔진 레벨 수정). **검증**: `node --check`로 engine.js 문법 확인, `python3 -c "json.load"`로 21개 config.json 전부 파싱 정상 확인, Playwright로 카드 그리드 조회수(297/268/256/231/219/205 정상)·villain-index 완료자수(171명으로 정상화)·footer padding 동기화(282px 일치) 재검증, 콘솔 에러 없음. 사용자 요청으로 이 수정을 커밋+푸시로 즉시 저장(평소 작업 배치 없이 자주 커밋하는 방침 재확인). 이하 v0.6.8 상세 → |
| 2026-07-11 | v0.6.8 | **몰입테스트/MBTI존 카드 UI 리디자인 — 이모지 원형 카드 → 커버 이미지 포스터 카드.** 상세는 위 "2. 기능 현황" 표의 "🧪 심리테스트 전면 재구축" 행 v0.6.8 항목 참고. poomang.com·방구석연구소 레퍼런스로 "카드가 밋밋하고 AI스럽다"는 지적을 받아, 이미 존재하던 각 테스트의 `assets/cover.webp`를 실제 카드에 처음 사용하도록 `externalTestsFeedHTML()` 전면 재작성(3:4 포스터 타일+그라데이션 스크림+제목/훅 오버레이+금은동 순위 리본, 히어로 배너도 이미지 배경으로 확대). Artifact 실사진 목업으로 먼저 확인받고(1차 배포 시 라이트모드 대비 버그 발견해 다크 전용으로 수정 후 재배포) 범위를 확정. 조회수는 `baseCount`(200~300 임의 초기값)+실제 클릭 카운팅, `formatCount()`로 1만 이상만 "만" 단위 축약. 캐릭터/성향/취향/국민테스트 4개 카테고리의 잠금카드는 스타일만 통일(무드 그라데이션+이모지 워터마크+자물쇠), 실콘텐츠 커버 아트 신규 제작은 2단계로 보류 |

---

## 5. 진행 중 결정사항 로그 (기획 논의에서 합의된 것)
- 로또 조합기는 "다른 사이트와 차별화" 목적으로 4가지 모드(랜덤/직접지정/운세연동/통계기반) 설계 → **v0.0.42에서 통계기반까지 4개 모드 전부 완료**
- 꿈해몽 검색 UX: 드롭다운 강제 없이 "통합설명 먼저 → 하위 칩으로 점진적 심화" 방식 채택
- 카카오 공유는 JS 키 없어서 보류, Web Share API로 우선 대응 → **v0.0.41에서 착수 완료.** Kakao Developers 앱 생성 후 JavaScript 키/도메인 등록까지 마쳐 Web Share API와 병행 제공
- 라이트 모드는 순백(#fff) 대신 은은한 블루그레이 톤으로 설계 (사용자 요청). 기본 테마는 계속 다크 유지, 라이트는 선택 옵션
- 마이홈은 별도 메뉴로 유지하지 않고 홈 화면에 완전 통합하기로 결정 (사용자 요청 — "적절히 합쳐줘")
- **Phase 2 확장(2026-07-02) 합의사항** — 상세는 `PRD.md` "9. 확장 로드맵" 참고:
  - 진행 순서: A.메뉴개편 → B.신규 인지테스트 6종+α → C.난이도체계 → D.백엔드(v0.0.40 1단계 착수) → E.동물비유카드 → F.레이더차트(완료) → G.라이트모드 일괄반영
  - **백엔드(Supabase) — 2026-07-03 착수 확정, v0.0.40에서 1단계 완료.** 인증은 로그인 UI 없는 Anonymous Auth 채택(나중에 카카오 연결 가능하도록 manual linking도 활성화해둠). 댓글 기능은 모더레이션 리스크 때문에 스키마에서 제외(사용자 결정, 재검토 전까지 보류). service_role 키는 클라이언트에 절대 두지 않고, 퍼센타일 집계 등 서버 전용 작업에만 사용 예정(Cloudflare Functions 등)
  - 난이도 체계: 신규 인지테스트·두뇌나이(스트룹)는 쉬움/보통/어려움 3단계. MBTI·ADHD는 간단/정밀 2모드(난이도 아닌 진단 깊이 개념). 오늘의 운세는 이번 라운드 제외
  - 재미 요소(친구대결/오늘의챌린지/사운드/칭호 등)는 백로그로 기록만 해두고, 구조가 안정될 때까지 구현 보류

---

## 파일 구조 및 역할

```
testseries/
├── index.html        # 메인 SPA 진입점. 모든 섹션 div가 여기에 선언됨
├── style.css         # Tailwind로 커버 안 되는 커스텀 CSS (애니메이션, 게이지 바 등)
├── data.js           # 정적 데이터 파일 (꿈 해몽, 운세, MBTI 결과, 동물 비유 카드 등)
├── app.js            # 전체 SPA 로직, 이벤트 핸들러, 상태 관리
├── kakao-share.js    # 카카오 SDK 초기화 + 텍스트 전용 공유 헬퍼(성향형 7개 테스트가 아직 사용)
├── supabase-client.js # Supabase 익명 인증 + 결과 이중 기록
├── ads.txt           # Google AdSense 승인 판매자 확인 파일 (v0.2.6~, publisher ID: pub-4825324689294427)
├── robots.txt        # 전체 크롤러 허용 + Sitemap 위치 명시 (v0.2.8~, v0.5.1~ Sitemap 줄 추가)
├── sitemap.xml       # 검색엔진 제출용 사이트맵 (v0.5.1~) — 해시 라우팅 SPA라 루트 URL 1개만 등재
├── llms.txt          # AI/LLM 크롤러용 사이트 소개+전체 섹션 링크 목록 (v0.4.8~, PageSpeed "에이전트 접근성" 권장사항 대응)
├── assets/brand/      # 브랜드 로고 원본+가공 파일 (v0.3.4~) — logo-source.png(원본), logo-icon.png(512px 투명 아이콘, 사이트 전역 사용), apple-touch-icon.png, favicon-32.png
├── assets/worldcup/   # (레거시, v0.4.0~v0.6.0) 구 밈 8개 단일토너먼트 실사진 — v0.6.1 팩 시스템 교체 후 미사용, 삭제하지 않고 방치(정리 후보)
├── worldcup/images/{packId}/{itemId}.webp   # 이상형 월드컵 이미지형 팩 사진 (v0.6.1~, v0.6.2~ 4팩 96장 전량 완료) — process_worldcup_images.py가 생성, 없으면 앱이 placeholder로 안전 폴백(현재는 전 팩 실사진 있음)
├── worldcup/packs/  # 월드컵 이미지 소싱 문서(v0.6.2~) — README.md(팩별 소싱 방식 차이 설명), {packId}.prompts.md(AI 생성 팩 전용 프롬프트, 현재 gwamol-emotion-16만)
├── worldcup-images-raw/  # (.gitignore) 월드컵 이미지 원본(리사이즈 전) 임시 보관 폴더 — 커밋 안 함
├── worldcup-sources/  # 월드컵 이미지형 팩(CC0 3팩)의 검색 키워드 참고자료(*.sources.json, v0.6.1~) — 앱이 읽지 않는 순수 소싱 참고용
├── functions/
│   ├── api/           # Cloudflare Pages Functions (로또 통계, 퍼센타일 배치집계, 제휴문의 Slack 전달)
│   └── share/[section].js  # 공유 랜딩 페이지 — og:image 등 동적 메타태그 서빙 후 SPA로 리다이렉트 (v0.0.49~)
├── share-cards/       # 전체 15개 테스트+월드컵 이모지형 팩 공유용 정적 이미지(JPEG, v0.0.49~, v0.6.1~ worldcup-*.jpg 64장 추가)
├── scripts/
│   ├── generate-share-cards.js  # share-cards/ 재생성 스크립트 — 데이터(animalCards/mbtiResults/등급표/dreamData 등) 바뀌면 재실행 필요
│   ├── generate-phase4-share-cards.js  # 심리테스트존/밸런스게임 공유 이미지 생성(v0.2.3~) — AppData.psychTests/balanceGames를 그대로 순회하므로 신규 콘텐츠 추가 시 재실행만 하면 됨
│   ├── generate-og-card.js  # 루트 페이지 og:image(share-cards/og-default.jpg) 생성(v0.5.1~) — 문구/톤 바뀌면 재실행
│   ├── generate-worldcup-share-cards.js  # 이상형 월드컵 emoji형 팩(lazy-hell-32/superpower-32) 공유카드 64장 생성(v0.6.1~) — 신규 emoji형 팩 추가 시 재실행
│   ├── process_worldcup_images.py  # 이상형 월드컵 이미지형 팩 파이프라인(v0.6.1~, Python/Pillow) — worldcup-images-raw/{packId}/{itemId}.(png|jpg) → worldcup/images/{packId}/{itemId}.webp(최대 800px)
│   └── generateWorldcupImages.js  # 이상형 월드컵 AI 오리지널 팩(gwamol-emotion-16) 전용 이미지 생성기(v0.6.2~) — OpenAI Images API(gpt-image-1) 호출 후 process_worldcup_images.py 자동 연계. 실행: `node scripts/generateWorldcupImages.js gwamol-emotion-16`, 필요 환경변수 OPENAI_API_KEY(.env, .env.example 참고)
├── CLAUDE.md         # 아키텍처 규칙 (이 파일)
├── BUSINESS_TEMPLATES.md  # 제휴 문의/콘텐츠 협업 요청 이메일 서식 (코드와 무관한 운영 문서, v0.2.2~)
├── CONTENT_PROMPTS.md # 심리테스트존 신규 콘텐츠를 AI로 생성할 때 쓰는 프롬프트 서식 (v0.2.4~)
└── HISTORIC_LOG.md   # 히스토리 로그 (세션 간 이어서 작업할 때 반드시 먼저 읽기)
```

---

## index.html 구조 규칙

```html
<!-- 섹션 네이밍 규칙 -->
<div id="section-home"   class="section hidden"> ... </div>
<div id="section-mbti"   class="section hidden"> ... </div>
<div id="section-dream"  class="section hidden"> ... </div>
<div id="section-fortune" class="section hidden"> ... </div>
<div id="section-brain"  class="section hidden"> ... </div>
<div id="section-adhd"   class="section hidden"> ... </div>
<div id="section-lotto"  class="section hidden"> ... </div>  <!-- v0.0.9 추가 -->
```
※ `section-mypage`는 v0.0.10에서 제거됨 — 마이홈 내용은 `section-home` 내부의 `#home-mypage-container`로 통합 (`renderHomeMypage()` 참고)

- 모든 섹션은 `section` 클래스를 공유하고, 초기에는 `hidden` 상태
- active 섹션만 `hidden` 제거 + `fade-in` 애니메이션 적용
- Tailwind 클래스 우선, 커스텀 CSS는 style.css에만 작성

---

## data.js 규칙

```javascript
// 네임스페이스: window.AppData 사용
window.AppData = {
  quotes: [...],          // 오늘의 인생 한마디: { text, author, role } 객체 배열, 365개(인물 어록+속담/격언) 완성 (v0.0.22~v0.0.27)
  dreamData: [...],       // 꿈 해몽 데이터 배열 (v0.0.9~ 구조 변경, 아래 참고)
  fortuneData: {...},     // 운세 데이터 객체 (띠별, v0.0.9~ luckyNum 필드 포함)
  mbtiQuestions: [...],       // MBTI 간단 모드 12문항 배열
  mbtiQuestionsExtra: [...],  // MBTI 정밀 모드 추가 12문항 (v0.0.28~, mbtiQuestions와 합쳐 24문항으로 사용)
  mbtiResults: {...},         // MBTI 16유형 결과 객체 (간단/정밀 공용)
  adhdQuestions: [...],       // ADHD 간단 모드 10문항 배열
  adhdQuestionsPrecise: [...],// ADHD 정밀 모드 20문항 (v0.0.28~, 각 문항에 domain:'inattention'|'hyperactivity' 태그)
  adhdResults: [...],         // ADHD 결과 등급 배열 (간단/정밀 공용, 정밀은 점수/3 환산 후 대입)
};
```

### dreamData 구조 (v0.0.9~)
"검색 → 통합설명 먼저 → 하위 관련꿈 칩(chip)" UX를 위해 테마+하위variants 구조로 개편함.
```javascript
{
  keywords: ["뱀", "뱀꿈", "구렁이"],   // 테마 검색 키워드
  title: "뱀이 나오는 꿈",              // 테마 통합 제목
  summary: "...", detail: "...", lucky: "...", luckyNum: "...", action: "...",
  variants: [                          // 하위 세부 꿈 (없으면 빈 배열)
    { keywords: ["뱀에게 물리다"], title: "뱀에게 물리는 꿈", detail: "...", lucky: "...", luckyNum: "...", action: "..." }
  ]
}
```
- 총 174개 테마(v0.0.32~v0.0.39: 59→74개, v0.1.1: PRD 10-4 1단계로 74→174개 대량 확장), 테마당 최소 2개(v0.1.1 신규 100개는 4개씩) variants 보유, 총 variants 546개
- 검색은 테마 키워드 + 모든 variants 키워드를 동시에 매칭
- variants가 있는 테마는 모달 하단에 칩 형태로 노출, 클릭 시 로딩 없이 즉시 전환

### fortuneData 구조 (v0.0.9~, v0.0.35~ categories/variants로 개편)
띠별 데이터에 `luckyNum` 필드 추가 (로또 조합기 "오늘의 운세·꿈 연동" 모드에서 사용).
v0.0.35~v0.0.36에서 기존 `base`(4개 카테고리 고정 문장) 구조를 `categories: [{ title, variants: [{positive, detail}, ...] }]`로 개편 — 카테고리(총운/재물운/애정운/건강운)마다 여러 벌의 문구를 두고 `dailyQuoteSeed()`(오늘의 인생 한마디와 동일한 24시간 KST 시드) 기반으로 매일 다른 variant를 결정론적으로 노출. **12띠×4카테고리×7벌 = 336개 문장 목표를 v0.0.36에서 전부 채움**(진행 히스토리는 HISTORIC_LOG.md 참고). 새로운 variant를 추가할 땐 각 띠의 기존 캐릭터성(예: 쥐=분석력, 소=성실함, 호랑이=대담함, 뱀=통찰력 등)을 유지하는 톤으로 작성할 것 — data.js 코드 주석에 띠별 성격 요약이 없으므로 기존 variants[0](최초 문구)을 참고.

## localStorage 키 규칙 (v0.0.9~)
- `ranking_{section}`: 섹션별 결과 기록 (mbti/fortune/brain/adhd)
- `comments_{section}`: 섹션별 댓글 임시 저장
- `done_{section}`: 마이홈 완주 현황 플래그 ('1'이면 완료) — mbti/dream/fortune/brain/adhd
- `app_nickname`: 대표 닉네임. 마이홈뿐 아니라 v0.0.30~ 닉네임을 쓰는 모든 테스트(13개)의 시작화면 입력창 프리필에도 공용으로 사용 — 한 곳에서 저장하면 전부 반영됨
- `visit_streak`, `last_visit_date`: 연속 방문 스트릭
- `last_fortune_luckynum`, `last_dream_luckynum`: 로또 조합기 "운세·꿈 연동" 모드 시드값
- `theme`: `'light'` | `'dark'` (없으면 다크가 기본값) — v0.0.10 다크/라이트 토글

---

## 다크/라이트 테마 아키텍처 (v0.0.10~)

**절대 개별 요소에 `dark:`/`light:` variant를 달지 말 것.** 이 프로젝트는 빌드 도구가 없고 app.js가 Tailwind 유틸리티 클래스가 박힌 HTML 문자열을 대량으로 생성하기 때문에, 요소 하나하나에 라이트 모드 클래스를 추가하는 방식은 유지보수가 불가능하다. 대신 **CSS 변수 remap** 방식을 쓴다.

- `index.html`의 `tailwind.config`(Tailwind CDN 런타임 설정)에서 `slate` 팔레트 전체와, "부드러운 강조 박스"(`bg-{color}-900|800|700` + 투명도 + `text-{color}-200|300|400`)에 쓰이는 blue/indigo/violet/emerald/rose/amber/yellow의 일부 shade를 `rgb(var(--x) / <alpha-value>)` 형태로 remap해 둠.
- `style.css`의 `:root`(`html`, 다크=Tailwind 표준값)와 `html.light`(라이트 override) 두 블록에서 실제 색상값을 정의. 라이트 값은 순백이 아닌 블루그레이 계열로 설계됨.
- `app.js`의 `toggleTheme()`이 `<html>`에 `light` 클래스를 토글하고 `localStorage.theme`에 저장. `index.html` `<head>`의 인라인 스크립트가 렌더 전에 즉시 반영해 FOUC를 방지함.
- **500/600 shade(버튼·뱃지 원색)는 remap 대상이 아님** — 항상 선명한 원색 유지가 목적이므로 손대지 않는다.
- **새 UI를 추가할 때**: 중립 표면(카드/입력창/페이지 배경 등 slate 계열) 위에 올라가는 텍스트는 `text-white`를 절대 하드코딩하지 말고 `text-slate-100`(또는 hover 시 `text-slate-50`)을 쓸 것 — 그래야 라이트 모드에서도 자동으로 대비가 유지된다. `text-white`는 violet-600/blue-600/amber 그라데이션처럼 테마와 무관하게 항상 채도 높은 색상 배경 위에서만 사용한다.
- 새로운 accent 색상이나 shade가 필요해지면, 같은 패턴(`tailwind.config`에 remap 추가 + `style.css`에 dark/light 값 추가)을 따를 것.

---

## app.js 규칙

```javascript
// 전역 상태 네임스페이스: window.App
window.App = {
  state: {
    currentSection: 'home',
    // 섹션별 상태
    mbti: { nickname: '', answers: [], step: 0 },
    brain: { nickname: '', difficulty: null, answers: [], step: 0 },
    adhd: { nickname: '', answers: [], step: 0 },
    fortune: { year: null },
  },
  // 메서드
  navigate(sectionId) { ... },
  showLoader(callback) { ... },  // 3초 광고 프리로더
};
```

---

## SPA 네비게이션 규칙

- `navigate(sectionId)` 호출 → 모든 `.section` div에 `hidden` 추가 → 해당 섹션만 `hidden` 제거 + `fade-in` 클래스 추가
- URL hash 변경: `location.hash = '#' + sectionId`
- 뒤로가기 지원: `hashchange` 이벤트 리스닝
- **사이드바 "테스트" 그룹 (v0.0.11~)**: MBTI/두뇌나이/ADHD처럼 "인지·성향 테스트" 성격의 신규 섹션은 `#nav-group-tests-items` 안에 `nav-item nav-subitem` 형태로 추가할 것. 꿈해몽/운세/로또처럼 검색·도구·유틸 성격인 것은 그룹 밖 최상위 `nav-item`으로 유지. 그룹은 `.nav-group.open` 클래스로 펼침 상태 표시, `localStorage.nav_tests_collapsed`로 접힘 여부 기억, 그룹 내부 항목이 active가 되면 `navigate()`가 자동으로 그룹을 펼침

---

## 광고 프리로더 규칙

- 결과 화면 진입 전 3초 딜레이
- 중앙에 `<div id="ad-placeholder">` — 실제 애드센스 코드 삽입 위치
- `<!-- [광고] Google AdSense 삽입 위치 -->` 주석 필수

---

## 버전 관리 규칙

- **버전 증가 방식(2026-07-04 확정, v0.0.54 이후부터 적용)**: 세 자리(`X.Y.Z`) 각각을 0~9만 쓰는 자리수처럼 취급 — 일반적인 semver(기능/호환성 의미 부여)가 아니라 그냥 "10에서 자리 올림하는 카운터"임에 주의.
  - Z(패치)가 9에서 한 번 더 오르면 → Z=0, Y(마이너) +1 (예: 0.0.9 다음은 0.0.10이 아니라 0.1.0)
  - Y가 9에서 한 번 더 오르면 → Y=0, X(메이저) +1 (예: 0.9.9 다음은 1.0.0)
  - **v0.0.54가 마지막 구버전 표기** — 다음 버전 변경부터는 이 새 규칙으로 전환해 **v0.1.0**부터 다시 시작(v0.0.54를 v0.5.4 등으로 소급 변환하지 않음). 그 다음은 v0.1.1, v0.1.2 … v0.1.9, v0.2.0 순.
- 의미 있는 변경마다 버전 1씩 증가 (위 자리올림 규칙 적용)
- 변경 시 index.html 최상단 주석 + HISTORIC_LOG.md 동시 업데이트
- 버전 형식: `<!-- vX.Y.Z | 5-in-1 Dashboard SPA -->`
- **사이드바 하단에 사용자에게 실제로 노출되는 버전 표시(`<p class="text-slate-600 text-xs">vX.Y.Z · 날짜</p>`, index.html)도 매번 같이 업데이트할 것** — v0.0.46부터 v0.0.53까지 이 표시를 깜빡하고 안 올려서 실제 화면엔 8버전이나 뒤처진 숫자가 보이고 있었던 적이 있음(v0.0.54에서 발견해 수정)
- **v0.6.5~: `index.html`이 로드하는 자체 정적 자산(`style.css`/`data.js`/`supabase-client.js`/`kakao-share.js`/`app.js`)의 `<link>`/`<script>` 태그에 `?v=X.Y.Z` 쿼리스트링을 붙여둠 — 버전을 올릴 때마다 이 5곳의 `?v=`도 새 버전으로 같이 바꿀 것.** Cloudflare Pages가 정적 자산에 `Cache-Control: public, max-age=14400`(4시간)을 붙이는데, 파일명 자체가 버전 없이 고정(`app.js` 등)이라 `index.html`만 새로 받고 JS/CSS는 브라우저 캐시에서 구버전을 계속 쓰는 "마크업은 최신인데 로직은 구버전" 불일치가 실제로 발생한 적 있음(v0.6.5 배포 직후 사용자가 새 사이드바 메뉴는 보이는데 그 메뉴가 참조하는 로직은 반영 안 된 화면을 스크린샷으로 제보 — 원인은 배포 문제가 아니라 이 캐시 불일치였음, `curl`로 라이브 파일을 직접 조회해 실제 배포본은 이미 정상임을 확인 후 진단). `?v=`를 안 올리면 이 캐시버스팅 자체가 무의미해지므로 반드시 함께 갱신.

---

## 확장 Placeholder UI 규칙 (모든 결과 페이지 하단 공통)

```html
<!-- ① 통계 비교 지면 (추후 실데이터 교체) -->
<!-- ② 랭킹/공유 지면 (로컬스토리지 연동) -->
<!-- ③ 댓글 지면 (추후 백엔드 연동) -->
```

---

## 커밋 메시지 규칙
`git commit` 시 아래 형식을 따른다.
```
타입: 내용 (버전)
```
- 타입: `feat`(기능추가) / `fix`(버그수정) / `docs`(문서수정) / `refactor`(구조개선)
- 예시: `feat: 로또 조합기 추가 (v0.0.9)`, `fix: 꿈해몽 텍스트 잘림 수정 (v0.0.4)`
- 여러 기능이 섞인 경우 커밋을 나눠서 진행 (한 커밋 = 한 목적)

## 이어서 작업 시 필수 체크리스트

0. `PRD.md`에서 프로젝트 목적/규칙/제약을 파악 (거의 안 바뀜, 최초 1회 숙지)
1. 이 문서 상단 "2. 기능 현황" 표 + "5. 진행 중 결정사항 로그"를 먼저 읽어 전체 기획 현황 파악 — **실행 기준**
2. `HISTORIC_LOG.md`는 최신 세션(맨 위) 위주로 훑어 직전 작업 맥락만 파악 (전체를 매번 다 읽을 필요는 없음)
3. 현재 버전 확인 후 작업 시 다음 버전으로 업데이트
4. 기획 모호한 부분 → 임의 코딩 금지, 즉시 질문
5. 새 기능/수정사항이 확정되면 이 문서의 "2. 기능 현황" 표와 "4. 변경 이력"에 동시 반영
